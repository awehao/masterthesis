# AI／神經網路導入研究方向紀錄

紀錄日期：2026-09-11。  
性質：研究方向討論與建議，尚未定案，也不代表已實作或已驗證的成果。

## 1. 原題與目前進度

原論文題目：**動態環境下移動機械臂的預測導航與受約束操作**。

依目前報告，第一階段已有底盤預測導航、動態障礙感知、GMPC／CBF 與 Raw-scan Safety Shield；第二階段已建立全向底盤與六軸手臂的全身運動學、安全約束，以及整合任務與限制的單步 QP 協同預抓取基線。

全身多步 W-GMPC、實際抓取與接觸操作仍待完成；腕部深度尚未接入控制鏈。預抓取成功不等於已完成開門或拉抽屜。

依據：[第五次進度報告](第五次進度報告.md)、[第二階段計畫](第二階段計畫.md)。第二階段計畫第九節已預留學習式感知與任務參數估計。

## 2. 導入原則

讓 AI 提供**預測結果、感知結果或操作參數**，由既有 GMPC／全身 QP 執行受約束控制，可以維持原本題目的主軸。

- AI 的價值需對應原有問題，例如目標位姿未知、障礙物非等速度運動，或物體運動軸未知。
- 初期只選一個 AI 模組，避免同時增加感知、接觸、控制與訓練的除錯負擔。
- 保留輪速、關節與碰撞限制及下游安全檢查；安全效果仍取決於感測、模型、延遲與問題可行性，不能宣稱 Shield 能保證所有 AI 誤判皆安全。
- AI 增益與控制器增益分開比較，不同時更換感知、控制方法及安全設定後歸因於單一模組。
- 原題可以保留；加入 AI 本身不自動構成研究貢獻。

## 3. 方向一：把手偵測與三維操作位姿估計

**目前優先建議：最順接既有全身預抓取與第二階段操作主線。**

神經網路從影像辨識把手或關鍵點，再結合深度與幾何方法估計把手三維位置、方向及候選抓取位姿。目標是把目前指定的預抓取目標，逐步改為由機器人觀測取得。

```text
RGB-D 影像
  → 神經網路辨識把手／關鍵點
  → 深度與幾何估計三維位姿
  → 不確定度、一致性與可達性檢查
  → 全身 QP／後續 W-GMPC
  → 協同預抓取，後續延伸至抓取與操作
```

前置工作是建立相機深度、標定與座標轉換介面，確認觀測誤差，再接入任務目標。偵測框或像素中心不足以直接提供完整六維抓取位姿。

### 可研究的問題

> 視覺估計誤差如何影響全身受約束操作？加入不確定度檢查與重新觀測，能否提高任務成功率？

只接入一個偵測器較偏工程整合；需以受控實驗量化感知誤差、重新觀測與任務表現的關係。

### 建議比較

| 組別 | 目標來源 | 比較目的 |
|---|---|---|
| A | 真值／可靠標定位姿 | 建立理想目標輸入下的控制參考 |
| B | 視覺估計位姿 | 量化感知誤差造成的影響 |
| C | 視覺估計＋不確定度檢查／重新觀測 | 量化檢查與重新觀測的效果 |

三組保持控制器、限制與任務條件相同。量測位置／姿態誤差、任務成功率、碰撞與最小間距、完成時間，並記錄拒絕操作與重新觀測次數。重新觀測時間需納入總任務時間，拒絕或逾時不能直接從成功率分母刪除。

先以預抓取驗證，待接觸基線完成後再延伸至操作；兩者採不同成功判準。W-GMPC 完成後，再另做固定感知條件下的單步／多步比較。

## 4. 方向二：動態障礙物軌跡預測

**最直接對應題目的「預測導航」，適合沿用已有導航與模擬評測。**

目前 `kalman_tracker.py` 使用等速度模型，`gmpc.py` 也以等速度外推障礙物位置。可保留 KF 做狀態估計，加入小型 GRU／LSTM，根據歷史觀測預測未來軌跡，或學習等速度外推的修正量。

```text
LiDAR 偵測與追蹤
  → 障礙物歷史狀態
  → 神經網路多步軌跡預測
  → GMPC／預測式避障
  → 安全檢查與速度執行
```

現有位置／速度資料介面需擴充為可傳遞多步預測及有效性資訊，並檢查控制器內部所有使用等速度外推的位置。

### 可研究的問題

> 當障礙物減速、轉彎或走走停停時，學習式預測能否降低預測誤差，並減少導航急停與反覆修正？

### 驗證重點

- 比較等速度、等加速度等合理基線與神經網路，不只比較有／無 AI。
- 納入非等速度情境；若場景主要是等速度直線運動，神經網路未必有優勢。
- 同時量測軌跡預測誤差與閉迴路導航表現：碰撞、完成率、時間、急停及計算延遲。
- 訓練與測試按完整軌跡或場景切分，避免相鄰影格洩漏；模型輸入不得使用未來真值。
- 驗證低信心、追蹤中斷與逾時時的回退行為。預測更準不等於控制更安全。

## 5. 方向三：櫃門／抽屜運動軸估計

**與受約束操作結合深入，但建議在接觸操作基線完成後再做。**

AI 提供機構類型（平移、旋轉或未知）、候選軸方向與位置，以及不確定度，接到原本規劃的 1-DOF 旋量模型。

```text
視覺初始估計
  → 多視角觀測／必要時低速小位移探測
  → 依把手運動軌跡更新幾何運動軸
  → 一致性與可行性檢查
  → 全身受約束操作
```

單張影像可能看不到鉸鏈或滑軌，視覺輸出應視為初始先驗，不能直接當成精準物體模型。探測也需在已建立接觸與限制處理後執行。

建議比較真值軸、視覺初始軸、視覺加幾何更新三組；量測機構分類、軸方向與軸線位置誤差、操作成功率及抓取殘差。有可信力／力矩資訊時再加入接觸負載評估。

## 6. 優先順序與建議範圍

| 主要目的 | 建議選擇 |
|---|---|
| 順著目前全身預抓取完成操作主線 | 把手偵測＋三維位姿估計 |
| 利用成熟導航系統研究神經網路增益 | 動態障礙物軌跡預測 |
| 深入研究未知家具的運動約束 | 運動軸估計，接觸基線完成後加入 |

本次討論的優先建議為方向一，研究敘述可暫定為：

> 以學習式把手感知提供操作目標，研究視覺誤差下的全身受約束預抓取與操作。

此為建議範圍，尚未選定模型、資料集、訓練規模或時程。AI 原本是第二階段選配；若納入正式核心，需同步調整工作範圍，保留原有控制主張所需的單步／多步比較與接觸操作證據。

## 7. 參考資料

- [第二階段計畫](第二階段計畫.md)：核心研究問題、第九節 AI 感知與任務參數層。
- [第五次進度報告](第五次進度報告.md)：目前完成項目與尚待完成工作。
- [Kalman tracker](src/ammr_wholebody_mpc/ammr_wholebody_mpc/kalman_tracker.py)：現有等速度追蹤模型。
- [GMPC](src/ammr_wholebody_mpc/ammr_wholebody_mpc/gmpc.py)：現有障礙物預測與控制實作。
- [Where2Act（ICCV 2021）](https://openaccess.thecvf.com/content/ICCV2021/html/Mo_Where2Act_From_Pixels_to_Actions_for_Articulated_3D_Objects_ICCV_2021_paper.html)：從影像與深度預測關節式物體可操作區域及動作的相關研究，非本專案已採用模型。
- [Trajectron++（ECCV 2020）](https://arxiv.org/abs/2001.03093)：學習式多主體軌跡預測的相關研究，非本專案已採用模型。
