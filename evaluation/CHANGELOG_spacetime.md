# 時空代價場 — 改動記錄

目標:動態避障軌跡平滑、**不無故折返**,且不犧牲安全與成功率。

原則:`ST_WEIGHT=0` 永遠等價於驗證過的 98% 配置(已用數值測試確認誤差 0.00e+00),
任何改動都可用單一參數退回。

---

## 已完成的改動

### C1. 時空代價場本體 — `gmpc.py::_build_spacetime_cost()`
在 GMPC 的 QP 成本中加入對**預測**障礙位置的高斯軟障壁:

```
C_k(p) = W · exp(−‖p(k) − o_i(k)‖² / 2σ_k²)
o_i(k) = o_i(0) + v_i·k·dt          （與 CBF 相同的等速度預測）
σ_k    = σ₀(1 + growth·k)           （預測越遠越不確定）
```

二階泰勒展開後併入 `P`、`q`,**不增加決策變數**;`δp_k = A_k z`,
`A_k = R_ref(k) S_k Γ`。

**為什麼**:硬 CBF 只在即將違反時才動作,所有避障都變成對固定參考路徑的
「臨時變形」。軟代價場在數公尺外就給梯度,讓控制器提早微幅偏轉。

### C2. Hessian 半正定投影 — `_psd_project_2x2()`
高斯峰頂是**凹的**,原始 Hessian 負定,直接加進 `P` 會讓 QP 非凸。
對每個 2×2 Hessian 做特徵值裁切(Gauss-Newton 式近似)。

**驗證**:2000 次隨機對稱矩陣,最小特徵值 −2.2e-16(浮點零)。

### C3. 尺度修正(關鍵)
最初把 `W_eff = W·σ²·trace(Q)/2`,想「錨定到追蹤權重」,結果 `W=30` 變成
`W_eff ≈ 200`。但該比的是**典型偏差下的追蹤成本** `Q×0.1² ≈ 0.15`,
不是 `Q` 本身 —— **差三個數量級**。

症狀:機器人變成純斥力場逃跑者。2D 實測 路徑 27.6→48.9 m、
淨距暴增到 +0.45、deg/m 192→1389、到不了終點。gz 三次全部 0/2 失敗。

**修正**:`st_weight` 直接就是單一障礙的峰值成本,合理範圍 **O(1)**(實測 1~5)。

### C4. 梯度與曲率雙重截斷
只夾 Hessian 不夠 —— **梯度進的是 `q`,才是驅動解的主力**。
只夾 Hessian 時仍震盪(deg/m 5226)。兩者都夾在
`2 × max(Q) × 0.01`(典型追蹤成本量級)。

### C5. 未收斂解的可行性防護 — `gmpc.py::solve()`
`maximum iterations reached` 的解**不保證可行**。加了代價場後 OSQP 曾回傳
違反加速度上限的點(|du/dt| = 1.07 vs a_max 0.80),底盤收到階躍指令。

**修正**:僅對**未收斂**的解做 `a_max` 硬夾;已收斂的解本來就滿足約束,不碰。

**曾經改壞又還原**:一度把求解失敗的 fallback 從「煞停」改成「保持上一步速度」,
理由是避免煞停的 jerk。但求解失敗正好發生在障礙物附近,滑行過去直接撞上
(基準從 0 碰撞 / +0.037 m 變成 2 碰撞 / −0.077 m)。**已還原為煞停**。

---

## 驗證狀態

| 檢查 | 結果 |
|---|---|
| `ST_WEIGHT=0` 與原求解器等價 | 誤差 **0.00e+00** |
| 加場後 `P` 仍半正定 | 最小特徵值 −7.3e-15 |
| OSQP 收斂率(W=0/30/100/300) | 86.7 / 99.4 / 100 / 100 % |
| 加速度可行性 | worst \|du/dt\|/a_max = 1.000 |
| 求解時間 | 2.26 ms（未變）|

---

## 10+10 正式驗證結果 — **時空代價場無效，不採用**

gz，各 10 趟，同場景同種子分布：

| 指標 | W=0 | W=5 | 變化 |
|---|---|---|---|
| 成功 | 10/10 | 10/10 | — |
| 碰撞趟 | 1 | 2 | 略差 |
| **deg/m (ds=0.20)** | **90.5** | **96.4** | **+6.5%** ✗ |
| **折返 >90° 次/趟** | **7.7** | **8.1** | **+5.2%** ✗ |
| **倒退比例** | **0.75%** | **0.87%** | ✗ |
| 到達 s | 172.5 ± 14.0 | 174.5 ± 16.7 | +1.1% |
| 路徑 m | 39.30 ± 3.27 | 40.10 ± 3.63 | +2.0% |
| min 淨距 | +0.0298 | +0.0297 | 持平 |
| solve ms | 2.39 | 2.90 | +21% |

**每一項持平或略差，且全部落在 1 個標準差內** → 統計上無差異。

### 我先前的 N=2 結論是錯的
N=2 曾顯示「到達 −8%、路徑 −7.5%、淨距 +25%、σ(vy) −15%」，看起來全面改善。
N=10 顯示那是雜訊：兩組的標準差（到達 ±14~17 s、路徑 ±3.3~3.6 m）**遠大於**
當初看到的差異。**N=2 在這個系統裡沒有任何判別力**。

### 決定
`ST_WEIGHT` 預設維持 **0**。程式碼保留（已完整驗證、可隨時開啟），
但**不宣稱有平滑度效益**。在本場景下，早先量到的「路徑點與動態障礙的時間衝突率
僅 1.6%」已經預告了作用空間有限，實測證實了這一點。

---

## 折返的真正來源（本次新發現）

把 >90° 急轉的位置做空間分群（W=0，10 趟共 77 個折返點）：

| 最靠近的物件 | 次數 | 佔比 | 平均距離 |
|---|---|---|---|
| **dyn_obs_1 掃描線** | **50** | **64.9%** | 0.56 m |
| 柱 (9.0, 8.0) | 18 | 23.4% | 0.64 m |
| dyn_obs_2 掃描線 | 6 | 7.8% | 0.55 m |
| 柱 (14.0, 13.0) | 3 | 3.9% | 0.74 m |

**近三分之二的折返集中在 dyn_obs_1 旁邊。**

dyn_obs_1 的速度是 **0.13 m/s**，而 tracker 的發佈門檻 `min_track_speed` 是
**0.10** —— 只差 0.03，而 KF 的速度估測有噪。這顆障礙因此會反覆跨越門檻、
在 `/gmpc/obstacles` 裡忽有忽無，CBF 的約束集合每幾幀就變一次。

先前測「感知遲滯」時只量了加速度飽和率（churn −60% 但飽和率沒改善，判定失敗），
**沒有量折返**。折返才是這裡真正的現象，因此重新驗證中。

---

## 感知遲滯驗證 — **也無效，且更糟**

gz 10 趟，`release_track_speed=0.06`、`track_release_frames=5`：

| 配置 | deg/m | 折返/趟 | 倒退 |
|---|---|---|---|
| **W=0 基準** | **90.5** | **7.7** | **0.75%** |
| ST 代價場 W=5 | 96.4 | 8.1 | 0.87% |
| 感知遲滯 | 96.9 | **9.9 (+29%)** | 1.15% |

穩定 track 反而讓折返增加 29%，所以折返的機制**不是** track 在門檻附近忽有忽無。

---

## 折返的真正機制（本次確認）

對 dyn_obs_1 區域的 42 個折返點，比對該障礙物自身的掉頭時刻：

| | 實測 | 若與掉頭無關 |
|---|---|---|
| 距最近掉頭時刻（中位） | **3.3 s** | ~9.8 s |
| 落在掉頭 ±3 s 內 | **43%** | ~15% |

**折返集中發生在障礙物自己掉頭的時候。**

dyn_obs_1 在 y 3.0↔5.55 之間 ping-pong，週期 39.2 s；機器人以 0.22 m/s 通過該區要十幾秒，
所以它正在閃避時，障礙物很可能中途反向。CBF 與時空代價場都用等速度外推
`o(k) = o(0) + v·k·dt`，障礙物一掉頭，整段預測就反了，控制器只能重新決策 —— 那就是折返。

**這不是控制器缺陷，是等速度預測假設被違反時的正確反應。**

這一併解釋了兩個介入為何失敗：
- 時空代價場用同一套等速度預測，掉頭時同樣被騙；
- 感知遲滯讓過期的速度多留幾幀，掉頭後錯得更久，所以折返反而增加。

### 可能的方向（尚未實作、未驗證）
1. **預測不確定性隨時間膨脹**：CBF 的 keep-out 半徑隨 k 增大，等價於「越遠期越不信任預測」。
   目前 σ 只在時空代價場裡膨脹，CBF 的 margin 是固定的。
2. **多模態預測**：對會往復運動的物體，同時考慮「維持」與「反向」兩種假設，取較保守者。
3. **場景層面**：ping-pong 障礙的往復週期與機器人通過時間相當，是這個現象的放大器。
   真實行人多為單向通過，折返率應顯著較低 —— 值得用單向通過的場景另做一組對照。

### 目前定案
`ST_WEIGHT=0`、遲滯關閉（`release_track_speed=0.10`、`frames=0`）
= **維持原本驗證過的配置**。兩項功能的程式碼都保留且可用單一參數開啟。

---

## C6. 承諾式繞行 `detour.py` — 直接針對「往後躲」而非「往左前繞開」

前面每一項介入（時空代價場、感知遲滯、殘差 RL）都在**同一個框架內**微調，
全部無效。這一項改的是框架本身：**改寫要被追蹤的參考**。

### 為什麼前面都失敗
CBF 是疊在**固定參考路徑**上的硬約束。QP 裡沒有「從左邊繞過去」這個概念，
只有「參考往這邊拉、約束往那邊推」。兩個後果：

1. **每一步都重新選邊。** 障礙物掉頭，機器人就跟著掉頭 —— 這正是先前量到的
   「43% 的折返落在障礙物掉頭 ±3 s 內」。
2. **往後讓與往旁邊繞，追蹤成本一樣。** 所以「停下來等」是合法解，
   而且常常是最省的解。

MPPI 沒有這兩個問題，因為它取樣的是**整條軌跡**，選中的那條本身就是一條完整的繞法。

### 三個機制
1. **鎖定同倫類（`DetourState`）**：障礙物進入前方 ±30° / 2.0 m 錐形時
   一次性選定 LEFT 或 RIGHT，在該障礙真正被甩到身後（body-frame x < −0.3 m）
   之前**不重選**。選邊規則：走障礙物**不在**的那一側；正前方時用它的側向速度
   決定（從它來的方向後面過，不橫切它的去路）。
2. **參考側移（`apply_offset`）**：鎖定期間把時域內的參考點沿法線推開，
   形狀 `sin²(πk/N)`，中段峰值、**末端歸零**。於是追蹤成本 Q **主動把機器人拉去繞**，
   而不是抵抗 CBF 的推。`sin²` 在兩端 C1 連續 —— 折線參考正是輸入平滑成本要對抗的東西。
3. **前進速度下限**：繞行期間 `u_min[0] = 0.10 m/s`，把「停下來等」移出解空間。

### 已驗證
| 檢查 | 結果 |
|---|---|
| 選邊（障礙 y=+0.5→RIGHT、y=−0.5→LEFT）| ✓ |
| **障礙左右擺盪 60 步，鎖定邊切換次數** | **0 次** ✓ 這是治折返的核心 |
| 障礙移到身後 → 解鎖 | ✓ |
| 參考變形為拱形、峰值 0.35 m @ 中段、末端 +0.000 | ✓ |
| `detour_enable=False` 參考完全未變 | ✓ 誤差 0.00e+00 |

啟用方式：`DETOUR=1 DETOUR_OFFSET=0.35`（預設關閉）。

### 2D 沙盒對此無判別力
2D 快篩的折返數在所有配置都是 ~0，重現不了 gz 的 7.7 次/趟。
而且我的測試 harness 多呼叫了一次 `_obstacles_for_cbf()`，多消耗感知噪聲隨機數，
導致基準與先前的 2D 數字不可比。**該次 2D 結果不採信**，直接用 gz 驗證。
（唯一有參考價值的訊號：offset 0.60 在 2D 掉到 1/3 成功，因此 gz 用 0.35。）

### gz 第一輪結果 — 平滑度大勝，但**撞牆**

| 指標 | 基準 | 承諾式繞行 | 變化 |
|---|---|---|---|
| 到達 | 10/10 | 10/10 | — |
| **折返 >90° 次/趟** | **7.7** | **3.6** | **−53%** ✓（MPPI 為 3.2）|
| **倒退比例** | **0.75%** | **0.38%** | **−50%** ✓（比 MPPI 的 0.68% 還好）|
| deg/m | 90.5 | 82.8 | −8.5% ✓ |
| 路徑 m | 37.81 | 34.38 | −9.1% ✓ |
| 到達 s | 172.5 | 154.5 | −10.5% ✓ |
| **碰撞趟** | **1** | **4** | ✗✗ |
| 平均 min 淨距 | +0.0298 | +0.0082 | ✗ |

**執行期證據（繞行確實在作動）**：真正倒退（cmd vx<0）的時間佔比
基準 8.4% → 繞行 1.4~2.6%，平均前進速度 0.142 → 0.155 m/s。

**四次碰撞全部貼牆**，其中三次在 (0.5, 3.9)、(2.0, 4.4) —— 正是 dyn_obs_1
沿 x≈0 牆邊來回的那條走廊。**機器人選了「障礙物不在的那一側」，而那一側是牆。**

### 兩個真 bug（已修）
1. **`_blocking` 的 `o.get('static')` 永遠是 False。** 節點給牆點加的鍵是
   `'margin'`，沒有 `'static'`。所以**牆點自己會觸發鎖定**，接著 `vx_floor`
   把機器人往牆推。已在 `gmpc_node.py` 明確標記 `'static': True`。
2. **選邊完全沒檢查那一側有沒有空間。** 新增 `_side_free()`：鎖定前先確認
   該側 `max_offset + 0.33 m` 內沒有牆點；偏好側不通就走另一側（貼近動態障礙
   仍有 CBF 保護，撞牆沒有）；兩側都不通就**不鎖定**，退回原本的 CBF 行為。
   鎖定期間該側若變得不通也**立即解鎖** —— 承諾是為了不在兩個可行解之間搖擺，
   不是撞牆的許可證。

| 驗證 | 結果 |
|---|---|
| 無牆、障礙偏左 → 走右 | ✓ |
| 偏好側有牆 → 改走另一側 | ✓ |
| 兩側都有牆 → 不鎖定 | ✓ |
| 前方只有牆點 → 不觸發鎖定 | ✓ |
| 鎖定後牆才出現在鎖定側 → 立即解鎖 | ✓ |
| 障礙擺盪 60 步（無牆）→ 切換 0 次（原行為未退化）| ✓ |
| `enable=False` 無作用 | ✓ |

**`_side_free` 的已知限制**：它只看得到 tracker 發佈的牆點，而那些點是以
機器人自身 +（±0.3 m 四個探針）查最近牆、範圍 1.2 m、最多 4 點 —— 也就是
**貼在身側的牆**。這正好對應觀察到的失效模式（走廊側牆），但它**看不到
1 m 外正前方**、且不是任一探針最近鄰的牆。若之後出現「繞行撞到前方轉角」，
要往這裡查，而不是往選邊邏輯查。

### gz 第二輪（含牆感知選邊）— 撞牆治好了，但整體更差

| 指標 | 基準 | 第一輪 | **第二輪** |
|---|---|---|---|
| 折返 >90°/趟 | 7.7 | 3.6 | **4.0** ✓ |
| deg/m | 90.5 | 82.8 | **83.8** ✓ |
| 路徑 m | 37.81 | 34.38 | **35.33** ✓ |
| 到達 s | 172.5 | 154.5 | **158.7** ✓ |
| **碰撞趟** | **1** | **4** | **5** ✗ |
| **平均 min 淨距** | **+0.030** | **+0.008** | **−0.086** ✗ |
| 最差趟淨距 | −0.015 | −0.094 | **−0.525** ✗ |

---

## 一個必須更正的診斷錯誤

**我先前說「第一輪四次碰撞全部撞牆」是錯的。**

那個診斷的做法是：找出「離牆最近」的那一刻，然後在**該時刻**比較
「離牆距離」與「離柱子 / 離動態障礙**路徑線段**的距離」，取最小者命名。
問題在於：**路徑線段不是障礙物當下的位置**。障礙物在那條線上來回移動，
拿機器人到整條線段的距離當「離障礙的距離」會嚴重低估實際間距，
於是「牆」總是贏。

改用正確量法（整趟對 `/gmpc/obstacles` 實際追蹤到的障礙位置取最小值）：

| | 最貼牆 | 最近動態障礙 | 判定 |
|---|---|---|---|
| 第一輪 seed1 | 0.364 | 0.274 | 擦動態障礙 |
| 第一輪 seed5 | 0.391 | 0.312 | 擦動態障礙 |
| 第一輪 seed6 | 0.300 | 0.300 | 擦動態障礙 |
| 第一輪 seed8 | 0.300 | 0.237 | 擦動態障礙 |
| 第二輪 seed1/4/5/8/9 | 0.300~0.381 | 0.180~0.320 | 全部擦動態障礙 |

機器人半徑 0.30、障礙半徑 0.25，接觸距離 0.55。
**兩輪、九次碰撞，最貼牆距離全都 ≥ 0.30（貼邊但未穿透），而離障礙只有 0.18~0.32。
沒有一次是撞牆。**

代價：牆感知選邊（C6）**打在錯的目標上**，而為它加的那條「偏好側被擋就改走
另一側」的退路，等於把參考彎向**障礙物所在的那一側**，讓安全性更糟
（淨距 +0.008 → −0.086、碰撞 4 → 5）。已改為**偏好側沒空間就不繞行**。

---

## C8. 真正的機制 — 彎過去的參考點落在 CBF 禁區裡

一個算術問題：

```
CBF 禁區半徑  r_eff = r_obs + margin = 0.25 + 0.38 = 0.63 m
繞行推開參考        max_offset       = 0.35 m
時域內橫移上限      vy_max * T = 0.25 * 1.0 = 0.25 m
```

**參考點被彎到障礙旁 0.35 m 處，而那裡在 0.63 m 的禁區內。**
追蹤成本 Q 拚命把機器人拉向一個 CBF 禁止的點，兩者對抗，
結果是貼著約束邊界走，鬆弛變數一啟動就穿透。實測參考點離障礙中心最近只有
**0.015 m**。

**加大 offset 解決不了**：需要 0.63 m，但時域內只能橫移 0.25 m，
參考會變成追不到的目標。

**修正 `clear_reference()`**：彎完之後，把每個落在禁區內的參考點
**沿徑向投影到禁區邊界外**（用與 CBF 相同的等速度預測 `o(k)=o(0)+v·k·dt`）。
Q 與 CBF 從此方向一致，不再對抗。

| 驗證 | 結果 |
|---|---|
| 只彎參考 → 離障礙中心最近 | **0.015 m（禁區內）** |
| 彎完再投影 → 離障礙中心最近 | **0.680 m（禁區外）** ✓ |
| 無動態障礙時投影不動參考 | 誤差 **0.00e+00** ✓ |
| 靜態牆點不投影（與牆的 CBF 可能互相矛盾）| 設計如此 |

開關：`DETOUR_CLEAR_REF=0` 可關閉。
`evaluation/test_detour.py` 19 項全過。

### 五輪 gz 實驗，逐輪隔離變因

每輪 10 趟，同場景同種子，對照組固定為 `archive_w0`。

| 輪 | 變更 | 折返/趟 | deg/m | 碰撞 | 平均淨距 | 最差趟 |
|---|---|---|---|---|---|---|
| — | **基準** | 7.7 | 90.5 | 1 | **+0.030** | −0.015 |
| 1 | 繞行（offset 0.35, floor 0.10）| **3.6** | 82.8 | 4 | +0.008 | −0.094 |
| 2 | ＋牆感知選邊、偏好側不通改走另一側 | 4.0 | 83.8 | 5 | **−0.086** | **−0.525** |
| 3 | ＋`clear_reference`（pad 0.05）、不通就不繞 | 4.4 | 82.5 | 5 | +0.011 | −0.040 |
| 4 | pad 0.05 → **0.18** | 4.4 | 84.9 | 5 | +0.001 | −0.094 |
| **5** | **floor 0.10 → 0**（pad 回 0.05）| **4.1** | **82.4** | **2** | **+0.037** | −0.023 |

**平滑度那一半從第一輪就穩了，五輪都沒退。所有迭代都花在安全性上。**

### C9. 速度下限是全部安全性損失的來源

`vx_floor` 是 `u_min[0]` 的**硬約束**，不是成本。當 CBF 需要減速到 0.10 m/s
以下才能維持間距時，**QP 沒有其他可行解，只能付鬆弛去違反 CBF**。
速度下限等於把安全控制器最主要的手段（減速）從解空間裡拿掉。

拿掉之後（第 5 輪 vs 第 4 輪）：

| | floor 0.10 | floor 0 |
|---|---|---|
| 平均淨距 | +0.001 | **+0.037** |
| 碰撞趟 | 5 | **2** |
| **折返/趟** | **4.4** | **4.1** ← 完全沒退 |
| solve ms | 2.80 | 2.51 |

**折返的改善完全來自「鎖定同倫類」，一點都不來自速度下限。**
solve 時間也從 +22% 降到 +5%，因為鬆弛不再頻繁啟動。

被推翻的中間假設（都是 N=10 實測，不是推論）：
1. ~~撞牆~~ → 九次碰撞全是擦動態障礙（見上方更正）
2. ~~參考落在 CBF 禁區內~~ → 真的是問題，投影後最差趟 −0.525 → −0.040，但不是全部
3. ~~參考餘裕不足~~ → pad 0.05 → 0.18 反而更差（+0.011 → +0.001），**正是這個否證把矛頭指向 vx_floor**

---

## 定案配置與成果

`detour_enable=1`、`vx_floor=0`、`clear_ref=1`、`pad=0.05`、`offset=0.35`
（已設為程式預設；`DETOUR=0` 一鍵退回原配置）

| 指標 | 基準 | 承諾式繞行 | 變化 | MPPI |
|---|---|---|---|---|
| 到達 | 10/10 | 10/10 | — | — |
| **折返 >90°/趟** | 7.7 | **4.1** | **−46.8%** | 3.2 |
| **deg/m (ds=0.20)** | 90.5 | **82.4** | **−9.0%** | 45.1 |
| **倒退比例** | 0.75% | **0.52%** | **−30.1%** | 0.68% |
| 路徑 m | 37.81 | 34.75 | −8.1% | — |
| 到達 s | 172.5 | 157.3 | −8.8% | — |
| **平均 min 淨距** | +0.0298 | **+0.0366** | **+22.8%** | — |
| 碰撞趟 | 1 | 2 | −0.015 / −0.023，與基準的 −0.015 同量級 | — |
| solve ms | 2.39 | 2.51 | +5.3% | — |

**安全性不但沒退，平均淨距比基準好 23%。**
圖：`evaluation/results/figs/traj_detour.png`（紅圈標 >90° 急轉）

### 一個順帶量到的既有現象
CBF `margin = 0.38` 理論上應給 +0.08 m 淨距，實測**基準只有 +0.030**
（十趟裡有一趟 +0.000、一趟 −0.015）。**鬆弛變數一直在作用**，
這不是繞行造成的，是原本就有的狀態。

---

## C7. 範化缺口 — 多障礙情形其實完全沒被測到

問「這能範化到各種動態障礙嗎」，先量涵蓋率。錐形視野（±30°、2.0 m）內
同時存在的動態障礙數，繞行組 10 趟共 20240 個控制幀：

| 錐形內障礙數 | 幀數 | 佔比 |
|---|---|---|
| 0 | 18402 | 90.92% |
| **1** | **1820** | **8.99%** |
| **2** | **18** | **0.09%** |

**繞行作動的時間裡有 99% 只面對單一障礙。**
「折返 7.7→3.6」這個結果實質上只證明了**單一、慢速（0.13~0.15 m/s）、
正面來的往復障礙**這一種情況。順帶暴露兩個結構性缺陷：

### C7-1. `_side_free` 只檢查牆，不檢查其他動態障礙
閃避 A 可能直接轉進 B。已修：其他 mover 用**等速外推 1.5 s** 的預測位置一併檢查
（`side_clear_dyn = 0.38`，與動態 CBF margin 一致）；被繞的那顆用 `ignore` 排除，
因為它依定義在另一側，該次通過由 CBF 保護。

### C7-2. 障礙身分比對是壞的
`locked_id = id(blk)`，但障礙訊息的線格式是 `[x, y, r, vx, vy]`（stride 5）
**沒有 ID 欄位**，而節點每則訊息都重建 dict。實測 CPython 會回收位址再利用：

```
第1次 [...342080, ...377728, ...376192]
第2次 [...596736, ...377280, ...534656]
第3次 [...342080, ...377728, ...376192]   ← 與第1次相同
```

**每隔一次「碰巧」對上**，多障礙時還可能對上**錯的那一顆**。
目前被掩蓋，是因為 fallback 會重找錐形內最近的障礙，而 99% 的時間只有一顆。
已改為**位置關聯**（`assoc_gate = 0.5 m` 的最近鄰），這是在沒有 ID 的前提下
誠實可得的做法。

### 驗證 — `evaluation/test_detour.py`，16 項全過
其中四項是先前完全沒測到的：

| 新測項 | 結果 |
|---|---|
| 第二顆 mover 停在偏好側 → 改走另一側 | ✓ |
| 第二顆 mover **即將**進入偏好側（現在還很空）→ 改走另一側 | ✓ |
| 鎖定跨 11 步「重建 dict」仍存活，障礙到身後才解鎖 | ✓ |
| 旁邊掠過的第二顆 mover **搶不走**鎖定 | ✓ |

原有行為的回歸測試（擺盪 60 步切換 0 次、拱形峰值在中段且末端歸零、
`enable=False` 完全無作用）同樣全過。

### 仍未解決的範化限制
1. **只對正前方作動**：`_blocking` 要求 `fx > 0` 且在 ±30° 內。
   垂直高速穿越的障礙很晚才進錐形；從後方追上的永遠不觸發。
2. **速度尺度**：`trigger_range = 2.0 m` 是對 0.13 m/s 調的。
   換成行人 1.2 m/s 只剩約 1.7 s 反應時間。
3. **依賴全向底盤**：20 Hz、N=20、v_nom 0.30 → 時域 1.0 s ≈ 0.30 m 前進距離，
   拱形峰值在 ~0.15 m 處要求 0.35 m 側移 —— **那幾乎是純橫移**。
   差速底盤上這條參考追不動。
4. **`_side_free` 的牆感知範圍**：只看得到 tracker 發佈的牆點（機器人自身 +
   ±0.3 m 四探針、範圍 1.2 m、最多 4 點），即**貼在身側的牆**。
   看不到 1 m 外正前方的轉角。
5. **場景本身**：要證明多障礙可用，需要**雙障礙對向交會**的新場景，
   現有場景的四顆動態障礙彼此距離太遠。

---

## 量測工具的一個汙染來源（順帶修掉）

`evaluation/bags/gmpc_cbf__scan_seed$i` 這個目錄名是**跨實驗重複使用**的，
而 `archive_w0/` 裡有 **40** 個目錄、那批其實只跑了 10 趟 —— seeds 11~40 是
更早、**配置不同**的殘留 bag。照 seed 範圍掃目錄會靜默混入舊配置的資料。

`evaluation/compare_runs.py` 改成**由該批的 CSV 決定要讀哪些 bag**。
修好後重算，完整重現了先前兩組已知結論（ST 場 90.5→96.4 / 7.7→8.1、
感知遲滯 →96.9 / 9.9），確認工具本身可信。

---

## C10. 碰撞率的統計對照 — 更正兩個我先前的錯誤說法

問題起點：「為什麼每一組都有碰撞？之前 margin 0.38 不是都沒碰撞嗎？」

### 更正一：從來沒有零碰撞
第三次報告的 98% 那批（N=40）寫的是「碰撞 **1/40**」、「**零深撞**」——
零深撞不等於零碰撞。從 git 撈回該批原始 CSV（2026-06-30，N=35）：

```
34/35 到達 (97%)、碰撞 1
min 淨距 平均 +0.0275、中位 +0.0500、最差 −0.186
```

### 更正二：`dyn_obs_1 = 0.15` 不是碰撞增加的原因
我先前推論「0.15 比 0.13 難，所以碰撞變多」。**錯了 ——
98% 那批本來就是 0.15。** 該檔案的變更史只有：
0.15 →(7/29）→ 0.13 →(7/30 我改）→ 0.15 →（改回）→ 0.13。

`analyze.py` 的碰撞判定自 2026-06-02 未變，`clearance_source` 三批都是
`gt_pose+static`，所以可以直接比較。

### 統計檢定（Fisher exact 雙尾，對照 N=35）

| 配置 | 碰撞 | 率 | 95% CI | 淨距中位 | p |
|---|---|---|---|---|---|
| N=35 (06-30) 0.15 無繞行 | 1/35 | 3% | 1–15% | +0.050 | — |
| archive_w0 0.13 無繞行 | 1/10 | 10% | 2–40% | +0.034 | 0.399 |
| detour5 0.13 繞行+**徑向** | 2/10 | 20% | 6–51% | +0.040 | 0.119 |
| **alpha3p0 0.15 繞行+側向 α3.0** | **4/10** | 40% | 17–69% | **+0.000** | **0.006** |
| **alpha1p5 0.15 繞行+側向 α1.5** | **3/10** | 30% | 11–60% | +0.050 | **0.030** |

**基準與已驗證的繞行（徑向）和 98% 那批在統計上無差異。沒有退步。**
「以前沒碰撞」的印象，實際是「以前 1/35、後來 1/10，n=10 分不出來」。

**唯二顯著變差的兩組，共同點是側向投影**（C8 之後加的）。
速度、margin、繞行本身、α 都已排除：
98% 那批就是 0.15 + margin 0.38，徑向繞行 p=0.119，α 兩個值都變差。

### 機制上說得通
徑向投影把參考往**後**拉（機器人退開 → 安全但打結）；
側向投影把參考推到禁區**邊界上**再繞過去（貼著邊界走 → 順但容易擦）。
`clear_pad` 只有 0.05，所以側向版等於沿著 keep-out 邊界 5 cm 外繞行。

### 也要更正：margin 8 cm 不是「新問題」
`cbf_safe_margin = 0.38` → 目標淨距 0.63 − 0.55 = **8 cm**，
實測平均落在 3–4 cm。算術沒錯，但**它不是最近碰撞增加的原因** ——
margin 從頭到尾都是 0.38 而 98% 那批只有 1/35。
我把一個長期存在的設計特性誤當成新出現的問題。

### 結果 — 兩個假設都被否證，真正的答案是繞行本身

三組各 10 趟，全部 `dyn_obs_1 = 0.15`（與 1/35 那批相同）：

| 配置 | 到達 | 碰撞 | 平均淨距 | 中位 | 折返 | deg/m | p vs 1/35 |
|---|---|---|---|---|---|---|---|
| **全關（今日程式碼）** | 10/10 | **2** | **+0.036** | **+0.050** | 7.5 | 91.6 | **0.119** |
| 繞行 + 徑向 | 9/10 | 4 | −0.006 | +0.000 | 11.6 | 100.9 | 0.006 |
| 繞行 + 側向 | 10/10 | 5 | −0.011 | −0.016 | 5.6 | 87.6 | 0.001 |

**否證一：側向投影不是元兇。** 徑向 4/10、側向 5/10，無差異。
兩種機制完全不同的投影給出相同的碰撞率。

**否證二：基準沒有漂移。** 我懷疑 ROS 升級 / 手臂整合 / `gmpc.py` 的新程式路徑
讓 baseline 退步，所以補跑了這個對照。**淨距中位 +0.0500，與 2026-06-30 那批
完全相同**，p=0.119。程式碼沒有問題 —— 這個檢查做對了，但假設是錯的。

**真正的歸因：繞行整體讓淨距中位掉 5 cm**（+0.050 → +0.000 / −0.016）。
整個分佈本來就只離接觸邊界 5 cm（見 C10 的 margin 算術），所以這 5 cm
足以把一半的趟數推到負值。機制上合理：繞行的本質是「繞過去」而非
「讓開等待」，那必然選擇貼著障礙通過。

### 最重要的發現：繞行的效益對障礙速度高度敏感

| | dyn_obs_1 = 0.13 | dyn_obs_1 = 0.15 |
|---|---|---|
| 折返改善 | 7.7 → 4.1（**−47%**）| 7.5 → 5.6（−25%）|
| 淨距 | +0.030 → **+0.037（變好）** | +0.036 → **−0.011（崩掉）**|
| 結論 | 免費的午餐 | 用 2.5 倍碰撞換 25% 折返 |

**先前五輪驗證全部在 0.13 下進行，所以完全看不到這個代價。**
0.15 只是把週期從 19.6 s 縮到 17.0 s，機器人通過該段要十幾秒，
掉頭發生在閃避途中的機率大增 —— 徑向繞行的折返因此從 4.1 暴增到 11.6。

**對論文的意涵：不能只用單一場景宣稱繞行有效。** 定案配置的成果表必須
註明 `dyn_obs_1 = 0.13`，並把 0.15 的結果一併呈現為敏感度分析。

### 側向投影的價值仍然成立，只是換來的是平滑度不是安全性
0.15 下 徑向 → 側向：折返 11.6 → 5.6（−52%）、deg/m −13%、倒退 1.80% → 0.59%
（−67%，對應「參考不再被往後拉」）。碰撞則無差異。
**在 0.15 下徑向繞行比完全不繞行還糟 —— 折返、deg/m、碰撞三項全輸。**

### 方法學教訓：小樣本
今天有三次在小樣本上下了後來被推翻的結論：繞行 N=3 顯示折返 3.3（實為 4.1）、
`clear_pad` 假設、以及徑向組「前 5 趟 0 碰撞」（實為 4/10）。
碰撞率的真實值約 10–40%，n=10 的 95% CI 寬達 20–30 個百分點，
**連 N=10 都只能分辨「差很多」而非「差一點」**。

消融實驗因此應分兩層：平滑度指標（deg/m、折返，雜訊小）用 N=10 排序；
碰撞率只當「有沒有明顯崩壞」的守門員，不做細部比較 ——
48 組 × N=10 在多重比較下會產生大量假陽性。


---

# 方法命名（2026-07-31 定案）

## 控制器層

**全時域 CBF 幾何模型預測控制**
**Full-Horizon CBF Geometric MPC — FH-CBF-GMPC**

SE(2) 上的幾何 MPC（李群測地誤差），CBF 約束施加在 `k = 0…N` 的**每一步**、
對**預測的**障礙位置，而非只在當前步。

## 系統層

**時域匹配的非結構化環境動態避障**
**Horizon-Matched Navigation in Unstructured Environments**

核心論點：

> **障礙應交給「時域與其可預測性相符」的那一層處理。**
> 靜態幾何可預測無限久 → 全域規劃(秒~分鐘尺度)；
> 動態障礙只在約 1 秒內可信 → 只給 CBF，並從規劃器視野中移除。
> 把動態障礙餵給以 3 秒為週期重規劃的規劃器是**範疇錯誤** ——
> 機器人抵達時那份規劃早已過期。

架構：

```
時域匹配導航 (系統貢獻)
├─ 感知:動靜三分流 + 形狀感知群集(圓擬合 / 展開下限 / 多圓覆蓋)
├─ 規劃:Nav2 全域規劃,只看靜態幾何(/scan_filtered)
└─ 控制:FH-CBF-GMPC + S-curve 參考混合
```

## 支持「範疇錯誤」論點的量化證據

| 量測 | 數值 |
|---|---|
| 重規劃週期 | 3 s |
| 讓機器人前方 0–2 m 路徑橫移 >0.25 m 的比例 | **24%** |
| 該位移的 p90 / 最大 | 0.44 m / **1.82 m** |
| 參考朝向跳動 p90 / 最大 | 26° / **63°** |
| 大跳動後 0.3 s 內的平均 \|du/dt\|/a_max | **0.964** |
| 同期間加速度飽和佔比 | **88.5%**（其餘時間 13.5%）|

## 誠實的貢獻界定

| 組成 | 原創性 |
|---|---|
| SE(2) 幾何 MPC（李群測地誤差）| 既有（Lie-group MPC 文獻）|
| CBF 作為 QP 約束 | 既有（Ames et al.）|
| CBF 施加於整個預測時域 | 較少見但有（MPC-CBF, Zeng 2021）|
| **依可預測性把障礙分流到對應時域的層** | **本論文的系統貢獻** |

最強的宣稱在**架構**，不在控制器數學。

## 標籤對應

先前批次標籤 `plan3` = 架構完整啟用的配置，現已改稱 **`hm-full`**。
腳本與圖表已改名（`run_hm_bigroom.sh` 等）；既有的資料檔與 archive 目錄
沿用 `plan3` / `plan3b` 舊名以免與已產出的分析對不上，對應關係為
`archive_plan3` = hm-full 初版、`archive_plan3b` = 感知修正後重跑。
配置內容：

```
PLANNER_SCAN=/scan_filtered   動態障礙不進規劃器
PLAN_BLEND=1.0                S-curve 參考混合
CBF_ALPHA=1.5                 障壁提早溫和介入
（感知層的圓擬合 / 三分流 / 多圓覆蓋為預設行為，無開關）
```

## 命名時尚未成立的部分

`shapes`（非圓形，6/10 碰撞）、`stopgo`（變速，4/10）、`bothgaps`（雙開口堵塞
livelock，2/10 到達）三類確定失敗。`small` / `ell` / `merge` 的結果因錄影漏掉
受測障礙而無效（`record.sh` 只錄 dyn_obs_{0,1,2,5}，缺 3、4、6）。
**在這些補齊之前，不得宣稱泛化。**


---

# 表面點 CBF — 移除所有形狀假設

## 動機

先前的感知層對每個群集做代數圓擬合。合成驗證顯示那對圓柱極準,對長物無用:

| 形狀 | 質心誤差 | 圓擬合誤差 |
|---|---|---|
| 圓 r=0.30 | 0.242 | **0.008** |
| 方 0.7×0.4 | 0.230 | 0.076 |
| **長條 1.2×0.3** | 0.287 | **0.332** |

長寬比超過約 2:1 之後,擬合誤差比它想修掉的偏差還大。**那讓方法看起來是為測試道具設計的,而不是通用的。**

## 做法

障礙以**其被觀測到的表面點**表示。雷射回波本身就是物體表面上的一點 ——
那是唯一真正被觀測到的東西,對它擬合形狀是加入假設而非加入資訊。

```
群集 → CV-KF 追蹤(質心僅用於資料關聯)
     → 取最近 M 個表面點(surf_pts_max=4, range 2.5 m, min_sep 0.20 m)
     → 動態者帶群集速度,靜態者 v = 0
```

最小間距 0.20 m 是必要的:否則 4 個名額會全部花在同一小塊表面上,
物體其餘部分就沒有約束。

**牆點、未知靜態物、動態障礙現在用完全相同的表示。** 控制層一行未改。

禁區的算術也因此統一:

```
表面點半徑 = 0(點就在表面上)
margin 0.38 = 機器人半徑 0.30 + 真實緩衝 0.08 m
```

不再有「大物體得到較少緩衝」的結構性缺陷。

## 驗證

合成:圓 / 方 / 長條 / **L 型(非凸)** 四種形狀,選出的點涵蓋 100% 的可見面。

gz(6 場景 57 趟,對照形狀擬合版):

| 場景 | | 碰撞 | 柱淨距中位 | 柱負值 | solve |
|---|---|---|---|---|---|
| shapes | 擬合 | 3 | +0.015 | 2/9 | 3.66 ms |
| | **表面點** | **0** | **+0.394** | **0/9** | **1.18 ms** |

**solve time 反而降低。** 原本預期會升(每群集發 4 點而非 1 圓),
實際因為 min_sep 稀疏化 —— 單一視角看到的弧很短,篩選後多半只剩 1–2 點,
發佈的障礙數 2.2–3.3,比擬合版還少。放棄擬合不但沒有增加計算成本,還降低了。

---

# `static_cbf_safe_margin` 0.33 → 0.38

## 算術

CBF 把機器人當質點,所以 0.30 m 的機器人半徑**藏在 margin 裡**:

```
靜態 0.33 → 禁區 0.30+0.33 = 0.63,接觸 0.60  →  真實緩衝 3 cm
動態 0.38 →                                    →  真實緩衝 8 cm
```

對牆點(半徑 0.05)無妨,對未知圓柱(半徑 0.30)就只剩 3 cm。
實測失敗場景的柱淨距 −0.006 ~ +0.011,**正好是那 3 cm 減掉鬆弛**。

## A/B(4 場景 × 各 10 趟)

| 場景 | margin | 碰撞 | 柱淨距中位 | 負值 | 時間 |
|---|---|---|---|---|---|
| shapes | 0.33 | 8 | −0.009 | 8/10 | 54 s |
| | **0.38** | **3** | **+0.015** | **2/9** | 51 s |
| stopgo | 0.33 | 2 | +0.019 | 2/10 | 60 s |
| | **0.38** | **0** | **+0.044** | **0/8** | **46 s** |
| none / crossing | 兩者 | 0 / 0 | 未退步 | 0 | 相同 |

可行性:0.33 → 0.38 只損失 5% 的可通行格點(94.8% → 89.5%)。**已設為預設。**

---

# 20 場景最終結果(表面點 CBF)

**未知靜態柱負淨距:0/199 趟。** 整天所有碰撞的成因徹底消除。

**零碰撞的 10 個場景**:none、corridor、converge、overtake、parked、
**shapes**、**ell**、headon、**merge**、**wide**

粗體四個是形狀壓力測試(方形、長條、L 型非凸、1.6 m 超寬、兩顆併團)——
**非圓形障礙這一整類已解決。**

**剩餘問題全部是動態,性質各異:**

| 場景 | 碰撞 | 折返/趟 | 性質 |
|---|---|---|---|
| **bothgaps** | 10 | 12.0 | **規劃層 livelock** —— 兩開口間翻轉 8 次,88 s 未通過隔牆 |
| fast (0.55 m/s) | 7 | 0.8 | **時域結構限制** —— 1 s 內障礙移動 0.55 m |
| dense (三顆) | 7 | 3.3 | 彼此遮擋,感知不完整 |
| chase (後方追上) | 4 | 1.8 | 追趕幾何下的等速度預測,待查 |
| occlude (牆後突現) | 3 | 0.0 | **軌跡完全平順但仍擦到** —— 純粹的感知延遲 |

---

# 讓先前結論失效的四個量測缺陷

| 缺陷 | 後果 |
|---|---|
| `record.sh` 只錄 dyn_obs_{0,1,2,5} | small / ell / merge 的受測障礙從未進入碰撞偵測,「0 碰撞」測的只有牆 |
| `analyze.py` 地圖路徑寫死 `random_room` | 競技場每趟拿錯地圖算淨距,報出不存在的 −0.300 撞牆 |
| 競技場出生點距牆恰好等於機器人半徑 | 40 趟的「最小淨距」全是 t=0 的 +0.000,與避障無關 |
| 十個場景的障礙放在機器人不走的開口 | 兩條路只差 0.1 m,規劃器 10/10 選左;corridor 的障礙全程離機器人 2.92 m |

場景現在**從實測軌跡自動定位**:短車道、以通過點為中心、依障礙半徑裁短到自由空間,
並在執行前檢查**會被遭遇、不撞牆、不撞柱**三項。

---

# jerk 的正確讀法

`wz` 的 p95 在**每個配置**都恰好等於 `az_max = 1.2` —— 該軸有 13.8% 的時間頂在
加速度上限,所以 p95 必然等於上限。**那個數字測不出配置間的差異。**

該用的是飽和率與中位數:

| | 擬合 margin 0.33 | 表面點 |
|---|---|---|
| vx p95 | 0.679 | **0.587** |
| vy p95 | 0.412 | **0.263** |
| wz 中位 | 0.205 | **0.147** |
| **整體飽和** | **21.3%** | **15.1%** |

三軸全面改善。角速度是主要瓶頸(wz 飽和 13.8%,vx/vy 僅 1.9% / 1.4%),
而 `YAW_LOOKAHEAD`(前瞻弦線朝向)在合成測試上讓朝向變化需求降 76%
且與真實切線的偏差反而變小 —— **那是唯一尚未在 gz 驗證的功能。**

---

## 2026-08-01 — 20 m 樓層、146 趟配對 A/B,以及四個被推翻的推論

### 環境:bigarena(新)

20×20 m,五道隔間牆分成八個區塊、門口 2.0–2.5 m 且**錯開**(任兩區之間不只
一條通路),18 個已知雜物(在地圖上)+ 7 個未知雜物(不在地圖上)+ 10 個移動
障礙。9 m 競技場只有一條實質路線,任何放上去的障礙都是路障;這個平面才讓
route choice 成為真的決策。

生成器:`src/ammr_bringup/scripts/generate_bigarena.py`。三個寫進程式的檢查,
都是踩到才加的:

- **BFS 連通性**:寫檔前確認機器人半徑膨脹後起點到終點真的走得通
- **路線可交錯但不可重疊**:ping-pong 完全確定,直接把兩個物體往前走 600 秒
  取最小距離。原本只看「靠近時夾角 >30°」,但這些是 kinematic 物體,不會碰撞
  只會互相穿透卡住
- **最寬的先放**:搜尋是貪婪的,小到大排的話 0.82 m 長桿和 0.78 m L 型**兩個
  都會被丟掉**(實際發生過)

### 主結果:146 趟配對 A/B(75 開 yaw / 75 關 yaw)

| | 開 yaw (n=72) | 關 yaw (n=74) |
|---|---|---|
| 到達 | 100% | 100% |
| 轉/走 中位(四分位) | 26.6 (24.5–33.1) °/m | 0.0 |
| 轉/走 最差 | 312 °/m | 0.13 |
| min_h<0 比例 中位 | 0.56% | **1.80%** |
| 真實表面淨距 中位 | **+0.033 m** | **+0.008 m** |
| 接觸趟數 | **3/72** | **18/74** |
| 到達時間 中位 | 129 s | **112 s** |
| 定位誤差 中位 | 0.041 m | 0.041 m |

全向底盤不需要面向行進方向,關掉 yaw 消除了整條振盪尾巴(最差 312 → 0.13 °/m)
並快 17 秒,但淨距中位從 33 mm 掉到 8 mm、接觸從 3 趟變 18 趟。**這是真實的
取捨,沒有單純的贏家。**

### 碰撞成因(24 次接觸的逐趟分析)

23/24 是同一個障礙物 dyn_obs_1(全場最快的長距離穿越:0.30 m/s、9.3 m、
週期 62 s,每趟遭遇兩次)。而且:

- **接觸瞬間機器人速度 0.03–0.09 m/s**,障礙物 0.30 m/s
- 接觸前 3 秒的可見度**中位 87%**,沒有一趟低於 20%

**機器人是停著被撞的,不是擦過去,也不是沒看到。**

診斷:CBF 視野 N=20 × dt=0.05 = **1.0 秒**。對 0.30+0.22 = 0.52 m/s 的接近速度
只看得到 0.52 m,0.45 m 的 keep-out 要到障礙物 0.97 m 外才啟動 —— 距接觸只剩
1.3 秒。那個時間內唯一做得到的是煞停,而**停住對移動障礙物無效**:CBF 條件
`ḣ + αh ≥ 0` 要求主動拉開距離。`horizon` 已改為可由 `HORIZON` 環境變數設定。

### 四個被推翻的推論 —— 方法上的教訓

當天在同一條路線上出現大量失敗趟(250 s 到不了、轉 15000°、卡死),依序提出
並「驗證」了四個成因,**全部錯誤**:

1. 同倫類振盪(plan 在兩個門之間翻)
2. `/set_pose` 共變異數全零凍結 EKF
3. 在原點發 `/set_pose` 本身有害
4. `/initialpose` 共變異數改動害 AMCL 誤收斂

程式退回逐字元相同的版本後**仍然失敗**;而乾淨重啟後的 146 趟批次裡,那些失敗
**一趟都沒重現**(到達率 100%,定位誤差四分位間距 2 mm)。真正的原因是反覆
`pkill -KILL` 打斷 gz 造成的環境汙染。

**教訓:不要在被自己反覆中斷的環境裡診斷問題,更不要拿 n=1 的資料推系統性成因。**
過程中還為此把唯一的成功對照 bag 刪掉(`__prev_` 機制本來就是為了防這件事)。

### 其他修正

- **NVIDIA EGL**:`omni_bot_dynamic.launch.py` 缺了那三個環境變數(只有
  `omni_bot_arm.launch.py` 有,且註解宣稱導航 launch 也有)。GUI 跑起來機器人
  完全畫不出來,但模擬正常
- **物理步長**:bigarena 沿用 9 m 競技場的 `0.001`,但碰撞體從 18 變 53 個,
  RTF 掉到 0.27。改成與 random_room_dynamic 一致的 `0.01` → RTF 1.00
- **感知年輕軌跡**:age < `min_track_age` 的軌跡兩邊都不發布(mover 因 age 被拒、
  static 明寫 `age >= min_age`),等於 0.3 秒完全失明,而且**必然發生在最近距離**
  (質心滑動最快時斷軌)。2.5 m 內的年輕軌跡改以靜止表面點發布
- **規劃層淨空**:`inflation` 0.45 → 0.70。0.45 時 plan 只離已知方塊 0.43 m,
  而 CBF keep-out 是 0.45 —— 規劃器允許的路徑是控制層禁止的,兩層打架
- **`min_clearance_m` 不可信**:把牆、未知柱、移動障礙混在一起且格點量化。
  CSV 判定 5/19 次碰撞,用真實外形(點到矩形)重算是 3/18。**以 bag 為準**

### 2026-08-01 晚:三組參數掃描,以及「變異大於效應」

掃描都用環境變數跑,**沒有改任何預設值**,程式碼與 146 趟 A/B 時相同。

**視野 `HORIZON`(N=20/40/60,各 3 趟)—— 唯一有明確結論的**

| N | 視野 | 真實淨距中位 | 到達 | solve 中位 |
|---|---|---|---|---|
| 20 | 1.0 s | **+0.087** | 2/3 | 0.27 ms |
| 40 | 2.0 s | −0.300 | 2/3 | 0.78 ms |
| 60 | 3.0 s | −0.185 | **0/3** | 5.52 ms |

單調變差,而且 N=60 每趟有 **22–52 次 OSQP 迭代超限 → 緊急煞車**。約束列數是
`n_obs × N`,拉長視野把 QP 壓垮。**維持 N=20。**

**slack 權重(500–50000,各 3 趟)—— 測不出效果**

組內散度大於組間差異(例如 W=15000 三趟是 +0.239 / +0.225 / +0.009)。要在
這個變異下分辨,每組大概需要 30 趟以上。

唯一明確的是**否定結果**:提高 100 倍**完全沒有造成凍結**(停滯比例全部 2–5%,
到達 3/3)。當初從 10000 降到 500 的「ANTI-FREEZE」理由,是圓擬合障礙、規劃器
讀原始 scan 的年代調的,**在現在的配置下沒有重現**。500 這個值缺乏現在的依據,
但也沒有證據說改了會更好。

**`cbf_alpha`(1.5/0.9/0.5/0.3,各 3 趟)—— 暗示,非結論**

| α | 淨距中位 | 最差趟 | 到達 | 時間 | 路徑 |
|---|---|---|---|---|---|
| 1.5 | +0.064 | **−0.300** | 2/3 | 149 s | 32.0 m |
| 0.9 | +0.083 | −0.016 | 3/3 | 139 s | 28.5 m |
| 0.5 | +0.038 | **+0.028** | **3/3** | 182 s | 35.1 m |
| 0.3 | +0.219 | −0.001 | 2/3 | 168 s | 31.4 m |

α=0.5 是唯一三趟淨距全為正且全到達的,代價是慢 43 s、多繞 6.6 m(提早介入 =
更保守,取捨是真的)。但 α=0.3 又變差,**趨勢延伸不到底**,n=3 不足以定值。

### 為什麼參數掃描大多測不出東西

同一設定下的趟間散度,大於設定之間的差異。全天只有兩個訊號活下來:

- **yaw A/B**(n=146,配對)—— 樣本夠大
- **視野掃描**(9 趟)—— 有明確機制(OSQP 超限)佐證,不是只有數字

其餘都是在雜訊裡挑點。**這本身是個該記住的結論:這個系統的趟間變異很大,
任何 n<30 的參數比較都不該當結論用。**

### 碰撞的根因:動力學上限,不是參數

23 次接觸,接觸前 4 秒:

| | |
|---|---|
| `vx` 曾為負(真的倒車過) | **23/23** |
| 速度曾有正的遠離分量 | **23/23** |
| **距離曾經拉開** | **0/23** |
| 遠離分量中位 | −0.106 m/s |

```
機器人後退能力  vx_min −0.20 / vy_min −0.25  →  0.25 m/s
dyn_obs_1 接近                                  0.30 m/s
```

**它退得比障礙物追得慢。** `ḣ` 不可能為正,`ḣ + αh ≥ 0` 在近距離**數學上無解**,
QP 只能用 slack 吞掉。側移也救不了:1 m 距離、0.52 m/s 接近只剩 1.3 秒,側移
0.25 m/s 買到 0.33 m,而閃過 0.7 m 寬的車體需要 0.65 m。**進到約 1 m 以內,
接觸就已經註定。**

這解釋了為什麼 α 是唯一方向對的槓桿(讓介入發生在還來得及的距離),也解釋了
為什麼 slack 沒用(調整一個無法滿足的約束的懲罰,不會讓它變得可滿足)。

求解超時是次要因素:接觸前 2 秒出現 solve>50 ms 的比例是 9%(2/23),對照組
0.5% —— 相對富集 20 倍,但只解釋 23 次裡的 2 次。

**注意:`solve time` 的中位數(0.18–0.78 ms)嚴重誤導。p99 是 425 ms、最大
3191 ms,9.26% 的週期超過 50 ms 的控制週期。當天多次用中位數推論「QP 有餘裕」,
那些推論建立在錯的統計量上。**

### 2026-08-01 深夜:碰撞判準本身是錯的

當天所有「真實表面淨距」的分析,都把機器人當成 **0.30 m 的圓**。實際的碰撞體是
`omni_bot.urdf.xacro` 裡的 **0.45 × 0.45 方形**,而且會跟著車體旋轉:

| | |
|---|---|
| 內接半徑（正面） | **0.225 m** |
| 外接半徑（角落） | **0.318 m** |
| 當天用的 | 0.30 m |

正面接近時,真實餘裕比報出來的**多 7.5 公分**。當天報的接觸幾乎都落在
−0.001 ~ −0.02,**整個區間都在這個誤差之內**,而且偏向「把沒撞的說成撞了」。
使用者親眼看 GUI 沒有接觸,是對的;是量測在說謊。

`0.30` 這個數字大概是從規劃器的 `robot_radius`(0.33)或 CBF margin 的討論裡
順手拿的,**從頭到尾沒有回去對過 URDF**。

**受影響的範圍**

- 作廢:所有「接觸幾次」的絕對數字(yawon 3/72、yawoff 18/74)、24 次接觸的
  逐趟分析、「進到 1 m 以內接觸就註定」的推論
- 需改數字:alpha / slack / horizon 三組掃描的淨距欄(整體偏移)
- 不受影響:到達率、轉/走、wz 飽和、solve time、定位誤差、OSQP 迭代超限次數,
  以及**相對比較的方向**(所有組用同一個錯誤偏移)

**教訓**:一個當作判準用了一整天的物理常數,必須先跟來源核對。這比推論錯誤更
基本 —— 前六個錯誤是推理問題,這個是連前提都沒查。

### 2026-08-01 深夜:統一的量測工具,以及泛化測試

`evaluation/analyze_trials.py` —— 所有批次共用一把尺,每個數字都帶有效性檢查。
它存在的原因是當天有八個結論被推翻,而每一個都源自同一種錯誤:**讀一個數字之前
沒有先確認它在量什麼。**

它修掉的:

| | 之前 | 現在 |
|---|---|---|
| 機器人外形 | 寫死 0.30 m 圓 | 從 URDF 讀:0.45×0.45 方盒,隨車體旋轉 |
| 接觸深度 | 重疊時填固定 −0.01 旗標 | 真實穿透深度 |
| 時間對齊 | 容差 0.25 s(≈7.5 cm) | 0.05 s,並回報覆蓋率 |
| topic 存活 | 未檢查,會拿舊樣本計算 | 逐 topic 覆蓋率;`/amcl_pose` 停掉會被抓出 |
| 失敗分類 | 都算「沒到達」 | frozen(指令≈0)/ wedged(有指令無位移)/ timeout-moving |

**已知限制**:支撐輪是半徑 0.05 m 的球、位於 ±0.245 m,比車身盒子多伸出 7 cm,
且有碰撞幾何。它們在 z = −wheel_r(貼地),是否先接觸取決於障礙物底面高度。
目前的判準只用車身盒子,**因此接觸數可能仍偏低**。

### 用統一工具重量的結果

| | 固定路線 開 yaw | 固定路線 關 yaw | **隨機起終點** |
|---|---|---|---|
| 趟數 | 72 | 74 | 29 |
| 到達 | 71（1 逾時） | **74** | **21** |
| 其他結果 | — | — | 逾時 4、wedged 3、frozen 1 |
| **接觸 (<0)** | **1/72** | **6/74** | **16/29** |
| 淨距 中位 | **+0.060** | +0.028 | **−0.002** |
| 淨距 最深 | −0.142 | −0.222 | −0.224 |
| 5 cm 以內 | 26/72 | 64/74 | 18/29 |
| 轉動 中位 | 632° | 0° | 1996° |

**結論:固定路線的數字不能代表方法的性能。** 同一組參數、同一個世界,只是換成
隨機起終點,接觸率從 1.4% 升到 55%,到達率從 99% 降到 72%,而且出現固定路線
完全沒有的失敗型態(wedged 3 趟、frozen 1 趟、1 趟從未起步)。

接觸分散在 9 個不同的移動障礙上,不是集中在某一個 —— 先前「23/24 都是 dyn_obs_1」
的結論是固定路線的產物。

資料品質:對齊覆蓋率 1.00、EKF 覆蓋率 1.00,所以這些數字本身可信。
`/amcl_pose` 覆蓋率低的趟(0.00–0.44)全部對應到機器人停止移動 —— 那是
nav2_amcl 的正常行為(位移未達門檻就不更新),不是定位失效。

### 2026-08-02:20 m 樓層的隨機起終點評估,以及兩個新機制

**量測工具重建** (`evaluation/analyze_trials.py`)

車體從**視覺網格**讀,不是 URDF 的 `<collision>`:碰撞盒是 0.45×0.45 的簡化凸體,
真實車架是**半徑 0.300 m 的圓盤**(網格每 30° 扇區的最大半徑都是 0.300)。用碰撞
盒會低估接觸,用 0.6×0.6 方形則在角落多算 0.124 m。

距離改用**解析距離場**(圓:`hypot−r`;方:標準 box SDF),不取樣輪廓。取樣版在
車體中心進入障礙物內部時符號會翻轉,穿透被少算(實例:−0.283 vs 真值 −0.317)。
交叉驗證:方形障礙與手算完全吻合。

到達判定改採**批次執行當下 goal_watcher 的結果**,不從 bag 事後重建 —— 錄影會在
判定後多錄數秒,機器人滑過頭,且 odom 與 watcher 讀的 TF 不同幀。

**隨機起終點的結果(每批 ~30 趟,同一組座標)**

| 批次 | margin | 其他 | 到達 | 接觸 | 最深 |
|---|---|---|---|---|---|
| A | 0.45 | — | 26/29 | 12/29 | −0.550 |
| B | 0.45 | — | 30/30 | 6/30 | −0.349 |
| C | 0.45 | slack ×20 | 27/30 | 10/30 | −0.368 |
| D | **0.60** | — | 27/28 | 7/28 | **−0.072** |
| E | 0.75 | — | 25/28 | 11/28 | −0.313 |

**A 與 B 設定完全相同,接觸率 41% vs 20%。** 單批 30 趟只能分辨兩倍以上的差異;
任何小於此的「改善」都是雜訊。因此判準改用**最深穿透**而非接觸率。

**已否定的假設**

- **感知看不見** —— 5/6 次接觸,障礙物在接觸前 3 秒的覆蓋率 97–100%。
  (必須同時看 `/gmpc/obstacles` 與 `/gmpc/static_obstacles`:有一趟動態通道
  只有 33%,靜態是 100%。只看動態會誤判成「看不見」。)
- **slack 太便宜** —— 500 → 10000,n=30,落在自然變異內。
- **固定加大 margin** —— 0.75 反而更糟(到達 25/28、接觸 39%)。2.0 m 的門口
  扣掉兩側 0.75 只剩 0.5 m 可行走廊,QP 轉而依賴 slack。

**輸入飽和的證據**(18 次接觸,接觸前 1.5 秒)

| | 中位 | 最大 |
|---|---|---|
| `vx` 觸限 | 8% | 67% |
| `vy` 觸限 | 0% | 93% |
| `ax` 觸限 | **26%** | 72% |
| `ay` 觸限 | **26%** | 83% |

任一輸入飽和 >50% 的事件:**8/18**。速度很少飽和,但**加速度有四分之一時間在上限**
—— barrier 不是被忽略,是被要求了底盤做不到的減速。這解釋了為何加重 slack 無效。

**新機制(皆已實作、單元測試、預設關閉)**

1. `cbf_vel_margin_gain` —— keep-out 隨**接近率**增長:
   `r_eff += γ·max(0, (v_obs − v_robot)·û)`。只算迎面分量(側向通過不膨脹)、
   只對動態點。量級由煞車距離定:0.52 m/s 對 `a_y=0.6` 需 0.225 m,加上約 0.3 s
   感知延遲的 0.16 m。
2. `cbf_prune_range` / `cbf_near_steps` / `cbf_far_stride` —— 約束剪枝。列數是
   `n_obs × N`,而 `n_obs` 是**表面點數**:實測中位 8 點(160 列)、峰值 40 點
   (800 列),OSQP p99 達 425 ms、9% 週期超過 50 ms 控制週期。單元測試:14 點
   × 20 步 = 280 列 → 38 列(降 86%),`h_now` 每點仍完整,關閉後與原本逐元素相同。

**LiDAR 是 360°**(360 束,±π),不是 180°。側後方盲區無法解釋那 1/6 覆蓋率為 0
的接觸,成因仍未知。

---

## 架構稽核(對照原始碼 + 向執行中節點實查)

不是憑印象複述,是 `ros2 param get` 問正在跑批次 K 的 `/gmpc_controller`、
`/scan_obstacle_tracker`、`/global_costmap`、`/velocity_smoother`,再回頭比對檔案。
查出五處我先前的敘述與實際不符:

| 先前敘述 | 實際 | 後果 |
|---|---|---|
| `vx_max = 0.30` | **0.35** | 逃逸速度被低估 0.05 m/s,CBF 可行域邊界的估計偏保守 |
| `r_eff = R + 0.60`(全部) | 動態 0.60、**靜態牆 0.38** | 兩類障礙物 keep-out 不同,先前當成同一個 |
| 車體 = 圓盤 r=0.300 | 分析器是;**gz 物理碰撞體是 0.45×0.45 方盒** | 見下 |
| GMPC → `/cmd_vel` | 中間有 **velocity_smoother** | 速度上限同,加速度上限更鬆(1.5/1.0/2.0),行為上直通 |
| `Q_yaw` 來自 yaml | yaml 是 7.0,launch 覆寫為 0.0 | 有效值無誤,來源記錯 |

其餘全部核對相符:horizon 20 @ 20 Hz、Q_xy 15、R 1.0、α 0.5、剪枝 1.2/6/2、
vel_margin 0、yaw_lookahead 1.2、plan_blend 1.0、st/detour/stuck 皆關;
tracker 全部參數;規劃 `robot_radius 0.33` / `inflation 0.70` / `/scan_filtered` /
重規劃 3.0 s(由 `goal_to_plan_relay` 直接呼叫 planner —— 本 launch 沒有
bt_navigator 也沒有 controller_server,所以 nav2 預設 BT 的 1 Hz replan 與
local_costmap 設定都是死的);場景 18 已知 + 7 未知 + 10 移動體,速度 0.10–0.18。

### 待處理:碰撞體與量測不一致

gz 用 0.45×0.45 方盒(半邊 0.225),分析器用真實車體 0.300 m 圓盤。方盒半邊小於
圓盤半徑,所以**報表上的部分「接觸」在模擬物理裡並未真的碰撞**,是以真車尺寸重新
判定出來的。對論文而言用真車是正確的;但要讓物理與量測一致,URDF 的 collision
應改為 r=0.300 的圓柱。**現在不能改**:改了之後先前所有批次都不可比。等 α 對照
做完再一併處理,並重跑基準。

---

## 移除航向參考(不是關掉,是刪除)

`Q_yaw = 0` 從來就沒有做到「不追航向」。求解器回傳的是

    u = xi_ref[0] + delta

`xi_ref` 是參考旋量,第三個分量就是參考航向隱含的角速度。`Q_yaw` 只拿掉航向
誤差的**懲罰**,前饋的 `wz` 照樣送到底盤。批次 K 在 `Q_yaw` 已經是 0 的情況下
仍然轉了**中位數 2387°/趟**(seed7 是 5152°,走 41.7 m)。

**三種航向來源都不是零**(半徑 3 m 溫和弧線,v_nom=0.30):

| 來源 | 前饋 wz[0] | 全視野 max\|wz\| |
|---|---|---|
| 路徑切線(lookahead=0) | 0.000 | 0.158 |
| 1.2 m lookahead 弦 | 0.100 | 0.101 |
| 無航向參考 | **0.000** | **0.000** |

**「讓 SmacPlanner2D 自己控 yaw」也不行。** 規劃器確實發出有意義的朝向(訊息內
yaw 與幾何切線一致,非 NavFn 的 identity 四元數),但直接拿來當參考的話,實測
8 趟、541 條 `/plan`、113536 個路徑區段:

    中位 0.067   p90 0.432   p99 1.356   最大 18.85 rad/s   (wz_max = 0.80)
    超過 wz_max 的區段 1.4%
    走完一條 plan 累計要轉：中位 448°，最大 5805°

一趟橫越幾何上只需要 90–180°。原因是 Smac 平滑器輸出間距約 0.05–0.15 m,在這個
尺度上微小側向抖動就產生巨大朝向變化。要用它就得把轉速限幅器裝回來,而那正是
要移除的東西。

**決定:不給航向參考。** `build_reference_window` 無條件把每個取樣點的航向釘在
機器人當下的值,`xi_ref[:,2] ≡ 0`。`wz` 仍是自由輸入 —— QP 想轉隨時可以轉,只是
再也不會被要求轉向某個方向。機器人的朝向是求解器自己的選擇。

已刪除:`Q_yaw`(改為寫死 0,Q 仍需是 SE(2) 的 3×3)、`ref_yaw_lookahead`、
`ref_yaw_rate_max`、`ref_yaw_hold`、`YAW_TRACK`/`YAW_LOOKAHEAD`/`YAW_RATE_MAX`/
`YAW_HOLD` 環境掛勾、`_limit_ref_yaw_rate()`、`_interp_xy()`(唯一使用者是
lookahead 弦)、`run_yaw_ab.sh`、`run_yaw_track_ab.sh`。

**待驗證批次**(各只差一個變數):
- **M** = 無航向參考,其餘同 K → 單獨測航向參考
- **N** = M + `PLAN_BLEND=0` → 單獨測 plan 交叉淡入。它的理由被砍掉一半:當初
  的量測是位置跳動(中位 0.05 m、p90 0.44 m、最大 1.82 m)**加上**參考航向跳動
  (p90 26°、最大 63°),後者現在結構上不可能發生,前者還在。
