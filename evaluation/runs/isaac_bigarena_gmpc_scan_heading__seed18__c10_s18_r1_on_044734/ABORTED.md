# 啟動流程失敗，**不是**導航結果

中止點：就緒檢查 `!! lifecycle /planner_server active 逾時 60s`，`exit=2`。
與 `r3_s8_off_180328` 同一種 Nav2 生命週期啟動失敗。

**未進入任務、未發目標**，因此不列為該 seed 的導航結果。
依 `PREREG_heading_10seed_20260912.md` 的處置規則，以相同版本與設定
於另一 domain 重跑。

同一趟 log 內的「雷射掃到自身」「超出 1.1 mm」屬靜態幾何檢查的例行輸出，
成功的趟同樣有，**不是失敗原因**。
