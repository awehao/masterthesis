# v2_on_095217：啟動流程失敗，**不是** ON 導航結果

中止點：`/case_start` 之後的移動確認，`exit=6`。

## 是誤判，不是情境失敗

中止訊息是 10 個移動體全部「樣本不足（0）」。同一段時間的 bag 顯示：

* `/model/dyn_obs_*/pose` 每顆各 **1247 筆**（Isaac 一直在發布）
* 驅動目標對「由 YAML 獨立重算的排程」逐點相同（0.0000 mm）
* Isaac 真實位置對排程 中位 2.00–2.81 mm
* `/case_start` 1 則、`phase_epoch = 102.610 s`（有限值，已採用）

所以障礙物確實依排程移動，是 `case_start_check.py moving` 的固定取樣視窗
在 DDS 訂閱配對完成前就結束，拿到 0 筆。

## 已修正

`evaluation/case_start_check.py`：把「等訂閱配對」與「取樣視窗」分成兩段，
先等到每個主題至少收到一則（上限 `--discover`，預設 12 s）再清空並開始計時。
修正後「零樣本」才真的代表那個移動體沒有發位姿。

## 本趟仍然有用的資料

三個新增錄製項目都確認收到資料，錄製變更本身有效：

| topic | 筆數 |
|---|---|
| `/wheel_guard/status` | 1275 |
| `/cmd_vel_smoothed` | 1349 |
| `/gmpc/diag_v2` | 1234 |

`/gmpc/heading` 為 0 筆屬預期 —— 該主題只在控制迴圈有路徑時發布，
本趟在發目標前就中止。

端點隔離乾淨：`/goal_pose` 訂閱者 3 個，與 OFF 相同
（起跑前已清掉 2026-09-10 17:41／17:45 遺留的 `gui:=true` launch）。
