# 啟動失敗，非任務結果

`solver_freespace` 場景檢查**誤判**而中止，尚未下任何命令。

誤判原因：關鍵字表含 `'box'`，且掃描範圍包含**機器人自身**的 prim，
於是比對到 `/World/omni_bot/.../base_camera_link/box` ——
那是相機的網格，不是場景中的接觸目標。

處置：把關鍵字比對改為**白名單式的場景組成檢查**
（`/World` 的直接子節點只能是機器人、地面與環境用 prim），
並**排除機器人自身子樹**。修正後以相同設定重跑。

本目錄不計為任務結果。
