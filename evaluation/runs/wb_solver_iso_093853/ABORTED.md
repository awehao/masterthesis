# 啟動失敗，非任務結果（第二次場景檢查誤判）

`solver_freespace` 白名單檢查把 `/World` 下的 `Physics_Materials` 與 `Looks`
判為非預期物件而中止。那兩個是 USD 匯入產生的**資源組織節點**（型別為空字串），
不是接觸目標。

累計兩次誤判，都出在「用名稱／型別判斷什麼是接觸目標」。
處置：改為**語意檢查** —— 掃描機器人與地面**以外**是否存在碰撞體
（`UsdPhysics.CollisionAPI`）。材質、Looks、燈光都沒有碰撞體，不會再誤判。

本目錄不計為任務結果。
