# Both openings patrolled at once: no route exists until one clears, so waiting is the only correct answer.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_bothgaps

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [1.50, 2.80]
    end:    [1.50, 4.00]
    speed:  0.2
    radius: 0.25
    height: 1.0
  - name:   dyn_obs_6
    start:  [5.10, 4.00]
    end:    [5.10, 2.80]
    speed:  0.2
    radius: 0.25
    height: 1.0
