# Faster mover overtaking from behind up the same approach: _blocking needs fx > 0, so the detour cannot see it at all and only the CBF answers. Routed through the gap -- the straight diagonal is wall.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_chase

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [5.30, 1.20]
    end:    [4.70, 5.20]
    speed:  0.34
    radius: 0.25
    height: 1.0
