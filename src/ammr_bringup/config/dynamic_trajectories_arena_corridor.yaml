# Mover sweeping ALONG the approach to the right gap: the shared-corridor geometry, where there is nothing to go around.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_corridor

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [5.10, 0.90]
    end:    [5.10, 3.10]
    speed:  0.15
    radius: 0.25
    height: 1.0
