# Oblique crossing: the general closing angle, between the perpendicular and parallel cases already covered.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_diagonal

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [0.40, 2.60]
    end:    [5.40, 0.20]
    speed:  0.28
    radius: 0.25
    height: 1.0
