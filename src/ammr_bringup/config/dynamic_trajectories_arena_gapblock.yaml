# Mover patrolling the RIGHT gap. The gap is too narrow to pass it, so the answers are to wait or to re-route through the left gap.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_gapblock

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [1.50, 2.80]
    end:    [1.50, 4.00]
    speed:  0.22
    radius: 0.25
    height: 1.0
