# Mover coming down the approach to the right gap towards the robot: closing speed is the sum of both, the shortest warning of any geometry here. Routed through the gap because the diagonal between the two openings is solid wall.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_headon

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [4.70, 5.20]
    end:    [5.30, 1.20]
    speed:  0.3
    radius: 0.25
    height: 1.0
