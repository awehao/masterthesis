# Two movers 0.5 m apart: close enough that clustering may join them into one blob whose centre belongs to neither.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_merge

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [0.00, 1.00]
    end:    [6.60, 1.00]
    speed:  0.26
    radius: 0.25
    height: 1.0
  - name:   dyn_obs_6
    start:  [0.00, 1.50]
    end:    [6.60, 1.50]
    speed:  0.26
    radius: 0.25
    height: 1.0
