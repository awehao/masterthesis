# Mover hidden behind the divider, emerging into the right gap. Tests what happens when the obstacle is simply not observable until it is close -- the case a 1.0 s horizon has least room for.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_occlude

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [6.60, 3.85]
    end:    [4.90, 3.85]
    speed:  0.35
    radius: 0.25
    height: 1.0
