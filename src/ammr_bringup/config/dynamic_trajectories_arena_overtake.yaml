# A slower mover going the SAME way through the right gap: _blocking requires fx > 0, so the detour was never designed for this.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_overtake

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [5.10, 1.40]
    end:    [5.10, 5.60]
    speed:  0.1
    radius: 0.25
    height: 1.0
