# A 0.7x0.4 box and a 1.2x0.3 cart, one each side of the divider. Circle fitting degenerates on a flat face and a single lidar view cannot see an object's depth, so this is where the covering discs are actually tested.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_shapes

dynamic_obstacles:

  - name:   dyn_obs_1
    start:  [1.20, 1.80]
    end:    [7.60, 1.80]
    speed:  0.25
    radius: 0.25
    height: 1.0
  - name:   dyn_obs_2
    start:  [7.60, 6.80]
    end:    [1.20, 6.80]
    speed:  0.25
    radius: 0.25
    height: 1.0
