# A 0.15 m mover: few laser returns, so it may be clustered away entirely rather than merely localised badly.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_small

dynamic_obstacles:

  - name:   dyn_obs_3
    start:  [1.50, 2.80]
    end:    [1.50, 4.00]
    speed:  0.18
    radius: 0.25
    height: 1.0
