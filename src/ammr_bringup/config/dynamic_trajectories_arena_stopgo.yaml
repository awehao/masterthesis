# Short, fast sweep across the approach: the mover reverses every ~2 s, so constant-velocity extrapolation is wrong most of the time.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_stopgo

dynamic_obstacles:

  - name:   dyn_obs_0
    start:  [5.10, 1.90]
    end:    [5.10, 2.70]
    speed:  0.35
    radius: 0.25
    height: 1.0
