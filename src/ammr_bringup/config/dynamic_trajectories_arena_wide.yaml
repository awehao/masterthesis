# A 1.6 m body, wider than either opening: it can never be in a gap and be passed, so timing is the whole problem.
#
# Arena scenario, generated by
# src/ammr_bringup/scripts/generate_arena_scenarios.py
# Run with:  ARENA=1 TRAJ=arena_wide

dynamic_obstacles:

  - name:   dyn_obs_5
    start:  [0.67, 2.39]
    end:    [1.47, 1.99]
    speed:  0.22
    radius: 0.25
    height: 1.0
