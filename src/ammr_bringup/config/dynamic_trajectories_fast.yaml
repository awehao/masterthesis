# Perpendicular crossing at 0.40 m/s: the 2.0 m detour trigger leaves ~5 s of warning.
#
# Generated by evaluation/make_scenarios.py from a recorded
# baseline route, so the obstacles are actually MET. Validate with
#   python3 evaluation/check_scenario.py fast

dynamic_obstacles:

  - name:   dyn_obs_2
    start:  [4.33, 7.70]
    end:    [6.89, 5.17]
    speed:  0.4
    radius: 0.25
    height: 1.0
