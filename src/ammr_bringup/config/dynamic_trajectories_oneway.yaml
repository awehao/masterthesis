# Long one-way traversal (period >> encounter), approximating a pedestrian rather than a ping-pong prop.
#
# Generated by evaluation/make_scenarios.py from a recorded
# baseline route, so the obstacles are actually MET. Validate with
#   python3 evaluation/check_scenario.py oneway

dynamic_obstacles:

  - name:   dyn_obs_2
    start:  [4.33, 7.70]
    end:    [6.89, 5.17]
    speed:  0.3
    radius: 0.25
    height: 1.0
