# A slower mover going the SAME way: _blocking needs fx > 0, so this is the geometry the detour never targeted.
#
# Generated by evaluation/make_scenarios.py from a recorded
# baseline route, so the obstacles are actually MET. Validate with
#   python3 evaluation/check_scenario.py overtake

dynamic_obstacles:

  - name:   dyn_obs_2
    start:  [4.13, 4.94]
    end:    [7.09, 7.93]
    speed:  0.12
    radius: 0.25
    height: 1.0
