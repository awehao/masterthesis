# A stationary "dynamic" obstacle: tests the static/dynamic split that left a pillar unowned.
#
# Generated by evaluation/make_scenarios.py from a recorded
# baseline route, so the obstacles are actually MET. Validate with
#   python3 evaluation/check_scenario.py parked

dynamic_obstacles:

  - name:   dyn_obs_2
    start:  [5.61, 6.43]
    end:    [5.61, 6.43]
    speed:  0.0
    radius: 0.25
    height: 1.0
