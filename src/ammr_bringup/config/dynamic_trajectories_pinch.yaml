# Mover in a passage too narrow to go around: the "do not commit, yield instead" branch.
#
# Generated by evaluation/make_scenarios.py from a recorded
# baseline route, so the obstacles are actually MET. Validate with
#   python3 evaluation/check_scenario.py pinch

dynamic_obstacles:

  - name:   dyn_obs_2
    start:  [4.44, 4.56]
    end:    [2.12, 3.97]
    speed:  0.2
    radius: 0.25
    height: 1.0
