# 未來工作（Future Work）

> 本檔記錄已驗證為「正確方向、但屬獨立子題 / 超出本論文範圍」的延伸工作。
> 本論文核心貢獻為：**LiDAR 感知未知動態障礙物 + SE(2) GMPC + 全時域 CBF 避障 + 動/靜雙層架構**。
> 以下各項皆為「讓系統從受控評估走向真實部署」所需，但各自為獨立研究題目，列為未來工作。

---

## 1. 未知障礙物環境下的穩健定位（最關鍵）

### 動機
本論文評估時，定位變數以無漂移里程計（數位孿生真值）固定，以**隔離並量化感知+避障的貢獻**（控制變數法）。
實驗中觀察到：純 **AMCL（2D scan-match 對先驗靜態地圖）在未知障礙物比例高時會崩潰**：
- 動態障礙物 + 未知靜態物（不在先驗地圖）→ 大量「對不上地圖」的雷射回波 → 粒子濾波收斂到錯誤位姿
- 失敗呈兩態：**瞬間跳變（jump）** 與 **持續錯估（drift）**
- 嘗試 `robot_localization` EKF 融合（/odom + /amcl_pose + outlier rejection）可吸收**跳變**，但**擋不住持續錯估** → 後期仍飄移
- AMCL 一旦飄移，下游全毀（costmap 標錯、planner 繞錯、控制器追錯、map-frame 感知錯）

→ 此為 2D scan-match 定位的**本質極限**，非調參可根治。

### 方向（文獻支持）
**即時 SLAM 取代純 AMCL**——將偵測到的未知靜態物**即時寫入佔據網格地圖**，使其從「干擾雜訊」變為「地圖的一部分」，scan-matching 不再錯亂：

| 方案 | 適用性（本平台 2D LiDAR、無 IMU） | 備註 |
|---|---|---|
| **slam_toolbox** | ✅ 最務實 | ROS2 標準 2D SLAM；localization 模式可用先驗地圖 + 即時更新新靜態物；pose-graph 最佳化（含迴圈閉合）較 AMCL 粒子濾波穩健 |
| **Cartographer** | ⚠️ 可行但調校複雜 | 2D 模式可用，submap/loop-closure 參數多 |
| **Hector SLAM** | ⚠️ ROS2 支援較弱 | 純 scan-match、無迴圈閉合，特徵稀疏處易漂 |
| **FAST-LIO（LiDAR-Inertial）** | ❌ 需先加硬體 | 需 **IMU + 3D LiDAR**；局部一致性高、不易被未建圖物/人影帶偏；本平台為 2D 雷射、無 IMU，須先擴充感測器 |

### 與本研究感知的整合
SLAM 解決**未知靜態物**（寫入地圖），但**動態障礙物仍會干擾 scan-matching**（SLAM 亦靠 scan-match）。
完整方案 = **SLAM + 動態物 masking**：利用本研究 `scan_obstacle_tracker` 已估出的動態物位置，將對應角度的雷射點從 SLAM/localization 的輸入 scan 濾除 → SLAM 只對靜態結構建圖/定位。

### 參考文獻方向
- 即時建圖更新 / 佔據網格更新（Hector SLAM、EAST 環境感知安全追蹤導航）
- 緊耦合 LiDAR-Inertial Odometry（FAST-LIO）：高頻 IMU + LiDAR 緊耦合，局部一致性高
- 多感測器融合（EKF / AUKF）：LiDAR + IMU + 輪式里程計

---

## 2. 感知座標系：本體座標 + Ego-motion 補償（免疫定位漂移）

### 動機
本研究 `scan_obstacle_tracker` 之動/靜分離依「速度門檻」。追蹤座標系的選擇有三難：
- **map (amcl)**：amcl 跳變使靜態牆看似移動 → 假障礙物（naive 40%，加過濾後 73%）
- **odom**：模擬中為真值可行，但**真實世界輪式 odom 會漂** → 靜態物在 odom 中看似移動 → 不可轉移真機
- **body frame + ego-motion 補償**：理論最穩、真機可部署，本論文未實作

### 方向
在**感測器/本體座標**聚類追蹤，以機器人**瞬時速度（twist）**補償自身運動：
- 關鍵：**累積位置（pose）會漂，但瞬時速度（twist，來自輪速/IMU）短期準、不累積**
- 每幀以 twist×dt 預測「靜止物應在哪」，與實測位置之差即障礙物真實運動 → 估速度
- CBF 所需的「相對距離（雷射直接量）+ 相對速度（ego 補償）」**完全不需絕對座標** → 免疫定位誤差/漂移
- 此為文獻所指之 body-frame tracking，最符合「真實感知 pipeline」論述

---

## 3. 感知延遲補償（降低動態場景碰撞）

### 動機
真實感知 vs 真值的本質差異：scan 偵測有延遲（`min_track_age`=3 連續幀 + KF 速度收斂 ~0.3–0.5s）→ CBF 反應慢半拍 → 動態場景 min clearance 偏緊、偶有擦撞（真值版無此延遲）。

### 方向
- 降 `min_track_age`（早發布，權衡假陽性）
- KF 速度過程噪 `sigma_vel` 調大（速度估更快收斂）
- CBF `safe_margin` 加大以補償偵測延遲
- 或在 CBF 中加入偵測延遲之預測提前量

---

## 4. 控制與求解效能

- **CBF QP 求解時間**：scan 偵測之障礙物較噪 → QP 變難、常達 max_iter(8000)，solve_time 升至 5–45 ms（真值版 ~1.7 ms）。可調 max_iter / QP 條件化，驗證 real-time trade-off。
- **OSQP polish / warm-start**：進一步壓低求解時間與抖動。

---

## 5. 論文後段（既有規劃）

- Whole-body MPC：SE(2) 底座 + 7-DOF 手臂協調（透過伴隨矩陣 Ad_G 與手臂 Jacobian 線性疊加）
- 接觸操作（開門 / 拉抽屜）：等式約束 + 力/力矩限制；ESO 抗擾、Passivity 接觸穩定
- Monte Carlo 自動化（多隨機種子 × 多方法，box plot）、CBF 參數靈敏度分析（α、d_safe、slack 的安全 vs 效率 Pareto）

---

## 評估方法學說明（供報告誠實陳述）

> 本論文於數位孿生平台中，**以無漂移里程計固定定位變數**，俾隔離並量化「LiDAR 感知 + CBF 避障 + 雙層架構」之貢獻。
> 此為標準之控制變數實驗設計；上列**未知障礙物環境下之穩健定位（SLAM/LIO）**與**本體座標感知**為使系統走向真實部署之延伸工作，各自為獨立研究題目，列為未來工作。
