# 第一階段實作總結：動態環境下的移動底盤預測導航

> 本文只記錄**已實作並經實驗驗證**的內容。被否證的假設與失敗的調參過程不列入，
> 相關記錄見 `第四次進度報告.md` 第七節。
>
> 平台：ROS 2 Jazzy + Gazebo Harmonic，四輪全向底盤 `omni_bot`。
> 最後更新：2026-08-26

---

## 摘要

第一階段建立了一套完整的動態避障導航系統，核心是 **SE(2) 幾何模型預測控制
(GMPC)** 搭配**全時域預測式控制屏障函數 (CBF)**，並在其下方加入一層不依賴任何
分類的 **Raw-scan Safety Shield**。

在 100 條隨機路線的 `bigarena` 場景中，與 nav2 的兩個基線在**相同硬體運動上限**
下比較，配對 83 條路線的結果：

| 方法 | 碰撞 | 到達 | 最深穿透 |
|---|---|---|---|
| **GMPC + CBF + Shield** | **1 / 83** | **83 / 83** | **0.1 cm** |
| nav2 MPPI | 6 / 83 | 64 / 83 | 36.4 cm |
| nav2 RPP | 30 / 83 | 60 / 83 | 57.5 cm |

唯一一次接觸的估計穿透深度為 1 mm，約為掃描誤差餘裕 $\varepsilon_{\text{scan}} = 0.03$ m
的 $1/30$，亦小於 Shield 使用的總幾何餘裕 $\varepsilon = 0.05$ m。由於此深度已接近碰撞
真值、LiDAR 測距與圓盤近似三者的建模解析度，應描述為**極淺的邊界接觸**，不宜用來
主張 Shield 理論上不應發生碰撞 —— 這三個量並非同一個尺度。

---

## 一、系統架構

### 1.1 資料流

```
       /scan (原始)
          │
          ├──────────────────────────────────────┐
          │                                      │
     scan_relay                                  │
     （遮蔽自體結構扇區）                          │
          │                                      │
     /scan_filtered                              │
          │                                      │
   scan_obstacle_tracker                         │
   （群集 → KF 追蹤 → 動/靜分流 → 表面點）        │
          │                                      │
   /gmpc/obstacles                               │
   /gmpc/static_obstacles                        │
          │                                      │
      gmpc_node                                  │
      （SE(2) GMPC + 全時域 CBF，凝聚式 QP）      │
          │                                      │
   /cmd_vel_nav                                  │
          │                                      │
   velocity_smoother                             │
          │                                      │
   /cmd_vel_pre_shield                           │
          │                                      │
   scan_safety_shield  ◄───────────────────────┘
   （直接讀原始 /scan，不經任何分類）
          │
      /cmd_vel  →  gz VelocityControl
```

**關鍵設計**：屏障層讀的是**原始 `/scan`**，不是 `/scan_filtered`。被感知層過濾掉的
點，正是這一層必須看得到的那一組。

### 1.2 主要節點與軟體模組

**ROS 節點**（有 `main()` 與 `console_scripts` entry point）

| 套件 | 節點 | 職責 | 主實驗啟用 |
|---|---|---|---|
| `ammr_wholebody_mpc` | `gmpc_node` | SE(2) GMPC + CBF 主控制器 | ✓ |
| | `scan_obstacle_tracker` | 群集、KF 追蹤、動靜分流、表面點 | ✓ |
| | `scan_safety_shield` | 原始掃描安全屏障 | ✓ |
| | `goal_to_plan_relay` | 目標 → 全域路徑 | ✓ |
| | `obstacle_aggregator` | 由 gz 真值取得動態障礙物 | ✗ 僅 `truth` 模式 |
| `ammr_bringup` | `scan_relay` | 自體遮擋扇區遮蔽 | ✓ |
| | `omni_drive_controller` | 輪速 ↔ 車體速度轉換 | ✓ |
| | `dynamic_obstacle_driver` | 場景動態體驅動 | ✓ |
| | `map_publisher` / `odom_tf_broadcaster` | 地圖與 TF | ✓ |

`obstacle_aggregator` 的啟動條件是 `IfCondition(use_truth)`，因此在
`chain_three100` 使用的掃描模式中**並未啟動**；它是真值基線用的對照組件。

**內部模組**（被上述節點 import，非獨立節點）

| 模組 | 職責 |
|---|---|
| `gmpc.py` | 凝聚式 QP 建構與 OSQP 求解 |
| `se2.py` | SE(2) 李群運算（`log` / `exp` / `ad`） |
| `kalman_tracker.py` | 等速模型 KF |
| `path_processor.py` | 路徑重取樣與時間參數化 |
| `detour.py` | 側向繞行偏移 |
| `arm_limits.py` | Lite 6 運動包絡（第二階段用） |

---

## 二、硬體層：由輪系推導的真實上限

先前使用的 $v_x = \pm 0.35$ m/s **超出硬體 26%**，所有用它算出的逃逸速度與可行性
邊界都偏樂觀。第一階段把上限改為從輪系 Jacobian 推導。

### 2.1 底盤 Jacobian

四個全向輪，半徑 $r = 0.05$ m，軸距 $L = 0.245$ m，
輪速上限 $\omega_{\max} = 5.55$ rad/s，輪加速度上限 $\alpha_{\max} = 125$ rad/s²。

$$v_x = \frac{r}{2}(\omega_d - \omega_b), \qquad
  v_y = \frac{r}{2}(\omega_a - \omega_c), \qquad
  \dot\varphi = \frac{r}{4L}\sum_i \omega_i$$

兩個平移軸使用**不同的輪對**，因此可獨立飽和；旋轉共用四輪。

### 2.2 導出的上限

| 量 | 值 | 說明 |
|---|---|---|
| $v_{x,\max} = v_{y,\max}$ | **0.2775 m/s** | $r\omega_{\max}$ |
| 對角合速度 | **0.3924 m/s** | $\sqrt2 \times 0.2775$ |
| $\dot\varphi_{\max}$ | **1.1327 rad/s** | $\frac{r}{L}\omega_{\max}$，僅在零平移時 |
| $a_{x,\max} = a_{y,\max}$ | **6.25 m/s²** | |
| $\ddot\varphi_{\max}$ | **25.51 rad/s²** | 底盤偏航角加速度（非輪角加速度） |

**三個方法（GMPC、MPPI、RPP）現已統一使用這組上限。** 實測每趟指令峰值中位數
確認：三方 $\max|v_x|$、$\max|v_y|$ 皆為 0.278，合速度峰值 0.392（RPP 因純追蹤
特性 $v_y \equiv 0$）。

### 2.3 輪速耦合約束

速度盒約束 $|v_x| \le 0.2775 \wedge |v_y| \le 0.2775$ 並不等價於輪速可行。由
Jacobian 反解可得真正的約束是一個多面體：

$$|v_x \pm L\dot\varphi| \le r\omega_{\max}, \qquad
  |v_y \pm L\dot\varphi| \le r\omega_{\max}$$

寫成矩陣形式是**雙邊**不等式（OSQP 的 $l \le Az \le u$ 形式）：

$$-r\omega_{\max}\mathbf{1} \;\le\; W\xi \;\le\; r\omega_{\max}\mathbf{1},
\qquad
W = \begin{bmatrix} 0 & 1 & L \\ -1 & 0 & L \\ 0 & -1 & L \\ 1 & 0 & L \end{bmatrix}$$

加速度耦合同理：

$$-r\alpha_{\max}\Delta t\,\mathbf{1} \;\le\; W(u_k - u_{k-1})
  \;\le\; r\alpha_{\max}\Delta t\,\mathbf{1}$$

每個時間步加入 4 條速度列與 4 條加速度列。這使得**高速轉彎時平移上限自動收緊**，
與實機行為一致。（`gmpc.py`，`wheel_coupling: True`）

---

## 三、控制層：SE(2) 幾何 MPC

### 3.1 為何用李群而非 $(x, y, \theta)$

先前的 $(x,y,\theta)$ MPC 失敗於兩點：角度繞行（wrap-around）造成誤差跳變，以及
只在當前狀態線性化，導致需要轉彎的時域立刻離開小擾動區。SE(2) 表述兩者都解決。

### 3.2 誤差狀態與動態

決策變數為相對於參考體速的偏差：

$$z = [\delta\xi_0; \delta\xi_1; \dots; \delta\xi_{N-1}] \in \mathbb{R}^{3N},
\qquad \delta\xi_k = u_k - \xi_{\text{ref}}(k)$$

測地誤差與其線性化動態：

$$e_0 = \log\!\left(X_{\text{ref}}(0)^{-1} X_{\text{now}}\right)^\vee, \qquad
  e_{k+1} = A_d(k)\, e_k + \Delta t\, \delta\xi_k$$

$$A_d(k) = I - \Delta t \cdot \mathrm{ad}(\xi_{\text{ref}}(k))$$

**關鍵**：線性化在時域的**每一步 $k$** 都繞著 $\xi_{\text{ref}}(k)$ 進行，而非只繞
當前狀態。因此一個需要連續轉彎的時域，仍是對著「已經在轉」的參考做修正，誤差始終
留在小擾動區。且 $\log$ 恢復的 $\theta \in (-\pi, \pi]$、平移分量為體座標，機器人
轉過 $\pm\pi$ 時誤差向量仍平滑指回參考。

### 3.3 凝聚式 QP

$$E = [e_1; \dots; e_N] = \Phi e_0 + \Gamma z$$

$$J = \sum_{k=1}^{N-1} e_k^\top Q e_k + e_N^\top Q_f e_N
    + \sum_{k=0}^{N-1} \delta\xi_k^\top R\, \delta\xi_k
    + \sum_{k} \Delta u_k^\top S\, \Delta u_k$$

其中 $\Delta u$ 項須先寫成 $z$ 的仿射函數。以 $u_k = \xi_{\text{ref}}(k) + \delta\xi_k$
展開，$\Delta u = Dz + c$，$D$ 為一階差分矩陣，$c$ 收集參考體速的逐步差與接縫項
$\xi_{\text{ref}}(0) - \xi_{\text{prev}}$（使第一步從機器人**實際施加**的速度平滑接上）。
於是

$$J = (\Phi e_0 + \Gamma z)^\top \bar{Q}(\Phi e_0 + \Gamma z)
    + z^\top \bar{R} z + (Dz + c)^\top \bar{S}(Dz + c)$$

$$P = 2\left(\Gamma^\top \bar{Q}\Gamma + \bar{R} + D^\top \bar{S} D\right), \qquad
  q = 2\left(\Gamma^\top \bar{Q}\Phi e_0 + D^\top \bar{S} c\right)$$

堆疊為 OSQP 標準式 $l \le A_{\text{total}} z \le u$ 求解。

### 3.4 已驗證的參數

| 參數 | 值 | 理由 |
|---|---|---|
| `control_frequency` | 20 Hz | $\Delta t = 0.05$ s |
| `horizon` | 20 步 | 1.0 s 前瞻（H30/H40 反而退化） |
| `v_nominal` | 0.22 m/s | 低於底盤單軸上限 0.2775；bigarena 動態體實測 0.10–0.14 m/s |
| $Q_{xy}$ | 15.0 | 由 25 降低，減少過度積極追蹤 |
| $R_{v}$ | 2.0 | 由 1.0 提高，抑制 KF 抖動傳入 |
| $S_{v}$ | 15.0 | $\Delta u$ 平滑代價 |
| $Q_f$ 倍率 | 5.0 | 終端權重 |

### 3.5 移除航向參考

全向底盤的航向與平移解耦。追蹤路徑切線方向的航向參考只會製造不必要的旋轉並消耗
輪速預算（旋轉共用四輪，會直接壓縮平移上限）。移除後航向自由，QP 可把全部輪速
預算用於平移避障。

---

## 四、感知層

### 4.1 自體遮擋遮蔽（`scan_relay`）

底盤四根對角支柱位於 45° / 135° / 225° / 315°，會產生自體回波。以

- `blocked_centers_deg = 45, 135, 225, 315`
- `blocked_halfwidth_deg = 10.0`

遮蔽這四個扇區。

### 4.2 群集與地圖相減

| 參數 | 值 | 說明 |
|---|---|---|
| `cluster_gap` | 0.30 m | 相鄰回波間距超過即切分 |
| `min_cluster_pts` | 2 | |
| `max_cluster_radius` | **1.20 m** | 由 0.60 放寬 |
| `static_inflation` | 0.50 m | 距離牆面此範圍內的回波視為靜態 |

`max_cluster_radius` 由 0.60 放寬到 1.20 是必要的修正。`extent` 定義為離群集質心
最遠的回波，因此側向觀察的長條物只貢獻一半長度：

- `dyn_obs_2` (1.2 × 0.3 m) → extent 0.60，**正好卡在門檻上**
- `dyn_obs_5` (1.6 × 0.4 m) → extent 0.80，**永遠被丟棄**

實測兩次與 `dyn_obs_2` 的接觸中，最近距離時 CBF 收到的表面點數為**零** —— 不是很少，
是沒有。原 0.60 m 門檻會**確定性地**排除大型延伸物體，因此放寬至 1.20 m。牆面本來就已由地圖
相減（0.50 m 膨脹）移除，所以此上限最終保留下來只用於排除明顯的牆面殘餘與異常大型
叢集，**不再承擔精細的動靜態判定功能**。

### 4.3 追蹤與動/靜分流

在 **odom 座標系**追蹤而非 map：AMCL 的位姿跳變會讓靜止牆面看起來在移動，破壞速度
閘門。追蹤後再轉換到 map 發布，AMCL 偏移在 CBF 實際使用的相對距離中會抵銷。

等速模型 Kalman 濾波器，關聯與發布門檻：

| 參數 | 值 |
|---|---|
| `assoc_gate` | 0.80 m |
| `track_timeout` | 0.60 s |
| `min_track_age` | 3 次更新 |
| `min_track_speed` | 0.10 m/s |
| `static_window_s` | 2.0 s |
| `min_net_speed` | 0.05 m/s |

**淨位移動靜分流**：除了瞬時速度，另外檢查 2.0 s 窗口內的**平均**速度
$\|\Delta p\| / \Delta t \ge 0.05$ m/s。瞬時速度會被 KF 抖動與質心滑移污染，淨位移
對此穩健得多。

### 4.4 表面點表示

障礙物不以單一質心加半徑表示，而是取叢集上最靠近機器人的數個**表面點**：

| 參數 | 值 |
|---|---|
| `surf_pts_max` | 4 點 / 叢集 |
| `surf_pts_range` | 2.5 m |
| `surf_pts_min_sep` | 0.20 m |

理由：質心對延伸物體是無意義的幾何參考。一個 1.6 m 長的物體，質心離它的可見表面
可達 0.8 m，用「質心 + 半徑」的圓形近似會嚴重高估或低估間距。表面點直接描述機器人
實際會撞到的那一面，且每一幀依最近優先重新選取。

---

## 五、安全層之一：全時域預測式 CBF

### 5.1 屏障函數

對第 $i$ 個障礙物、時域第 $k$ 步：

$$h_i(k) = \|p(k) - o_i(k)\|^2 - r_{\text{eff}}^2$$

其中 $o_i(k)$ 由等速模型外推。有效半徑為

$$r_{\text{eff},i} = r_i + m_i$$

$r_i$ 是障礙物輸入自帶的半徑，$m_i$ 是該類障礙物使用的 keep-out margin。
**車體半徑並未另外相加** —— margin 本身已承擔車體尺度與安全間距。

對直接落在障礙物表面的動態表面點，追蹤器發布 $r_i = 0$（點已經在表面上），
keep-out 全部來自 $m_i = 0.60$ m；地圖牆點則帶自身的點半徑與較小的靜態 margin。
因此動態 keep-out 是 0.60 m，而非 $0.30 + 0 + 0.60 = 0.90$ m。

離散 CBF 條件，帶鬆弛：

$$\dot h_i(k) + \alpha\, h_i(k) \ge -\varepsilon_k$$

代價中加入 $\rho \sum_k \varepsilon_k^2$。**這是「全時域」的關鍵**：約束不只施加在
當前步，而是整條預測時域的每一步，因此控制器能對「一秒後才會發生的接近」提前反應，
而不是等到距離已經很近才煞車。

### 5.2 約束剪枝

全時域 × 全障礙物會讓 QP 規模爆炸。以兩個機制控制：

- `cbf_prune_range = 1.2` m：超出此距離的障礙物不生成約束
- `cbf_near_steps = 6` / `cbf_far_stride = 2`：前 6 步逐步加入，之後每 2 步取一次

### 5.3 實驗使用的參數

| 參數 | 值 |
|---|---|
| `cbf_alpha` | 0.5 |
| `cbf_safe_margin` | 0.60 m |
| `margin_mode` | fixed |
| `cbf_slack_weight` | $5 \times 10^2$ |
| `cbf_eps0_scale` | 30.0 |
| `cbf_danger_thresh` | 0.4 |

`margin_mode = fixed` 是實驗結論：推導式（隨速度變化的）margin 讓碰撞從 7 次上升到
39 次。

### 5.4 可行性邊界

給定逃逸速度 $v_{\text{esc}}$、障礙物速度 $v_{\text{obs}}$，CBF 能保證安全的最小
起始距離為

$$d^* = \frac{-(v_{\text{esc}} - v_{\text{obs}}) + \sqrt{(v_{\text{esc}} - v_{\text{obs}})^2 + \alpha^2 r_{\text{eff}}^2}}{\alpha}$$

此式描述的是**一維徑向、等速接近、且以最大可用逃逸速度沿該方向遠離**時的局部可行性
邊界。若障礙物持續徑向接近且其速度不低於可用逃逸速度，則**僅靠沿該方向退離**無法長期
維持安全 —— 此時 $\dot h$ 無法為正，$\dot h + \alpha h \ge 0$ 在 keep-out 內無解。

但實際的全向底盤仍可能以**側向運動繞開**，因此這不是「任何 CBF 都無法保證安全」的
全域結論。它的用途是界定場景設計：動態體速度若設得太高，失敗會來自幾何而非控制器，
該情境就無法測量方法本身。`bigarena` 的動態體因此設在 0.10–0.14 m/s（由實驗 bag 的 `/model/dyn_obs_*/pose`
位移實測，與歸檔 YAML 逐一吻合）。

---

## 六、安全層之二：Raw-scan Safety Shield

### 6.1 動機

第一階段最重要的診斷結論：**感知看得到障礙物 ≠ 它會進入 CBF。**

一個回波要成為 CBF 約束，需通過五道硬閘門：群集 → 建立追蹤 → 年齡 ≥ 3 →
瞬時速度 ≥ 0.10 → 淨位移 ≥ 0.05。決定性案例 route 27：追蹤在 100% 的週期都存在、
年齡中位數 218、KF 位置準確，但**只有 23% 的週期被發布**。68% 的拒絕來自瞬時速度
閘門，而該障礙物的設定速度正好是 0.10，門檻也正好是 0.10。

把門檻降到 0.05 只讓發布率從 23% 升到 32%，因為 KF 估出的速度是 0.049 而非真值
0.100 —— 質心沿著 1.6 m 長物體的可見面滑動。

瞬時速度與淨位移兩道閘門建立在**同一個有偏的質心估計**上，偏差方向一致，因此
**單純調整這兩道門檻無法可靠排除延伸物體的漏發問題**；在本次 0.10 與 0.05 m/s 的
實驗中，降低門檻僅帶來部分改善（23% → 32%）。這不等於窮舉過所有門檻值，但足以說明
問題出在估計量本身而非門檻大小。

結論：需要一層**完全不需要分類**的保護。

### 6.2 屏障設計

直接對原始掃描的每一個回波 $i$ 施加一階屏障。設 $n_i$ 為指向該回波的單位法向、
$r_i$ 為回波在機體座標的位置：

$$n_i^\top\!\left(v + \omega J r_i\right) \le \alpha\,(d_i - d_{\text{stop},i}),
\qquad J = \begin{bmatrix} 0 & -1 \\ 1 & 0\end{bmatrix}$$

公式保留 $\omega J r_i$ 以描述一般非圓形車體因旋轉產生的表面速度。

**但對目前的圓盤近似，這一項恆為零。** 實作取 $r_i = R\,n_i$，而 $J n_i \perp n_i$，故

$$n_i^\top J r_i = R\, n_i^\top J n_i = 0$$

（數值驗證：2000 個隨機方向下 $\max|n_i^\top J r_i| = 2.8\times10^{-17}$）

幾何上這是對的：圓盤自轉不改變圓周到外部點的徑向間距。因此目前 Shield 只透過速度盒
約束 $\omega$，不透過屏障列。**等第二階段加入非圓形底盤或機械手臂，這一項才會真正
產生作用** —— 屆時 $r_i$ 不再平行於 $n_i$。

動態停止距離：

$$d_{\text{stop},i} = d_0 + v_{\text{app},i}\,\tau_{\text{eff}}
                     + \frac{v_{\text{app},i}^2}{2 a_{\text{brake}}} + \varepsilon$$

四項分別是：靜止時的基本間隙、感測與致動延遲期間走過的距離、煞停距離、量測餘裕。

### 6.3 求解：Motzkin 鬆弛

不解 QP，改用**逐次投影到最違反的半空間**，迭代次數有界以保證執行時間有上限：

- 正常階段固定執行最多 **6 次**最違反半空間投影（`proj_iters = 6`）
- 若殘餘違反仍超過門檻，切換至「不得繼續接近」的較弱可行集合，並執行**有界的額外
  投影（最多 30 次）**；必要時輸出零命令
- **速度盒約束列入投影集合**（共 6 列），而非事後裁切 —— 事後裁切會破壞已滿足的
  障礙物約束

因此最壞情況的執行次數仍有上限，但**不等於每個週期都只執行 6 次**。固定 6 次只保證
執行時間有界，不保證最終命令滿足全部約束 —— 這正是降級路徑存在的理由。

### 6.4 參數

| 參數 | 值 | 意義 |
|---|---|---|
| `alpha` | 2.0 | 屏障鬆弛率 |
| `d0` | 0.05 m | 零速間隙 |
| `tau` | 0.15 s | 感測 + 控制 + 致動延遲 |
| `a_brake` | 6.25 m/s² | 由輪系 Jacobian 而來 |
| `eps_scan` | 0.03 m | 測距雜訊餘裕 |
| `eps_footprint` | 0.02 m | 車體擬合餘裕 |
| $\varepsilon$ 合計 | **0.05 m** | |
| `range_of_interest` | 2.0 m | |
| `emergency_d` | 0.15 m | 低於此距離單一回波也生效 |
| `scan_timeout` | 0.5 s | 掃描過期則限速 |
| `stale_speed` | 0.05 m/s | 過期時的速度上限 |
| `max_points` | 360 | 每週期成本上限 |

### 6.5 診斷輸出

發布 `/shield/diag`，18 個欄位：週期編號、是否作用、點數、最小距離、速度修正量、
輸入/輸出三軸速度、掃描年齡、是否過期、投影前後違反數、迭代次數、是否降級、
是否未解決。

---

## 七、實驗設計

### 7.1 場景 `bigarena`

- 約 **20 × 20 m** 的開放場地（座標範圍 $[-1.4, 18.4]^2$）
- **10 個動態障礙物**：混合方塊與圓柱，尺寸 0.15 m 半徑到 1.6 × 0.4 m
- **7 個未知靜態障礙物**：不在地圖中，只能由感測發現
- **16 段牆體**

設計原則：**動態體不會針對機器人的當前位置做反應式追撞**。它們沿固定交通軌跡運行，
遭遇由固定軌跡、隨機起訖點與時序三者共同產生。

（障礙物的數量、尺寸、速度與軌跡本身仍是人為設計的，因此不宜宣稱「完全沒有安排」；
可宣稱的是場景不針對受測控制器做適應。）

### 7.2 隨機起訖點

**100 條**隨機起訖點路線構成計畫測試集合，所有方法跑同一組。

**83 條**是三個方法皆留下完整有效紀錄的**共同配對子集**，統計比較一律在這 83 條上進行。
各方法因 bring-up 失敗損失的趟次不同（96 / 92 / 91），若直接比較三組不同的路線集合，
路線難度會洩漏進結果。全文請持續區分這兩個數字。

### 7.3 量測方法

| 項目 | 做法 |
|---|---|
| 車體幾何 | 半徑 0.300 m 圓柱（與視覺網格一致，先前的 0.45 m 方盒使模擬車體小 7.5 cm） |
| 碰撞判定 | 對 gz **真值**位姿計算間距，非控制器自身的估計 |
| 量測窗口 | 截斷於首次抵達，避免抵達後的殘餘運動污染 |
| 平滑度 | $\lvert a\rvert$ 的 p95（非 std，std 被 DDS 時序離群值主導） |

### 7.4 趟間變異

同路線、同設定重複執行時，最小間距的差異約為 **0.043–0.479 m**，而部分設定之間的
效應約為 **0.11 m**。注意 $0.043 < 0.11 < 0.479$：

**單趟隨機變異可能低於、接近、也可能高於參數效應。** 因此設定之間的比較必須採用
配對路線、重複實驗與週期層級的機制指標，不能依賴少數趟次的平均值。

零事件的單側 95% 上界以 $3/N$ 估計。

---

## 八、實驗結果

### 8.1 三方對照（配對 83 條路線）

| 方法 | 碰撞 | 到達 | 最深穿透 | 時間平均速度 | 到達時間 |
|---|---|---|---|---|---|
| **GMPC + CBF + Shield** | **1** | **83/83** | **0.1 cm** | 0.209 m/s | 102 s |
| nav2 MPPI | 6 | 64/83 | 36.4 cm | 0.373 m/s | 48 s |
| nav2 RPP | 30 | 60/83 | 57.5 cm | 0.226 m/s | 75 s |

> **速度欄的定義**：抵達前的累積里程 ÷ 抵達時間，逐趟計算後取中位數，直接從 bag 的
> `/odom` 重算。
>
> 不可使用 `batch.csv` 的 `path_length_m / arrival_time_s`：前者累加**整趟**里程，
> 後者只計到**首次抵達**，兩者窗口不一致。該比值對 MPPI 會得到 0.392（恰好等於理論
> 對角上限 $\sqrt2 \times 0.2775$）且單趟最大達 0.504，**超過物理上限**，是視窗
> 不匹配造成的假象。修正後三方皆低於 0.3924。

未配對的全量：GMPC 1/96 且 96/96 到達；MPPI 7/92 且 69/92；RPP 34/91 且 65/91。

**到達率與碰撞率必須一起看**：放棄困難路線的控制器會用那些路線換來低碰撞數。
MPPI 有 23% 的路線未能到達。

### 8.2 CBF × Shield 二乘二消融（配對 28 條，四組皆 28/28 到達）

| 組 | CBF | Shield | 碰撞 | 間距中位 | 最差 |
|---|---|---|---|---|---|
| A | ✗ | ✗ | 22 | −0.300 | −0.514 |
| B | ✓ | ✗ | 3 | +0.180 | −0.300 |
| C | ✗ | ✓ | 2 | +0.080 | −0.300 |
| **D** | ✓ | ✓ | **0** | +0.150 | **+0.022** |

三個要點：

1. **在這 28 條配對路線上，兩層呈現互補作用**。單獨使用任一層仍觀察到 2–3 次碰撞、
   最深 0.30 m；只有組合配置 D 未觀察到碰撞，且**最差間距為正**。這是本批路線上的
   觀察結果，不足以證明對所有場景的邏輯必要性。
2. **兩層作用機制不同**。CBF 把整個間距分布抬高（中位數 +0.180），因為它提前避開；
   Shield 讓分布維持在低位（+0.080）但守住尾端。
3. **C 並不笨拙**。原本預期「只有 shield」會煞車太晚、繞路差、卡住 —— 實際上它每趟
   都到達，且比 B 更快、路徑更短。所以保留兩層的理由不能建立在 C 很差，而是建立在
   尾端行為上。

### 8.3 週期層級歸因（96 趟，201,700 個控制週期）

> 本節與 8.4 的所有數字由 `evaluation/summarise_shield_attribution.py` 直接讀 bag 產生，
> 寫入 `evaluation/results/shield_attribution.json`；圖表與本文皆讀該檔，無任何手抄常數。

| 事件 | 定義 | 週期數 | 比例 |
|---|---|---|---|
| CBF 建立約束 | 該週期 CBF builder 產生至少一條屏障列 | 201,252 | 99.8% |
| 進入危險區 | 最小屏障值 $h < 0.4$ | 109,148 | 54.1% |
| CBF $h < 0$ | | 16,302 | 8.1% |
| Shield 實際介入 | 修改了輸出命令（$\lvert\Delta v\rvert > 10^{-4}$） | 3,695 | **1.83%** |

「建立約束」指 QP 中**存在**該列，不代表該約束是 binding 的，也不代表 $h$ 低於任何
門檻 —— 這三者意義不同。

在 **3,695** 個 Shield 介入週期中，有 **3,460** 個
能對齊到 0.2 s 內的 CBF 診斷值：

- 1,683 個（**48.6%**）同時有 CBF $h < 0$（兩層都認為危險）
- 1,777 個（**51.4%**）的 CBF $h \ge 0$ —— CBF 未判定為危險，Shield 仍然介入

其餘 **235 個未納入**（無可對齊的診斷值）。**分母是
3,460，不是 3,695**；若誤用後者，後一項會變成
48.1%。

`/gmpc/diag` 並非穩定 20 Hz（單趟 2,407 則對 shield 的 3,252 則），對齊間隔 p99 約
0.22 s，因此門檻取 0.20 s；取 0.05 s 會排除 15% 的週期，且被排除的並非隨機樣本。

**趟間變異很大**：同一批的前 40 趟得到 63.0%，後 56 趟得到 40.6%。這個比例應理解為
批次層級的匯總，不宜用少數趟次推斷。

那 51.4% 表示**兩層對危險的判定並不完全重合**：Shield 能補上 CBF 未判定為危險的
近距離情況，因此不是冗餘的。

但要說清楚推論的界線：$h \ge 0$ 只代表 CBF 算出的最小屏障值未進入危險區，**不足以**
斷定該障礙物沒有進入 CBF 集合、或分類鏈把它濾掉了。障礙物也可能已在集合內，只是
CBF 的 margin、預測位置或幾何表示與 Shield 不同。要把原因確定下來，需要逐障礙物、
逐週期的集合成員診斷，目前尚未實作。

### 8.4 Shield 只作用在尾端

兩層的作用距離必須先講定義，不同定義得到的數字差很多。以 40 趟、81,029 個
`/gmpc/diag` 週期重算最近 CBF 障礙物距離（欄位 `near_d`）：

| 狀態 | 週期數 | 距離中位 | p25 – p75 |
|---|---|---|---|
| CBF：QP 存在屏障列 | 181,927 | **0.801 m** | 0.709 – 0.967 |
| CBF：且 $h < 0.4$ | 101,826 | 0.717 m | 0.641 – 0.763 |
| CBF：且 $h < 0$ | 14,726 | 0.590 m | 0.497 – 0.776 |
| Shield：修改命令時 | 3,695 | 0.125 m | 0.106 – 0.161 |

正文採最容易解釋的定義：**QP 實際建立至少一條屏障列時，最近障礙物距離中位數為
0.801 m；Shield 實際修改命令時，介入距離中位數為
0.125 m。** 另外兩個定義一併列出，避免只挑好看的數字。

無論採哪個定義，分工都一致：**CBF 在 0.59–0.80 m
就開始作用，Shield 只在約 0.12 m 介入**。

也因此，**單靠 Shield 無法達到零碰撞**：當機器人被兩側夾擊時屏障不可行，此時 Shield
輸出零速。

適用範圍要講清楚。**在原始掃描有效且未過期、且投影約束可滿足時**，Shield 的設計目標
是限制機器人主動朝可見回波接近。它**不**涵蓋以下情況：

- 障礙物主動撞向靜止的機器人
- 掃描不到、被車體遮擋、或掃描過期
- 幾何上已不可避免的接近
- 投影未收斂而進入降級模式時

且動態停止距離使用的是**指令**接近速度與**估計**的煞車能力，兩者都不是真值。
因此這是一個有條件的設計目標，不是嚴格意義上的安全保證。

### 8.5 平滑度

| 方法 | $\lvert a_x\rvert$ p95 | $\lvert a_y\rvert$ p95 | $\lvert\ddot\varphi\rvert$ p95 |
|---|---|---|---|
| GMPC + CBF + Shield | 0.569 | 0.565 | **0.172** |
| MPPI | 0.073 | 0.592 | 2.050 |
| RPP | 0.000 | 0（無側移） | 1.207 |

- 平移加速度較大（0.569 對 0.073 m/s²），源於**主動調速避障**；但僅佔硬體包絡
  $0.569 / 6.25 = 9.1\%$，加速度飽和比例 0.029%。
- **旋轉平滑度優於基線一個數量級**（0.172 對 2.050 rad/s²）。全向底盤以側移取代
  轉向，旋轉指令幾乎不變。
- 平移方向反轉率 0.41 次/秒（MPPI 0.07），安全層讓反轉率從純 GMPC 的 0.18 增至
  0.41。這是目前已知的可改善項。

---

## 九、輔助工具與基礎設施

| 項目 | 內容 |
|---|---|
| 評估管線 | `evaluation/analyze.py`（單趟 → 指標列）、`run_omnibot_dynamic.sh`（批次） |
| 批次腳本 | `chain_three100.sh`、`chain_ablation2x2.sh`、`chain_shield*.sh` 等 |
| 繪圖 | `plot_three100.py`、`plot_ablation2x2.py`、`plot_shield_attrib.py` |
| 診斷主題 | `/gmpc/diag`（22 欄）、`/shield/diag`（18 欄）、`/gmpc/tracks_debug`（每追蹤 12 欄） |
| 歸因統計 | `summarise_shield_attribution.py` → `shield_attribution.json` + `SHIELD_ATTRIBUTION_SUMMARY.md`，圖表與報告皆讀此 JSON |
| 場景生成 | `ammr_bringup/scripts/generate_arena.py` |
| 視覺化 | Foxglove Bridge（`ws://localhost:8765`） |
| 顯示卡設定 | 混合 PRIME 機器需強制 NVIDIA EGL（已寫入 launch） |

每個控制週期都無條件發布 `control_cycle_id`，使得 GMPC 與 Shield 的診斷可以逐週期
對齊 —— 8.3 節的歸因分析完全依賴這一點。

---

## 十、第一階段的方法學結論

1. **感知的「看得見」與控制的「受約束」是兩件事。** 中間的分類鏈是實際的失效點，
   而不是控制器本身。

2. **當兩道閘門吃同一個有偏估計時，僅調門檻難以從根本消除這類共同偏差。** 質心速度
   同時餵給瞬時速度與淨位移兩道閘門，兩者的偏差方向一致，所以調整門檻只會同時放寬
   或同時收緊，無法把「延伸物體的質心估計偏低」這件事分離出來。

3. **需要一層不做分類的保護。** Raw-scan Shield 的價值不在它常常作用（只有
   1.83% 的週期），而在它能補上 CBF 未判定為危險的近距離情形。在可對齊
   CBF 診斷值的 Shield 介入週期中，有 51.4% 發生於 $h \ge 0$ 的狀態，
   說明兩層的危險判定並不完全重合；但僅憑此結果**不能**斷定原因一定是障礙物未進入
   CBF 集合。

4. **層與層之間的順序有實際後果。** 屏障必須放在平滑器**之下**、直接讀原始掃描；
   放在上面或讀過濾後的掃描，都會讓它繼承它本要防範的那個失效模式。

5. **趟間變異必須先量。** 單趟隨機變異的範圍（0.043–0.479 m）橫跨了參數效應量
   （約 0.11 m），可能低於、接近或高於它。在這種情況下，任何基於少數趟次平均的
   結論都不可信，必須改用配對路線與週期層級指標。

---

## 附錄：完整實驗設定

```bash
MASK_HW=10.0                    # scan_relay 遮蔽半寬（度）
BIGARENA=1  TRAJ=bigarena_traffic
MARGIN_MODE=fixed               # 非推導式
CBF_SAFE_MARGIN=0.60
CBF_ALPHA=0.5
CBF_PRUNE_RANGE=1.2
CBF_FAR_STRIDE=2
PLANNER_SCAN=/scan_filtered
PLAN_BLEND=0.0
POSE_SOURCE=odom
HARD_K0_STATIC=0
SHIELD=1
HORIZON=20                      # dt=0.05s → 1.0s 前瞻
```
