# 第三次進度 — 修改紀錄（自第二次進度報告後）

> 第二次報告結束狀態：ammr_base 機器人、GMPC + 全時域 CBF、**以 Gazebo 真值障礙物**達動態避障 90%。
> 第三次主軸：① 升級到帶感測器的真實 omni 平台 ② **以真實 LiDAR 感知取代真值障礙物** ③ 動/靜雙層避障 ④ 穩健定位。

---

## 一、平台移植：ammr_base → omni_bot（新平台）

從下載的 ROS1 / Gazebo-Classic 模型 `my_omnibot_description` 移植到 **ROS2 Jazzy + gz Sim 8 (Harmonic)**。

**新套件 `src/my_omnibot_description/`**：
- `urdf/omni_bot.urdf.xacro`（+ `rim.xacro`/`roller.xacro`/`lidar.xacro`/`camera.xacro`）：底盤 mesh + 4 全向輪（視覺）+ LiDAR + RealSense D435i
- 控制沿用 cmd_vel 級：gz `VelocityControl` 吃 `/cmd_vel`；4 顆零摩擦支撐球撐起底盤
- LiDAR：Classic `libgazebo_ros_laser` → gz `gpu_lidar`
- 相機：Classic realsense plugin → gz `rgbd_camera` + **外部 `realsense2_description`**（apt 安裝）

**移植踩到的坑與修法**：
| 問題 | 修法 |
|---|---|
| 車體/滾子 mesh 不顯示（gz 解不開 `package://`）| xacro 內改絕對 `file://$(find ...)` 路徑 |
| 機器人自身 odom 漂（dead-reckoning 積分歪掉，終點離真值 40m）| 改用 gz `OdometryPublisher` 真值里程計 → `/odom_raw` → `odom_tf_broadcaster` |
| LiDAR 被底盤 4 根對角柱遮擋（自體回波 → amcl 崩）| `scan_relay` 加角度遮罩（~45/135/225/315° 各 ±15°）|

**靜態導航驗證**：GMPC tracking RMSE **0.049m**、GMPC+CBF 皆 success（solve < 0.5ms）。

---

## 二、Sprint B：真實 LiDAR 感知（核心貢獻）

**新節點 `src/ammr_wholebody_mpc/.../scan_obstacle_tracker.py`**：
```
/scan → 聚類(相鄰分群) → Hungarian 關聯 → 每 track 一個 KF → /gmpc/obstacles [x,y,r,vx,vy]
```
**完全不讀障礙物真值**，純從 LiDAR 偵測未知動態物，格式與真值版 `obstacle_aggregator` 相同 → GMPC+CBF 直接吃。

**感知架構演進（重要的試誤過程）**：
| 版本 | 真實成功率 | 問題/結論 |
|---|---|---|
| map-frame + 地圖背景相減（naive）| **40%** | amcl 跳變 → 牆落到空地格 → 一幀 11 個牆幻影 → CBF 卡死 |
| + 速度門檻 + 界外拒絕 + 膨脹 0.5 | **73%** | 過濾掉靜止牆漏判（速度門檻最關鍵）|
| odom-frame 追蹤（免疫 amcl）| —（驗證可行後棄用）| 真實世界 odom 會漂、不可轉移真機 |
| **回到 map-frame + 過濾**（定案）| **73%** | 全程真實 amcl，剩 4 趟失敗為 amcl 定位飄移（非感知）|

**消融結論**：你加的感知過濾把成功率 **40% → 73%**（同 amcl 框架）。

---

## 三、動/靜雙層避障架構

- **靜態物（含未知）**：nav2 costmap obstacle_layer 從 /scan 標記 → planner 繞行
- **動態物**：scan_obstacle_tracker（速度門檻）→ CBF 預測式閃避
- 速度門檻同時擔任「動/靜分流」：會動的進 CBF、不會動的留給 planner
- 世界加 **3 個未知靜態障礙物**（黃色、不在地圖中）測試雙層；動態障礙物改黑色、dyn_obs_1 軌跡終點調離起點

---

## 四、穩健定位（最大的攻關）

**問題**：amcl（純 2D scan-match 對先驗地圖）遇未知障礙物（動態+未知靜態）→ scan-match 錯亂 → 定位飄移/崩潰 → 污染下游全部。

**解法演進**：
1. EKF 融合（robot_localization：/odom + /amcl_pose + outlier rejection）→ 擋得住跳變、擋不住持續錯估（不夠）
2. 評估 SLAM（slam_toolbox / Cartographer / FAST-LIO）→ 大子題，列未來工作
3. **beam-skip + EKF（定案，成功）**：
   - **amcl beam-skip**（`likelihood_field_prob` + `do_beamskip`，120 beams）：**忽略打到未知物的光束**，只用對得上地圖的牆定位
   - **EKF**：融合 /odom + /amcl_pose、平滑輸出、拒絕殘餘跳變
   - **結果：6 個未知物壓在路徑上、定位完全不飄** → 不需 SLAM，且真機可部署
- 順手把 GMPC 用的 nav 設定從誤導的 `nav2_params_mppi.yaml` 拆出乾淨的 **`nav2_localization.yaml`**（移除 MPPI controller 段）

---

## 五、評估框架與工程修正

- **新增 `evaluation/run_omnibot_static.sh`**：靜態批次
- **新增 `evaluation/run_omnibot_dynamic.sh`**：動態批次，`scan` vs `truth` 可切換
- harness 修正：pidfile 單例鎖、trap 真退出、record.sh 乾淨 finalize（避免壞 bag）、goal 連發 5 次（治 DDS 漏送）、headless gz、EKF 加入清理
- 視覺化 RViz → **Foxglove**（依使用者習慣）
- **jerk 問題已解**：`gmpc.py` 已改為接受 OSQP `maximum iterations reached` 之次佳解、不緊急煞車 → 動態 jerk 0.56/0.23/0.67（vs 舊版 spike 18/6/36），**無需 velocity_smoother**

---

## 六、新增 / 修改檔案總表

**新增**
- `src/my_omnibot_description/`（整包：urdf×5、meshes、launch×3、config×2）
- `src/ammr_wholebody_mpc/ammr_wholebody_mpc/scan_obstacle_tracker.py`
- `src/my_omnibot_description/config/{ekf_fusion.yaml, nav2_localization.yaml}`
- `evaluation/run_omnibot_static.sh`、`run_omnibot_dynamic.sh`
- `未來工作.md`、本檔

**修改**
- `src/ammr_bringup/ammr_bringup/scan_relay.py`（角度遮罩參數）
- `src/ammr_bringup/config/dynamic_trajectories.yaml`（dyn_obs_1 終點 → (0,3.0)）
- `src/ammr_bringup/worlds/random_room_dynamic.sdf`（障礙物改黑、+3 未知靜態物）
- `src/ammr_wholebody_mpc/setup.py`（scan_obstacle_tracker entry）

---

## 七、關鍵成果與數據

| 項目 | 結果 |
|---|---|
| 平台 | omni_bot（ROS2/gz）+ LiDAR + 深度相機，靜態 RMSE 0.049m |
| **真實感知避障** | naive 40% → **感知過濾 73%**（全程真實 amcl）|
| **穩健定位** | beam-skip + EKF → **未知障礙物環境定位不飄**（不需 SLAM）|
| 雙層架構 | planner 繞未知靜態 + CBF 閃動態，已驗證 |
| jerk | 0.56/0.23/0.67（已解，無 spike）|
| solve time | 動態 scan 場景 5–6ms（max ~45ms，仍 < 50ms 週期）|

---

## 八、已知限制（誠實陳述）

- **碰撞**：scan 偵測延遲（min_track_age + KF 收斂 ~0.3–0.5s）→ CBF 慢半拍 → min clearance 偏緊、偶有擦撞（真實感知 vs 真值的本質代價，可調 safe_margin）
- **solve time**：scan 障礙物較噪 → CBF QP 偶達 max_iter → 求解變慢
- 動態障礙物為 kinematic（碰撞當失敗誠實記錄）

---

## 九、未來工作（詳見 `未來工作.md`）

- 穩健定位升級：SLAM（slam_toolbox / Cartographer）/ LiDAR-Inertial（FAST-LIO，需加 IMU+3D）+ 動態物 scan masking（beam-skip+EKF 已涵蓋多數情況，SLAM 為極端未知環境之替代）
- body-frame + ego-motion 補償感知（免疫 odom 漂移）
- 感知延遲補償（降碰撞）
- Whole-body MPC + 接觸操作

---

## 十、待收尾（進行中）

- [ ] scan 15 趟 + truth 15 趟對照（證明 scan ≈ truth）
- [ ] success 以 `final_dist<0.5` 計分（誠實）
- [ ] 雙層 demo 出圖
- [ ] 更新進度報告大綱
