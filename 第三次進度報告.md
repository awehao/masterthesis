# 第三次進度報告 — 自第二次報告後之所有修改(2026/06/04 → 06/29)

> 論文題目：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments（動態環境下移動機械臂預測導航與受約束操作）
> 報告者：陳文顥　指導教授：陳榮順 教授　單位：國立清華大學
>
> 本文由 git 變動歸納（6/4→6/29、200+ 次 auto-commit、40+ 個原始檔）。每項以「**做了什麼 → 為什麼 → 優點 → 缺點/代價**」陳述，誠實標示已驗證與待補。
>
> **第二次報告結束狀態**：`ammr_base` 點質點 + SE(2) GMPC + 全時域預測式 CBF，**以 Gazebo 真值障礙物**達動態避障 success 9/10 = 90%（baselines 30–40%）。
> **第三次主軸**：① 升級到帶感測器的真實 omni 平台 ② **真實 LiDAR 感知取代真值** ③ 動/靜雙層避障 ④ 穩健定位 ⑤ **公平 benchmark 與量測方法學**。

---

# 第一部分：研究背景與架構

## 壹、研究目標與系統架構

### 總目標
建立基於 **ROS2** 之移動操作機器人系統,於**動態環境**中以**底座與手臂全身協調控制**完成**接觸操作**任務(開門、拉抽屜等),並以**數位孿生**(gz Sim)平台驗證。

### 本期聚焦：Phase 1 — 具預測能力的動態避障導航
全身操作之前,底座須能在動態、未知環境中安全且即時地導航到位。本期把這條主線做到「**真實感知 + 穩健定位 + 可實機部署**」的程度,並建立可公平比較的量化框架。

### 方法堆疊
| 層 | 方法 | 角色 |
|---|---|---|
| 控制 | **SE(2) 幾何 MPC(GMPC)**,全向底盤 B=I | 軌跡追蹤 + 側移利用 |
| 安全 | **全時域預測式 CBF** + KF 等速度預測 | horizon 內動態避障保證 |
| 感知 | **`scan_obstacle_tracker`**(LiDAR 聚類+KF)| 偵測未知動態物 |
| 規劃 | nav2 planner + costmap | 已知/未知**靜態**繞行 |
| 定位 | **amcl beam-skip + EKF** | 未知物環境不飄 |

### 方法數學(精要)

**SE(2) GMPC** — 決策變數 $\delta\xi_k = \xi_k - \xi_{\mathrm{ref}}(k)$,實際輸入 $u = \xi_{\mathrm{ref}} + \delta\xi$;體座標測地誤差 $e = \log(X_{\mathrm{ref}}^{-1}X)^\vee$。成本整理為 OSQP 標準二次型:
$$J=\sum_{k=1}^{N-1} e_k^\top Q\,e_k + e_N^\top Q_f\,e_N + \sum_{k=0}^{N-1}\delta\xi_k^\top R\,\delta\xi_k$$
約束:速度 box $u_{\min}\le \xi_{\mathrm{ref}}(k)+\delta\xi_k\le u_{\max}$、加速度 $|u_k-u_{k-1}|\le a_{\max}\Delta t$。

**全時域預測式 CBF** — 對每障礙 $i$、每步 $k$,障礙等速度預測 $o_i(k)=o_i(0)+v_i k\Delta t$($v_i$ 來自 KF);障壁函數與安全約束:
$$h_i(k)=\lVert p(k)-o_i(k)\rVert^2 - r_{\mathrm{eff},i}^2,\qquad \dot h_i(k)+\alpha\,h_i(k)\ge -\varepsilon_k$$

**★ 本期關鍵修改** — CBF 把機器人當**點**,故有效半徑須含機器人半徑,且**動/靜分離**:
$$r_{\mathrm{eff},i}=r_i+\underbrace{m_i}_{\text{per-obstacle margin}},\qquad m_i=\begin{cases}0.38 & \text{動態障礙(開闊)}\\ 0.33 & \text{靜態牆點(窄縫)}\end{cases}$$
其中機器人實體半徑 = 0.30(0.6m 直徑圓盤);$m$ 同時涵蓋「機器人半徑 + 安全緩衝」。動態 0.38 留 8cm 緩衝擋感知延遲;靜態 0.33 較小以保留 0.9m 窄縫通行。

### 系統架構(ROS2 pipeline)
```
                    /scan ──► scan_obstacle_tracker ──┬─► /gmpc/obstacles  [x,y,r,vx,vy]  (動態)
                  (LiDAR)     (地圖相減→聚類→Hungarian   └─► /gmpc/static_obstacles (牆點 v=0)
                              →每track KF；速度門檻分流)            │
  /goal_pose ─► goal_to_plan_relay ─► planner_server ─► /plan      │
                                       (costmap:靜態)     │        │
                                                          ▼        ▼
                                              gmpc_controller (GMPC + 全時域 CBF)
                                                          │  動態 margin 0.38 / 靜態 0.33
                                                          ▼
                                         (velocity_smoother*) ─► /cmd_vel ─► gz
  定位:  /scan ─► amcl(beam-skip) ─► /amcl_pose ─┐
        /odom(gz真值里程計) ───────────────────► EKF ─► map→odom (穩定)
                                          *smoother 可開關(消融)
```

- **動/靜雙層**:planner 走 costmap 處理**已知靜態**、CBF 走 `/gmpc/obstacles` 處理**動態**;速度門檻把「會動/不會動」分流。
- **量測**:`record.sh` 錄 bag → `analyze.py`(真值座標 + 全障礙物碰撞 + p95 jerk)→ 五方法可比 CSV。

---

## 貳、上期回顧（第二次進度報告）

第二次報告完成「預測導航」主線的**演算法核心**,以 **Gazebo 真值障礙物**驗證:

1. **SE(2) 幾何 MPC(GMPC)**:修掉舊版 (x,y,θ) 在連續旋轉跨 ±π 時的誤差跳變發散;改用李群測地誤差 + 沿參考**多點線性化**;底盤為**全向**(輸入矩陣 B=I,側移 v_y 可控)。
2. **Kalman Filter 障礙物預測**:4 狀態等速度模型,估速度餵入控制器。
3. **全時域預測式 CBF**:對整個 horizon 每步加安全約束、含相對速度項;slack 設計(所有步軟、當下步罰權 ×30 near-hard)避免進安全圈瞬間無解;依 min_h 做 Q/slack gain-scheduling。
4. **量化評估框架** + 動態場景 4 方法 × N=10:**GMPC+CBF 9/10 = 90%**(RPP 30%、MPPI 40%、GMPC 30%);tracking RMSE 0.91m;solve 1.71ms。

**上期之限制(正是本期要解的)**：
| 上期限制 | 本期對策 |
|---|---|
| 障礙物用 **Gazebo 真值** → 不可實機 | **真實 LiDAR 感知**(主軸②)|
| 點質點、無感測器 | **帶 LiDAR/相機的 omni 平台**(主軸①)|
| 定位遇未知物會崩 | **beam-skip + EKF 穩健定位**(主軸④)|
| 行為僅 **reactive within horizon**(非繞路)| 動/靜雙層 + 未來 proactive detour |
| 量測:baselines 缺 channel、用估測座標 | **真值座標 + 全障礙物公平量測**(主軸⑤)|

---

# 第二部分：本期修改詳述（6/4 → 6/29）

## 變動總覽（依檔案）

| 類別 | 主要檔案 | 行數 |
|---|---|---|
| 真實感知 | `scan_obstacle_tracker.py`（新）| +493 |
| 評估/量測 | `analyze.py` | +286 |
| 平台 | `omni_bot.urdf.xacro` + `rim/roller/lidar/camera.xacro`（新）| +323 |
| baseline 對比 | `nav2_baseline_{mppi,rpp}.yaml`、`omni_bot_baseline.launch.py`（新）| +740 |
| 定位 | `nav2_localization.yaml`、`ekf_fusion.yaml`（新）| +268 |
| harness | `run_omnibot_dynamic.sh`、`run_omnibot_static.sh`（新）| +363 |
| 控制器 | `gmpc_node.py`(+51)、`gmpc.py`(+8)、`gmpc_params.yaml` | +60 |
| 場景 | `random_room_dynamic.sdf`、`dynamic_trajectories.yaml` | +105 |

---

## 一、平台移植：ammr_base（點質點）→ omni_bot（帶感測器真實平台）

**做了什麼**：把下載的 ROS1/Gazebo-Classic 模型 `my_omnibot_description` 移植到 **ROS2 Jazzy + gz Sim 8（Harmonic）**。新增 urdf（底盤 mesh + 4 全向輪 + 2D LiDAR + RealSense D435i）、launch×4、config×2。控制沿用 `cmd_vel` 級（gz `VelocityControl`）。

**為什麼**：第二次的點質點無法檢驗「真實感測 → 感知 → 控制」全鏈,也無法宣稱可實機部署。要做真實 LiDAR 感知,必先有會發 `/scan` 的真實平台。

**移植踩到的坑與修法**
| 問題 | 修法 |
|---|---|
| 車體/滾子 mesh 不顯示（gz 解不開 `package://`）| xacro 改絕對 `file://$(find …)` |
| 自身 odom 漂（dead-reckoning 積分,終點離真值 40m → AMCL 大跳）| 改 gz `OdometryPublisher` **真值里程計** → `/odom_raw` → `odom_tf_broadcaster` |
| LiDAR 被底盤 4 根對角柱遮擋（自體回波 → amcl 崩）| `scan_relay` 角度遮罩（45/135/225/315° 各 ±15°）|

- **優點**:有真實感測器、可量化、朝真機部署;靜態追蹤 RMSE **0.049m**。
- **缺點/代價**:`OdometryPublisher` 真值里程計是**模擬器特權**(真機需動捕/SLAM 提供真值);mesh 絕對路徑降低可攜性。**此真值里程計後來成為公平碰撞量測的基礎(見七)**。

---

## 二、真實 LiDAR 感知：`scan_obstacle_tracker.py`（+493 行,核心貢獻）

**做了什麼**
```
/scan → 地圖背景相減 → 相鄰聚類 → Hungarian 關聯 → 每 track 一個等速度 KF → /gmpc/obstacles [x,y,r,vx,vy]
```
**完全不讀障礙物真值**,純從 LiDAR 偵測未知動態物,輸出格式與真值版 `obstacle_aggregator` 相同 → GMPC+CBF 直接吃。並額外發 `/gmpc/static_obstacles`(靜態牆點,見三)。

**為什麼**:第二次用 Gazebo 真值障礙物,等於假設「已知所有障礙物位置速度」,不可實機。真實世界只有 LiDAR 點雲,要自己偵測+追蹤未知動態物。

**感知架構演進(試誤過程)**
| 版本 | 問題 / 結論 |
|---|---|
| map-frame + 背景相減（naive）| amcl 跳變 → 牆落空地格 → 牆幻影 → CBF 卡死 |
| **+ 速度門檻 + 界外拒絕 + 膨脹**（定案）| 濾掉牆幻影/靜態誤偵測,速度門檻最關鍵;剩餘失敗為 amcl 飄移(非感知)|
| odom-frame 追蹤(免疫 amcl)| 驗證後棄用:真機 odom 會漂、不可轉移 |

- **優點**:可處理**名單外未知動態物**;速度門檻同時擔任「動/靜分流」(會動→CBF、不會動→planner),大幅提升導航可靠度(量化見 §十四·B)。
- **缺點/代價**:偵測有**延遲**(min_track_age + KF 收斂 ~0.3–0.5s)→ 對快速動態物的 CBF 反應慢半拍。**但後續量測證實:這延遲不造成動態碰撞(margin 0.38 罩住),真正殘留的擦撞是靜態牆(見八)**。

### 二·A 感知層成果定性:成功 vs 已知限制

> tracker 在 **map(AMCL)幀**追蹤(`global_frame='map'`)。在此幀下 AMCL 跳變確實會把靜止牆/物推進「空地格」而看似在動,所以濾除假動態是感知層的核心攻關。

- ✅ **成功 —— 靜態誤偵測的濾除**:對 `/map` 背景相減後,用兩道閘把「不是真動態」的東西攔下:
  - `static_inflation 0.50`(≥ 最差 AMCL 平移誤差 ~0.3–1.3m 中的工作值):**把被 AMCL 跳變推進空地格的牆點直接丟掉**;
  - **速度門檻 `min_track_speed 0.10`**:把**原地高頻抖動(含 AMCL 跳變引入的假速度)**濾掉,只放行真正持續移動(v≈0.3)的動態物。
  - 兩道閘 + 每 track 的 KF 平滑,把「真實 LiDAR 偵測」從牆幻影卡死的狀態救回可用(量化貢獻見 §十四·B)。**速度門檻同時擔任「動/靜分流」**:會動 → 交 CBF、不會動 → 交 planner。
- ⚠️ **已知限制 —— 移動視角下的未知靜態柱**:靜止柱被移動平台觀測,**可見弧面/質心隨視角與遮擋而漂** → 產生**真實的 apparent velocity(中位 0.186、峰 2.2 m/s)**。
  - 這**不是定位誤差,是遮擋幾何(幾何光學現象)**;瞬時速度門檻濾不掉(拉高會誤殺最慢的真動態 dyn_obs_1=0.15)。
  - 我額外實作的 **0.8s 淨位移視窗 gate** 想用「平均速度」分離,但**靜態柱在移動視角下真的有淨位移 → 也分不出 → 已停用**(`min_net_speed=0.0`,詳見 §17.2)。
  - 後果:這些假動態**漏進 dynamic CBF**,但最終由 **costmap / planner 正確繞行兜底**,**不影響最終成功率**(見 §十四·B)。徹底解需語意分割 / 形狀級追蹤(§十六)。

> ⚠️ 注意用詞:濾除靜態假速度的功臣是**速度門檻 + static_inflation**,**不是**淨位移視窗(該 gate 已停用、屬失敗嘗試)。簡報請勿把淨位移視窗寫成「成功濾除 AMCL 假速度」。

---

## 三、動/靜雙層避障（含靜態 CBF backstop）

**做了什麼**
- **已知/未知靜態物**:nav2 costmap obstacle_layer 從 `/scan` 標記 → **planner 繞行**
- **動態物**:`scan_obstacle_tracker`(速度門檻)→ **CBF 預測式閃避**
- **靜態 CBF backstop（Solution 1）**:`scan_obstacle_tracker` 另取每扇區最近牆點(v=0)發 `/gmpc/static_obstacles`,`gmpc_node` 併入 CBF → 避免「閃動態物時側移撞牆」
- 世界加 4 個未知靜態黃色柱(`unknown_obs`,不在地圖中)

**為什麼**:planner 擅長已知靜態幾何但慢(秒級重規劃)、CBF 擅長動態但會把機器人擠向牆。雙層各司其職,靜態 backstop 補住「夾在動態與牆之間」死角。

- **優點**:分工清楚;靜態 backstop 補死角。
- **缺點/代價**:planner costmap **無時間維度**(不知障礙會走開)→ Start-occupied lockout 風險仍在(未來工作);靜態 CBF 與動態 CBF 早期共用邊距 → 窄縫卡死,後拆開(見六)。

---

## 四、穩健定位：amcl beam-skip + robot_localization EKF（最大攻關）

**做了什麼**(`nav2_localization.yaml` +222、`ekf_fusion.yaml` +46)
1. **amcl beam-skip**（`likelihood_field_prob` + `do_beamskip`）:**忽略打到未知物的光束**,只用對得上地圖的牆定位
2. **EKF**(融合 `/odom` 真值里程計 + `/amcl_pose`、平滑、拒殘餘跳變)→ 穩定 map→odom
3. 把 nav 設定從誤導的 `nav2_params_mppi.yaml` 拆出乾淨的 `nav2_localization.yaml`

**為什麼**:純 2D amcl 對先驗地圖 scan-match,遇未知障礙物 → 對不上 → 定位飄移/崩潰 → 污染下游全部(感知、控制、碰撞判定)。

- **優點**:**多個未知物壓在路徑上、定位仍不飄** → **不需 SLAM、真機可部署**。
- **缺點/代價**:amcl 估測仍有 **~3cm 平均、偶發 >1m** 瞬時誤差(實測 map→odom 平移 mean 2.9cm/max 1.3m)。**此誤差是後來「碰撞量測改用真值座標」與「貼牆輕擦」的根源(見七、八)**。

---

## 五、控制器修改：gmpc_node / gmpc / gmpc_params

**做了什麼**
- `gmpc_node.py`(+51):訂閱 `/gmpc/static_obstacles`、靜態牆點併入 CBF(`static_goal_clear=0.6` 避免終點被牆 CBF 擋)、新增 `static_cbf_safe_margin` 參數
- `gmpc.py`(+8):CBF 改為**每障礙物可帶自己的 margin**(`r_eff = radius + obs.get('margin', cfg.cbf_safe_margin)`)
- `gmpc_params.yaml`:`goal_tolerance_xy` 0.15 → **0.30**

**為什麼**
- 每障礙物 margin:動態(開闊、會動)與靜態(窄縫、已知)需要不同安全餘量(見六)
- goal tol 0.30:0.15 太嚴,機器人在終點前過衝→繞圈收不進去(見六-OSQP)

- **優點**:動靜分治、終點收斂改善。
- **缺點/代價**:多了要調的參數。

---

## 六、★ CBF 安全邊距重新設計：動/靜分離（本期後段核心)

**起因**:用「機器人實體半徑 0.30」量碰撞後,發現 gmpc_scan 會**真的擦進障礙** (動態最深 −7cm、牆 −5cm)。

**根因**:CBF 把機器人當**點**,故 `cbf_safe_margin` 必須 ≥ 機器人半徑。原 `cbf_safe_margin = 0.30 = 機器人半徑` → **安全餘量 = 0** → QP slack 或感知延遲就穿進去。

**公式**(CBF 有效半徑,動/靜分離):
$$h_i(k)=\lVert p(k)-o_i(k)\rVert^2 - r_{\mathrm{eff},i}^2,\qquad r_{\mathrm{eff},i}=r_i+m_i,\qquad m_i=\begin{cases}0.38 & \text{動態障礙(開闊)}\\ 0.33 & \text{靜態牆點(窄縫)}\end{cases}$$
機器人實體半徑 $R_{\mathrm{robot}}=0.30$(0.6m 直徑/2);CBF 視機器人為點,故 $m_i$ 須涵蓋 $R_{\mathrm{robot}}$ + 安全緩衝。動態 0.38 = 0.30 + 0.08(緩衝擋感知延遲);靜態 0.33 較小以保 0.9m 窄縫。

**修法演進**
| 設定 | 動態 keep-out | 靜態 keep-out | 結果 |
|---|---|---|---|
| 原本(共用 0.30)| 邊緣 0 緩衝 | 牆邊緣 5cm | 擦撞(動 −7/靜 −5cm)|
| 全域 bump 0.38 | +8cm | 牆邊緣 **13cm** | 擦撞 0 **但窄縫卡死**(離終點 3.2m 凍結)|
| **動/靜分離(定案)** | **0.38** | **0.33**(牆邊緣 +8cm) | ✅ 擦撞 0、min clearance +5cm、到終點 |

**附帶診斷:OSQP「emergency-braking」是誤導 log** — gmpc.py(`usable` 判定)其實把 `maximum iterations reached` 列為 usable、採次佳解(機器人沒真的煞停);gmpc_node 的 warn 字樣寫錯,實際無礙。

- **優點**:動靜分治符合物理直覺;**擦撞歸零又能鑽窄縫**(gmpc_scan 30 趟動態碰撞 **0**)。
- **缺點/代價**:`static=0.33` 為經驗值,極窄縫(<0.86m)仍可能卡;scan 模式因感知噪,牆擦邊**降低而非全消**(見八)。

---

## 七、★ 公平的碰撞量測：`analyze.py`（+286 行,量測方法學)

**做了什麼**:重建為四/五方法可比、純幾何的碰撞量測：
1. **機器人座標用 gz 真值**(raw `/odom`,drift-free),**不用 amcl 校正後 map pose** —— amcl 有 ~3cm/偶發 >1m 誤差,與要量的 cm 級間距同量級,會把定位噪當碰撞。實證:同一趟 truth,amcl 座標讀到 **−0.21m 假穿透**,換真值座標只剩 **−0.056m**。
2. **機器人半徑 0.30**(由 `chassis_frame.stl` 實測 0.6×0.6 圓盤確認;與 CBF margin、costmap robot_radius 一致)
3. **全障礙物**:真值動態(`/model/*/pose`)+ 地圖牆(distance transform)+ 未知靜態柱(SDF 解析);間距 surface-to-surface
4. **goal tol 0.30**、success 以 controller log 為準 + bag 補強

**公式**(surface-to-surface,用 gz 真值座標 $p_{\mathrm{true}}$):
$$\mathrm{clr}=\lVert p_{\mathrm{true}}-o\rVert - r_{\mathrm{obs}} - R_{\mathrm{robot}},\qquad \text{碰撞} \iff \mathrm{clr}<0,\qquad R_{\mathrm{robot}}=0.30$$
靜態牆以地圖 distance transform 取最近表面距離。**success**:$\lVert p_{\mathrm{true}}-\text{goal}\rVert<0.30$。

**為什麼**:第二次的 clearance 只對真值動態物、且 baselines 缺 channel 報 NaN,無法四方公平比較;且用 amcl 座標會把定位噪當碰撞。

- **優點**:零估測噪、四/五方同標準;揭露真實穿透;baselines 終於有真實 clearance。
- **缺點/代價**:依賴 gz 真值里程計(真機需另接真值源);success/到達仍用 amcl pose(控制器視角),CSV 有 `pose_source` 雙軌,需註明「碰撞用真值、到達用估測」。

---

## 八、★ jerk 量測修正：濾假 dt + p95

**起因**:gmpc_scan jerk_vx 平均 4.1 ± 8.6、最大 29,但中位數僅 0.53。

**診斷(確診為量測假象)**:DDS 偶爾以 **dt≈0.03ms** 重複投遞 cmd_vel(非真 20Hz 控制步),`accel=Δv/dt` 爆成 1310 m/s² → 汙染 std。實證:高 jerk(seed13)與乾淨(seed14)的**真實最大速度跳、accel p95、中位數完全相同**,只是抖動是否撞上小 dt。

**修法**(`analyze.py`):① `dt > 0.5×中位週期` 濾掉假筆 ② jerk 改 **p95(|accel|)**。

**公式**($\tilde{dt}$ = 中位控制週期):
$$\mathrm{jerk}=\mathrm{p95}\!\left(\left|\tfrac{\Delta u}{\Delta t}\right|\ \middle|\ \Delta t > 0.5\,\tilde{dt}\right)\quad\text{(取代原 } \mathrm{std}(\Delta u/\Delta t)\text{)}$$
原 std 被 $\Delta t\approx0.03$ms 的 DDS 假筆($\Delta u/\Delta t\to1310$)汙染;濾假筆 + p95 → 穩健。

**結果**:全 30 趟 jerk_vx 一致為 **0.800 ± 0.001**(原 outlier seed9/12/13/26 全歸位)。

- **優點**:穩健、可比、反映真實平滑度(這也正是第二次報告 future-work 寫的「jerk std→p95」)。
- **缺點/代價**:既有 CSV 需用新 metric 重算(bags 都在,無需重跑 sim)。

---

## 九、五方法公平 benchmark 框架：`run_omnibot_dynamic.sh`（+179）

**做了什麼**:單一 harness 分派五方法,**同世界、同 driver、同 amcl/EKF、同 goal/tol、同 costmap**：
- `gmpc_scan`:本方法 + 真實 LiDAR 感知(smoother ON)
- `gmpc_scan_nosm`:同上但 **velocity_smoother OFF**(平滑器消融)
- `gmpc_truth`:本方法 + 真值障礙(感知消融上界)
- `mppi` / `rpp`:Nav2 baselines

**costmap 公平對齊(本期後段補正)**:三方統一 `robot_radius 0.33`、`inflation_radius 0.45`、`cost_scaling 3.0`(global / local 兩個 costmap 皆同值)。先前 mppi 誤用 inflation 0.35、baselines 誤用 robot_radius 0.28(< 真實 0.30)→ 不公平,已修齊。(曾試 inflation 0.55 但有副作用,退回 0.45,見 §17.3)

- **優點**:一鍵跑「感知消融 + 平滑器消融 + baseline 對比」三條主線;條件對齊(同世界/driver/定位/goal-tol/costmap)、可重現。
- **缺點/代價**:rpp 為純路徑跟隨、無動態避障 → 動態場景近乎 DNF(屬「無避障下界」,需於論文說明);harness 單例鎖 + lifecycle 啟動偶有 race(`n_plan=0`:該趟 nav 沒 activate、robot 整趟沒動;最終統計**保守計入失敗**,N=40 分母用全部 40 趟)。

---

## 十、velocity_smoother 開關(`use_smoother`)

**做了什麼**:launch 加 `use_smoother` 參數可切換。ON:`gmpc→cmd_vel_nav→smoother→/cmd_vel`;OFF:gmpc 直送 `/cmd_vel`。

**為什麼 + 結論**:第二次 future-work 提「串 velocity_smoother 壓 jerk」。實作 + 消融後**定案關閉**(對 jerk 無實質幫助、又是 flake 來源,詳見 §十七),保留開關供日後 whole-body 需要削峰值時再開。

---

## 十一、★ 控制/路徑平滑度改進(定案)

### 11.1 控制 jerk → 輸入增量成本(Input Smoothness Cost)
**做了什麼**:在 GMPC 成本加控制增量懲罰 $\sum_k \Delta u_k^\top S\,\Delta u_k$($\Delta u_k=u_k-u_{k-1}$,以差分矩陣併入 P/q,不增決策變數),`S_vx/S_vy/S_w=15/15/8`(公式見附錄 B.5b)。
**為什麼**:原成本只罰「速度偏離參考」,不罰「速度變化」;這是最佳控制的標準「輸入平滑性成本」。**軟成本** → 巡航如絲般平滑,但 CBF 生死關頭仍可瞬間爆發加速度(保命)。
- **優點**:中位加速度 **↓63%**;且**路徑最短(38.0m)、到達最快(167s,比無 S 的 179s 快 7%)**——平滑控制少做無謂橫移 → 走得更直更省。S sweep 確認 S=15 為甜蜜點(見 §14B.4)。
- **缺點/代價**:p95 jerk 指標看不出(被 a_max 釘住,避障爆發在前 5%);其價值在「中位/巡航」平滑與效率。**定為標準。**

### 11.2 靠牆 chattering → map-based static-CBF
**起因**:robot 貼牆時左右抖(實測:抖動發生在離牆 0.63m、動態障礙在 2.26m 外 → 純靜態牆造成)。
**根因**:舊 static-CBF 每幀從 /scan 重挑最近牆點 → 牆點跳動(網格 snap、跨扇區、回波忽有忽無)→ 近硬 CBF 追跳動約束 → chatter。
**解法**:牆是已知的 → 改用**地圖 distance-transform 算最近牆點**(隨 robot 平滑移動、不跳)。
- **優點**:近牆抖動中位 **↓32%**、靜態牆擦 **→0**;且**可實機**(用 robot 本來就有的先驗地圖+定位,未知/變動物仍由即時 costmap 接手)。**定為標準。**

> **平滑度定案配置**:輸入平滑成本 **S=15** + **map-based static-CBF**。
> ⚠️ 另一個平滑面向「**xy 路徑扭曲**」以及多個沒成功的調校嘗試(inflation 0.55、位姿濾波、淨位移 gate、velocity_smoother 等)**集中於 §十七 誠實記錄**,不打斷此處的成功敘事。

---

## 十二、評估基礎建設

`record.sh`(規範化錄 bag)、`classify_failures.py`(SUCCESS/ALGO_FAIL/APPARATUS_FAIL 三分類)、`plot.py`(多 panel 對比圖)、`analyze_all_bags.sh`、`finalize_batch.sh`、`temp_logger.sh`(CPU/GPU 溫度記錄,因連跑數小時會過熱硬斷電)、視覺化 RViz→**Foxglove**。

- **優點**:全自動、可重現、含失敗分類與溫度監控。
- **缺點/代價**:硬體散熱不足(CPU idle 76°C)→ 長 batch 會過熱斷電,需清散熱/重矽脂治本。

---

## 十三、場景豐富化

`random_room_dynamic.sdf`(+88)、`dynamic_trajectories.yaml`:**4 個會動黑色障礙**(dyn_obs_0/1/2/5,半徑 0.25,速度 0.15–0.3)+ **4 個未知靜態黃色柱** + 地圖牆;修掉 dyn_obs 軌跡終點落牆內凍結的問題。

- **優點**:同時考驗動態閃避、未知靜態繞行、雙層分工;碰撞涵蓋全部障礙。
- **缺點/代價**:動態障礙為 kinematic(不受力),碰撞以幾何穿透誠實記錄。

---

## 十四、關鍵成果(已驗證)

| 項目 | 結果 |
|---|---|
| 平台 | omni_bot(ROS2/gz)+ LiDAR + 深度相機,靜態 RMSE 0.049m |
| 真實感知避障 | 定案 **gmpc_scan 98%**(39/40,N=40)|
| **動態避障安全** | scan 模式**動態碰撞 0**;總碰撞僅 1/40(淺擦牆)|
| 穩健定位 | beam-skip + EKF → 未知物環境**定位不飄**(不需 SLAM)|
| 即時性 | solve mean **2.2ms** / max 32ms(< 50ms 週期)|
| 追蹤 | RMSE **0.09m** |
| 平滑 | 輸入平滑成本 → 中位加速度 ↓63%;jerk p95 0.80(=a_max)|

> 三方完整對照(vs MPPI/RPP)+ S sweep 詳見下節 **§十四·B**。

---

## 十四·B、★ 最終驗證:N=40 三方 benchmark + S sweep 定案(2026-06-29)

> **定案配置**(S=15 輸入平滑成本 + map-based static-CBF + margin 0.38/0.33 + inflation 0.45 + goal tol 0.30)下,scan / mppi / rpp **各 40 趟**之最終結果。圖:`evaluation/results/figs/gmpc_vs_baselines_N40.png`、`trajectories_N40.png`、`sweep_S.png`。

### 14B.1 三方對照(N=40,分母皆 40)
| 指標 | **GMPC+CBF (ours)** | MPPI | RPP |
|---|---|---|---|
| **成功率** | **39/40 = 98%** | 33/40 = 82% | 4/40 = 10% |
| 碰撞趟 / 點數 | **1/40 (2)** | 3/40 (3) | **34/40 (55)** |
| min 間距 m | +0.03 | +0.07 | **−0.14(撞穿)** |
| 到達時間 s | 173±9 | 157±25 | 115±13 |
| 路徑長 m(/直線24m)| 39.8(1.66×)| 34.2(1.43×)| 30.3(1.26×)|
| 追蹤 RMSE m | 0.09 | 0.05 | 0.04 |
| jerk_vx / vy / wz | 0.80 / 0.60 / 1.20 | **0.19 / 0.60 / 0.39** | 0.11 / 0 / 1.76 |
| solve mean/p95/max ms | **2.2 / 19 / 32** | — | — |

**Headline**:真實 LiDAR 感知動態避障 **GMPC+CBF 98% ≫ MPPI 82% ≫ RPP 10%**,碰撞 1/40,real-time(2.2ms)。

### 14B.2 失敗模式(揭露方法本質)
- **GMPC(1趟)**:早期被障礙堵死、**安全停滯零碰撞**(寧停不撞)
- **MPPI(6趟)**:多為**中途卡死 3.4–4.7m**(短時域反應弱),幾乎零碰撞
- **RPP(34趟)**:卡 ~3.3m / ~21m 兩群、**幾乎都伴隨撞穿**(min間距 −0.14、55 碰撞點)、σ(vy)=0 不會側移 → **無動態避障下界**

### 14B.3 平滑度對比(誠實 trade-off)
- **MPPI 明顯較平滑**:jerk_vx **0.19**(其 a_max 1.5,只用 13%);GMPC jerk_vx **0.80 = 自身 a_max**(反應式避障打滿加速度)
- 本質:MPPI 採樣優化整條軌跡 → 溫和;GMPC 單步 QP + 硬 CBF → 即時猛修正。**換得 98% vs 82% 成功**(註:兩者 a_max 不同,GMPC 0.8/0.6/1.2 vs MPPI 1.5/1.0/2.0)
- 註:p95 jerk 對 GMPC 各軸=各自 a_max(避障爆發 >5% 時間 → 第95百分位頂在上限)

### 14B.4 S sweep(輸入平滑成本最佳值,7 點 × 5 趟)
| S | 成功率 | 到達s | 路徑m | σ(vy) |
|---|---|---|---|---|
| 0 | 4/4 | 184 | 41.3 | 0.077 |
| 5 | 4/4 | 178 | 41.0 | 0.077 |
| 10 | 5/5 | 172 | 39.5 | 0.070 |
| **15** | 5/5 | **166** | **37.9** | **0.066** |
| 20 | 5/5 | 166 | 38.0 | 0.067 |
| 25 | 5/5 | 179 | 41.3 | 0.074 |
| 30 | 5/5 | 174 | 40.0 | 0.069 |

**漂亮的 U 型曲線**:S=0→15 單調變好(到達↓10%、路徑↓8%、σvy↓15%),**S=15–20 觸底(甜蜜點)**,S>20 過阻尼變糟;全程碰撞 0(S 不影響安全)。→ **S=15 經數據確認為最佳,輸入平滑成本為有效且有理論依據之改進。**

### 14B.5 定案配置總表(所有最終參數一覽)
| 模組 | 參數 | 值 | 出處 |
|---|---|---|---|
| 控制 GMPC | control_freq / horizon / dt | 20Hz / 20 步 / 0.05s(1s lookahead)| §壹 |
| | Q_xy / Q_yaw / Qf_mult | 15 / 7 / 5 | §五 |
| | R_vx / R_vy / R_w | 2 / 2 / 1 | §五 |
| | **S_vx / S_vy / S_w(輸入平滑)** | **15 / 15 / 8** | §11.1、§14B.4 |
| | v_nominal / 速度上限 vx,vy,wz | 0.22 / 0.35,0.25,0.80 | §壹 |
| | 加速度上限 ax,ay,az | 0.8 / 0.6 / 1.2 | §壹 |
| CBF | cbf_alpha / slack_weight | 3.0 / 5e2 | §貳、六 |
| | **動態 margin / 靜態 margin** | **0.38 / 0.33** | §六 |
| | gain-scheduling(danger 時)| Q×0.2、slack×20、thresh 0.4 | §貳 |
| | static-CBF 來源 | **map distance-transform**(穩定)| §11.2 |
| 感知 | min_track_age / min_track_speed | 3 幀 / 0.10 m/s | §二、B.7 |
| | cluster_gap / max_cluster_radius / default_radius | 0.30 / 0.60 / 0.25 m | B.7 |
| | assoc_gate / track_timeout / static_inflation | 0.80 / 0.60s / 0.50 m | B.7 |
| 定位 | amcl beam-skip + EKF(/odom 真值 + /amcl_pose)| — | §四 |
| costmap | robot_radius / inflation / cost_scaling | 0.33 / 0.45 / 3.0(global=local)| §九 |
| 機器人 | 碰撞半徑(0.6m 圓盤)| 0.30 | §七 |
| 收斂 | goal_tolerance_xy | 0.30 | §五 |
| 量測 | 碰撞座標 / 半徑 / jerk | gz真值 / 0.30 / p95 | §七、八 |
| 平滑器 | velocity_smoother | **OFF**(`use_smoother` 開關)| §十、17.4 |

> 停用但保留程式碼的實驗開關(預設 0):`Q_y`、`static_activate_range`(gating)、`pose_lpf_alpha`、tracker `min_net_speed`(淨位移 gate)——皆證實對 xy 扭曲無效(§17.1),保留供日後再試。

---

## 十五、已知限制（誠實陳述）

- scan 偵測延遲 → 對快速動態物 CBF 慢半拍(但實測動態碰撞 0)
- **貼牆輕擦** ≤5cm:源於 amcl ~3–5cm 定位誤差,非感知,各方法皆有;已大幅改善(map-static + dyn_obs_2 移位 → N=40 僅 1/40),治本仍需更好定位
- 靜態邊距 0.33、inflation 0.45 為經驗值,極窄縫(<0.86m)仍可能卡(曾試 inflation 0.55 反而圍死角落終點,退回)
- planner costmap 無時間維度 → reactive within horizon,非 proactive detour
- **xy 路徑扭曲(~85°/m vs MPPI 37)**:反應式硬 CBF 即時避障的本質代價(8 種調參皆無法降,**詳見 §十七**);屬「主動避障特性」,換得高成功率 + 零深撞(數據見 §十四·B)
- **未知靜態延伸物**會騙過運動式辨識(質心漂移,**詳見 §十七**);由 costmap/planner 正確繞行,不影響成功率
- 碰撞量測依賴 gz 真值里程計(真機需另接真值源)
- 硬體散熱不足 → 長 batch 過熱斷電

---

## 十六、未來工作

- **局部軌跡優化**(CBF 嵌入短時域軌跡優化,取代「追全域路徑+單步 CBF」)→ 治 xy 路徑扭曲(§17.1)
- **語意分割 / 形狀級追蹤** → 分離未知靜態延伸物 vs 慢速動態(§17.2 的本質難題)
- **Proactive detour**:KF 預測投到 spatio-temporal costmap,讓 planner 規劃階段就繞開(治 Start-occupied lockout)
- 改善定位(EKF/amcl 調參或 SLAM)→ 根治貼牆輕擦
- CBF 參數(α、margin、slack、inflation)安全–效率 Pareto
- Whole-body MPC（SE(2) 底座 + 7-DOF 手臂）+ 接觸操作

---

## 十七、走過的死路:失敗的嘗試(誠實記錄)

> 本期試過但**無效或退回**的嘗試集中於此,與前面的成功改進分開陳述,供審查參考、避免重蹈。

### 17.1 xy 路徑扭曲 —— 8 種調校全失敗(本質問題)
**量化**:GMPC+CBF **~85°/m**,MPPI 僅 **37°/m**(全域規劃路徑本身平滑 1.5°/步 → 扭來自控制的橫向修正)。**試過 8 法,扭曲度全落 85–102,皆未降至 37**:
| 嘗試 | 扭曲度°/m | 結果 |
|---|---|---|
| 輸入平滑 S=15 | 85 | 基線(jerk/速度有改善,扭不變)|
| S=30 + 收緊 a_max | 100 | 加阻尼**更糟**(迴路延遲→過衝)|
| 位姿低通濾波 | 98 | 加延遲**更糟** |
| cbf_alpha 3→1.5(降增益)| 86 | 無效 |
| static-CBF gating(無威脅時關)| 92 | 無效 |
| 放寬橫向 Q_y | 87 | 無效 |
| tracker 淨位移 gate | (不變)| 無效(見 17.2)|

**根因(誠實)**:扭曲是**反應式單步 CBF 即時閃 4 個動態障礙**的必要動作 —— 扭=積極避障(故成功率高);MPPI 平滑=較不積極(故成功率較低)。**想消扭=消避障**(數據見 §十四·B)。加阻尼因迴路延遲反而更糟、降增益無效。→ **本質代價**,論文正面寫成「**主動避障特性**」;徹底解需把架構升級為「**局部軌跡優化**(CBF 嵌入短時域軌跡優化)」,列未來工作。

### 17.2 未知靜態柱漏進 dynamic CBF —— 運動式辨識皆失敗
靜止柱被移動平台觀測,可見弧面/質心隨視角漂移 → **apparent velocity 中位 0.186、峰 2.2 m/s** → 與慢速 mover 完全重疊。**瞬時速度門檻**(拉高會誤殺最慢動態 dyn_obs_1=0.15)、**0.8s 淨位移 gate**(質心真有淨位移、騙過)皆**無法分離**。→ 屬「移動平台觀測靜止延伸物」本質感知難題;目前由 **costmap/planner 正確繞行(backstop)**,不影響最終成功率(§十四·B)。徹底解需語意分割 / 形狀級追蹤。

### 17.3 inflation 0.55 —— 圍死角落終點
試 `inflation 0.45→0.55` 想逼路徑置中以減少貼牆,但**終點在角落 → 0.55 在終點周圍築高成本「護城河」→ robot 卡離終點 ~0.6m(2/4 失敗)→ 退回 0.45**。教訓:inflation 過高會圍死角落目標。

### 17.4 velocity_smoother —— 對 jerk 無實質幫助
同趟 cmd_vel 前後對比:smoother **只削峰值加速度(max 5→1.5)**,對 p95/中位 jerk **無改善**(gmpc.py 次佳解 + §11.1 輸入平滑成本已罩),且是 lifecycle flake 來源 → **定案關閉**(保留 `use_smoother` 開關供日後 whole-body 削峰值)。

> 以上失敗嘗試的程式碼多保留為**參數開關(預設停用)**:`Q_y`、`static_activate_range`、`pose_lpf_alpha`、tracker `min_net_speed` → 隨時可再實驗。

---

# 附錄

## 附錄 A：簡報投影片大綱對應

| 投影片 | 內容 | 對應章節 |
|---|---|---|
| P1 | 封面 | — |
| P2 | 報告大綱 | — |
| P3 | **上期回顧 & 本期總覽**(上期限制→本期對策表)| 貳 |
| P4 | **研究目標與系統架構**(pipeline 圖 + 方法堆疊)| 壹 |
| P5 | 平台移植:omni_bot + LiDAR/相機(RMSE 0.049m)| 一 |
| P6 | **真實 LiDAR 感知** scan_obstacle_tracker(偵測未知動態物)| 二 |
| P7 | 動/靜雙層避障(planner 靜態 + CBF 動態)| 三 |
| P8 | 穩健定位:beam-skip + EKF(未知物不飄,不需 SLAM)| 四 |
| P9 | ★ **CBF 動/靜分離邊距**(擦撞→0,窄縫權衡表)| 五、六 |
| P10 | ★ **公平碰撞量測**(真值座標+全障礙)+ jerk p95 修正 | 七、八 |
| P11 | **公平 benchmark 框架**(感知/平滑器消融 + baseline,costmap 對齊)| 九、十 |
| P12 | ★ **控制平滑度改進**:輸入平滑成本 S(jerk↓63%、路徑最短)+ map-static(牆 chatter↓、牆擦→0)| 十一 |
| P13 | ★ **最終成果 N=40**:GMPC+CBF **98%** ≫ MPPI 82% ≫ RPP 10%、碰撞 1/40、solve 2.2ms、S sweep 釘 S=15 | 十四·B |
| P14 | 已知限制 + **走過的死路**(xy 扭曲本質、未知延伸物、失敗嘗試)+ 未來工作 | 十五、十七、十六 |
| P15 | Thank you | — |

> 失敗嘗試(§十七)建議放**最後**(P14 或備用 slide),不穿插在中段成功敘事;主線 P5–P13 全是「做了什麼+成功數據」。

> 視覺素材建議:P4 架構圖、P6 感知流程、P9 擦撞前後對照、P12 trajectories_N40.png + sweep_S.png、P13 gmpc_vs_baselines_N40.png + 動態場景影片、Foxglove CBF 安全圈。

---

## 附錄 B：公式總表

> 標示:◆【文獻】既有方法　★【本研究修改】　✦【本研究整合】

### B.1 SE(2) 幾何基礎　◆【Solà 18 / Lynch & Park 17】
$$\hat\xi=\begin{bmatrix}0&-\omega&v_x\\\omega&0&v_y\\0&0&0\end{bmatrix},\quad \dot X = X\hat\xi,\quad
\mathrm{ad}(\xi)=\begin{bmatrix}0&-\omega&v_y\\\omega&0&-v_x\\0&0&0\end{bmatrix},\quad e=\log\!\big(X_{\mathrm{ref}}^{-1}X\big)^{\vee}$$

### B.2 GMPC（幾何模型預測控制）
- ◆ 連續誤差動態:$\dot e=-\mathrm{ad}(\xi_{\mathrm{ref}})\,e+(\xi-\xi_{\mathrm{ref}})$
- ★ 離散(沿參考多點線性化,全向 $B=I$):$e_{k+1}=A_d(k)e_k+\Delta t\,\delta\xi_k,\ \ A_d(k)=I-\Delta t\,\mathrm{ad}(\xi_{\mathrm{ref}}(k))$
- ◆ 凝聚式預測:$E=\Phi e_0+\Gamma z,\quad z=[\delta\xi_0,\dots,\delta\xi_{N-1}]^\top$
- ◆ 成本:$J=\sum_{k=1}^{N-1}e_k^\top Q e_k+e_N^\top Q_f e_N+\sum_{k=0}^{N-1}\delta\xi_k^\top R\,\delta\xi_k$
- ◆ QP:$\min_z \tfrac12 z^\top P z+q^\top z,\quad P=2(\Gamma^\top\bar Q\Gamma+\bar R),\quad q=2\Gamma^\top\bar Q\Phi e_0$
- ◆ 約束:$u_{\min}\le\xi_{\mathrm{ref}}(k)+\delta\xi_k\le u_{\max},\quad |u_k-u_{k-1}|\le a_{\max}\Delta t$
- 實作:OSQP,`maximum iterations reached` 採**次佳解**不急煞($u=\xi_{\mathrm{ref}}+\delta\xi_0$),僅真不可解才 $u=0$

### B.3 Kalman Filter（等速度模型）　◆【標準 CV-KF】
$$x=[p_x,p_y,v_x,v_y]^\top,\ y=[p_x,p_y]^\top,\ 
F=\begin{bmatrix}1&0&\Delta t&0\\0&1&0&\Delta t\\0&0&1&0\\0&0&0&1\end{bmatrix},\ H=\begin{bmatrix}1&0&0&0\\0&1&0&0\end{bmatrix}$$
- 預測 $\hat x^-=F\hat x,\ P^-=FPF^\top+Q$;更新 $K=P^-H^\top(HP^-H^\top+R)^{-1},\ \hat x=\hat x^-+K(y-H\hat x^-),\ P=(I-KH)P^-$

### B.4 全時域預測式 CBF　✦【整合;基礎 ◆ Ames 17 / Zeng 21】
- ✦ 障礙等速度預測(KF 速度):$o_i(k)=o_i(0)+v_i\,k\,\Delta t$
- ◆ 障壁函數:$h_i(k)=\lVert p(k)-o_i(k)\rVert^2-r_{\mathrm{eff},i}^2$
- ✦ 安全約束(含相對速度,嵌入 GMPC QP):$\dot h_i(k)+\alpha h_i(k)\ge-\varepsilon_k,\ \ \dot h_i(k)=2(p(k)-o_i(k))^\top(R(\theta_k)v_{\mathrm{body}}-v_{\mathrm{obs}})$
- ✦ Slack:所有步 $\varepsilon_k\ge0$,$\varepsilon_0$ 罰權為其他步 30 倍(near-hard 仍可解)
- ✦ Gain-scheduling:依 $\min_k h_k$ 動態調 $Q$(×0.20 at danger)與 slack 罰權(×20)

### B.5 ★ 本期新增:有效半徑(動/靜分離)
$$r_{\mathrm{eff},i}=r_i+m_i,\qquad m_i=\begin{cases}0.38 &\text{動態}\\0.33 &\text{靜態牆點}\end{cases},\qquad R_{\mathrm{robot}}=0.30\ (=0.6\text{m}/2)$$
CBF 視機器人為點,故 $m_i$ 須涵蓋 $R_{\mathrm{robot}}$ + 安全緩衝;動態留 8cm 擋感知延遲,靜態較小保窄縫。

### B.5b ★ 本期新增:輸入平滑成本(Input Smoothness Cost)
GMPC 成本加控制增量懲罰項(◆ 最佳控制標準作法):
$$J \mathrel{+}= \sum_{k=0}^{N-1}\Delta u_k^\top S\,\Delta u_k,\qquad \Delta u_k=u_k-u_{k-1}=(\delta\xi_k-\delta\xi_{k-1})+(\xi_{\mathrm{ref}}(k)-\xi_{\mathrm{ref}}(k-1))$$
以差分矩陣 $D$ 寫成 $\Delta u=Dz+c$,只改 $P\mathrel{+}=2D^\top\bar S D$、$q\mathrel{+}=2D^\top\bar S c$,**不增決策變數維度**;$u_{-1}=\xi_{\mathrm{prev}}$(從實際速度接續,無接縫跳變)。$S=\mathrm{diag}(15,15,8)$。軟成本 → 巡航平滑、CBF 危急仍可爆發。效果:中位加速度 ↓63%、路徑最短、到達最快。

### B.6 ★ 本期新增:量測公式（`analyze.py`）
- **碰撞間距**(surface-to-surface,**用 gz 真值座標** $p_{\mathrm{true}}$):
$$\mathrm{clr}=\lVert p_{\mathrm{true}}-o\rVert - r_{\mathrm{obs}} - R_{\mathrm{robot}},\qquad \text{碰撞} \iff \mathrm{clr}<0$$
靜態牆以地圖 distance transform 取最近表面;$R_{\mathrm{robot}}=0.30$。
- **jerk(平滑度)**:濾掉 $dt\le 0.5\,\tilde{dt}$ 的 DDS 假筆($\tilde{dt}$=中位週期),取
$$\mathrm{jerk}= \mathrm{p95}\!\left(\left|\tfrac{\Delta u}{\Delta t}\right|\right)$$
(原用 std 被 $dt{\approx}0.03$ms 的假象汙染 → 改 p95 穩健)
- **追蹤 RMSE**:$\sqrt{\tfrac1n\sum_i \min_j\lVert p_i - \mathrm{plan}_j\rVert^2}$;**success**:$\lVert p-\text{goal}\rVert<0.30$。

### B.7 ★ 本期新增:感知管線(`scan_obstacle_tracker.py`)

LiDAR `/scan` → 動態障礙 `[x,y,r,v_x,v_y]`,逐步如下(全程在 map/AMCL 幀):

1. **靜態背景相減**　◆【occupancy 相減】:把落在「已知地圖牆 + `static_inflation`=0.50m 膨脹帶」內的光束點丟掉(膨脹帶吸收 AMCL 平移誤差,避免牆被推進空地格成幻影)。
2. **角度相鄰聚類**　`cluster_adjacent`:點依掃描角排序,**相鄰點歐氏跳距 > `cluster_gap`=0.30m 即斷開**成新簇;簇取質心與外接半徑;拒絕 `n_pts < 2` 或半徑 > `max_cluster_radius`=0.60m(視為牆/雜訊),半徑下限補到 `default_radius`=0.25m。
3. **Hungarian 帶門檻關聯**　◆【Kuhn 1955 / `linear_sum_assignment`】:簇質心↔現有 track 以歐氏距離為成本求最小成本指派,**距離 > `assoc_gate`=0.80m 的配對作廢**;未配上的簇開新 track、未配上的 track 逾 `track_timeout`=0.60s 刪除。
$$\min_{\pi}\sum_i \lVert c_i - t_{\pi(i)}\rVert\quad\text{s.t.}\ \lVert c_i - t_{\pi(i)}\rVert \le 0.80$$
4. **每 track 等速度 KF**(B.3):質心為量測,輸出平滑 $[x,y,v_x,v_y]$;量測噪 `kf_sigma_meas`=0.05 反映質心抖動。
5. **發佈門檻(動/靜分流)**:track 須 `min_track_age`=3 幀(濾偵測雜訊)**且** $\lVert v\rVert \ge$ `min_track_speed`=0.10 m/s(只放行真動態 → CBF;不動的交 planner/costmap)才送 `/gmpc/obstacles`。
6. **靜態 CBF 牆點**:另以地圖 **distance-transform**(B 段 §11.2)取最近牆面點,發 `/gmpc/static_obstacles`(v=0)。

> **已知限制**:第 5 步的速度門檻無法分離「移動視角下質心漂移的未知靜態柱」(apparent velocity 中位 0.186、峰 2.2 m/s),試過的 0.8s 淨位移 gate 也分不出(已停用)→ 由 costmap/planner 兜底(§17.2)。

### 引用對照
| 方法 | 主要引用 |
|---|---|
| SE(2) Lie 工具/誤差 | Solà et al. 2018;Lynch & Park 2017 |
| Lie-group 誤差動態 | Bullo & Lewis 2005 |
| Condensed 線性 MPC | Borrelli, Bemporad, Morari 2017 |
| QP 求解器 | Stellato et al. 2020 (OSQP) |
| 控制障壁函數 CBF | Ames et al. 2017;Zeng, Zhang, Sreenath 2021 |
| Kalman Filter(等速度)| Kalman 1960 |
| Hungarian 指派(資料關聯)| Kuhn 1955 |
| AMCL / costmap / nav2 | Macenski et al. (Nav2) |

---

## 附錄 C:每頁投影片內容大綱(逐頁條列)

> 主線 P5–P13 全是「做了什麼 + 成功數據」;失敗(§十七)集中在 P14,**不穿插中段**。數據以本報告 §十四·B(N=40)為準。

### P1 — 封面
- 題目:動態環境下移動機械臂預測導航與受約束操作(Phase 1:預測式動態避障)
- 報告者:陳文顥　指導:陳榮順 教授　國立清華大學
- 一句副標:**真實 LiDAR 感知 + 穩健定位 + 可實機部署的動態避障**

### P2 — 報告大綱
- 上期回顧 → 系統架構 → 五大本期工作(平台/感知/避障/定位/量測)→ 最終 benchmark → 限制與未來

### P3 — 上期回顧 & 本期總覽
- 上期:`ammr_base` 點質點 + GMPC + 預測 CBF,**用 Gazebo 真值障礙**達 90%
- 上期 5 大限制 → 本期 5 大對策(用 §貳 對照表)
- **本期主軸**:① 真實平台 ② 真實 LiDAR 取代真值 ③ 動/靜雙層 ④ 穩健定位 ⑤ 公平量測
- 視覺:上期限制→本期對策表

### P4 — 研究目標與系統架構
- 總目標:全身協調 + 接觸操作;本期聚焦 Phase 1 底座導航
- **ROS2 pipeline 圖**(scan→tracker→CBF、planner→GMPC、amcl+EKF→定位)
- 方法堆疊表:控制 GMPC / 安全 CBF / 感知 tracker / 規劃 nav2 / 定位 amcl+EKF
- 視覺:架構圖(§壹)

### P5 — 硬體層:omni_bot 實機模型導入
- 點質點 → **0.6m 圓盤 omni 平台 + 2D LiDAR + RealSense D435i**;ROS1/Classic → **ROS2 Jazzy + gz Harmonic**
- 移植三坑→修法:mesh `file://`、**真值里程計**(取代 dead-reckoning 40m 漂)、LiDAR 自遮罩(45/135/225/315°±15°)
- 結果:靜態追蹤 **RMSE 0.049m**
- 視覺:omni_bot 3D 模型 + 感測器標註　|　伏筆:真值里程計後來成公平碰撞量測基礎

### P6 — 感知層:真實 LiDAR 動態偵測(`scan_obstacle_tracker`)
- 流程:`/scan`→**背景相減**→**角度相鄰聚類**→**Hungarian 關聯**→每 track **CV-KF**→速度門檻分流(B.7)
- **成功**:map 幀追蹤 + `static_inflation 0.50` + **速度門檻 0.10**(濾 AMCL 假速度/原地抖動)→ 濾掉牆幻影、救回真實 LiDAR 偵測的可用性(成功率數據留待 P13)
- ⚠️ **已知限制**:移動視角看未知靜態柱 → 質心漂移假速度(中位0.186/峰2.2)→ **遮擋幾何非定位誤差** → 漏進 CBF,由 planner 兜底(放 P14)
- 視覺:感知流程圖 + 聚類/追蹤截圖

### P7 — 動/靜雙層避障
- **planner+costmap** 處理已知/未知**靜態**繞行;**CBF** 處理**動態**閃避;速度門檻做分流
- **靜態 CBF backstop**:補「夾在動態與牆之間」死角
- 視覺:雙層分工示意

### P8 — 穩健定位:amcl beam-skip + EKF
- beam-skip:忽略打到未知物的光束,只用對得上地圖的牆定位
- EKF 融合 /odom 真值 + /amcl_pose → 穩定 map→odom
- 成果:**多未知物壓在路徑上仍不飄 → 不需 SLAM、可實機**
- 缺點:amcl 仍 ~3cm 平均誤差(後成貼牆輕擦根源)

### P9 — ★ CBF 安全邊距:動/靜分離
- 起因:用真實半徑 0.30 量,發現會真擦進障礙(動−7/牆−5cm)→ 根因 CBF 把機器人當點、margin 須 ≥ 0.30
- **動態 0.38 / 靜態 0.33**(動態留 8cm 擋感知延遲;靜態小以保 0.9m 窄縫)
- 結果表:共用0.30(擦撞)→ 全域0.38(卡死)→ **動靜分離(擦撞0+鑽窄縫)**
- 公式:$r_{\mathrm{eff}}=r_i+m_i$;視覺:擦撞前後對照

### P10 — ★ 公平的量測方法學
- 碰撞用 **gz 真值座標**(非 amcl,避免把定位噪當碰撞;實證 −0.21→−0.056m)、半徑 0.30、**全障礙物** surface-to-surface
- **jerk 修正**:濾 DDS 假 dt + 改 **p95**(原 std 被 dt≈0.03ms 汙染)
- 視覺:真值 vs amcl 座標碰撞差異圖

### P11 — 公平 benchmark 框架
- 單一 harness 五方法:gmpc_scan / gmpc_scan_nosm(平滑器消融)/ gmpc_truth(感知消融)/ mppi / rpp
- **三方對齊**:robot_radius 0.33、inflation 0.45、cost_scaling 3.0(global=local)
- 視覺:harness 結構 / 條件對齊表

### P12 — ★ 控制平滑度改進(定案)
- **輸入平滑成本 S=15**($\sum\Delta u^\top S\Delta u$):中位加速度↓63%、**路徑最短 38m、到達最快 167s**;S sweep U 型曲線釘 S=15
- **map-based static-CBF**:近牆 chatter↓32%、靜態牆擦→0、可實機
- 視覺:`trajectories_N40.png` + `sweep_S.png`

### P13 — ★ 最終成果:N=40 三方 benchmark
- **GMPC+CBF 98%(39/40)≫ MPPI 82% ≫ RPP 10%**;碰撞 **1/40**;solve **2.2ms**
- 失敗模式:GMPC 寧停不撞;MPPI 中途卡死;RPP 撞穿(無避障下界)
- 誠實 trade-off:MPPI jerk 較低(較不積極);GMPC 反應式避障打滿 a_max → 換得高成功
- 視覺:`gmpc_vs_baselines_N40.png` + 動態場景影片 + Foxglove CBF 安全圈

### P14 — 已知限制 + 走過的死路 + 未來工作
- **限制**:貼牆輕擦≤5cm(定位誤差,1/40)、xy 扭曲~85°/m(主動避障代價)、未知靜態延伸物漏 CBF(planner 兜底)、依賴真值里程計、散熱
- **失敗的嘗試(§十七)**:xy 扭曲 8 法全敗、淨位移 gate、inflation 0.55、velocity_smoother — 誠實列出避免重蹈
- **未來**:局部軌跡優化(治扭曲)、語意分割(分離靜態延伸物)、proactive detour、whole-body MPC + 接觸操作

### P15 — Thank you / Q&A
- 一句總結:**真實感知下動態避障 98%、零深撞、real-time,且方法學公平可重現**

> 視覺素材清單:P4 架構圖、P6 感知流程、P9 擦撞前後、P12 trajectories+sweep_S、P13 baselines 對比+影片+Foxglove。失敗內容只在 P14。
