# 第三次進度報告 — 自第二次報告後之所有改動、進度與優缺點

> 論文題目：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments（動態環境下移動機械臂預測導航與受約束操作）
> 報告者：陳文顥　指導教授：陳榮順 教授　單位：國立清華大學
>
> **第二次報告結束狀態**：`ammr_base` 機器人 + SE(2) GMPC + 全時域預測式 CBF，**以 Gazebo 真值障礙物**達動態避障 success 9/10 = 90%（baselines 30–40%）。
> **第三次主軸**：① 升級到帶感測器的真實 omni 平台 ② **以真實 LiDAR 感知取代真值障礙物**（scan vs truth 消融）③ 動/靜雙層避障 ④ 穩健定位 ⑤ **公平的碰撞量測與 CBF 安全邊距重新設計**（本次後段核心）。
>
> 本報告每節以「做了什麼 → 優點 → 缺點/代價」陳述，誠實標示已驗證與待補。

---

## 總覽：第二次承諾 vs 本期狀態

| 第二次報告之未來工作 | 本期狀態 |
|---|---|
| 真實感知 pipeline（LiDAR 取代真值）| ✅ `scan_obstacle_tracker` 完成；naive 40% → 過濾 73% |
| 控制平滑性精修（velocity_smoother）| ✅ 兩條路線：gmpc.py 次佳解（消 spike）+ 重新加回 velocity_smoother |
| 朝真機可部署 | ✅ 帶感測器 omni 平台 + 穩健定位（不需 SLAM）|
| Proactive detour（planner 整合 KF）| 🔶 仍列未來工作（雙層中 planner 走 costmap 靜態，CBF 走動態）|
| Whole-body MPC + 接觸操作 | 🔶 論文後段 |

**本期額外完成（非第二次承諾）**：四方法公平 benchmark 框架（scan/truth/mppi/rpp）、**以 gz 真值座標 + 機器人實體半徑的全障礙物碰撞量測**、**CBF 動/靜分離安全邊距**（解擦撞與窄縫卡死的取捨）。

---

## 一、平台移植：ammr_base → omni_bot（帶感測器真實平台）

從下載的 ROS1 / Gazebo-Classic 模型 `my_omnibot_description` 移植到 **ROS2 Jazzy + gz Sim 8（Harmonic）**：底盤 mesh + 4 全向輪 + **2D LiDAR** + **RealSense D435i 深度相機**。控制沿用 `cmd_vel` 級（gz `VelocityControl`）。

**移植踩到的坑與修法**
| 問題 | 修法 |
|---|---|
| 車體/滾子 mesh 不顯示（gz 解不開 `package://`）| xacro 內改絕對 `file://$(find …)` |
| 自身 odom 漂（dead-reckoning，終點離真值 40m）| 改 gz `OdometryPublisher` **真值里程計** → `/odom_raw` → `odom_tf_broadcaster` |
| LiDAR 被底盤 4 根對角柱遮擋（自體回波 → amcl 崩）| `scan_relay` 角度遮罩（45/135/225/315° 各 ±15°）|

- **優點**：從點質點升級為**有真實感測器、可量化、朝真機部署**的平台；靜態追蹤 RMSE **0.049m**、GMPC/GMPC+CBF 皆 success（solve < 0.5ms）。
- **缺點/代價**：`OdometryPublisher` 的真值里程計是**模擬器特權**（真機需以動捕/SLAM 提供真值）；mesh 絕對路徑降低套件可攜性。

---

## 二、真實 LiDAR 感知：`scan_obstacle_tracker`（核心貢獻）

```
/scan → 地圖背景相減 → 相鄰聚類 → Hungarian 關聯 → 每 track 一個等速度 KF → /gmpc/obstacles [x,y,r,vx,vy]
```
**完全不讀障礙物真值**，純從 LiDAR 偵測未知動態物，輸出格式與真值版 `obstacle_aggregator` 相同 → GMPC+CBF 直接吃。

**感知架構演進（試誤過程）**
| 版本 | 真實成功率 | 結論 |
|---|---|---|
| map-frame + 背景相減（naive）| 40% | amcl 跳變 → 牆落空地格 → 牆幻影 → CBF 卡死 |
| **+ 速度門檻 + 界外拒絕 + 膨脹**（定案）| **73%** | 速度門檻最關鍵；剩餘失敗為 amcl 飄移（非感知）|
| odom-frame 追蹤 | （驗證後棄用）| 真機 odom 會漂、不可轉移 |

- **優點**：可處理**名單外未知動態物**，是 scan-vs-truth 消融的基礎；速度門檻同時擔任「動/靜分流」。你加的過濾把成功率 **40% → 73%**（同 amcl 框架）。
- **缺點/代價**：偵測有**延遲**（min_track_age + KF 收斂 ~0.3–0.5s）→ CBF 慢半拍 → 比真值版更易擦邊（這正是 scan vs truth 要量化的代價，見第五、六節）。

---

## 三、動/靜雙層避障架構

- **靜態物（含未知）**：nav2 costmap obstacle_layer 從 `/scan` 標記 → **planner 繞行**
- **動態物**：`scan_obstacle_tracker`（速度門檻）→ **CBF 預測式閃避**
- **靜態 CBF backstop（Solution 1）**：另取每扇區最近牆點（v=0）餵 CBF，避免「閃動態物時側移撞牆」
- 世界加未知靜態障礙物（黃色、不在地圖中）測雙層

- **優點**：已知靜態交給成熟的 planner、動態交給反應快的 CBF，分工清楚；靜態 backstop 補住「夾在動態與牆之間」的死角。
- **缺點/代價**：planner 的 costmap **沒有時間維度**（不知障礙會走開）→ Start-occupied lockout 風險仍在（列未來工作）；靜態 CBF 與動態 CBF 共用邊距曾造成窄縫卡死（第五節已解）。

---

## 四、穩健定位：amcl beam-skip + robot_localization EKF（最大攻關）

**問題**：純 2D amcl 對先驗地圖 scan-match，遇未知障礙物 → 對不上 → 定位飄移/崩潰 → 污染下游全部。

**解法（定案）**
1. **amcl beam-skip**（`likelihood_field_prob` + `do_beamskip`）：**忽略打到未知物的光束**，只用對得上地圖的牆定位
2. **EKF**（融合 `/odom` 真值里程計 + `/amcl_pose`、平滑、拒殘餘跳變）→ 穩定 map→odom

- **優點**：**多個未知物壓在路徑上、定位仍不飄** → **不需 SLAM、真機可部署**；附帶把 nav 設定拆出乾淨的 `nav2_localization.yaml`。
- **缺點/代價**：amcl 估測仍有 **~3cm 平均、偶發 >1m** 的瞬時誤差（本期實測 map→odom 平移 mean 2.9cm / max 1.3m）—— 這個誤差若拿去算碰撞會造成**假性穿透**，是第六節改用真值座標的直接動機。

---

## 五、★ CBF 安全邊距重新設計：動/靜分離（本期後段核心）

**起因**：用「機器人實體半徑」量碰撞後，發現 gmpc_scan 會**真的擦進障礙物**（動態最深 −7cm、牆 −5cm）。根因不是 metric，而是 —

```
CBF keep-out = 障礙半徑 + cbf_safe_margin
機器人是圓盤、CBF 把機器人當「點」，故 cbf_safe_margin 必須 ≥ 機器人半徑 0.30
原本 cbf_safe_margin = 0.30 = 機器人半徑 → 安全餘量 = 0
→ 只要 QP slack 或 scan 追蹤延遲，就穿進去
```

**修法演進**
| 設定 | 動態 keep-out | 靜態 keep-out | 結果 |
|---|---|---|---|
| 原本（共用 0.30）| 邊緣 0 緩衝 | 牆邊緣 5cm | 擦撞（動 −7 / 靜 −5cm）|
| 全域 bump 0.38 | 邊緣 +8cm | 牆邊緣 **13cm** | 擦撞 0，**但窄縫卡死**（離終點 3.2m 凍結）|
| **動/靜分離（定案）** | **0.38**（+8cm 緩衝）| **0.33**（牆邊緣 +8cm，keep-out 0.38）| ✅ 擦撞 0、min clearance +5cm、**到終點** |

實作：`gmpc.py` 改為**每個障礙物可帶自己的 `margin`**（`r_eff = radius + obs.get('margin', cfg.cbf_safe_margin)`）；`gmpc_node` 給靜態牆點標上較小的 `static_cbf_safe_margin=0.33`。

- **優點**：**動靜分治符合物理直覺** —— 開闊區的移動障礙留足緩衝擋穿透，已知的牆只當 backstop 用小邊距、保留窄縫通行；**擦撞歸零又能到終點**（gmpc_scan 實測：0 碰撞、min clearance +0.05m、185s 到達）。
- **缺點/代價**：多一個需調的參數；`static=0.33` 仍是經驗值，**極窄縫（< ~0.86m）仍可能卡**；scan 模式因感知噪，牆擦邊只是**降低而非全消**（真值模式才完全 0 牆穿透）。

---

## 六、★ 公平的碰撞量測：gz 真值座標 + 機器人實體半徑 + 全障礙物

第二次報告的 `min_clearance` 只對真值動態物、且 baselines 缺 channel 報 NaN。本期重建為**四方法可比、純幾何**的量測（`evaluation/analyze.py`）：

1. **機器人座標用 gz 真值**（raw `/odom`，drift-free），**不用 amcl 校正後的 map pose** —— 因 amcl 有 ~3cm/偶發 >1m 誤差，與要量的 cm 級間距同量級，會把定位噪當成碰撞。（實證：同一趟 truth，用 amcl 座標讀到 −0.21m 假穿透，換真值座標只剩 −0.056m。）
2. **機器人半徑 0.30**（由 `chassis_frame.stl` 實測 0.6×0.6 圓盤確認；與 CBF `cbf_safe_margin` 及 costmap `robot_radius 0.33` 一致）。
3. **全障礙物**：真值動態物（`/model/*/pose`）+ 地圖牆（distance transform）+ 未知靜態柱（SDF 解析）；間距一律 surface-to-surface（兩半徑相減）。

- **優點**：**零估測噪、四方同標準**；揭露真實穿透（原版會被 amcl 噪掩蓋）；baselines（mppi/rpp）終於有真實 clearance 數字可比。
- **缺點/代價**：依賴 gz 真值里程計（真機需另接真值源）；`success/到達` 仍用 amcl pose（控制器視角），故 CSV 有 `pose_source` 雙軌，需在論文註明「碰撞用真值、到達用估測」的分工。

---

## 七、四方法公平 benchmark 框架（scan / truth / mppi / rpp）

`evaluation/run_omnibot_dynamic.sh` 改為單一 harness 分派四法，**同世界、同 driver、同 amcl/EKF、同 velocity_smoother、同 goal/tol**：
- `gmpc_scan`：本方法 + **真實 LiDAR 感知**
- `gmpc_truth`：本方法 + **真值障礙物**（感知消融上界）
- `mppi` / `rpp`：Nav2 baselines（FollowPath）

- **優點**：一鍵跑出**「感知消融（scan vs truth）」+「baseline 對比」**兩條主線；條件對齊、可重現。
- **缺點/代價**：rpp 為純路徑跟隨、**無動態避障**，動態場景近乎 DNF（本期單趟：卡離終點 9.8m、原地打轉）—— 對比公平但需在論文說明 rpp 屬「無避障下界」。

---

## 八、控制平滑：兩條路線

1. `gmpc.py` 接受 OSQP `max iterations` 之**次佳解、不緊急煞車** → 消掉 spike（動態 jerk 0.56/0.23/0.67 vs 舊版 spike 18/6/36）。
2. **重新加回 `nav2_velocity_smoother`**（gmpc → `cmd_vel_nav` → smoother → `/cmd_vel`），對 cmd_vel 做速率限制。

- **優點**：雙保險壓抑抖動，對下游 whole-body 接觸操作的末端穩定性鋪路。
- **缺點/代價**：smoother 引入額外延遲（rate-limit），與 CBF 反應速度有微小取捨；jerk metric 仍用 std（outlier 敏感），論文宜切 p95（待辦）。

---

## 九、場景豐富化

- **4 個會動黑色障礙**（dyn_obs_0/1/2/5，半徑 0.25，速度 0.15–0.3）+ **4 個未知靜態黃色柱**（unknown_obs，不在地圖中）+ 地圖牆。
- 修掉 dyn_obs 軌跡終點落在牆內導致凍結的問題。

- **優點**：同時考驗動態閃避、未知靜態繞行、雙層分工；碰撞量測涵蓋**全部**障礙物。
- **缺點/代價**：動態障礙為 kinematic（不受力），碰撞以幾何穿透誠實記錄而非物理推擠。

---

## 十、新增/修改檔案（本期後段）

**修改**
- `evaluation/analyze.py`：真值座標碰撞、機器人半徑 0.30、全障礙物 clearance、static distance-transform
- `evaluation/run_omnibot_dynamic.sh`：四方法分派 harness
- `src/ammr_wholebody_mpc/.../gmpc.py`：per-obstacle `margin`
- `src/ammr_wholebody_mpc/.../gmpc_node.py`：`static_cbf_safe_margin` 參數 + 靜態牆點標 margin
- `src/my_omnibot_description/launch/omni_bot_dynamic.launch.py`：`cbf_safe_margin 0.38`、velocity_smoother、truth 模式 static-CBF parity
- `src/ammr_bringup/{worlds,config}`：4 動態 + 4 未知靜態場景

（平台/感知/定位/雙層之新增檔案詳見 `第三次進度_修改紀錄.md`。）

---

## 十一、關鍵成果與單趟驗證（N=40×4 overnight 待補）

| 項目 | 結果 |
|---|---|
| 平台 | omni_bot（ROS2/gz）+ LiDAR + 深度相機，靜態 RMSE 0.049m |
| 真實感知避障 | naive 40% → 過濾 **73%**（全程真實 amcl）|
| 穩健定位 | beam-skip + EKF → 未知物環境**定位不飄**（不需 SLAM）|
| **碰撞量測** | 真值座標 + 半徑 0.30 + 全障礙物，四方同標準 |
| **CBF 邊距** | 動 0.38 / 靜 0.33；gmpc_scan 單趟 **0 碰撞、min clearance +0.05m、到終點 185s** |
| solve time | scan 場景 mean ~4.5ms / max ~39ms（< 50ms 週期）|

> ⏳ **待補**：四方法 × N=40 overnight 完整表（scan vs truth 消融 + baseline 對比）。

---

## 十二、已知限制（誠實陳述）與未來工作

**限制**
- scan 偵測延遲 → 動態擦邊只能**降低非全消**（真實感知 vs 真值的本質代價；可調 `cbf_safe_margin`）
- 靜態邊距 0.33 為經驗值，極窄縫仍可能卡（安全 vs 通行的取捨）
- planner costmap 無時間維度 → Start-occupied lockout 風險（reactive within horizon，非 proactive detour）
- 碰撞量測依賴 gz 真值里程計（真機需另接真值源）

**未來工作**
- **Proactive detour**：KF 預測投到 spatio-temporal costmap，讓 planner 規劃階段就繞開
- CBF 參數靈敏度（α、margin、slack）之安全–效率 Pareto
- jerk metric 切 p95；感知延遲補償
- body-frame + ego-motion 補償感知（免疫 odom 漂移）
- Whole-body MPC（SE(2) 底座 + 7-DOF 手臂）+ 接觸操作
