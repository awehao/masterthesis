# 第二次進度報告 — 逐頁內容（供 NotebookLM 生成簡報用）

> 論文題目：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments（動態環境下移動機械臂預測導航與受約束操作）
> 報告者：陳文顥　指導教授：陳榮順 教授　單位：國立清華大學
>
> 使用說明：每頁分「投影片要點」（放上簡報的文字）與「口頭講稿」（NotebookLM 生成敘述用）。標示 ✅＝已完成並驗證、🔶＝進行中/僅 smoke test、⚠️＝技術路線調整。

---

## P1 — 封面

**投影片要點**
- 標題：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments
- 副標：動態環境下移動機械臂預測導航與受約束操作
- 報告者：陳文顥｜指導教授：陳榮順 教授
- 國立清華大學｜第二次進度報告｜2026/06

**口頭講稿**
教授好，這是我的第二次進度報告。延續上次的題目，這次主要回報自上次報告以來在「預測導航」這條主線上的具體進展，重點是 MPC 區域控制器的實作完成、量化評估框架的建立，以及避障策略上的技術路線調整。

---

## P2 — 報告大綱

**投影片要點**
- 上次進度回顧與本次總覽
- 研究目標回顧
- 方法：SE(2) 幾何模型預測控制（GMPC）
- 方法：控制障壁函數（CBF）安全濾波器
- 系統整合：ROS2 + Nav2 架構
- 實驗結果：離線驗證、Baseline 對比、評估框架、動態避障
- 技術路線調整說明
- 未來工作

**口頭講稿**
今天的報告分成幾個部分：先回顧上次承諾與這次完成的對照，接著說明這次實作的核心方法 GMPC 與 CBF，然後是系統整合與實驗結果，最後誠實交代我做的兩個技術路線調整，以及接下來的工作。

---

## P3 — 上次進度回顧 & 本次總覽 ⭐

**投影片要點**（建議做成左右對照表）

| 第一次報告承諾 | 本期狀態 |
|---|---|
| MPC 區域控制器實作 | ✅ 完成，採 SE(2) 幾何 MPC（GMPC） |
| 量化評估實驗 | ✅ 完成，22 項指標 + Baseline 三方對比 |
| Kalman Filter 障礙物預測 | ✅ 完成，4 狀態等速度 KF，速度估計餵入控制器 |
| 動態障礙物場景建置 | ✅ 場景 + 預測式 CBF 完成；動態實跑成功到達 + 零碰撞；4 方法完整對比待補 |
| Behavior Tree 高層決策 | ⚠️ 先採 goal_to_plan_relay + 持續重規劃，BT 延後 |

- 一句總結：本期完成核心控制器、Kalman Filter 預測與全時域預測式 CBF 安全機制，並建立量化評估框架；唯一延後的是 Behavior Tree 高層決策

**口頭講稿**
先和上次的承諾對照。上次的未來工作我列了五項，這次大致都做到了。MPC 區域控制器做成了更強的幾何版本 GMPC；量化評估框架也建好了；Kalman Filter 障礙物預測我做了一個四狀態的等速度濾波器，而且它估出來的速度有真的餵進控制器做預測；動態場景和安全機制也建好、實際跑過了，只是四種方法的完整對比還在湊。唯一延後的是 Behavior Tree，我先用自寫的持續重規劃把主迴路打通，原因我後面說明。

---

## P4 — 研究目標回顧（精簡）

**投影片要點**
- 總目標：建立基於 ROS2 之移動操作機器人系統，於動態環境中以底座與手臂全身協調控制完成接觸操作任務，並以數位孿生平台驗證
- 本期聚焦：**Phase 1 — 具預測能力的動態避障導航**
  - 以 MPC 為核心的區域控制器（本期完成）
  - 動態障礙物安全約束（本期以 CBF 實現）
  - 量化評估指標：追蹤誤差／到達時間／控制平滑性／安全距離／任務成功率

**口頭講稿**
研究的總目標不變，是做一套能在動態環境中全身協調、完成接觸操作的移動機械臂系統。這次報告聚焦在第一階段，也就是具預測能力的動態避障導航。具體就是把 MPC 區域控制器做出來、加上動態障礙物的安全約束，並建立一套量化評估指標。

---

## P5 — 方法：為什麼用 SE(2) 幾何 MPC ✅

**投影片要點**
- 失敗教訓（4/14）：以 (x, y, θ) 為狀態的傳統 MPC，當機器人連續旋轉、角度跨越 ±π 時誤差不連續 → 控制器發散
- 修復做法：
  - 狀態改用 **SE(2) Lie group**；底盤位姿 $X \in SE(2)$，體座標速度 $\xi=(v_x,v_y,\omega)$，運動學 $\dot X = X\hat\xi$
  - 幾何測地誤差（角度自然 wrap、無不連續）：
    $$e \;=\; \log\!\big(X_{\mathrm{ref}}^{-1}\, X\big)^{\vee} \in \mathfrak{se}(2)$$
  - 沿參考軌跡**多點線性化**（取代單點線性化）
- 賣點：本研究底盤為 **Omni（全向）**，輸入矩陣 B = I；與 Tang et al. 2024 的 nonholonomic 版本形成差異化

**口頭講稿**
先說為什麼要用幾何 MPC。我在四月中第一版用最直覺的 (x, y, θ) 狀態做 MPC，結果發現機器人連續旋轉、角度跨過正負 π 的時候，誤差會跳變，控制器直接發散。修法是把狀態放到 SE(2) 這個李群上，誤差用對數映射定義，這樣角度的環繞是自然連續的；同時沿參考軌跡做多點線性化，而不是只在一個點。另外我的底座是全向輪，輸入矩陣是單位陣，這和文獻裡常見的非完整約束車輛不同，是我的差異點。

---

## P6 — 方法：GMPC 數學構成 ✅

**投影片要點**（決策變數 $\delta\xi_k \triangleq \xi_k - \xi_{\mathrm{ref}}(k)$）

1. 連續誤差動態（由 $\dot X = X\hat\xi$ 線性化推得）：
$$\dot e \;=\; -\,\mathrm{ad}(\xi_{\mathrm{ref}})\,e \;+\; (\xi - \xi_{\mathrm{ref}})$$

2. 離散化（Forward Euler，**沿參考多點線性化**）：
$$e_{k+1} = A_d(k)\,e_k + \Delta t\,\delta\xi_k,\qquad A_d(k) = I - \Delta t\,\mathrm{ad}\big(\xi_{\mathrm{ref}}(k)\big)$$

3. 凝聚式預測（堆疊 $E=\Phi e_0 + \Gamma z$，$z=[\delta\xi_0,\dots,\delta\xi_{N-1}]$）：
$$\Phi[i]=\prod_{k=0}^{i}A_d(k),\qquad \Gamma[i,j]=\Big(\prod_{k=j+1}^{i}A_d(k)\Big)\Delta t\,I\ \ (j\le i)$$

4. 成本函數 → OSQP 標準式 $\tfrac12 z^\top P z + q^\top z$：
$$J=\sum_{k=1}^{N-1} e_k^\top Q e_k + e_N^\top Q_f e_N + \sum_{k=0}^{N-1}\delta\xi_k^\top R\,\delta\xi_k$$
$$P = 2(\Gamma^\top \bar Q \Gamma + \bar R),\qquad q = 2\,\Gamma^\top \bar Q\,\Phi\,e_0$$

5. 約束（速度 box + 加速度有限差分）：
$$u_{\min}\le \xi_{\mathrm{ref}}(k)+\delta\xi_k \le u_{\max},\qquad |u_k - u_{k-1}|\le a_{\max}\Delta t$$

- 引用：Solà 18（Lie）、Borrelli 17（condensed MPC）、Stellato 20（OSQP）；完整推導見 MATH.md

**口頭講稿**
數學上是一條鏈。從 SE(2) 的測地誤差定義，線性化推出第一條的連續誤差動態，注意這裡的 ad 是參考速度的伴隨算子。第二步做前向尤拉離散化，這裡的 A_d 每一步都不同，因為它沿著參考軌跡逐點線性化，這正是修掉舊版發散的關鍵。第三步把整個預測時域用 Phi 和 Gamma 兩個矩陣壓縮成凝聚式。第四步把追蹤誤差加控制量的成本，整理成 OSQP 標準的二次型，得到 P 和 q。最後加上速度的箱型限制和加速度的有限差分限制，丟給 OSQP 求解。每條公式都有出處和逐步推導。

---

## P7 — 方法：Kalman Filter 預測 + 全時域預測式 CBF ✅

**投影片要點**

**(A) 障礙物預測 — 4 狀態等速度 Kalman Filter**，狀態 $x=[p_x,p_y,v_x,v_y]^\top$，量測 $y=[p_x,p_y]^\top$：
$$x_{k+1}=F x_k + w,\quad F=\begin{bmatrix}1&0&\Delta t&0\\0&1&0&\Delta t\\0&0&1&0\\0&0&0&1\end{bmatrix},\quad y_k=Hx_k+v,\quad H=\begin{bmatrix}1&0&0&0\\0&1&0&0\end{bmatrix}$$
- 預測：$\hat x^- = F\hat x$，$P^- = FPF^\top + Q$
- 更新：$K=P^-H^\top(HP^-H^\top+R)^{-1}$，$\hat x=\hat x^- + K(y-H\hat x^-)$，$P=(I-KH)P^-$

**(B) 全時域預測式 CBF**，對每個障礙物 $i$、每個預測步 $k$：
- 障礙物等速度預測：$o_i(k) = o_i(0) + v_i\, k\,\Delta t$（$v_i$ 來自 KF）
- 障壁函數：$h_i(k) = \lVert p(k) - o_i(k)\rVert^2 - (r_i + d_{\mathrm{safe}})^2$
- 速度層 CBF（含相對速度，故能提前反應）：
$$\dot h_i(k) + \alpha\, h_i(k) \;\ge\; -\,\varepsilon_k,\qquad \dot h_i(k)=2\big(p(k)-o_i(k)\big)^\top\!\big(R(\theta_k)\,v_{\mathrm{body}} - v_{\mathrm{obs}}\big)$$
- 線性化成 $z$ 的一條約束：$\;\mathrm{grad}_{\mathrm{body}}(k)\cdot\delta\xi_k + \varepsilon_k \ge b(k)$
- Slack：**$\varepsilon_0 = 0$（當下步為硬約束）**；$k\ge1$ 之 $\varepsilon_k$ 帶 L2 罰項 $\rho\,\varepsilon_k^2$，僅在前瞻時域真不可解時啟用 → QP 永遠可解
- Safer fallback：OSQP 不可解時輸出 $u = 0$（緊急煞車），不退回參考速度

**口頭講稿**
避障的安全機制是兩段。第一段，我用一個四狀態的等速度 Kalman Filter 追蹤每個障礙物，從位置量測同時估出它的位置和速度。第二段，把速度餵進控制障壁函數，而且我做的是全時域版本——不是只看當下這一步，而是對整個預測時域的每一步都加安全約束，並用 KF 估的速度假設障礙物等速度往前推，預測它未來在哪裡。這樣相對速度會進到約束裡，機器人能提前繞行，而不是等到撞前一刻才急煞。為了避免和加速度限制衝突時 QP 無解，我加了鬆弛變數，讓安全約束變成盡力而為、求解器永遠有解；萬一真的無解就直接零速度急煞。

---

## P8 — 系統整合：ROS2 + Nav2 架構 ✅ / ⚠️（BT 延後說明）

**投影片要點**（放架構流程圖）
```
/goal_pose ─► goal_to_plan_relay ─action─► planner_server (SmacPlanner2D)
                                                  │
                                                  ▼  /plan (Path)
[/model/dyn_obs_*/pose] ─► obstacle_aggregator ─► /gmpc/obstacles
                          (內含 Kalman Filter)      [x, y, r, vx, vy]
                                                  │
                                                  ▼
                                          gmpc_controller ─► /cmd_vel
                                          (GMPC + 預測式 CBF)
                                                  │
                                                  ▼
              診斷 topics：/gmpc/solve_time_ms、/gmpc/min_h、/gmpc/cbf_zones
```
- `obstacle_aggregator` 內含 Kalman Filter：將 pose 串流轉成含速度的障礙物狀態 `[x, y, r, vx, vy]`
- GMPC 控制器消費全域路徑 + 障礙物狀態，輸出速度命令
- 不接 Nav2 BT，自寫 `goal_to_plan_relay`：`/goal_pose` → ComputePathToPose action；relay 加 1 Hz timer 持續重規劃（行為等同 BT）
- 診斷 topics 可在 Foxglove 視覺化（CBF 安全圈、求解時間、最小障壁值）
- 路線說明：先用 relay + 持續重規劃打通主迴路，Behavior Tree 延後到高層決策階段

**口頭講稿**
這是整個系統在 ROS2 上的整合。目標點進來，由我寫的 relay 轉成路徑規劃 action，規劃器吐出全域路徑；障礙物這邊，聚合節點裡面就跑著 Kalman Filter，把模擬器的位置串流轉成帶速度的障礙物狀態發出去。GMPC 控制器同時消費路徑和障礙物狀態，算出速度命令。我還加了診斷 topic，可以在 Foxglove 上即時看到 CBF 的安全圈、每步求解時間和最小障壁值。這裡說明一下，我沒有直接用 Nav2 的 Behavior Tree，而是先用 relay 加一赫茲的持續重規劃把主迴路打通，行為上等同 BT；真正的 Behavior Tree 高層決策留到後面的階段。

---

## P9 — 實驗一：離線軌跡追蹤驗證 ✅

**投影片要點**（放結果表）

| 軌跡 | RMSE_xy | RMSE_yaw | 平均求解時間 | 不可解次數 |
|---|---|---|---|---|
| 直線 | 0.036 m | 0.074 rad | 0.11 ms | 0 |
| 側移 | 0.072 m | 0.074 rad | 0.26 ms | 0 |
| 對角 | 0.048 m | 0.074 rad | 0.15 ms | 0 |
| S 曲線 | 0.025 m | 0.049 rad | 0.13 ms | 0 |
| **連續旋轉（跨 ±π）** | **0.020 m** | **0.027 rad** | **0.09 ms** | **0** |

- 純 Python 離線原型，5 條參考軌跡全數通過，零不可解
- 重點：連續旋轉 25 秒、跨越 ±π 的情境，舊版必爆，新版反而 RMSE 最小 → 證實幾何化修復有效

**口頭講稿**
先看離線的驗證。我用純 Python 寫了一個離線原型，跑五條參考軌跡。結果是五條全部通過，全程沒有任何一次求解失敗，平均求解時間都在零點幾毫秒。最值得講的是最後一條——連續旋轉二十五秒、角度跨過正負 π——這正是我四月那版會爆掉的情境，但幾何化之後，它反而是五條裡追蹤誤差最小的。這直接證明了 SE(2) 的修復是有效的。

---

## P10 — 實驗二：ROS2 整合 Baseline 三方對比 ✅

**投影片要點**（goal = (17, 17)，參數已對齊）

| 指標 | RPP | MPPI | GMPC | 較佳者 |
|---|---|---|---|---|
| 到達時間 (s) | 96.2 | **79.8** | 91.2 | MPPI |
| 追蹤 RMSE (m) | **0.075** | 0.165 | 0.143 | RPP |
| 路徑長度 (m) | 28.5 | 27.3 | **27.0** | GMPC |
| **側移平滑度 smooth_vy** | 0.000 | 0.015 | **0.044** | **GMPC（Omni 賣點）** |
| 重規劃次數 | 98 | 79 | 92 | 持續重規劃正常 |

- 為公平對比，三者底盤速度/加速度上限已對齊
- **誠實結論**：靜態長距離追蹤下 GMPC ≈ MPPI（略慢、RMSE 略大）；GMPC 明確勝出處為**側移利用（Omni 全向特性）**；RPP 在追蹤精度與平滑度上仍最佳
- 真正能體現 GMPC + CBF 優勢的，是動態場景（見 P12）

**口頭講稿**
接著是接上 ROS2 之後，把我的 GMPC 和兩個 baseline——也就是 Nav2 內建的 RPP 和 MPPI——放在同一個目標、同一組對齊過的底盤限制下比較。我要誠實說，在這種靜態的長距離追蹤情境，GMPC 大致和 MPPI 打平，到達時間稍慢一點、追蹤誤差稍大一點。GMPC 明確贏的地方是側移的利用，因為它是全向底盤、能側移，這也是它的賣點。RPP 在追蹤精度上仍是最強的。我的判斷是，GMPC 真正的優勢要在動態避障場景才看得出來，那是下一張的內容。

---

## P11 — 實驗三：量化評估框架 ✅

**投影片要點**
- 評估管線：`record.sh`（規範化錄 rosbag）→ `analyze.py`（bag → CSV，22 項指標）→ `plot.py`（4 方法 × 13 指標長條圖）
- 22 項指標分類：
  - 任務完成：成功率、到達時間、總時間、路徑長度
  - 追蹤精度：tracking RMSE
  - 控制平滑性：vx/vy/wz 標準差
  - Jerk：加速度標準差
  - 安全：最小淨距、碰撞次數
  - 求解效能：平均/p95/最大求解時間
- 意義：呼應第一次承諾的量化評估，後續所有方法皆以此框架公平對比

**口頭講稿**
為了讓比較有依據，我把整套評估自動化了。流程是先用一個腳本規範化地錄製 rosbag，再用分析腳本把它轉成包含二十二項指標的 CSV，最後畫成四種方法、十三項指標的長條圖。指標涵蓋任務完成、追蹤精度、控制平滑性、jerk、安全距離和求解效能。這呼應了上次承諾的量化評估，之後不管加什麼方法，都用同一套框架公平比較。

---

## P12 — 實驗四：動態場景預測式避障（GMPC + 全時域 CBF）✅

**投影片要點**（動態場景，含 KF 預測 + 全時域 CBF，實跑結果）

| 指標 | GMPC + CBF（動態實跑） | 解讀 |
|---|---|---|
| 任務成功 | **是（成功到達）** | 動態場景仍完成導航 |
| 到達時間 | 144.6 s | — |
| 追蹤 RMSE | **0.084 m** | 比靜態 corner_goal（0.143 m）更佳 |
| 碰撞數 | **0** | 安全機制成立 |
| 最小淨距 | **0.286 m** | 守住 ≈ 安全邊界 d_safe = 0.30 m |
| 求解時間 mean / p95 | **2.4 ms / 6.1 ms** | 即時，遠快於 MPPI |

- 在動態場景中同時達成：成功到達 + 零碰撞 + 淨距守住 + 即時求解 + 低追蹤誤差
- 全時域 CBF 結合 KF 速度預測，使機器人能對移動障礙物提前反應、平滑繞行
- 評估注意事項：指標須在 **map 座標系**計算（早期 odom 座標分析會誤判 success=False，已修正）
- **尚未完成**：4 方法（RPP / MPPI / GMPC / GMPC+CBF）動態場景完整對比仍待補齊

**口頭講稿**
這一頁是動態場景的結果，也是這次最關鍵的成果。GMPC 加上 Kalman Filter 預測和全時域 CBF，在有移動障礙物的場景下，同時達成了五件事：成功到達目標、全程零碰撞、最小淨距守在二十八公分、貼著我設的三十公分安全邊界、求解時間平均只有兩點四毫秒、而且追蹤誤差只有八公分,比靜態場景還更好。這代表全時域 CBF 結合速度預測，讓機器人能對移動障礙物提前反應、平滑繞行,而不是撞前急煞。補充一點評估上的細節:這些指標必須在 map 座標系計算,我早期用 odom 座標分析時會誤判成沒到達,這個已經修正。接下來要把四種方法的完整對比補齊。

---

## P13 — 範圍取捨說明（Behavior Tree 延後）⭐

**投影片要點**
- 唯一延後項：Behavior Tree 高層決策 → **本期先用 goal_to_plan_relay + 持續重規劃**
  - 理由：先確保核心控制迴路（GMPC + KF + 預測式 CBF）正確且可量測；BT 的等待/重規劃/任務切換決策，待動態場景行為穩定後再導入
- 安全機制選型補充：採 CBF 而非僅靠 costmap inflation，因 CBF 能結合 KF 預測做前瞻避障，並提供形式化的安全約束
- 原則：先打通「控制器 + 預測 + 安全」核心，高層決策層最後疊加，避免一次堆疊難以除錯

**口頭講稿**
這頁交代範圍取捨。上次承諾的五項裡，唯一延後的是 Behavior Tree。我本期先用自寫的 relay 加持續重規劃把主迴路打通，先確保核心的控制器、Kalman Filter 和預測式 CBF 是對的、而且量得到數據；真正的 Behavior Tree 高層決策，例如該等待還是重規劃、任務切換，我想等動態場景的行為穩定之後再加上去。順帶補充安全機制的選型：我用 CBF 而不是只靠 costmap 膨脹，是因為 CBF 能結合 Kalman Filter 的預測做前瞻避障，也提供比較形式化的安全約束。整體原則就是先把控制、預測、安全這個核心打通，高層決策最後再疊，比較好除錯。

---

## P14 — 未來工作

**投影片要點**
- 立即（最高優先）：補齊 4 方法 × 動態場景完整 benchmark（RPP / MPPI / GMPC / GMPC+CBF），量化 CBF 的避障增益
- 短期：
  - Monte Carlo 自動化（多隨機種子 × 多方法，輸出 box plot）
  - CBF 參數靈敏度分析（α、d_safe、slack 權重對「安全 vs 效率」的影響）
- 中期：以 `/scan` LiDAR 聚類偵測取代 Gazebo ground-truth（朝真機可部署）；Behavior Tree 高層決策導入
- 論文後段：
  - Whole-body MPC（SE(2) 底座 + 7-DOF 手臂協調）
  - 接觸操作（開門/拉抽屜的等式約束 + 力/力矩限制）

**口頭講稿**
最後是未來工作。最優先的是把四種方法在動態場景的完整對比補齊，用數據量化 CBF 帶來的避障增益。短期把實驗自動化、跑多個隨機種子，並做 CBF 的參數靈敏度分析，看 alpha、安全距離、鬆弛權重怎麼在安全和效率之間取捨。中期把障礙物來源從模擬器真值換成 LiDAR 偵測，朝真機部署靠近，並導入 Behavior Tree 做高層決策。論文後段就回到全身協調控制和接觸操作這兩個大目標。

---

## P15 — Thank you

**投影片要點**
- Thank you!
- （備註：動態避障建議現場播放影片/GIF）

**口頭講稿**
以上是我這次的進度，謝謝教授，請教授指教。

---

### 給 NotebookLM 的補充提示
- 風格：研究進度報告，語氣務實、誠實；陳述結果時區分「已完成有數據」與「初步/待補」。
- 已完成且有數據：P5–P12（GMPC 方法 + KF + 預測式 CBF + 離線驗證 + Baseline 對比 + 評估框架 + 動態場景成功避障）。
- 主成果：P12——動態場景成功到達 + 零碰撞 + 淨距守住 + 求解 ~2.4 ms + 追蹤 RMSE 0.084 m；4 方法完整對比待補齊。
- 範圍取捨：P13——唯一延後項為 Behavior Tree（先用 relay + 持續重規劃替代）。
- 視覺素材建議：P8 架構圖、P9/P10 結果表、P12 動態場景影片/截圖、Foxglove 的 CBF 安全圈畫面。

---

## 附錄：公式總表（完整版，供查閱/備用投影片）

### 符號定義
| 符號 | 意義 |
|---|---|
| $X\in SE(2)$ | 底盤位姿（3×3 齊次矩陣） |
| $\xi=(v_x,v_y,\omega)\in\mathfrak{se}(2)$ | 體座標速度（body twist） |
| $\hat\xi$ | $\xi$ 對應的李代數元素（3×3） |
| $R(\theta)$ | 2×2 旋轉矩陣 |
| $\mathrm{Ad}_X,\ \mathrm{ad}(\xi)$ | 大/小伴隨算子 |
| $e$ | 體座標測地誤差 |
| $\delta\xi_k$ | 決策變數：與參考速度的偏差 |

### A. SE(2) 幾何基礎
$$\hat\xi=\begin{bmatrix}0&-\omega&v_x\\\omega&0&v_y\\0&0&0\end{bmatrix},\qquad \dot X = X\hat\xi$$
$$\mathrm{ad}(\xi)=\begin{bmatrix}0&-\omega&v_y\\\omega&0&-v_x\\0&0&0\end{bmatrix},\qquad e=\log\!\big(X_{\mathrm{ref}}^{-1}X\big)^{\vee}$$

### B. GMPC（幾何模型預測控制）
- 連續誤差動態：$\dot e=-\mathrm{ad}(\xi_{\mathrm{ref}})\,e+(\xi-\xi_{\mathrm{ref}})$
- 離散誤差模型：$e_{k+1}=A_d(k)e_k+\Delta t\,\delta\xi_k,\ \ A_d(k)=I-\Delta t\,\mathrm{ad}(\xi_{\mathrm{ref}}(k))$
- 凝聚式預測：$E=\Phi e_0+\Gamma z$
- 成本：$J=\sum_{k=1}^{N-1}e_k^\top Q e_k+e_N^\top Q_f e_N+\sum_{k=0}^{N-1}\delta\xi_k^\top R\,\delta\xi_k$
- QP：$\min_z \tfrac12 z^\top P z+q^\top z$，$P=2(\Gamma^\top\bar Q\Gamma+\bar R)$，$q=2\Gamma^\top\bar Q\Phi e_0$
- 約束：$u_{\min}\le\xi_{\mathrm{ref}}(k)+\delta\xi_k\le u_{\max}$，$\ |u_k-u_{k-1}|\le a_{\max}\Delta t$

### C. Kalman Filter（等速度模型）
- 狀態/量測：$x=[p_x,p_y,v_x,v_y]^\top$，$y=[p_x,p_y]^\top$
- 預測：$\hat x^-=F\hat x,\ P^-=FPF^\top+Q$
- 更新：$K=P^-H^\top(HP^-H^\top+R)^{-1},\ \hat x=\hat x^-+K(y-H\hat x^-),\ P=(I-KH)P^-$

### D. 全時域預測式 CBF
- 障礙物預測：$o_i(k)=o_i(0)+v_i\,k\,\Delta t$
- 障壁函數：$h_i(k)=\lVert p(k)-o_i(k)\rVert^2-(r_i+d_{\mathrm{safe}})^2$
- 安全約束：$\dot h_i(k)+\alpha h_i(k)\ge-\varepsilon_k$，$\ \dot h_i(k)=2(p(k)-o_i(k))^\top(R(\theta_k)v_{\mathrm{body}}-v_{\mathrm{obs}})$
- Slack：$\varepsilon_0=0$（硬）；$k\ge1$ 帶罰項 $\rho\,\varepsilon_k^2$

### 引用對照
| 方法 | 主要引用 |
|---|---|
| SE(2) Lie 工具、誤差 | Solà, Deray, Atchuthan (2018)；Lynch & Park (2017) |
| Lie-group 誤差動態 | Bullo & Lewis (2005)；Mahony et al. (2008) |
| Condensed 線性 MPC、約束 | Borrelli, Bemporad, Morari (2017) |
| QP 求解器 | Stellato et al. (2020) OSQP |
| 控制障壁函數 CBF | Ames et al. (2017)；離散/MPC 版 Zeng, Zhang, Sreenath (2021) |
