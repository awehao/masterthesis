# 第二次進度報告 — 逐頁內容（供 NotebookLM 生成簡報用）

> 論文題目：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments（動態環境下移動機械臂預測導航與受約束操作）
> 報告者：陳文顥　指導教授：陳榮順 教授　單位：國立清華大學
>
> 使用說明：每頁分「投影片要點」（放上簡報的文字）與「口頭講稿」（NotebookLM 生成敘述用）。標示 ✅＝已完成並驗證、🔶＝進行中/僅 smoke test、⚠️＝技術路線調整。

---

## P1 — 封面

**投影片要點**
- 標題：Predictive Navigation and Constrained Manipulation of Mobile Manipulators in Dynamic Environments
- 副標：動態環境下移動機械臂預測導航與受約束操作
- 報告者：陳文顥｜指導教授：陳榮順 教授
- 國立清華大學｜第二次進度報告｜2026/06

**口頭講稿**
教授好，這是我的第二次進度報告。延續上次的題目，這次主要回報自上次報告以來在「預測導航」這條主線上的具體進展，重點是 MPC 區域控制器的實作完成、量化評估框架的建立，以及避障策略上的技術路線調整。

---

## P2 — 報告大綱

**投影片要點**
- 上次進度回顧與本次總覽
- 研究目標回顧
- 方法：SE(2) 幾何模型預測控制（GMPC）
- 方法：控制障壁函數（CBF）安全濾波器
- 系統整合：ROS2 + Nav2 架構
- 實驗結果：離線驗證、Baseline 對比、評估框架、動態避障
- 技術路線調整說明
- 未來工作

**口頭講稿**
今天的報告分成幾個部分：先回顧上次承諾與這次完成的對照，接著說明這次實作的核心方法 GMPC 與 CBF，然後是系統整合與實驗結果，最後誠實交代我做的兩個技術路線調整，以及接下來的工作。

---

## P3 — 上次進度回顧 & 本次總覽 ⭐

**投影片要點**（建議做成左右對照表）

| 第一次報告承諾 | 本期狀態 |
|---|---|
| MPC 區域控制器實作 | ✅ 完成，採 SE(2) 幾何 MPC（GMPC） |
| 量化評估實驗 | ✅ 完成，22 項指標 + Baseline 三方對比 |
| 動態障礙物場景建置 | 🔶 場景與 CBF stack 建好，完整 benchmark 進行中 |
| Kalman Filter 障礙物預測 | ⚠️ 改採 CBF 安全濾波器（單步），KF/全 horizon 延後 |
| Behavior Tree 高層決策 | ⚠️ 改採 goal_to_plan_relay + 持續重規劃，BT 延後 |

- 一句總結：本期完成核心控制器與量化評估框架，並就避障策略做出有意識的技術路線調整

**口頭講稿**
先和上次的承諾對照。上次的未來工作我列了五項。其中 MPC 區域控制器和量化評估框架這兩項已經完整交付，而且 MPC 我做成了更強的幾何版本 GMPC。動態場景與安全機制建好了，但完整的對比實驗還在跑。另外兩項——Kalman Filter 預測和 Behavior Tree——我做了路線調整，改用 CBF 安全濾波器和持續重規劃先把核心打通，原本的方案延後到下一階段，這部分我後面會專門說明理由。

---

## P4 — 研究目標回顧（精簡）

**投影片要點**
- 總目標：建立基於 ROS2 之移動操作機器人系統，於動態環境中以底座與手臂全身協調控制完成接觸操作任務，並以數位孿生平台驗證
- 本期聚焦：**Phase 1 — 具預測能力的動態避障導航**
  - 以 MPC 為核心的區域控制器（本期完成）
  - 動態障礙物安全約束（本期以 CBF 實現）
  - 量化評估指標：追蹤誤差／到達時間／控制平滑性／安全距離／任務成功率

**口頭講稿**
研究的總目標不變，是做一套能在動態環境中全身協調、完成接觸操作的移動機械臂系統。這次報告聚焦在第一階段，也就是具預測能力的動態避障導航。具體就是把 MPC 區域控制器做出來、加上動態障礙物的安全約束，並建立一套量化評估指標。

---

## P5 — 方法：為什麼用 SE(2) 幾何 MPC ✅

**投影片要點**
- 失敗教訓（4/14）：以 (x, y, θ) 為狀態的傳統 MPC，當機器人連續旋轉、角度跨越 ±π 時誤差不連續 → 控制器發散
- 修復做法：
  - 狀態改用 **SE(2) Lie group**，誤差以 `log(X⁻¹·X_ref)` 定義（角度自然 wrap，無不連續）
  - 沿參考軌跡**多點線性化**（取代單點線性化）
- 賣點：本研究底盤為 **Omni（全向）**，輸入矩陣 B = I；與 Tang et al. 2024 的 nonholonomic 版本形成差異化

**口頭講稿**
先說為什麼要用幾何 MPC。我在四月中第一版用最直覺的 (x, y, θ) 狀態做 MPC，結果發現機器人連續旋轉、角度跨過正負 π 的時候，誤差會跳變，控制器直接發散。修法是把狀態放到 SE(2) 這個李群上，誤差用對數映射定義，這樣角度的環繞是自然連續的；同時沿參考軌跡做多點線性化，而不是只在一個點。另外我的底座是全向輪，輸入矩陣是單位陣，這和文獻裡常見的非完整約束車輛不同，是我的差異點。

---

## P6 — 方法：GMPC 數學構成（一頁帶過）✅

**投影片要點**（呈現「數學鏈」而非堆公式）
- 連續誤差動態：`ė = −ad(ξ_ref)·e + δξ`
- 離散化（Forward Euler）：`A_d = I − dt·ad(ξ_ref)`
- Condensed QP：`P = 2(Γᵀ Q̄ Γ + R̄)`，`q = 2 Γᵀ Q̄ Φ e₀`
- 約束：速度 box + 加速度有限差分上下限
- 公式來源與推導：對照 Tang et al. 2024（ADJ scheme, eq. 17），完整推導見 MATH.md

**口頭講稿**
數學上我不展開細節，就講一條鏈。從 SE(2) 的誤差定義推出連續的誤差動態，做前向尤拉離散化，再把整個預測時域壓縮成一個凝聚式的二次規劃問題，目標函數同時懲罰追蹤誤差和控制量，約束是速度的箱型限制加上加速度的有限差分。每一條公式我都有出處和逐步推導，整理在一份數學文件裡，和 Tang 2024 的伴隨方案完全對應。

---

## P7 — 方法：CBF 安全濾波器 ✅（單步）/ ⚠️（取代 KF 的說明）

**投影片要點**
- 障壁函數：`h(p) = ‖p − o‖² − (r + d_safe)²`
- 安全約束：`ḣ + α·h ≥ 0`（單步，塞在 δξ₀）
- Slack 變數 ε ≥ 0 + 高罰項 ρ·ε²：與加速度約束衝突時退化為 best-effort，QP 永遠可解
- Safer fallback：OSQP 不可解時輸出 u = 0（緊急煞車），不退回參考速度（會撞）
- 路線說明：先用 CBF 提供直接的安全保證；KF 的全 horizon 預測排在下一步

**口頭講稿**
避障的安全機制我用控制障壁函數。定義一個障壁函數衡量機器人到障礙物的距離，要求它的變化率滿足一個不等式，把這條約束加進 QP。為了避免和加速度限制衝突時 QP 無解，我加了鬆弛變數和高懲罰，讓安全約束變成盡力而為，保證求解器永遠有解；萬一真的無解，就直接輸出零速度急煞，而不是退回參考速度去撞上去。這裡也順帶交代，我先用 CBF 是因為它能直接給出安全保證，比較直接；上次提到的 Kalman Filter 預測我把它和全時域 CBF 一起排到下一步。

---

## P8 — 系統整合：ROS2 + Nav2 架構 ✅ / ⚠️（取代 BT 的說明）

**投影片要點**（放架構流程圖）
```
/goal_pose ─► goal_to_plan_relay ─action─► planner_server (SmacPlanner2D)
                                                  │
                                                  ▼  /plan (Path)
[/model/dyn_obs_*/pose] ─► obstacle_aggregator ─► /gmpc/obstacles
                                                  │
                                                  ▼
                                          gmpc_controller ─► /cmd_vel
                                                  │
                                                  ▼
              診斷 topics：/gmpc/solve_time_ms、/gmpc/min_h、/gmpc/cbf_zones
```
- 不接 Nav2 BT，自寫 `goal_to_plan_relay`：把 `/goal_pose` 轉成 ComputePathToPose action
- 持續重規劃：relay 加 1 Hz timer，未到達即重發 plan 請求（行為等同 BT）
- 診斷 topics 可在 Foxglove 視覺化（CBF 安全圈、求解時間、最小障壁值）
- 路線說明：先用 relay + 持續重規劃打通主迴路，Behavior Tree 延後到高層決策階段

**口頭講稿**
這是整個系統在 ROS2 上的整合。流程是：目標點進來，由我寫的 relay 節點轉成路徑規劃的 action，規劃器吐出全域路徑；同時障礙物聚合節點從模擬器拿障礙物位置；GMPC 控制器消費這兩者，輸出速度命令。我還加了幾個診斷 topic，可以在 Foxglove 上即時看到 CBF 的安全圈、每步求解時間和最小障壁值。這裡也說明，我沒有直接用 Nav2 的 Behavior Tree，而是先用 relay 加上一赫茲的持續重規劃把主迴路打通，行為上等同 BT；真正的 Behavior Tree 高層決策我留到後面的階段。

---

## P9 — 實驗一：離線軌跡追蹤驗證 ✅

**投影片要點**（放結果表）

| 軌跡 | RMSE_xy | RMSE_yaw | 平均求解時間 | 不可解次數 |
|---|---|---|---|---|
| 直線 | 0.036 m | 0.074 rad | 0.11 ms | 0 |
| 側移 | 0.072 m | 0.074 rad | 0.26 ms | 0 |
| 對角 | 0.048 m | 0.074 rad | 0.15 ms | 0 |
| S 曲線 | 0.025 m | 0.049 rad | 0.13 ms | 0 |
| **連續旋轉（跨 ±π）** | **0.020 m** | **0.027 rad** | **0.09 ms** | **0** |

- 純 Python 離線原型，5 條參考軌跡全數通過，零不可解
- 重點：連續旋轉 25 秒、跨越 ±π 的情境，舊版必爆，新版反而 RMSE 最小 → 證實幾何化修復有效

**口頭講稿**
先看離線的驗證。我用純 Python 寫了一個離線原型，跑五條參考軌跡。結果是五條全部通過，全程沒有任何一次求解失敗，平均求解時間都在零點幾毫秒。最值得講的是最後一條——連續旋轉二十五秒、角度跨過正負 π——這正是我四月那版會爆掉的情境，但幾何化之後，它反而是五條裡追蹤誤差最小的。這直接證明了 SE(2) 的修復是有效的。

---

## P10 — 實驗二：ROS2 整合 Baseline 三方對比 ✅

**投影片要點**（goal = (17, 17)，參數已對齊）

| 指標 | RPP | MPPI | GMPC | 較佳者 |
|---|---|---|---|---|
| 到達時間 (s) | 96.2 | **79.8** | 91.2 | MPPI |
| 追蹤 RMSE (m) | **0.075** | 0.165 | 0.143 | RPP |
| 路徑長度 (m) | 28.5 | 27.3 | **27.0** | GMPC |
| **側移平滑度 smooth_vy** | 0.000 | 0.015 | **0.044** | **GMPC（Omni 賣點）** |
| 重規劃次數 | 98 | 79 | 92 | 持續重規劃正常 |

- 為公平對比，三者底盤速度/加速度上限已對齊
- **誠實結論**：靜態長距離追蹤下 GMPC ≈ MPPI（略慢、RMSE 略大）；GMPC 明確勝出處為**側移利用（Omni 全向特性）**；RPP 在追蹤精度與平滑度上仍最佳
- 真正能體現 GMPC + CBF 優勢的，是動態場景（見 P12）

**口頭講稿**
接著是接上 ROS2 之後，把我的 GMPC 和兩個 baseline——也就是 Nav2 內建的 RPP 和 MPPI——放在同一個目標、同一組對齊過的底盤限制下比較。我要誠實說，在這種靜態的長距離追蹤情境，GMPC 大致和 MPPI 打平，到達時間稍慢一點、追蹤誤差稍大一點。GMPC 明確贏的地方是側移的利用，因為它是全向底盤、能側移，這也是它的賣點。RPP 在追蹤精度上仍是最強的。我的判斷是，GMPC 真正的優勢要在動態避障場景才看得出來，那是下一張的內容。

---

## P11 — 實驗三：量化評估框架 ✅

**投影片要點**
- 評估管線：`record.sh`（規範化錄 rosbag）→ `analyze.py`（bag → CSV，22 項指標）→ `plot.py`（4 方法 × 13 指標長條圖）
- 22 項指標分類：
  - 任務完成：成功率、到達時間、總時間、路徑長度
  - 追蹤精度：tracking RMSE
  - 控制平滑性：vx/vy/wz 標準差
  - Jerk：加速度標準差
  - 安全：最小淨距、碰撞次數
  - 求解效能：平均/p95/最大求解時間
- 意義：呼應第一次承諾的量化評估，後續所有方法皆以此框架公平對比

**口頭講稿**
為了讓比較有依據，我把整套評估自動化了。流程是先用一個腳本規範化地錄製 rosbag，再用分析腳本把它轉成包含二十二項指標的 CSV，最後畫成四種方法、十三項指標的長條圖。指標涵蓋任務完成、追蹤精度、控制平滑性、jerk、安全距離和求解效能。這呼應了上次承諾的量化評估，之後不管加什麼方法，都用同一套框架公平比較。

---

## P12 — 實驗四：CBF 動態避障（進行中）🔶

**投影片要點**（明確標示「進行中 / smoke-tested」）
- 已驗證（smoke test）：
  - 四個 ROS2 節點皆可執行、launch 起 7 個節點通過
  - 障礙物聚合節點實測「20 Hz 追蹤 3 個動態障礙物」
  - CBF 單元測試 5 個 case 通過（遠距不影響、近距減速、邊界急煞）
- **尚未完成**：完整 4 方法 × 動態場景 benchmark（下一步第一優先）
- **預期結果**（假設，待驗證）：
  - 碰撞數：GMPC（無 CBF）≥ 1 次撞 / **GMPC + CBF = 0**
  - 最小淨距：GMPC + CBF 永遠 ≥ d_safe = 0.30 m
  - 求解時間：GMPC < 1 ms vs MPPI 約 5–15 ms

**口頭講稿**
這一頁我要特別誠實：動態避障的 stack 已經建好，但完整的對比實驗還沒跑完。目前做到的是煙霧測試——四個節點都能跑、整套 launch 起得來、障礙物聚合能在二十赫茲追三個動態障礙物，CBF 的單元測試五個情境也都通過，包括遠距不干擾、近距減速、邊界急煞。但四種方法在動態場景下的完整 benchmark 還沒跑，這是我下一步的第一優先。下面這幾個數字是我的預期、還沒驗證：我預期沒有 CBF 的 GMPC 會撞，加了 CBF 之後碰撞歸零、最小淨距永遠守在三十公分以上，而求解時間 GMPC 會明顯比 MPPI 快。

---

## P13 — 技術路線調整說明 ⭐

**投影片要點**
- 調整一：Kalman Filter 預測 → **先用 CBF 安全濾波器**
  - 理由：CBF 提供直接、可證的安全保證，先把安全機制打通；KF + 全 horizon 預測列為下一步
- 調整二：Behavior Tree → **先用 goal_to_plan_relay + 持續重規劃**
  - 理由：先確保核心控制迴路與重規劃行為正確；BT 高層決策（等待/重規劃/任務切換）留待具備動態資料後再導入
- 原則：先打通「控制器 + 安全」核心，預測與高層決策依序疊加，避免一次性堆疊難以除錯

**口頭講稿**
這頁專門交代我做的兩個調整，因為這和上次的承諾不一樣，我不想含糊帶過。第一，本來說要做 Kalman Filter 預測，我先改用 CBF 安全濾波器，因為它能直接給出安全保證，我想先把安全機制打通；KF 和全時域預測我排到下一步。第二，本來說要用 Behavior Tree，我先用自寫的 relay 加持續重規劃，先確保核心控制迴路是對的；真正的 BT 高層決策等動態資料齊了再加。整體原則是先打通控制器加安全這個核心，再一層層往上疊預測和決策，這樣比較好除錯。

---

## P14 — 未來工作

**投影片要點**
- 立即（最高優先）：完成 4 方法 × 動態場景的完整 benchmark
- 短期：
  - 全 horizon CBF + Kalman Filter 動態預測（提前繞行，而非急煞）
  - Monte Carlo 自動化（多隨機種子 × 多方法，輸出 box plot）
- 中期：以 `/scan` LiDAR 聚類偵測取代 Gazebo ground-truth（朝真機可部署）
- 論文後段：
  - Whole-body MPC（SE(2) 底座 + 7-DOF 手臂協調）
  - 接觸操作（開門/拉抽屜的等式約束 + 力/力矩限制）

**口頭講稿**
最後是未來工作。最優先、也是馬上要做的，就是把動態場景的完整對比實驗跑完。短期我要把 CBF 升級成全時域、結合 Kalman Filter 做障礙物預測，這樣機器人能提前繞行而不是撞前急煞；同時把實驗自動化，跑多個隨機種子。中期把障礙物來源從模擬器的真值改成從 LiDAR 偵測，朝真機可部署靠近。論文後段就回到全身協調控制和接觸操作這兩個大目標。

---

## P15 — Thank you

**投影片要點**
- Thank you!
- （備註：動態避障建議現場播放影片/GIF）

**口頭講稿**
以上是我這次的進度，謝謝教授，請教授指教。

---

### 給 NotebookLM 的補充提示
- 風格：研究進度報告，語氣務實、誠實；勿將「預期結果」陳述為「已得到的結果」。
- 已完成且有數據：P5–P11（GMPC 方法 + 離線驗證 + Baseline 對比 + 評估框架）。
- 進行中/僅煙霧測試：P12（CBF 動態避障）。
- 技術路線調整：P7、P8、P13（KF→CBF、BT→relay）需如實說明理由。
- 視覺素材建議：P8 架構圖、P9/P10 結果表、P12 動態場景影片/截圖、Foxglove 的 CBF 安全圈畫面。
