# 第二階段計畫：通用 1-DOF 關節式物體之全身協同操作

> 平台：四輪全向移動底盤 `omni_bot` ＋ UFACTORY Lite 6 機械手臂  
> 軟體環境：ROS 2 Jazzy ＋ Gazebo Harmonic  
> 文件定位：本文件是**第二階段實作與驗證計畫**，不是既有成果報告。  
> 狀態標記：✅ 已完成、🟡 進行中、⬜ 待實作、🔬 待驗證。

---

## 摘要

第一階段已完成未知動態環境中的移動底盤導航，建立 SE(2) 幾何模型預測控制
（Geometric Model Predictive Control, GMPC）、全時域預測式控制屏障函數
（Control Barrier Function, CBF），以及不依賴障礙物分類與追蹤的 Raw-scan Safety
Shield。第二階段將在此基礎上，加入 UFACTORY Lite 6 機械手臂，研究移動底盤與手臂
在接觸式操作中的全身協同控制。

第二階段的核心任務是操作只有一個運動自由度的關節式物體，例如平移式抽屜與旋轉式
櫃門。系統將以旋量理論統一描述 revolute 與 prismatic 關節，並以

$$
\mathcal Q=SE(2)\times\mathbb R^6\times\mathbb R
$$

表示「全向底盤＋六軸手臂＋一軸目標物體」的廣義組態。末端執行器的操作誤差則定義於
$SE(3)$ 任務空間。實作將採漸進方式：先完成固定底盤的手臂操作，再完成導航與操作的
循序整合，最後才開放底盤與手臂同時運動的 W-GMPC（Whole-Body GMPC）。

本階段不預先宣稱絕對安全或通用泛化。所有結果將限定在感測有效、物體模型可辨識、
控制問題可行及硬體能力足夠的條件下，並以配對實驗、模組消融及未參與調參的測試物體
驗證。

---

## 一、研究目標與範圍

### 1.1 主要研究目標

建立一套可同時控制全向底盤與六軸機械手臂的全身協同控制架構，使機器人能：

1. 導航至關節式物體附近的可操作底盤位姿。
2. 以末端執行器對準並抓取把手。
3. 在維持抓取幾何關係的條件下，操作一個 1-DOF 物體關節。
4. 同時滿足底盤輪速、關節限制、自碰撞及環境碰撞限制。
5. 面對小幅模型與把手位姿誤差時，維持可控且不過載的接觸行為。

### 1.2 研究任務

本階段包含兩類代表性任務：

- **Prismatic joint：抽屜拉取**  
  把手沿固定滑軌方向平移，幾何與控制問題相對單純，作為第一項最小可行任務。
- **Revolute joint：櫃門開啟**  
  把手繞固定鉸鏈軸運動，末端路徑為圓弧，需同時調整位置、姿態與全身可操作空間。

### 1.3 本階段不直接宣稱的內容

以下能力不在尚未驗證前寫成既有成果：

- 不宣稱對任意家具、任意把手或任意 1-DOF 物體皆可零修改操作。
- 不宣稱在感測失效、接觸模型錯誤或物理不可行情境下保證零碰撞。
- 不宣稱無力感測器方法能精準重建完整六維接觸 wrench。
- 不把 AI 模組視為第二階段完成的必要條件。
- 不在固定底盤操作尚未穩定前，直接宣稱已完成全身 W-GMPC。

---

## 二、第一階段可繼承的技術資產

### 2.1 已驗證的導航結果 ✅

三種方法在相同 83 條有效配對路線上的結果如下：

| 方法 | 到達 | 碰撞 | 最深穿透 |
|---|---:|---:|---:|
| GMPC＋CBF＋Shield | 83/83 | 1/83 | 0.1 cm |
| nav2 MPPI | 64/83 | 6/83 | 36.4 cm |
| nav2 RPP | 60/83 | 30/83 | 57.5 cm |

第一階段應以兩個指標分開陳述：

- **總體到達率：** $83/83=100\%$。
- **無碰撞到達率：** $82/83=98.8\%$。

唯一一次接觸的估計穿透深度約為 1 mm，應描述為極淺邊界接觸，而不是宣稱碰撞率為零。
上述到達、碰撞與間距指標使用相同量測窗口，皆截斷於首次抵達時刻，避免抵達後的殘餘
運動或動態體經過終點造成統計污染。

### 2.2 已驗證的底盤限制 ✅

| 物理量 | 實驗使用值 |
|---|---:|
| $v_{x,\max},v_{y,\max}$ | 0.2775 m/s |
| $\dot\varphi_{\max}$ | 1.1327 rad/s |
| $a_{x,\max},a_{y,\max}$ | 6.25 m/s² |
| $\ddot\varphi_{\max}$ | 25.51 rad/s² |

底盤控制器已包含輪速耦合限制：

$$
-r\omega_{\max}\mathbf 1
\leq W\xi_b
\leq r\omega_{\max}\mathbf 1,
$$

其中

$$
\xi_b=\begin{bmatrix}v_x&v_y&\dot\varphi\end{bmatrix}^{\top},
\qquad
W=
\begin{bmatrix}
0&1&L\\
-1&0&L\\
0&-1&L\\
1&0&L
\end{bmatrix}.
$$

第二階段必須保留此限制，因為底盤旋轉與平移會競爭相同的輪速預算。

### 2.3 已驗證的分層安全架構 ✅

第一階段控制鏈為：

```text
GMPC-CBF → velocity smoother → Raw-scan Safety Shield → 底盤
```

- GMPC-CBF：根據追蹤與預測結果提前繞行。
- Raw-scan Safety Shield：直接讀取未經動態分類的 `/scan`，提供近距離命令修正。

此架構可繼續保護底盤，但**不能直接視為手臂安全層**。手臂伸出後，整台機器人不再適合
以固定圓盤描述，因此第二階段必須另外建立連桿層級的碰撞距離與速度約束。

### 2.4 實驗可追溯性基礎 ✅

第一階段已建立實驗 manifest 與單一統計來源，能保存：

- Git commit 與 dirty diff。
- 實際使用的環境變數與 ROS 參數。
- 場景、軌跡、地圖與起訖點檔案及 SHA-256。
- bag 完整性、節點狀態與參數擷取狀態。

第二階段所有操作實驗應沿用相同機制，並額外保存手臂模型、關節限制、物體旋量軸、
抓取轉換與操作初始條件。

---

## 三、目前整合狀態

### 3.1 已完成項目 ✅

- Lite 6 已接合至全向底盤模型。
- 已建立全身 URDF/Xacro 的基本結構。
- 第一階段導航、感知、CBF 與 Shield 程式可作為底盤子系統。
- Lite 6 關節位置限位已依實機安全邊界對齊。
- 全身 9-DOF Jacobian 已完成數值驗證：最大誤差 $8.23\times10^{-11}$、rank 6、
  冗餘自由度為 3。
- `arm_detection_points` 已發布 12 個偵測點至最近障礙物表面的向量；目前尚無安全控制器
  消費此資料。

### 3.2 仍需完成的模型驗證 🟡

手臂接到底盤不等於全身模型已可用。以下項目必須逐項確認：

1. `map → odom → base_link → arm_base → end_effector` TF 鏈連續且方向正確。
2. 六個關節的軸方向、零位、上下限及單位正確。
3. collision geometry 與 visual geometry 尺度一致。
4. 收折姿態不會與底盤自碰撞。
5. Gazebo、RViz、MoveIt 與控制器使用相同關節名稱。
6. 底盤移動時，末端在世界座標中的位姿連續且符合正向運動學。
7. Lite 6 的速度、加速度、負載與控制介面限制完成實機確認並寫入控制約束。目前速度
   3.14 rad/s 與加速度仍採手冊值，600 g 負載尚未寫入任何 W-GMPC 約束，因此本項
   **尚未完成**。

本次對齊的位置限位與原廠完整包絡差異如下：

| 關節 | 原始設定 | 實機安全限位 | 差異 |
|---|---:|---:|---:|
| joint1／4／6 | $\pm360^\circ$ | $\pm168.2^\circ$ | 單側減少 $191.8^\circ$ |
| joint3 | $300^\circ$ 範圍 | $168.2^\circ$ 範圍 | 減少 $131.8^\circ$ |
| joint2／5 | 原廠包絡 | 各端再內縮 $10^\circ$ | 保留額外安全邊界 |

W-GMPC 必須使用實機安全限位，而不是原廠完整包絡；否則最佳化器可能產生模擬可行、
實機控制器拒絕的軌跡。

### 3.3 模型整合驗收標準

- 固定底盤，逐一運動六個關節，RViz、Gazebo與關節回授方向一致。
- 固定手臂關節，平移與旋轉底盤，末端世界位姿與TF計算一致。
- 隨機抽樣至少 100 組合法關節角，URDF FK 與控制程式使用的 FK 誤差低於設定容許值。
- 收折、預抓取及操作姿態皆通過自碰撞檢查。

---

## 四、通用 1-DOF 關節式物體模型

### 4.1 全身組態空間

令底盤位姿為 $X_b\in SE(2)$、手臂關節為 $q_a\in\mathbb R^6$、物體關節量為
$q_d\in\mathbb R$，則全系統組態為

$$
q_{\mathrm{wb}}=(X_b,q_a,q_d)
\in SE(2)\times\mathbb R^6\times\mathbb R.
$$

此系統共有 10 個廣義座標：底盤 3 軸、手臂 6 軸及物體 1 軸。末端執行器位姿
$T_{WE}\in SE(3)$ 是由底盤與手臂組態映射得到的任務空間量，不應將底盤本身寫成
$SE(3)$。

### 4.2 單自由度旋量表示

以單位旋量

$$
\hat S_d=
\begin{bmatrix}
\omega_d\\v_d
\end{bmatrix}
\in\mathbb R^6
$$

統一描述物體的一個關節自由度。

#### Revolute joint

若關節軸的單位方向為 $\hat\omega$，軸線通過點 $q$，則空間旋量可寫成

$$
\hat S_d=
\begin{bmatrix}
\hat\omega\\q\times\hat\omega
\end{bmatrix}
=
\begin{bmatrix}
\hat\omega\\-\hat\omega\times q
\end{bmatrix}.
$$

此模型用於門或櫃門繞鉸鏈轉動。

#### Prismatic joint

若滑軌單位方向為 $\hat v$，則

$$
\hat S_d=
\begin{bmatrix}
0\\\hat v
\end{bmatrix}.
$$

此模型用於抽屜沿滑軌平移。

### 4.3 物體與把手位姿

令：

- $T_{WA,0}$：物體關節基座在世界座標中的零位姿。
- $T_{AH,0}$：物體零位時，把手相對關節基座的固定轉換。
- $[\hat S_d]$：旋量的 $4\times4$ 矩陣形式。

則把手在世界座標中的位姿可寫為

$$
T_{WH}(q_d)
=T_{WA,0}\exp\!\left([\hat S_d]q_d\right)T_{AH,0}.
$$

此式同時適用於旋轉與平移關節，差別只在 $\hat S_d$ 的內容。

### 4.4 固定抓取約束

令 $T_{WE}(X_b,q_a)$ 為末端位姿，$T_{EG}$ 為末端工具座標到實際抓取接觸座標的固定
轉換。抓取成功後，應滿足

$$
T_{WE}(X_b,q_a)T_{EG}=T_{WH}(q_d).
$$

此等式的意義是：末端抓取點與把手保持同一位姿，而非只讓末端位置追隨把手。

實際系統需允許小幅誤差，因此控制器可最小化 $SE(3)$ 抓取誤差，而不一定把此式作為
完全不可松弛的硬等式。

### 4.5 泛化範圍

旋量表示統一了 revolute 與 prismatic 的運動學形式，但不代表更換物體時完全不需要
其他資訊。每個新物體至少仍需提供或估計：

- 關節類型與旋量軸 $\hat S_d$。
- 關節零位姿 $T_{WA,0}$。
- 把手固定轉換 $T_{AH,0}$。
- 合法關節範圍 $q_{d,\min},q_{d,\max}$。
- 抓取方向、可接近區域與碰撞幾何。
- 操作阻力或等效摩擦範圍。

因此本階段的「通用」是指**控制器共享同一數學介面與最佳化結構**，不是對任意家具零設定。

---

## 五、W-GMPC 全身協同控制架構

### 5.1 全身控制輸入

定義底盤體速度與手臂關節速度組成的控制向量：

$$
\nu=
\begin{bmatrix}
v_x&v_y&\omega&\dot q_a^{\top}
\end{bmatrix}^{\top}
\in\mathbb R^9.
$$

物體關節速度 $\dot q_d$ 可以作為預測狀態或輔助決策變量，但它不是機器人致動器直接
施加的命令。機器人透過末端接觸使物體運動。

### 5.2 末端位姿誤差

末端任務定義於 $SE(3)$。採用左不變誤差：

$$
e_L=
\log\!\left(
T_{WH,\mathrm{ref}}^{-1}T_{WE}T_{EG}
\right)^{\vee}
\in\mathbb R^6.
$$

此形式在世界座標共同左乘時保持不變。若未來改用右不變誤差，應明確寫為

$$
e_R=
\log\!\left(
T_{WE}T_{EG}T_{WH,\mathrm{ref}}^{-1}
\right)^{\vee},
$$

並同步調整Jacobian與擾動定義，不能只更換名稱。

### 5.3 速度層抓取一致性

在固定抓取下，末端抓取點與物體把手的twist應一致。統一至同一表達座標後，可寫成

$$
J_G(X_b,q_a)\nu
=J_H(q_d)\dot q_d.
$$

- $J_G$：全身控制速度到抓取點twist的Jacobian。
- $J_H$：物體關節速度到把手twist的Jacobian。

若兩側twist使用不同座標表達，必須通過Adjoint轉換後才能比較。

### 5.4 最佳化目標

W-GMPC可採用下列基本成本：

$$
J=
\sum_{k=0}^{N-1}
\left(
e_k^{\top}Q_e e_k
+\nu_k^{\top}R\nu_k
+\Delta\nu_k^{\top}S\Delta\nu_k
+w_d e_{d,k}^2
\right)
+e_N^{\top}Q_f e_N.
$$

其中：

- $e_k$：末端與把手參考位姿的 $SE(3)$ 誤差。
- $e_{d,k}$：物體關節量與目標開度的誤差。
- $R$：控制輸入大小權重。
- $S$：控制增量平滑權重。
- $Q_f$：終端任務誤差權重。

### 5.5 底盤與手臂的權重分配

第二階段可研究不同的控制增量權重：

$$
S=\operatorname{diag}(S_b,S_a).
$$

- 較大的 $S_b$：減少重型底盤的頻繁反轉與高頻抖動。
- 較小的 $S_a$：允許手臂承擔較細微的末端誤差修正。

此方法應稱為**不對稱控制增量權重分配**，不稱為“動能分流”，除非成本中實際加入
質量矩陣形成的動能項

$$
\frac12\dot q^{\top}M(q)\dot q.
$$

權重分配的效果必須通過消融實驗驗證，不能預先宣稱能降低能耗或結構共振。

### 5.6 W-GMPC 約束集合

控制器至少應包含：

1. 底盤速度、加速度與輪速耦合限制。
2. 手臂關節位置、速度與加速度限制。
3. 物體關節範圍限制。
4. 抓取點運動一致性約束。
5. 手臂自碰撞距離限制。
6. 底盤、手臂與環境的碰撞距離限制。
7. 操作度或奇異點回避項。
8. 必要時的接觸力或估計負載限制。

其中關節位置限制必須直接採用實機安全限位。速度與加速度在完成實測前標記為手冊值；
600 g 負載則需轉換為可檢查的末端負載、關節力矩或操作姿態限制，不能只留在文字規格中。

---

## 六、分階段實作策略

### 6.1 階段 A：固定底盤的手臂控制 ⬜

先固定底盤，只驗證手臂與物體操作，避免導航誤差與全身最佳化同時進入問題。

實施順序：

1. 關節位置控制。
2. 正向運動學與Jacobian驗證。
3. 數值IK或MoveIt末端位姿規劃。
4. 預抓取、抓取、釋放動作。
5. 固定底盤抽屜拉取。
6. 固定底盤櫃門開啓。

驗收標準：

- 至少5組不同合法初始關節姿態能夠到達預抓取位姿。
- 無關節超限、無自碰撞、無環境非預期碰撞。
- 抽屜與櫃門任務分別記錄成功率、末端誤差及最大關節速度。
- 失敗必須區分為 IK 不可達、規劃失敗、抓取失敗、控制逾時與碰撞。

### 6.2 階段 B：導航與操作循序整合 ⬜

採用狀態機串接第一與第二階段：

```text
NAVIGATE
  ↓
BASE_ALIGN
  ↓
ARM_PREGRASP
  ↓
GRASP
  ↓
CONSTRAINED_OPERATION
  ↓
RELEASE
  ↓
RETREAT
```

每個狀態需定義：

- 進入條件。
- 成功條件。
- timeout。
- 安全停止條件。
- 失敗恢復路徑。

此階段底盤與手臂不同時運動。目的是先驗證“導航到正確操作位置”與“操作本身”能夠穩定
串接。

### 6.3 階段 C：操作底盤位姿選擇 ⬜

導航終點不能只要求靠近物體，還必須位於手臂可操作區域。候選底盤位姿可依據

$$
J_{\mathrm{base}}(X_b)
=w_nJ_{\mathrm{nav}}
+w_rJ_{\mathrm{reach}}
+w_mJ_{\mathrm{manip}}
+w_cJ_{\mathrm{clearance}}
$$

評分，其中：

- $J_{\mathrm{nav}}$：導航成本。
- $J_{\mathrm{reach}}$：把手可達性。
- $J_{\mathrm{manip}}$：手臂操作度與奇異點距離。
- $J_{\mathrm{clearance}}$：底盤與全身碰撞間距。

驗收時應報告候選數、可達率、最終操作成功率及因底盤位姿不佳導致的失敗比例。

### 6.4 階段 D：全身 W-GMPC ⬜

當前三階段均穩定後，才開放底盤與手臂同時運動。初期只處理已知物體模型與已知把手
位姿，不同時加入視覺估計誤差、動態障礙物與未知接觸阻力。

建議逐步增加難度：

1. 無障礙、已知抽屜模型。
2. 無障礙、已知櫃門模型。
3. 加入靜態環境障礙。
4. 加入把手位姿小擾動。
5. 加入物體關節軸小擾動。
6. 最後才加入導航誤差與動態環境。

---

## 七、接觸順應控制規劃

### 7.1 為何需要順應控制

純運動學軌跡假設鉸鏈軸、滑軌方向及抓取轉換完全正確。實際系統存在安裝誤差、物體
彈性、摩擦與把手位姿誤差。如果控制器強制追蹤錯誤軌跡，可能產生側向內力、馬達過流
或抓取脫落。

### 7.2 優先實施順序

1. 若硬體可用，先以外接六軸力／力矩感測器建立可驗證基準。
2. 確認Lite 6是否提供可用且時間同步的關節力矩或馬達電流回授。
3. 建立摩擦、重力及傳動誤差基線。
4. 再評估動量殘差觀測器是否具有足夠可觀測性。
5. 最後才將估計外力接入導納控制。

### 7.3 動量殘差觀測器候選方案 🔬

定義廣義動量

$$
p(q,\dot q)=M(q)\dot q.
$$

機器人動力學為

$$
M(q)\ddot q+C(q,\dot q)\dot q+g(q)+\tau_f
=\tau_m+\tau_{\mathrm{ext}}.
$$

動量觀測器的目標是估計外部關節力矩 $\hat\tau_{\mathrm{ext}}$，而不是直接宣稱得到精確
的六維末端力。若接觸集中於末端，可再利用

$$
\tau_{\mathrm{ext}}=J_E(q)^{\top}F_{\mathrm{ext}}
$$

估計

$$
\hat F_{\mathrm{ext}}
=(J_E^{\top})^{\dagger}\hat\tau_{\mathrm{ext}}.
$$

此估計會受到Jacobain條件數、摩擦模型、馬達力矩估計及非末端接觸影響，必須與真值或
外接感測器比較後才能評價精度。

### 7.4 幾何導納控制候選形式 🔬

可在末端切空間中定義修正量 $\Delta x\in\mathbb R^6$：

$$
M_a\Delta\ddot x+D_a\Delta\dot x+K_a\Delta x
=\hat F_{\mathrm{ext}}-F_{\mathrm{ref}}.
$$

再將修正後的參考轉換回 $SE(3)$：

$$
T_{\mathrm{ref,mod}}
=T_{\mathrm{ref}}\exp([\Delta x]).
$$

導納參數必須限制輸出位移、速度與積分量，並設置估計異常時的fallback。是否採用此方案，
取決於前述外力估計是否通過驗證。

---

## 八、全身安全層規劃

### 8.1 幾何表示

目前收折狀態的實際幾何量測如下：

| 項目 | 水平半徑 |
|---|---:|
| 手臂＋夾爪收折後的最大包絡 | 0.263 m |
| 第一階段底盤碰撞圓柱 | 0.300 m |
| 收折狀態剩餘餘裕 | **0.037 m** |

手臂基座安裝位置為 $(0.11,0,0.28)$ m，相對底盤中心向前偏移 0.11 m。因此手臂只要向
底盤碰撞圓柱外伸出約 3.7 cm，第一階段的2D底盤CBF與Raw-scan Safety Shield便不再能
完整代表全身碰撞幾何。**連桿層安全不是後續附加功能，而是開始全身同動實驗前的必要
條件。**

第二階段擬將底盤與手臂連桿近似為一組capsule或其他簡單凸幾何，並計算：

- 連桿與環境距離。
- 連桿之間的自碰撞距離。
- 連桿與目標物體非預期部位的距離。

目前2D LiDAR掃描平面約為 $z=0.2275$ m，而手臂主要工作高度約為 $z=0.33$--$0.83$ m，
兩者並不重疊；場景障礙物高度可達1.0 m，因此2D掃描無法觀測手臂可能發生的碰撞。
腕部D435i雖已加入模型，但目前設定為 `use_gazebo_camera=false`，尚未掛載Gazebo深度
感測器，也沒有可供安全層使用的深度資料流。

因此Month 1必須先決定3D距離來源：

1. **控制器開發階段：** 使用已知場景SDF、解析碰撞模型或Gazebo真值，隔離控制問題。
2. **真實感知階段：** 啟用腕部深度相機並建立3D環境表示；啟用前先量測影像串流對運算
   負載、控制週期與散熱的影響。

3D-SDF是候選距離來源之一，但必須驗證更新率、梯度穩定性與場景建圖來源。在沒有真實
3D資料前，只能主張已知模型或模擬真值下的連桿安全，不能宣稱未知3D環境防護。

### 8.2 連桿點速度

對第 $i$ 個連桿最近點 $p_i$，其速度由全身Jacobian計算：

$$
\dot p_i=J_{p_i}(q)\nu.
$$

因此全身近距離安全約束應寫成

$$
n_i^{\top}J_{p_i}(q)\nu
\leq
\alpha_i\left(d_i-d_{\mathrm{stop},i}\right).
$$

- $n_i$：由機器人最近點指向障礙物的單位法向。
- $d_i$：機器人幾何與障礙物之間的表面距離。
- $J_{p_i}$：全身控制速度到連桿最近點速度的Jacobian。

目前 `arm_detection_points` 已發布12個連桿偵測點指向最近障礙物表面的向量，可直接取得
$d_i$ 與 $n_i$；全身9-DOF Jacobian亦已完成數值驗證，最大誤差為 $8.23\times10^{-11}$、
rank為6、冗餘自由度為3。因此本節並非從零開始，剩餘工作是完成座標與時間同步、由各
偵測點取得 $J_{p_i}$、建立安全約束列，並驗證最壞計算時間與殘餘違反。

但現有 `arm_detection_points` 的障礙物來源屬於已知模型／模擬資訊，不等同真實3D感知。
它可先作為控制與約束生成介面的驗證來源，之後再替換成經驗證的深度感知距離場。

底盤圓盤的

$$
v+\omega\times r_i
$$

只是上述Jacobian形式的剛體特例，不能單獨代表機械臂連桿運動。

### 8.3 動態停止距離

候選停止距離沿用第一階段的結構：

$$
d_{\mathrm{stop},i}
=d_{0,i}
+v_{\mathrm{app},i}\tau_{\mathrm{eff}}
+\frac{v_{\mathrm{app},i}^2}{2a_{\mathrm{brake},i}}
+\varepsilon_i.
$$

但手臂的 $a_{\mathrm{brake},i}$ 不能直接沿用底盤6.25 m/s²，必須由關節減速度限制與
當前Jacobian映射得到或採取保守下界。

### 8.4 求解方式

第一階段Motzkin逐次投影可作為候選方案，但第二階段約束同時包含底盤與六個關節，且
Jacobian行不再具有相同尺度。實施前需驗證：

- 約束列正規化方式。
- 固定迭代次數下的殘餘違反。
- 關節盒限制是否會破壞已滿足的碰撞約束。
- 多連桿夾擊時的不可行情形與降級策略。
- 最壞執行時間，而非僅報告平均時間。

在實測前不寫死“0.15 ms”或“剛性保證”。

### 8.5 安全主張的適用範圍

未來若實驗支持，最多可主張：

> 對於感測可見、幾何模型有效、速度位於機器人可逃逸範圍且控制問題可行的障礙物，
> 全身預測控制與反應式安全層維持安全優先的控制行為。系統不宣稱對感測失效、模型嚴重
> 錯誤或物理不可避免情境提供絕對保證。

---

## 九、感知與任務參數層：Learning-based Kinematic Affordance and Semantic Goal Guidance

### 9.1 功能定位

第二階段將學習式 AI 限定於**高階感知與任務參數估計**，不直接產生底盤速度、手臂
關節命令或安全約束。此層主要回答三個問題：

1. **What：** 場景中的可操作物體與目標物品是什麼。
2. **Where：** 把手、目標物品與候選抓取位置在哪裡。
3. **How it may move：** 物體較可能是 revolute 或 prismatic 機構，以及可能的運動軸。

W-GMPC 負責根據這些參數產生全身協同運動；CBF 與全身 Safety Shield 則獨立負責硬體
限制與安全條件。因此整體分工為：

$$
\boxed{
\text{AI：What 與 Where}
\quad
\text{幾何控制：How}
\quad
\text{安全層：Whether it is safe}
}
$$

### 9.2 最小必要感知

第二階段核心控制至少需要下列資訊：

- 物體關節類型。
- 關節軸或滑軌方向。
- 把手的六維位姿與可抓取區域。
- 物體合法開度範圍。
- 抽屜內指定物品的候選位姿與抓取姿態。
- 機器人、目標物體與環境的碰撞幾何。

初期控制實驗使用 Gazebo 真值或人工標定值，以隔離控制問題。固定底盤操作及 W-GMPC
穩定後，再逐步換成視覺估計，避免同時除錯感知、接觸與控制。

### 9.3 結構化 AI 輸出

AI 不只輸出「門」或「抽屜」標籤，而應輸出控制器可檢查的結構化任務參數：

$$
\mathcal Y_{\mathrm{AI}}
=
\left\{
\hat T_{WH},
\hat c_d,
\hat S_d,
\hat q_{d,\mathrm{range}},
\hat T_{HG},
\hat T_{WO},
\mathcal G_O,
\Sigma,
\gamma
\right\}.
$$

其中：

- $\hat T_{WH}\in SE(3)$：把手在世界座標中的估計位姿。
- $\hat c_d$：機構類型，取值為 `revolute`、`prismatic` 或 `unknown`。
- $\hat S_d$：候選 1-DOF 旋量軸。
- $\hat q_{d,\mathrm{range}}$：估計的合法關節範圍。
- $\hat T_{HG}$：把手座標到建議抓取座標的轉換。
- $\hat T_{WO}\in SE(3)$：抽屜內目標物品的估計位姿。
- $\mathcal G_O$：目標物品的候選抓取集合。
- $\Sigma$：幾何估計的不確定度。
- $\gamma$：模型對此次預測的信心分數。

所有 AI 輸出先經過範圍、一致性與可達性檢查，再轉換成 W-GMPC 的參考目標與物體參數。

### 9.4 把手偵測與抓取位姿預測 ⬜

把手感知不能只輸出影像中的像素中心，而應估計：

- 把手中心的三維位置。
- 把手長軸方向與表面法向。
- 建議接近方向與夾爪閉合方向。
- 可抓取區域及候選抓取姿態。
- 遮擋程度、信心與位姿不確定度。

輸出 $\hat T_{WH}$ 與 $\hat T_{HG}$ 後，操作底盤位姿選擇器先檢查手臂可達性、操作度與
環境間距，再交給 W-GMPC 執行。

### 9.5 機構辨識與運動學可操作性 ⬜

學習模型先估計機構類型：

$$
\hat c_d
\in
\{\mathrm{revolute},\mathrm{prismatic},\mathrm{unknown}\},
$$

並提供候選旋量軸 $\hat S_d$。但單張影像通常只能提供運動學先驗，不足以精確恢復鉸鏈
軸位置或滑軌方向。因此規劃採用：

```text
視覺初始估計
  → 多視角重新觀測
  → 必要時執行低速、小位移探測動作
  → 根據把手觀測軌跡更新旋量軸
  → 通過一致性檢查後開始正式操作
```

探測資料可透過最小化觀測把手位姿與旋量模型預測的誤差，更新 $\hat S_d$、關節零位姿
與不確定度。AI 提供的是初始先驗，最終運動學參數仍須接受幾何觀測驗證。

### 9.6 抽屜內物品的語意目標引導 ⬜

當任務為「打開抽屜並取出指定物品」時，語意模型負責從語言指令與場景中選擇目標，
例如辨識「紅色的筆」所對應的物品實例。

模組分工如下：

- VLM 或語意模型：決定哪一個物品符合使用者指令。
- 幾何視覺：估計該物品的三維位置與姿態 $\hat T_{WO}$。
- 抓取模型：產生候選抓取集合 $\mathcal G_O$。
- 可達性檢查：排除碰撞、超出工作空間或接近奇異點的抓取。
- W-GMPC：在硬體與安全約束內執行選定抓取。

語意模型不直接提供公分級控制座標，也不直接輸出關節命令。

### 9.7 信心、不確定度與失效降級

當

$$
\gamma<\gamma_{\min}
\quad\text{或}\quad
\lambda_{\max}(\Sigma)>\sigma_{\max}
$$

時，系統不得直接進入正式操作，而依序執行：

1. 改變相機或底盤觀測角度。
2. 重新偵測把手、機構與目標物品。
3. 執行低速探測動作以更新運動軸。
4. 若仍無法確認，將機構標記為 `unknown` 並拒絕操作。

若操作過程中的實際把手軌跡持續偏離 $\hat S_d$ 的預測，或抓取誤差、接觸負載超過
門檻，系統應停止、退回安全姿態並重新估計，不得強迫錯誤模型繼續執行。

### 9.8 與安全層的獨立性

AI 輸出可以改變操作目標與候選運動學模型，但不能停用、放寬或繞過：

- 底盤輪速與加速度限制。
- 手臂關節位置、速度及加速度限制。
- 自碰撞與環境碰撞限制。
- W-GMPC／CBF 的安全約束。
- 全身反應式 Safety Shield。

全身安全層應直接使用幾何距離、關節狀態與機器人模型建立限制，不把語意分類結果當成
安全成立的必要前提。因此 AI 誤判不應直接轉化為未受限制的控制命令。

### 9.9 感知層驗證與消融

感知與任務參數層應獨立評估：

| 子任務 | 指標 |
|---|---|
| 把手偵測 | precision、recall、三維位置／姿態誤差 |
| 機構分類 | revolute／prismatic／unknown 混淆矩陣 |
| 旋量軸估計 | 軸方向角誤差、軸線距離誤差、滑軌方向誤差 |
| 物品選擇 | 語意目標選擇正確率 |
| 物品抓取 | grasp success、碰撞率、姿態誤差 |
| 不確定度 | 信心校準誤差、拒絕後的錯誤率 |

建議消融順序：

1. 真值任務參數＋W-GMPC：量測控制器上限。
2. 單視角 AI 估計＋W-GMPC：量測直接視覺誤差。
3. AI 估計＋多視角觀測：量測重新觀測的貢獻。
4. AI 估計＋探測更新：量測運動學修正的貢獻。
5. 移除信心拒絕機制：驗證 fallback 是否降低高風險誤操作。

在完成上述消融前，不宣稱AI提升操作成功率或泛化能力。

---

## 十、實驗設計

### 10.1 實驗分層

#### E1：模型與FK/Jacobian驗證

- 固定底盤與固定物體。
- 隨機抽樣合法關節姿態。
- 比較數值微分、解析Jacobian與模擬真值。

#### E2：固定底盤操作

- 抽屜與櫃門分別測試。
- 使用已知物體參數與已知把手位姿。
- 評估手臂控制本身。

#### E3：導航後循序操作

- 隨機底盤起點。
- 導航至操作位姿後鎖定底盤。
- 評估導航、對準與操作串接成功率。

#### E4：全身協同操作

- 底盤與手臂同時運動。
- 與固定底盤方法進行配對比較。

#### E5：模型誤差與泛化

- 關節軸位置擾動。
- 把手位姿擾動。
- 摩擦或阻力變化。
- 未參與調參的物體尺寸及初始開度。

### 10.2 核心評估指標

| 類別 | 指標 |
|---|---|
| 任務 | 完成率、操作時間、最終開度誤差 |
| 導航 | 到達率、導航時間、操作位姿誤差 |
| 末端 | 位置誤差、姿態誤差、抓取約束殘差 |
| 底盤 | 路徑長度、速度與加速度p95、輪速違反率 |
| 手臂 | 關節速度／加速度p95、關節限制違反率、最小操作度 |
| 安全 | 環境碰撞、自碰撞、最小間距、最深穿透 |
| 接觸 | 最大估計外力、過流／保護停機次數、抓取脫落次數 |
| 計算 | 求解時間p50／p95／p99、deadline miss率、fallback次數 |

### 10.3 成功條件

單趟任務成功至少需同時滿足：

1. 物體達到目標開度容許範圍。
2. 機器人與非目標環境無碰撞。
3. 無自碰撞。
4. 無關節與輪速硬限制違反。
5. 無馬達保護停機或控制器異常終止。
6. 在規定時間內完成並進入安全釋放狀態。

目標物體上的預期抓取接觸不能與非預期碰撞混為一談，評估程式必須分別標記。

### 10.4 建議消融實驗

| 編號 | 底盤運動 | 全身GMPC | 連桿安全層 | 順應控制 | 目的 |
|---|---:|---:|---:|---:|---|
| A | 固定 | ✗ | 基本碰撞檢查 | ✗ | 手臂操作基線 |
| B | 循序對準 | ✗ | 基本碰撞檢查 | ✗ | 驗證操作位姿選擇 |
| C | 同時運動 | ✓ | ✗ | ✗ | W-GMPC本體貢獻 |
| D | 同時運動 | ✓ | ✓ | ✗ | 連桿安全層貢獻 |
| E | 同時運動 | ✓ | ✓ | ✓ | 順應控制貢獻 |

若順應控制尚未通過外力估計驗證，不執行E組，也不把它列入已完成成果。

### 10.5 泛化驗證原則

- 調參物體與最終測試物體分開。
- 抽屜與櫃門都必須包含未參與調參的尺寸、開度與初始底盤位姿。
- 參數凍結後才生成最終測試集合。
- 同一組條件採用配對方式比較。
- 報告樣本數、失敗類型與信賴區間，不只報告平均成功率。

---

## 十一、實驗可追溯性

第一階段manifest系統已完成並通過驗收：可記錄Git commit與dirty diff、輸入檔SHA-256、
實際節點的resolved parameters、bag完整性與執行狀態，且歸檔失敗時採fail-closed。第二
階段必須從第一趟實驗開始沿用，不得等實驗結束後再補建紀錄。

每一趟第二階段實驗的manifest需在既有內容上新增：

- 全身URDF/Xacro與mesh hash。
- Lite 6關節限制與控制器參數。
- 物體模型、關節類型與旋量軸。
- $T_{WA,0}$、$T_{AH,0}$與$T_{EG}$。
- 目標關節開度與初始開度。
- 底盤與手臂初始狀態。
- 使用的碰撞幾何與SDF版本。
- W-GMPC、順應控制與安全層resolved parameters。
- Git commit、dirty diff、bag完整性與分析腳本版本。
- 到達、碰撞與最小間距的統計窗口定義；三者均以首次抵達時間截斷。

所有圖表與正文數字應由bag或單一機器可讀摘要自動產生，避免手抄常數造成漂移。

---

## 十二、六個月開發時程

### Month 1：全身模型與固定底盤控制

- 完成TF、URDF、關節限制與碰撞模型驗證。
- 完成Lite 6 FK、Jacobian與IK測試。
- 將實機安全位置限位寫入控制約束，並標記仍待實測的速度、加速度與600 g負載限制。
- 決定3D距離資料採用已知模型／Gazebo真值或腕部深度感知，並評估D435i串流的運算與
  散熱成本。
- 將12個 `arm_detection_points` 與全身Jacobian接成連桿安全約束原型。
- 建立預抓取、抓取與釋放流程。

**里程碑 M1：** 固定底盤下，手臂可重複到達抽屜預抓取位姿，且無關節超限及自碰撞；
3D距離來源與連桿安全介面完成選型及初步驗證。

### Month 2：固定底盤1-DOF操作

- 完成已知參數抽屜拉取。
- 完成已知參數櫃門開啓。
- 建立物體旋量介面與操作狀態機。

**里程碑 M2：** 固定底盤下完成兩類1-DOF任務，並具有可分類的失敗記錄。

### Month 3：導航與操作串接

- 建立操作底盤位姿候選與評分。
- 串接導航、對準、抓取、操作與釋放。
- 從第一趟起沿用已驗收的manifest與評估管線。
- 完成連桿安全約束原型，作為全身同動的進入條件。

**里程碑 M3：** 從隨機導航起點出發，循序完成至少一種操作任務。

### Month 4：W-GMPC全身協同

- 建立 $SE(2)\times\mathbb R^6$ 全身預測模型。
- 加入底盤輪速與手臂關節限制。
- 加入抓取一致性與操作關節目標。
- 預留整合與量測問題的除錯緩衝，不以單次成功取代可重現驗證。

**里程碑 M4：** 在連桿安全介面已啟用、無外部障礙的條件下，底盤與手臂可同時運動完成
抽屜或櫃門任務。若尚未達成穩定重複結果，優先保留固定底盤與循序操作成果。

### Month 5：全身安全整合與順應候選方案

- 整合並強化前期建立的連桿capsule距離與Jacobian約束。
- 驗證自碰撞與環境碰撞安全層。
- 評估關節力矩資料品質與動量觀測器可行性。
- 只有外力估計通過驗證且時程仍有餘裕時，才接入導納控制；否則明確列為未來工作。

**里程碑 M5：** 完成全身安全層消融；順應控制依驗證結果決定是否納入。

### Month 6：泛化測試與論文整理

- 凍結參數。
- 執行未參與調參的抽屜與櫃門測試。
- 完成配對消融、失敗分類與統計分析。
- 整理論文方法、限制與實驗結果。

**里程碑 M6：** 形成可重現的第二階段實驗資料與論文結果，不以單一成功展示取代統計驗證。

---

## 十三、主要風險與降級方案

| 風險 | 影響 | 降級方案 |
|---|---|---|
| 手臂關節力矩資料不可用或雜訊過大 | 無法可靠估計外力 | 使用位置／速度控制完成運動學任務；順應控制列為未來工作 |
| 全身 QP 計算逾時 | 控制週期不穩定 | 縮短時域、減少碰撞點、分層求解或先採用速度層 QP |
| 2D LiDAR與手臂工作高度不重疊 | 伸出底盤包絡後無法完整避障 | Month 1決定使用已知場景SDF／Gazebo真值或啟用D435i；明確限制能力範圍 |
| 腕部深度串流造成運算或散熱負擔 | 控制deadline miss或硬體降頻 | 先做負載量測；必要時降低解析度／頻率，控制驗證先使用已知模型 |
| 抓取幾何誤差過大 | 產生內力或脫落 | 降速、加入誤差界限、重新對準或安全釋放 |
| 操作底盤位姿不可達 | 手臂IK失敗 | 重新選擇候選底盤位姿，不強迫手臂進入奇異區 |
| 安全約束不可行 | 機器人被夾住或無法退離 | 停止操作、釋放物體、退回預操作姿態並重新規劃 |
| 模型過度複雜導致進度失控 | 無法形成可驗證成果 | 保留固定底盤與循序操作作為最低可交付成果 |

---

## 十四、第二階段最低可交付成果

若完整 W-GMPC、無力感測順應與 AI 模組無法全部在時程內完成，第二階段最低可交付成果定義為：

> 機器人能夠從隨機起點導航至經過可達性檢查的操作底盤位姿，在底盤固定的條件下，
> 使用統一 1-DOF 物體介面完成抽屜與櫃門操作；系統具有關節限制、自碰撞檢查、環境碰撞
> 檢查、失敗恢復與可重現實驗紀錄。

完整目標則為：

> 在已知或可可靠估計的1-DOF物體模型下，底盤與六軸手臂透過
> $SE(2)\times\mathbb R^6$ W-GMPC同時運動，維持抓取一致性，並在輪速、關節與連桿安全
> 限制內完成prismatic及revolute兩類操作任務。

---

## 十五、預期研究貢獻

若後續實驗成立，本階段可形成以下貢獻：

1. **統一 1-DOF 物體介面：** 以旋量軸統一表示抽屜與櫃門的運動學約束。
2. **移動底盤與手臂的全身幾何預測控制：** 在
   $SE(2)\times\mathbb R^6$ 組態上協調底盤與機械臂，並在 $SE(3)$ 任務空間追蹤把手。
3. **底盤輪系與手臂關節的共同約束：** 將第一階段輪速多面體與手臂關節限制納入同一控制框架。
4. **連桿層級安全約束：** 以全身Jacobian描述非圓形連桿最近點速度，擴展第一階段的近距離安全概念。
5. **可重現的移動操作評估：** 對抽屜與櫃門進行配對、消融及未參與調參場景驗證。

上述項目在實驗完成前均屬於**預期貢獻**，最終論文只保留有程式、bag、統計與可重現實驗
支持的部分。
