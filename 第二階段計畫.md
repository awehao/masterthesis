# 第二階段計畫：通用 1-DOF 關節式物體之全身協同操作

> 平台：四輪全向移動底盤 `omni_bot` ＋ UFACTORY Lite 6 機械手臂  
> 軟體環境：ROS 2 Jazzy ＋ Gazebo Harmonic  
> 文件定位：本文件是**第二階段實作與驗證計畫**，不是既有成果報告。  
> 狀態標記：✅ 已完成、🟡 進行中、⬜ 待實作、🔬 待驗證。
> 計畫更新：2026-09-12；本次細化視覺預抓取分支與 LSTM 導航延伸，並依已讀驗收紀錄更新現況。
> 逐階段實驗、驗收門檻、圖表座標軸與數據欄位，見
> [第二階段計畫_審查與成果規格.md](第二階段計畫_審查與成果規格.md)。本文件描述研究方法；
> 配套文件定義 S0–S9 的執行與交付規格。新增門檻／樣本數皆為規劃值，不是已取得成果。
> 本次只更新計畫及配套 S8 規格，未實作 AI、未執行新實驗。最新功能證據與限制見 §3.1。

---

## 摘要

第一階段已完成未知動態環境中的移動底盤導航，建立 SE(2) 幾何模型預測控制
（Geometric Model Predictive Control, GMPC）、全時域預測式控制屏障函數
（Control Barrier Function, CBF），以及不依賴障礙物分類與追蹤的 Raw-scan Safety
Shield。第二階段將在此基礎上，加入 UFACTORY Lite 6 機械手臂，研究移動底盤與手臂
在接觸式操作中的全身協同控制。

第二階段的核心任務是操作只有一個運動自由度的關節式物體，例如平移式抽屜與旋轉式
櫃門。系統將以旋量理論統一描述 revolute 與 prismatic 關節，並以

$$
\mathcal Q=SE(2)\times\mathbb R^6\times\mathbb R
$$

表示「全向底盤＋六軸手臂＋一軸目標物體」的廣義組態。末端執行器的操作誤差則定義於
$SE(3)$ 任務空間。實作將採漸進方式：先驗證自由空間全身控制與安全介面，再建立固定
底盤的接觸操作、導航與操作的循序整合，最後比較整合式單步 QP 與具有多步預測的
W-GMPC（Whole-Body GMPC）。自由空間同動可提前驗證，不等於已完成接觸式全身操作。

本階段不預先宣稱絕對安全或通用泛化。所有結果將限定在感測有效、物體模型可辨識、
控制問題可行及硬體能力足夠的條件下，並以配對實驗、模組消融及未參與調參的測試物體
驗證。

核心研究問題為：**在相同感知、硬體與安全條件下，多步全身預測是否比已整合相同限制
的單步 QP，更能提前調整底盤位置與手臂臂形，完成抽屜／櫃門操作？** 單步方法同樣可能
完成任務；預測時域的效益與代價必須實測。Isaac Sim 用於介面移植與動力學敏感度驗證，
其真實度取決於模型、致動器與接觸參數的校準，不因更換模擬器而自動成立。

本次將 **S8-V「把手視覺感知與三維預抓取位姿估計」列為優先 AI 分支**：從 RGB-D
辨識把手，利用幾何估計與可達性檢查提供目標，先接已凍結的單步 QP，之後才延伸至
已驗收的接觸操作。研究視覺誤差、品質檢查及重新觀測對任務成功率與時間的影響。
首版保留已知機構類型、運動軸與環境碰撞模型；視覺目標估計不等於已完成 3D 安全感知。
此分支有明確交付範圍，但不取代核心 B／P 比較。LSTM 障礙物預測另列為第一階段導航
選配延伸，先做預測效益診斷，未通過前不承諾模型訓練。

---

## 一、研究目標與範圍

### 1.1 主要研究目標

建立一套可同時控制全向底盤與六軸機械手臂的全身協同控制架構，使機器人能：

1. 導航至關節式物體附近的可操作底盤位姿。
2. 以末端執行器對準並抓取把手。
3. 在維持抓取幾何關係的條件下，操作一個 1-DOF 物體關節。
4. 同時滿足底盤輪速、關節限制、自碰撞及環境碰撞限制。
5. 面對小幅模型與把手位姿誤差時，維持可控且不過載的接觸行為。

### 1.2 研究任務

本階段包含兩類代表性任務：

- **Prismatic joint：抽屜拉取**  
  把手沿固定滑軌方向平移，幾何與控制問題相對單純，作為第一項最小可行任務。
- **Revolute joint：櫃門開啟**  
  把手繞固定鉸鏈軸運動，末端路徑為圓弧，需同時調整位置、姿態與全身可操作空間。

### 1.3 本階段不直接宣稱的內容

以下能力不在尚未驗證前寫成既有成果：

- 不宣稱對任意家具、任意把手或任意 1-DOF 物體皆可零修改操作。
- 不宣稱在感測失效、接觸模型錯誤或物理不可行情境下保證零碰撞。
- 不宣稱無力感測器方法能精準重建完整六維接觸 wrench。
- 不把 AI 模組視為第二階段完成的必要條件。
- 不在固定底盤操作尚未穩定前，直接宣稱已完成全身 W-GMPC。

核心範圍為已知物體模型下的兩類 1-DOF 操作與單步／多步控制比較。視覺、無力感測器
順應、動態操作障礙與語意取物依 S8 分開列為延伸；抽屜內物品抓取不是開抽屜成功的
必要條件。若最後只完成循序操作，須同步縮小論文貢獻，不能沿用完整 W-GMPC 的主張。

### 1.4 本次新增工作的優先順序

| 層級 | 工作 | 範圍與定位 |
|---|---|---|
| 核心 | 已知模型下的接觸操作、B／P 比較、限制與失效驗證 | 維持 S0–S7／S9 主線，不以 AI 展示替代 |
| 優先 AI 分支 | S8-V 把手感知、三維位姿、品質檢查與重新觀測 | 先自由空間預抓取，再依接觸基線進度擴充；詳 §9.1–9.9 |
| 有條件導航延伸 | NAV-LSTM 多步障礙物預測 | 屬第一階段導航；先做 Oracle 診斷再決定是否訓練，詳 §9.11 |
| 後續選配 | 未知運動軸、摩擦殘差／順應、語意取物與人機協同 | 各自有前置條件，不與 S8-V 綁成同一里程碑，詳 §9.10 |

視覺研究問題為：**固定控制器與安全條件下，視覺位姿誤差如何影響全身預抓取／操作？
品質檢查與重新觀測是否能降低失敗，代價是多少額外時間與拒絕？** AI 分支的正式測試
不得占用或改寫尚未凍結的控制主比較；離線資料與相機介面可提前準備。

---

## 二、第一階段可繼承的技術資產

### 2.1 已驗證的導航結果 ✅

三種方法在相同 83 條有效配對路線上的結果如下：

| 方法 | 到達 | 碰撞 | 最深穿透 |
|---|---:|---:|---:|
| GMPC＋CBF＋Shield | 83/83 | 1/83 | 0.1 cm |
| nav2 MPPI | 64/83 | 6/83 | 36.4 cm |
| nav2 RPP | 60/83 | 30/83 | 57.5 cm |

第一階段應以兩個指標分開陳述：

- **總體到達率：** $83/83=100\%$。
- **無碰撞到達率：** $82/83=98.8\%$。

唯一一次接觸的估計穿透深度約為 1 mm，應描述為極淺邊界接觸，而不是宣稱碰撞率為零。
上述到達、碰撞與間距指標使用相同量測窗口，皆截斷於首次抵達時刻，避免抵達後的殘餘
運動或動態體經過終點造成統計污染。

### 2.2 已驗證的底盤限制 ✅

| 物理量 | 實驗使用值 |
|---|---:|
| $v_{x,\max},v_{y,\max}$ | 0.2775 m/s |
| $\dot\varphi_{\max}$ | 1.1327 rad/s |
| $a_{x,\max},a_{y,\max}$ | 6.25 m/s² |
| $\ddot\varphi_{\max}$ | 25.51 rad/s² |

底盤控制器已包含輪速耦合限制：

$$
-r\omega_{\max}\mathbf 1
\leq W\xi_b
\leq r\omega_{\max}\mathbf 1,
$$

其中

$$
\xi_b=\begin{bmatrix}v_x&v_y&\dot\varphi\end{bmatrix}^{\top},
\qquad
W=
\begin{bmatrix}
0&1&L\\
-1&0&L\\
0&-1&L\\
1&0&L
\end{bmatrix}.
$$

第二階段必須保留此限制，因為底盤旋轉與平移會競爭相同的輪速預算。

### 2.3 已驗證的分層安全架構 ✅

第一階段既有評測使用的分層控制鏈為：

```text
GMPC-CBF → velocity smoother → Raw-scan Safety Shield → 底盤
```

- GMPC-CBF：根據追蹤與預測結果提前繞行。
- Raw-scan Safety Shield：直接讀取未經動態分類的 `/scan`，提供近距離命令修正。

此架構可繼續保護底盤，但**不能直接視為手臂安全層**。手臂伸出後，整台機器人不再適合
以固定圓盤描述，因此第二階段必須另外建立連桿層級的碰撞距離與速度約束。

2026-09-11 的 Isaac 命令鏈測試另使用 `driver → smoother → wheel_limit_guard → Isaac`，
驗證的是輪級命令限制與故障停車，不能與上述含 Shield 的歷史鏈混稱為同一驗收。
後續須對實際啟動的完整鏈保存節點／topic 拓樸，檢查所有下游修正後的最終命令。
依據：[命令鏈整合驗收](evaluation/results/step4_chain_integration_20260911.md)。

### 2.4 實驗可追溯性基礎 ✅

第一階段已建立實驗 manifest 與單一統計來源，能保存：

- Git commit 與 dirty diff。
- 實際使用的環境變數與 ROS 參數。
- 場景、軌跡、地圖與起訖點檔案及 SHA-256。
- bag 完整性、節點狀態與參數擷取狀態。

第二階段所有操作實驗應沿用相同機制，並額外保存手臂模型、關節限制、物體旋量軸、
抓取轉換與操作初始條件。

---

## 三、目前整合狀態

### 3.1 已完成項目與現況界線

- Lite 6 已接合至全向底盤模型。
- 已建立全身 URDF/Xacro 的基本結構。
- 第一階段導航、感知、CBF 與 Shield 程式可作為底盤子系統。
- Lite 6 關節位置限位已依實機安全邊界對齊。
- 全身 9-DOF FK／Jacobian 已通過獨立 FK、KDL 與 Gazebo 位姿比對；200 組測試中的
  Jacobian 數值微分最大誤差 $3.645\times10^{-10}$、rank 均為 6。此為該模型版本的有限
  樣本結果，不代表所有構形皆不奇異；改模型後須重新驗收。
- 3D 距離與全身安全控制消費端已接通。原 12 點表示已由包含底盤的連桿表面認證取樣、
  完整近距離帶與速度修正取代；距離來源仍是已知模型，非深度感知。
- 有障礙案例早期 A 與 B（$\mu_{\mathrm{post}}=1$）停滯；後續 B 改用 μ=0.03 並修正
  排程，已建立 Gazebo 單步 QP 預抓取基線。同一已知靜態場景重複 3 次皆完成，位置終值
  2.293 ± 0.008 mm。後續採用 `--sched deadline` 版本，保留下游安全濾波器；這是有限
  場景的基線，非完整接觸或普遍安全證據。
- reason 7（整批距離過期）與 reason 8（距離列截斷）已完成案例驗收；空約束硬錯誤與
  0.02 rad 關節限位內縮已修補。限位卡住的底層原因未定位，正式回歸仍需驗證。
- Isaac 已有導航與操作介面進度。2026-09-12 固定底盤手臂測試已到達箱體錨定的
  預抓取位置，並通過工具軸方向判準；工具軸方向不等於完整三維姿態，沿途命令套用
  與實際軌跡一致性仍須驗收，不能視為 Gazebo 全身 QP 已完整移植。
- 2026-09-11 輪級命令限制與故障停車已有驗收；最終命令的 CBF 配對診斷尚未完成，
  不宣稱完整安全鏈已驗收，也不將模擬接收端歸零當成實機制動證明。
- 本次所讀紀錄尚未提供可宣稱成功的抽屜／櫃門接觸操作、多步 W-GMPC 或視覺目標
  閉迴路證據。相機模型／影像介面存在不等於已完成腕部 RGB-D 標定及安全感知。

現況依據：[單步基線與排程修正](evaluation/results/baseline_B_frozen_20260909.md)、
[第五次進度報告](第五次進度報告.md)、
[命令鏈驗收](evaluation/results/step4_chain_integration_20260911.md)、
[Isaac 固定底盤操作入口](evaluation/results/manip_stage1_20260912.md)。
後續同名紀錄若追加修正，以其中最新且已驗收的版本為準；本段不替正在進行的實驗定案。

### 3.2 模型回歸清單與尚未確認的硬體限制 🟡

模型項目 1–6 已有驗收紀錄；以下保留作為換模型／模擬器後的回歸清單。第 7 項仍未完成：

1. `map → odom → base_link → arm_base → end_effector` TF 鏈連續且方向正確。
2. 六個關節的軸方向、零位、上下限及單位正確。
3. collision geometry 與 visual geometry 尺度一致。
4. 收折姿態不會與底盤自碰撞。
5. Gazebo、RViz、MoveIt 與控制器使用相同關節名稱。
6. 底盤移動時，末端在世界座標中的位姿連續且符合正向運動學。
7. Lite 6 的速度、加速度、負載與控制介面限制完成實機確認並寫入控制約束。目前速度
   3.14 rad/s 與加速度仍採手冊值，600 g 負載尚未寫入任何 W-GMPC 約束，因此本項
   **尚未完成**。

本次對齊的位置限位與原廠完整包絡差異如下：

| 關節 | 原始設定 | 實機安全限位 | 差異 |
|---|---|---|---|
| joint1／4／6 | $-360^\circ \sim +360^\circ$ | $-168.2^\circ \sim +168.2^\circ$ | 單側各減 $191.8^\circ$ |
| joint2 | $-150.0^\circ \sim +150.0^\circ$ | $-140.0^\circ \sim +140.0^\circ$ | 單側各減 $10^\circ$ |
| joint3 | $-3.5^\circ \sim +300.0^\circ$ | $-3.5^\circ \sim +168.2^\circ$ | 上限減 $131.8^\circ$，下限不變 |
| joint5 | $-124.0^\circ \sim +124.0^\circ$ | $-114.0^\circ \sim +114.0^\circ$ | 單側各減 $10^\circ$ |

上下限分開列出，因為縮減並不對稱：joint3 只動上限，其餘關節兩端等量內縮。以「範圍」
單一數字描述會掩蓋這件事。

W-GMPC 必須使用實機安全限位，而不是原廠完整包絡；否則最佳化器可能產生模擬可行、
實機控制器拒絕的軌跡。

### 3.3 模型整合驗收標準

- 固定底盤，逐一運動六個關節，RViz、Gazebo與關節回授方向一致。
- 固定手臂關節，平移與旋轉底盤，末端世界位姿與TF計算一致。
- 隨機抽樣至少 100 組合法關節角，URDF FK 與控制程式使用的 FK 誤差低於設定容許值。
- 收折、預抓取及操作姿態皆通過自碰撞檢查。

---

## 四、通用 1-DOF 關節式物體模型

### 4.1 全身組態空間

令底盤位姿為 $X_b\in SE(2)$、手臂關節為 $q_a\in\mathbb R^6$、物體關節量為
$q_d\in\mathbb R$，則全系統組態為

$$
q_{\mathrm{wb}}=(X_b,q_a,q_d)
\in SE(2)\times\mathbb R^6\times\mathbb R.
$$

此系統共有 10 個廣義座標：底盤 3 軸、手臂 6 軸及物體 1 軸。末端執行器位姿
$T_{WE}\in SE(3)$ 是由底盤與手臂組態映射得到的任務空間量，不應將底盤本身寫成
$SE(3)$。

### 4.2 單自由度旋量表示

以關節旋量（角速度在前、線速度在後的排列）

$$
\hat S_d=
\begin{bmatrix}
\omega_d\\v_d
\end{bmatrix}
\in\mathbb R^6
$$

統一描述物體的一個關節自由度。

#### Revolute joint

若關節軸的單位方向為 $\hat\omega$，軸線通過點 $q$，則空間旋量可寫成

$$
\hat S_d=
\begin{bmatrix}
\hat\omega\\q\times\hat\omega
\end{bmatrix}
=
\begin{bmatrix}
\hat\omega\\-\hat\omega\times q
\end{bmatrix}.
$$

此模型用於門或櫃門繞鉸鏈轉動。$q$ 與 $\hat\omega$ 均在物體關節基座 $A$ 表達；
「單位」指 $\|\hat\omega\|=1$，不是把混合量綱的整個六維向量正規化。

#### Prismatic joint

若滑軌單位方向為 $\hat v$，則

$$
\hat S_d=
\begin{bmatrix}
0\\\hat v
\end{bmatrix}.
$$

此模型用於抽屜沿滑軌平移，$\|\hat v\|=1$ 且在 $A$ 表達。Revolute 的 $q_d$ 使用 rad，
prismatic 使用 m；成本與完成門檻不得直接共用未換算的數值。

### 4.3 物體與把手位姿

令：

- $T_{WA,0}$：物體關節基座在世界座標中的零位姿。
- $T_{AH,0}$：物體零位時，把手相對關節基座的固定轉換。
- $[\hat S_d]$：旋量的 $4\times4$ 矩陣形式。

則把手在世界座標中的位姿可寫為

$$
T_{WH}(q_d)
=T_{WA,0}\exp\!\left([\hat S_d]q_d\right)T_{AH,0}.
$$

此式同時適用於旋轉與平移關節，差別只在 $\hat S_d$ 的內容。

### 4.4 固定抓取約束

令 $T_{WE}(X_b,q_a)$ 為末端位姿，$T_{EG}$ 為末端工具座標到實際抓取接觸座標的固定
轉換，$T_{HG}$ 為把手座標到所選抓取座標的固定轉換。抓取成功後，應滿足

$$
T_{WE}(X_b,q_a)T_{EG}=T_{WH}(q_d)T_{HG}.
$$

此等式的意義是兩者的抓取座標保持重合；只有明確把把手座標定義在抓取點時，才令
$T_{HG}=I$。這與 §9 的抓取估計介面一致。

實際系統需允許小幅誤差，因此控制器可最小化 $SE(3)$ 抓取誤差，而不一定把此式作為
完全不可松弛的硬等式。

### 4.5 泛化範圍

旋量表示統一了 revolute 與 prismatic 的運動學形式，但不代表更換物體時完全不需要
其他資訊。每個新物體至少仍需提供或估計：

- 關節類型與旋量軸 $\hat S_d$。
- 關節零位姿 $T_{WA,0}$。
- 把手固定轉換 $T_{AH,0}$。
- 合法關節範圍 $q_{d,\min},q_{d,\max}$。
- 抓取方向、可接近區域與碰撞幾何。
- 操作阻力或等效摩擦範圍。

因此本階段的「通用」是指**控制器共享同一數學介面與最佳化結構**，不是對任意家具零設定。

---

## 五、W-GMPC 全身協同控制架構

### 5.1 全身控制輸入

定義底盤體速度與手臂關節速度組成的控制向量：

$$
\nu=
\begin{bmatrix}
v_x&v_y&\omega&\dot q_a^{\top}
\end{bmatrix}^{\top}
\in\mathbb R^9.
$$

物體關節速度 $\dot q_d$ 可以作為預測狀態或輔助決策變量，但它不是機器人致動器直接
施加的命令。機器人透過末端接觸使物體運動。

### 5.2 末端位姿誤差

末端任務定義於 $SE(3)$。採用左不變誤差：

$$
e_L=
\log\!\left(
T_{WG,\mathrm{ref}}^{-1}T_{WE}T_{EG}
\right)^{\vee}
\in\mathbb R^6.
$$

此形式在世界座標共同左乘時保持不變。若未來改用右不變誤差，應明確寫為

$$
e_R=
\log\!\left(
T_{WE}T_{EG}T_{WG,\mathrm{ref}}^{-1}
\right)^{\vee},
$$

並同步調整Jacobian與擾動定義，不能只更換名稱。

其中 $T_{WG,\mathrm{ref}}=T_{WH,\mathrm{ref}}T_{HG}$。目前預抓取程式的任務排列為
`[線速度／位置項; 角速度／朝向項]`，本文件旋量採 `[角; 線]`。實作 W-GMPC 時須明列排列
轉換，以及 world／body frame、抓取點與工具原點的轉換。幾何 Jacobian 的位置導數區塊
亦不一定等於 spatial twist 的線性區塊；不可只交換上下三列就視為完成轉換。
完整 $SE(3)$ log 誤差的預測線性化須處理對應的 log 微分，並以數值微分驗證。

### 5.3 速度層抓取一致性

在固定抓取下，末端抓取點與物體把手的twist應一致。統一至同一表達座標後，可寫成

$$
J_G(X_b,q_a)\nu
=J_{HG}(q_d)\dot q_d.
$$

- $J_G$：全身控制速度到抓取點twist的Jacobian。
- $J_{HG}$：物體關節速度到把手側抓取座標 twist 的 Jacobian，包含 $T_{HG}$。

若兩側twist使用不同座標表達，必須通過Adjoint轉換後才能比較。

### 5.4 最佳化目標

W-GMPC可採用下列基本成本：

$$
J=
\sum_{k=0}^{N-1}
\left(
e_k^{\top}Q_e e_k
+\nu_k^{\top}R\nu_k
+\Delta\nu_k^{\top}S\Delta\nu_k
+w_d e_{d,k}^2
\right)
+e_N^{\top}Q_f e_N.
$$

其中：

- $e_k$：末端與把手參考位姿的 $SE(3)$ 誤差。
- $e_{d,k}$：物體關節量與目標開度的誤差。
- $R$：控制輸入大小權重。
- $S$：控制增量平滑權重。
- $Q_f$：終端任務誤差權重。

位置與朝向須先按任務容許尺度分別正規化，例如 $\tilde e=[e_\omega/\theta_s;
e_v/\ell_s]$；物體開度使用 $\tilde e_d=(q_d-q_d^\ast)/s_d$。尺度、權重、參考速度
上限與控制增益分別記錄，不把 m 與 rad 直接當作相同精度要求。正式評估的位置誤差以
兩個位姿的平移差計算、朝向誤差以旋轉測地角計算，與 log 的線性區塊分開。

目前單步程式中的 $\mu_{\mathrm{post}}$ 是**手臂臂形偏好權重**，不是末端朝向權重。
其成本等價於

$$
\|J\nu-v_{\mathrm{task}}\|^2
+\mu_{\mathrm{post}}^2\|P_a(\nu-\nu_{\mathrm{post}})\|^2
+\lambda^2\|D_w^{-1}\nu\|^2,
$$

其中 $P_a$ 選取手臂分量。$\mu$ 掃描是在放鬆／加強回到偏好臂形的傾向；`kp_r` 改變
朝向參考速度，不等同直接改寫殘差成本權重。參照
`evaluation/wholebody_pregrasp.py` 的 `H`／`g` 組裝與 `compare_stall_solvers.py`。
先前紀錄把 $\mu$ 欄稱為「姿態權重」時，應讀作臂形偏好，不能據此宣稱移除了末端朝向任務。

### 5.5 底盤與手臂的權重分配

第二階段可研究不同的控制增量權重：

$$
S=\operatorname{diag}(S_b,S_a).
$$

- 較大的 $S_b$：減少重型底盤的頻繁反轉與高頻抖動。
- 較小的 $S_a$：允許手臂承擔較細微的末端誤差修正。

此方法應稱為**不對稱控制增量權重分配**，不稱為“動能分流”，除非成本中實際加入
質量矩陣形成的動能項

$$
\frac12\dot q^{\top}M(q)\dot q.
$$

權重分配的效果必須通過消融實驗驗證，不能預先宣稱能降低能耗或結構共振。

### 5.6 W-GMPC 約束集合

控制器至少應包含：

1. 底盤速度、加速度與輪速耦合限制。
2. 手臂關節位置、速度與加速度限制。
3. 物體關節範圍限制。
4. 抓取點運動一致性約束。
5. 手臂自碰撞距離限制。
6. 底盤、手臂與環境的碰撞距離限制。
7. 操作度或奇異點回避項。
8. 必要時的接觸力或估計負載限制。

其中關節位置限制必須直接採用實機安全限位。速度與加速度在完成實測前標記為手冊值；
600 g 負載則需轉換為可檢查的末端負載、關節力矩或操作姿態限制，不能只留在文字規格中。

### 5.7 預測模型與接觸耦合的必要補件

只有成本與當前 Jacobian 尚不足以定義 W-GMPC。至少需寫出

$$
X_{b,k+1}=X_{b,k}\exp(\Delta t[\xi_{b,k}]^\wedge),\qquad
q_{a,k+1}=q_{a,k}+\Delta t\dot q_{a,k},\qquad
q_{d,k+1}=q_{d,k}+\Delta t\dot q_{d,k}.
$$

每個預測步以預測構形重算抓取與碰撞幾何；說明離散化、線性化、warm start、信賴區域、
可行性檢查與首步執行。$\Delta\nu_0$ 由安全層最後接受的命令初始化，預測狀態則使用
實際回授；命令速度與量測速度不同時須建模追蹤誤差或低階致動動態。

$\dot q_d$ 必須透過抓取一致性與機器人運動耦合。S3 初期可用理想剛性抓取測運動學；
涉及阻力或力的主張時，另需物體被動動力學／準靜態負載模型，例如
$I_d\ddot q_d+b_d\dot q_d+g_d(q_d)+\tau_f=J_{HG}^{\top}F_G$，此例取固定等效質量／慣量；
若 $I_d$ 隨構形變動，須補相應速度耦合項。其中平移關節的 $I_d$ 對應質量、
廣義力為 N，旋轉關節則為慣量與 N·m。不能直接命令物體 $q_d$ 到目標來冒充操作成功。

理想固定連接與可滑脫的摩擦夾持須分組驗證。夾爪可開合不代表已可靠抓住把手；S3 要先
驗證接觸建立、負載傳遞、釋放與物體回動。600 g 酬載限制也不等於允許任意拉門力或腕部力矩。

---

## 六、分階段實作策略

為避免「階段 A／B」與現有「方法 A／B」混淆，開發階段改用 S0–S9；方法固定使用
F（固定底盤）、SEQ（循序）、A（任務後投影）、B（整合式單步 QP）、P（多步 W-GMPC）。
各階段完整工作、驗收與圖表規格見配套文件。

| 階段 | 工作與待回答問題 | 進入／退出依據 | 必交圖表 ID |
|---|---|---|---|
| S0 模型與量測 | 幾何、運動學、時間戳和執行端是否一致？ | 換模型後重新通過回歸；多數已有歷史證據 | F0-1～3、T0-1～2 |
| S1 安全與單步基線 | B 是否能在安全限制內完成有障礙預抓取？ | μ 掃描→必要時另輪尺度調整→修補回歸→版本凍結 | F1-1～5、T1-1～3 |
| S2 Isaac Sim 移植 | 相同命令在另一套已記錄的致動／接觸設定下如何執行？ | B 參考凍結後做閉迴路對照；模型匯入準備可現在開始 | F2-1～3、T2-1～2 |
| S3 固定底盤接觸 | 手臂是否真正透過抓取移動抽屜與門？ | 夾持與接觸模型通過；兩類物體分別驗收 | F3-1～4、T3-1～2 |
| S4 操作位姿與循序整合 | 導航終點能否支援整段操作？ | 整段開度可達性→導航／抓取／操作／釋放狀態機 | F4-1～3、T4-1～2 |
| S5 單步全身接觸 | 開放底盤是否增加可完成的操作範圍？ | F、SEQ、B 配對；B 接觸版獨立凍結 | F5-1～3、T5-1 |
| S6 多步 W-GMPC | 預測是否提供超過單步協調的改善？ | 相同限制、感知、調參預算；分離時域、權重與安全層效果 | F6-1～4、T6-1～2 |
| S7 凍結測試 | 效果是否跨場景、物體參數與誤差仍成立？ | 未參與調參的配對試驗、失敗分析與不確定度 | F7-1～3、T7-1～3 |
| S8 優先視覺／其他選配 | 視覺目標與重新觀測能否支援受約束操作？ | 優先 S8-V；V0–V1 可提前，V2 接凍結 B，V4 須有接觸基線；正式比較不干擾核心 | F8-V0～4、T8-V0～3；C／L 仍獨立 |
| S9 論文交付 | 哪些主張已有可追溯證據？ | 所有數字可回到 run 與分析版本，負結果同樣保留 | F9-1、T9-1 |

S1 已有指定場景的凍結 B 基線，換模型、平台或命令鏈後仍需回歸；近期先完成 S2 執行
一致性及安全鏈缺口，再推進接觸。S3 可沿用已驗證的 Gazebo 接觸環境，或在 S2 通過的 Isaac Sim 上
建立；選定後將主要接觸實驗平台凍結，不要求所有實驗在兩個模擬器各做一遍。Isaac Sim
若受硬體／接觸模型阻礙，可先推進 Gazebo 核心控制，並記錄平台限制。

操作位姿選擇保留導航、可達性、操作度與淨空成本，但應先以**整段物體開度軌跡**的 IK、
關節裕度及碰撞檢查篩除不可行候選，再對可行集合評分。只在關閉時摸得到把手並不足夠。
抽屜伸出與門板掃掠都會改變碰撞幾何，須隨 $q_d$ 更新。

狀態機保留 `NAVIGATE → BASE_ALIGN → ARM_PREGRASP → GRASP → CONSTRAINED_OPERATION
→ RELEASE → RETREAT`。每個狀態定義進入／成功／逾時條件與命令所有權。異常後先判定
保持抓取、受控卸載或釋放何者可行；不可把「釋放並倒退」設成所有接觸故障的共同預設。

S8-V 啟用時，在 `BASE_ALIGN` 後插入 `OBSERVE → ESTIMATE → VALIDATE`，通過後才進入
`ARM_PREGRASP`；不合格可進入有次數／時間上限的 `REOBSERVE`。這是待實作的狀態機
擴充，並非目前節點已有功能；目標鎖定、重觀測及接觸後更新規則見 §9.5。

---

## 七、接觸順應控制規劃

### 7.1 為何需要順應控制

純運動學軌跡假設鉸鏈軸、滑軌方向及抓取轉換完全正確。實際系統存在安裝誤差、物體
彈性、摩擦與把手位姿誤差。如果控制器強制追蹤錯誤軌跡，可能產生側向內力、馬達過流
或抓取脫落。

### 7.2 優先實施順序

1. 若硬體可用，先以外接六軸力／力矩感測器建立可驗證基準。
2. 確認Lite 6是否提供可用且時間同步的關節力矩或馬達電流回授。
3. 建立摩擦、重力及傳動誤差基線。
4. 再評估動量殘差觀測器是否具有足夠可觀測性。
5. 最後才將估計外力接入導納控制。

### 7.3 動量殘差觀測器候選方案 🔬

定義廣義動量

$$
p(q,\dot q)=M(q)\dot q.
$$

機器人動力學為

$$
M(q)\ddot q+C(q,\dot q)\dot q+g(q)+\tau_f
=\tau_m+\tau_{\mathrm{ext}}.
$$

動量觀測器的目標是估計外部關節力矩 $\hat\tau_{\mathrm{ext}}$，而不是直接宣稱得到精確
的六維末端力。若接觸集中於末端，可再利用

$$
\tau_{\mathrm{ext}}=J_E(q)^{\top}F_{\mathrm{ext}}
$$

估計

$$
\hat F_{\mathrm{ext}}
=(J_E^{\top})^{\dagger}\hat\tau_{\mathrm{ext}}.
$$

此估計會受到Jacobain條件數、摩擦模型、馬達力矩估計及非末端接觸影響，必須與真值或
外接感測器比較後才能評價精度。

### 7.4 幾何導納控制候選形式 🔬

可在末端切空間中定義修正量 $\Delta x\in\mathbb R^6$：

$$
M_a\Delta\ddot x+D_a\Delta\dot x+K_a\Delta x
=\hat F_{\mathrm{ext}}-F_{\mathrm{ref}}.
$$

再將修正後的參考轉換回 $SE(3)$：

$$
T_{\mathrm{ref,mod}}
=T_{\mathrm{ref}}\exp([\Delta x]).
$$

導納參數必須限制輸出位移、速度與積分量，並設置估計異常時的fallback。是否採用此方案，
取決於前述外力估計是否通過驗證。

---

## 八、全身安全層規劃

### 8.1 幾何表示

早期模型收折狀態的幾何量測如下（保留作為建立全身安全層的動機，非新模型驗收值）：

| 項目 | 水平半徑 |
|---|---:|
| 手臂＋夾爪收折後的最大包絡 | 0.263 m |
| 第一階段底盤碰撞圓柱 | 0.300 m |
| 收折狀態剩餘餘裕 | **0.037 m** |

手臂基座安裝位置為 $(0.11,0,0.28)$ m，相對底盤中心向前偏移 0.11 m。因此手臂只要向
底盤碰撞圓柱外伸出約 3.7 cm，第一階段的2D底盤CBF與Raw-scan Safety Shield便不再能
完整代表全身碰撞幾何。**連桿層安全不是後續附加功能，而是開始全身同動實驗前的必要
條件。**

第二階段早期曾考慮 capsule 或簡單凸幾何；現行採表面認證取樣（見 §8.2）。需涵蓋：

- 連桿與環境距離。
- 連桿之間的自碰撞距離。
- 連桿與目標物體非預期部位的距離。

高度一律以 **base_link** 座標表示（`base_footprint` 較 `base_link` 低 0.05 m）：
2D LiDAR 掃描平面約在 $z=0.2275$ m，手臂連桿收折時佔據 $z=0.280$--$0.772$ m，兩者
不重疊。若改以地面座標，則分別為 $0.2775$ m 與 $0.330$--$0.822$ m，結論相同。

**座標系必須標明**：第一階段有數個問題源自不同來源默默使用不同基準（量測窗口不一致、
無法追溯的中間數字），混用 `base_link` 與地面高度屬於同一類風險。

場景障礙物高度可達 1.0 m，因此 2D 掃描無法觀測手臂可能發生的碰撞。
上述包絡與高度是早期模型的量測，改掛載、車高或碰撞幾何後須以模型 hash 重新計算。
腕部 D435i 雖已加入模型，但目前記錄為 `use_gazebo_camera=false`，尚無經驗收、可供安全層
使用的深度資料流。

3D 距離來源依階段區分：

1. **控制器開發階段：** 使用已知場景SDF、解析碰撞模型或Gazebo真值，隔離控制問題。
2. **真實感知階段：** 啟用腕部深度相機並建立3D環境表示；啟用前先量測影像串流對運算
   負載、控制週期與散熱的影響。

3D-SDF是候選距離來源之一，但必須驗證更新率、梯度穩定性與場景建圖來源。在沒有真實
3D資料前，只能主張已知模型或模擬真值下的連桿安全，不能宣稱未知3D環境防護。

### 8.2 連桿點速度

對第 $i$ 個連桿最近點 $p_i$，其速度由全身Jacobian計算：

$$
\dot p_i=J_{p_i}(q)\nu.
$$

因此全身近距離安全約束應寫成

$$
n_i^{\top}J_{p_i}(q)\nu
\leq
\alpha_i\left(d_i-d_{\mathrm{stop},i}\right).
$$

- $n_i$：由機器人最近點指向障礙物的單位法向。
- $d_i$：機器人幾何與障礙物之間的表面距離。
- $J_{p_i}$：全身控制速度到連桿最近點速度的Jacobian。

原 12 點表示在 §10.10–10.11 發現涵蓋不足，現行版本使用全連桿表面認證取樣、完整
近距離帶及速度界修正，並納入底盤 primitive。距離式須扣除取樣涵蓋半徑；速度修正、
法向變化與完整帶的條件亦須保留，不能只膨脹距離而沿用代表點速度。reason 8 防止列被
截斷後仍誤用完整涵蓋的論證。座標與時間同步已有驗證，但不同版本數字應分開引用。

這些資料仍屬已知模型／模擬資訊，不等同真實 3D 感知。環境距離與自碰撞也需分開驗收：
通過離線自碰撞篩檢不代表已存在連續在線自碰撞約束。對兩個可動連桿的最近點，須使用
相對速度 $J_{p_i}\nu-J_{p_j}\nu$，不能把另一連桿當作靜止障礙。

底盤圓盤的

$$
v+\omega\times r_i
$$

只是上述Jacobian形式的剛體特例，不能單獨代表機械臂連桿運動。

### 8.3 動態停止距離

候選停止距離沿用第一階段的結構：

$$
d_{\mathrm{stop},i}
=d_{0,i}
+v_{\mathrm{app},i}\tau_{\mathrm{eff}}
+\frac{v_{\mathrm{app},i}^2}{2a_{\mathrm{brake},i}}
+\varepsilon_i.
$$

但手臂的 $a_{\mathrm{brake},i}$ 不能直接沿用底盤6.25 m/s²，必須由關節減速度限制與
當前Jacobian映射得到或採取保守下界。

此處的 $a_{\mathrm{brake},i}$ 應是指定條件下可保證的有效制動能力；從加速度盒算出的
最大可用投影，不自動等於受耦合限制、追蹤誤差及 $\dot J\nu$ 影響下的可保證下界。
把小估計值強制夾到較大的 `brake_floor` 會縮短停距，亦須驗證適用範圍。

另將正常控制延遲與故障偵測延遲分開：reason 7 現有案例為故障注入→命令歸零 417 ms，
已大於停距參數中的正常延遲 150 ms。安全停止功能通過不等於故障全程無碰撞已被證明。
Isaac Sim 與實機須重測命令歸零後的移動距離；Gazebo 直接寫速度所得的零距離不可外推。

上式的簡化安全列適用於靜態障礙及每週期凍結的停距。若宣稱完整動態 CBF 條件，需從
$h=d-d_{\mathrm{stop}}$ 推導 $\dot h$，納入障礙物相對速度及停距的時間變化；離散取樣、
求解與致動誤差也須有界或獨立量測。不能僅憑每週期線性殘差非正宣稱完整不變性證明。

### 8.4 求解方式

第一階段Motzkin逐次投影可作為候選方案，但第二階段約束同時包含底盤與六個關節，且
Jacobian行不再具有相同尺度。實施前需驗證：

- 約束列正規化方式。
- 固定迭代次數下的殘餘違反。
- 關節盒限制是否會破壞已滿足的碰撞約束。
- 多連桿夾擊時的不可行情形與降級策略。
- 最壞執行時間，而非僅報告平均時間。

在實測前不寫死“0.15 ms”或“剛性保證”。

若停距與速度修正由輸入速度凍結，記錄該線性化點，並以最終接受的速度重算相關物理
指標，區分「凍結列滿足」與「最終速度所對應條件滿足」。必要時迭代重建或採保守界，
不能把兩者視為同一件事。輸出零也可能違反加速度盒或已侵入狀態的退離條件；fallback
要分別記錄停止要求、動態限制覆寫與尚未解除的幾何違反。[CBF-QP 原始論文](https://arxiv.org/abs/1609.06408)

### 8.5 安全主張的適用範圍

未來若實驗支持，最多可主張：

> 對於感測可見、幾何模型有效、速度位於機器人可逃逸範圍且控制問題可行的障礙物，
> 全身預測控制與反應式安全層維持安全優先的控制行為。系統不宣稱對感測失效、模型嚴重
> 錯誤或物理不可避免情境提供絕對保證。

---

## 九、優先 AI 分支：把手視覺感知與三維預抓取位姿估計

### 9.1 功能定位與首版邊界

本次優先實作 S8-V，目標為**以影像取得操作目標，研究視覺誤差與重新觀測對全身
受約束預抓取／操作的影響**。AI 提供辨識結果與候選目標；幾何方法負責位姿重建、
一致性與可達性檢查；B／P 與既有安全層執行受限制的動作。這是模組分工，不是安全證明。

| 項目 | 首版來源／範圍 | 暫不納入 |
|---|---|---|
| 把手可見區域與關鍵點 | RGB 分割／關鍵點模型 | 任意家具零樣本泛化承諾 |
| 把手與候選抓取位姿 | 對齊深度、標定、局部幾何與抓取規則 | 像素中心直接作為完整六維目標 |
| 機構類型、運動軸、初始開度與合法範圍 | 已知模型／獨立標定，與視覺輸入分開標示 | 同時辨識未知旋量軸或合法開度 |
| 環境與自碰撞幾何 | 已驗收的已知模型距離介面 | 把目標分割直接當成完整 3D 安全感知 |
| 控制器 | 先用凍結 B；P 通過後另作比較 | 以視覺模型直接輸出關節或輪速 |
| 任務 | 先預抓取；接觸基線通過後操作 | 抽屜內取物、人機交接、VLM 阻抗調參 |

首批以限定類型的剛性把手為主，變化把手位置、尺寸、視角、光照與遮擋。機構參數
保持已知，不表示所有測試都固定在同一世界位置；每個物體的已知參數來源均須記錄。

### 9.2 感測、標定與座標轉換（V0）⬜

在訓練或接入模型前先驗收以下鏈路：

1. 確認實際相機來源、RGB／深度 topic、`CameraInfo`、解析度、深度單位與無效值。
   URDF 存在腕部相機 frame 或底盤相機已有 RGB，不等於腕部深度已輸出。
2. 驗證 RGB／深度對齊與內參；以已知尺寸、平面和距離檢查尺度及光學座標方向。
3. 取得腕部外參／手眼標定，並以未參與標定的姿態及目標驗證。模擬精確外參與實機
   標定誤差分組，不把模擬 TF 一致當成實機標定精度。
4. 以**影像擷取時刻**查詢手臂／底盤 TF；記錄影像、深度、關節與 TF 的時間差。
   缺少對應 TF 時拒絕該觀測，不用最新 TF 假裝同時刻資料。
5. 分別量測相機關閉、只串流、串流加推論的端到端負載，保存 CPU／GPU、記憶體、
   感知延遲、控制發布間隔與 deadline miss；降低感知負載後須重新驗證。

依 §4 的約定，$T_{AB}$ 將 B 座標中的點轉至 A。令 C 為相機光學座標、H 為把手、
G 為抓取接觸座標、E 為工具座標，擷取時間為 $t_s$：

$$
\hat T_{WH}=T_{WC}(t_s)\hat T_{CH},\qquad
T_{WE,\mathrm{grasp}}=\hat T_{WH}\hat T_{HG}T_{EG}^{-1}.
$$

預抓取位姿由抓取位姿沿已定義的接近軸退開：

$$
T_{WE,\mathrm{pre}}=T_{WE,\mathrm{grasp}}T_{\mathrm{offset}}(d_{\mathrm{pre}}).
$$

$T_{\mathrm{offset}}$ 是右乘、在工具局部座標定義的轉換；軸、符號與退開距離須在
RViz／模擬檢查，不能混用世界與工具方向。已知物體參數、預抓取與實際接觸目標各自保存。

### 9.3 把手辨識與幾何位姿估計（V1）⬜

建議先建立**小型分割／關鍵點模型＋深度幾何**基線，不預先指定必須採用大型模型：

```text
同步 RGB／深度＋擷取時間 TF
  → 把手 mask／關鍵點與偵測分數
  → 深度反投影、無效點與離群值處理
  → 把手主軸／關鍵點幾何＋鄰近表面法向
  → 把手位姿與候選抓取位姿
  → 估計品質、夾爪幾何與可達性檢查
```

先測規則幾何或人工標註 mask 下的位姿重建，分清誤差來自辨識、深度還是標定。
對稱把手可能有方向歧義，需定義等價姿態集合；不可把 PCA 軸正負的跳變直接傳給控制器。
只得到工具接近軸時，應標為方向估計，不能宣稱完整六维姿態已知。夾爪閉合方向、開口
需求、把手與櫃面空隙、接近路徑都需檢查，網路分數不能代替幾何可行性。

模型選型依未參與調參的開發資料比較辨識召回、位姿誤差、推論負載、可重現性與整合成本。
先使用可取得的預訓練權重，若特定把手失敗再考慮微調；訓練新網路不是本分支必備貢獻。
候選方法的能力界線如下：

| 候選 | 可提供的資訊 | 還需自行處理 |
|---|---|---|
| 小型分割／關鍵點模型 | 把手區域、關鍵點 | 深度、位姿、對稱性、抓取規則與校準 |
| Grounded-SAM | 文字引導偵測與影像分割 | 不能直接當成六維位姿輸出；另接深度與幾何 |
| Contact-GraspNet | 點雲中的六自由度抓取候選 | 候選與本平台夾爪相容性、把手空隙及可達性 |
| Where2Act／VAT-Mart | 可操作區域、動作或軌跡提案 | 不等於已提供精準把手位姿或本計畫旋量軸介面 |

這些是選型參考，並非已安裝或已採用。原始來源見 §9.12。

### 9.4 結構化輸出與控制介面 ⬜

以下為待實作的介面契約，不表示 repository 已存在同名 ROS topic／訊息。
實作可先用現有標準訊息組合；需要封裝時再新增型別，不能靠陣列順序隱含單位與語意。

| 欄位 | 定義與檢查 |
|---|---|
| `observation_id / target_id` | 觀測與物體身分；跨影格关联需保持一致，切換物體需重新驗證 |
| `stamp / frame_id` | 原始擷取時間與光學 frame；另記推論完成、控制接受時間，不能重貼新時間戳 |
| `handle_pose / grasp_candidates` | $\hat T_{WH}$ 與候選 $\hat T_{HG}$；候選需標座標、單位、夾爪開度與接近方向 |
| `pose_covariance / quality` | 指定切空間、分量順序、單位及估計方法；不可用偵測分數冒充位姿共變異數 |
| `detection_score / calibrated_score` | 原始分數與另行校準分數分開；無校準時明列 unavailable |
| `valid / reject_reason / valid_until` | 深度不足、TF 缺漏、過期、幾何歧義、不可達、目標不一致等原因 |
| `model_version / calibration_id` | 權重、前處理、標定與模型版本，可追溯到 hash |
| `known_object_model_id` | 機構類型、軸與開度範圍的已知來源；不能混稱 AI 預測 |

消費端先驗證有限值、旋轉合法性、時間與 frame、身分及幾何，再驗 IK、關節裕度、
環境／自碰撞與接近路徑。自由空間 V2 驗到預抓取路徑；V4 還要驗證整段物體開度的
可達性與掃掠幾何。缺少必需資訊時不得直接進入抓取。

視覺輸入與已知機構模型的一致性需要明確處理：V2 的目標可直接取自視覺；V4 則須檢查
其與已知 $T_{WH}(q_d)$ 是否相容。首版不因單張視覺誤差就重寫運動軸或物體碰撞模型。
不相容時重新觀測或拒絕；若日後允許更新把手局部轉換，需另列估計參數與消融。

### 9.5 目標管理與重新觀測（V2–V3）⬜

```text
BASE_ALIGN → OBSERVE → ESTIMATE → VALIDATE
                                  ├─ 通過 → ARM_PREGRASP → 目標再確認 → GRASP
                                  └─ 不通過 → REOBSERVE → ESTIMATE
                                               └─ 次數／時間用盡 → 拒絕本次操作
```

- 感知在獨立節點非同步執行，不能在 20 Hz 控制回呼內阻塞等待模型。仍要實測共享
  GPU／CPU 競爭；分開節點不自動解決計算延遲。
- 初版在預抓取動作開始前鎖定已驗證的目標版本，不逐影格跳換 TCP 參考。靜態物體
  的已鎖定世界目標可在已驗證的有效期與定位條件內使用；追蹤失效、觀測顯示物體
  移動或定位跳變時，重新規劃／停止任務推進，不無限沿用舊目標。
- 重新觀測先用預先定義的小幅觀測位姿候選，經 IK、沿途碰撞及命令所有權檢查後
  執行。轉動手臂／移動底盤本身也受限制，不能為改善視角穿越未知區域。
- 觀測間確認仍是同一把手；接受新目標前記錄位姿差、接受原因與版本。位姿差過大
  不直接跳變命令，先確認是否誤關聯或需要重新對準。
- V4 接觸後，抓取關係與物體開度由已驗證的接觸控制處理；視覺不能突然改寫抓取
  目標。若觀測與模型持續不一致，依接觸狀態決定保持、受控卸載或釋放。

需要在開發集凍結的參數包括：最大資料年齡、RGB／深度／TF 同步容差、有效深度比例、
幾何殘差、位姿不確定度門檻、目標更新差值、重觀測次數與總任務時間上限。
目前不指定無依據的通用數值；V0–V1 完成量測後、保留測試前填入版本化設定表。

### 9.6 不確定度與安全邊界 🔬

位姿品質可由深度殘差、多視角一致性、重複量測或模型估計取得。先在有獨立參考的
開發資料檢查「品質變差是否對應較大實際誤差」，再選門檻。若尚無可信的共變異數，
使用可量測的品質規則並如實命名，不先宣稱有機率安全保證。

如採六維局部誤差共變異數，指定分量排列並依各自的任務容許尺度正規化：

$$
\tilde\Sigma=D^{-1}\Sigma D^{-\top}.
$$

$D$ 對應位置與旋轉的容許尺度，不直接比較混合 m² 與 rad² 的特徵值。偵測分數 $\gamma$
不等於安全機率；接受率、接受後超出位姿容差的比例與拒絕率必須一起報告。

AI 不停用或放寬輪速、關節、碰撞與接觸模式限制，也不因高信心縮小已驗證安全邊距。
目標辨識與障礙距離是不同資料路徑；未觀測空間、遮挡與反光造成的深度缺失不能標成自由
空間。即使安全層保留，錯誤模型、接觸參數或物理不可行情境仍可能失敗。

### 9.7 五個里程碑與退出條件

| 里程碑 | 進入條件 | 工作／退出依據 | 成果 |
|---|---|---|---|
| V0 感測與標定 | 相機來源與模型版本明確 | 完成 §9.2；有獨立幾何／時間驗證與負載紀錄，凍結感測容許值 | F8-V0、T8-V0 |
| V1 離線位姿 | V0 通過、有標註與獨立參考 | 比較幾何參考與學習式辨識；開發集完成門檻與資料分割，保留集只評一次定案版本 | F8-V1、T8-V1 |
| V2 視覺預抓取 | V1 可提供合格目標；所用平台 B／命令鏈已驗收 | 固定控制器做真值／視覺對照；量末端相對物體誤差、姿態、間距、逾時與失敗 | F8-V2、T8-V2 |
| V3 品質檢查與重觀測 | V2 基線、觀測候選路徑可行 | 分開比較品質拒絕與重觀測，固定次數／時間；交付接受、拒絕、恢復與總成本 | F8-V3、T8-V1～2 |
| V4 視覺接觸操作 | V3 及同平台 S3／S4 接觸基線通過；全身同動另需 S5 | 在已知軸下抓取並完成抽屜／門操作，依 §10.3 驗開度、接觸、釋放與退離 | F8-V4、T8-V3 |

V0–V1 可在 S2／S3 準備時提前執行，V2–V3 不必等待 P；但不得以未驗收的 Isaac 軌跡
執行鏈做視覺效果歸因。V4 若使用固定底盤／循序方法須標明 F／SEQ，不能稱全身同動。
正式 B／P 控制比較先使用共同已知感知；視覺條件下的 B／P 另列擴充比較。

### 9.8 消融、資料分割與判準

感知方法使用 VGT／VRAW／VGATE／VREOBS，避免與控制器 A／B／P 混淆：

| 感知組別 | 提供目標的方式 | 比較用途 |
|---|---|---|
| VGT | 真值或可靠獨立標定 | 理想目標來源的參考；其餘控制與負載條件一致 |
| VRAW | 單次視覺估計，不啟用品質門檻與重觀測 | 感知誤差影響；仍保留必需資料有效性、幾何及安全檢查 |
| VGATE | 同模型、單次估計，加品質門檻，失敗即拒絕 | 單獨量品質拒絕的效果與完成率代價 |
| VREOBS | 同門檻，加受限的重新觀測 | 相對 VGATE 隔離重新觀測的恢復效果與時間成本 |

主要三組為 VGT／VRAW／VREOBS；VGATE 用於分辨拒絕與重新觀測的作用。
離線估計先用相同影像；閉迴路採相同物體／初始條件／擾動配對，路徑改變後不強求
影像完全相同。VGT 可讓模型照常推論但不採用其目標，以保持感知算力負載一致；額外
提供關閉感知的效能診斷時另行標示。控制器、幾何、觀測位姿候選及安全限制不得同輪改動。

資料按**物體實例／資產與完整錄製序列**分割 train／dev／test，不把相鄰影格拆到不同集。
相同 mesh 換材質不能當成新幾何泛化；未見實例、未見視角與未見把手類型分開報告。
模擬真值可用於訓練標籤與評估，但 VRAW／VGATE／VREOBS 推論不可讀取把手真值。
已知機構軸與碰撞模型是明示的控制輸入，不得暗中用來替代視覺目標或選擇最佳估計。

先用少量開發場景確認失敗類型與變異，再凍結保留物體、場景清單、重複次數與分析規則。
不把 §10.5 核心每方法 180 趟直接乘上所有視覺組別；另估可負擔樣本量，重複影格及控制
週期不是獨立樣本。信賴區間按物體／場景群集且保留方法配對，所有逾時、拒絕與失敗保留。

末端誤差分成「估計對獨立參考」「TCP 對控制目標」「TCP 對真實物體目標」三種；只追到
錯誤目標不算任務成功。預抓取可沿用凍結 B 的 5 mm／0.02 rad／保持 2 s／逾時 60 s
作**控制參考**，視覺端到端容許值須依夾爪與把手幾何另行凍結，不直接照搬成相機精度要求。
總任務時間從首次觀測開始，包含推論、拒絕判定、移動視角及重試；接觸另採 §10.3 判準。

### 9.9 圖表與最低視覺交付

| ID | 內容 | 必要量測／呈現 |
|---|---|---|
| F8-V0／T8-V0 | 座標鏈、標定與時間／負載 | 深度尺度、外參、保留姿態誤差、感知 age、端到端延遲、控制 deadline miss |
| F8-V1／T8-V1 | 估計誤差與品質曲線 | 位置 mm、旋轉 °、對稱性處理、深度缺失；接受率與接受後錯誤率、n 與門檻 |
| F8-V2／T8-V2 | VGT／VRAW／VGATE／VREOBS 預抓取配對 | 對真實目標的誤差、完成率、時間、間距、拒絕／逾時；逐例差異與區間 |
| F8-V3 | 重新觀測案例與失敗分布 | 每次觀測位姿、品質、目標版本、決策、時間；含無法恢復的案例 |
| F8-V4／T8-V3 | 視覺接觸操作（條件式） | 開度、抓取殘差、滑移、限制、碰撞、釋放／退離與端到端成功率 |

**分支最低交付為 V0–V3**：可重跑的感測與估計流程、固定控制器下的配對實驗、品質拒絕
與重觀測紀錄、保留測試及失敗分析。這只能支持「視覺引導預抓取」；V4 完成才加入
「視覺操作」主張。模型未改善結果也需報告，不以成功影片代替比較。

### 9.10 暫緩分支與重新進入條件

- **未知機構／旋量軸估計：** 接觸與把手觀測已穩定後，再做視覺先驗、多視角與低速
  探測更新。單張影像不能保證辨識被遮住的鉸鏈；首版 V4 不依賴此能力。
- **LSTM 摩擦／動力學殘差：** 先確認同步且可信的力矩／電流資料、無接觸辨識資料、
  傳統摩擦與 MLP 基線、獨立力參考，且能區分摩擦與外部接觸，才評估時序模型。
  移動底盤的動態耦合另需建模；不直接以固定底盤觀測器宣稱全身六維外力精度。
- **S8-L 抽屜內取物：** 開抽屜與獨立抓取流程通過後，才比較語意選擇、定位與夾取；
  沿用候選物品 $\hat T_{WO}$ 與抓取集合 $\mathcal G_O$，但它們不是 V0–V4 必需欄位。
- **VLM 調阻抗／人機協同／滑移學習：** 涉及額外接觸穩定性、資料與任務判準，留作
  獨立延伸；不能由物體材質標籤直接推定剛度，也不因模型信心高而縮減安全限制。

### 9.11 第一階段選配延伸：NAV-LSTM 障礙物軌跡預測 🔬

**定位：導航預測的候選子題，不是第二階段操作成功的必要條件。** 先判斷更準的障礙物
預測能否改善現有控制，再決定模型；不因採用 LSTM 就預設成功率或平滑度會提高。

#### N0：預測效益診斷，先不訓練

固定導航控制器、時域、地圖、限制與完整命令鏈，在模擬比較：

- **CV：** 現有追蹤器輸出與等速度外推。
- **Oracle：** 保留相同感知流程，只對當下已追蹤且能由評估器關聯的物體提供真值未來
  軌跡；不補上漏檢、不修正定位、不移除追蹤缺口，不能當成可部署方法。

對表面點表示，先建立有效的物體／局部表面對應，利用已知剛體運動推進相同局部點；
不得把整個物體質心直接替換所有表面點。無法建立對應的樣本與回退需記錄。非互動式
腳本障礙可提供確定未來；若障礙會回應機器人動作，錄製軌跡不等於另一控制策略下的
反事實未來，必須另建可查詢模型或限制診斷場景。

位置 k=0 保持相同追蹤輸入。若採平移物體，Oracle 可將真值未來位移加到同一估計起點，
隔離未來運動誤差；若改用絕對真值位置，需另列包含當前定位修正的診斷組，不混為預測增益。

比較無碰撞到達率、逾時、總時間、路徑長度、急停、速度變化與最小間距；轉頭反轉判準
事前固定並排除角度 wrap 的假反轉。以當前驗收版本重測，不沿用舊註解中的相關性當因果證據。
Oracle 是效益診斷而非嚴格性能上界：更準預測可能因控制器使用方式而沒有較佳結果。

**進入 N1 的條件：** 在開發場景事前定義有實際意義的效益門檻與可接受安全代價，配對結果
顯示至少一項主要導航指標值得改善，且機制與非等速度遭遇相關。未成立時先查漏檢、
追蹤身分、線性化、下游命令修改與排程；不直接啟動大規模 LSTM 訓練。

#### N1：離線模型比較

以過去 K 筆可取得的追蹤狀態、實際時間間隔與有效 mask 為輸入，預測未來 N 步；
K、N、取樣頻率與時域由當前控制設定及開發資料決定，不預先固定「一定能看見 1 秒」。
可先評估等速度殘差形式：

$$
\hat p_{t+k}=\hat p_t+k\Delta t\,\hat v_t+\Delta p_{\mathrm{LSTM},k}.
$$

比較 CV、等加速度、使用相同歷史窗口的 MLP 與小型 LSTM，保持資料、輸入資訊及調參
預算可比。量測逐時域誤差、ADE／FDE、誤差尾端、推論延遲；如預測不確定度再驗校準。
連續座標與 ego-motion 處理需一致；ID 切換、長缺口與新物體清空隱狀態，不能跨物體串接。

按完整場景、路線、物體運動腳本及錄製序列分割資料。保留未見轉向位置、速度與行為模式，
避免只學固定折返点；未来真值只作標籤／評估。沒有徵兆的突然轉向與多種可能未來必須
如實呈現，單一平均軌跡不一定是可安全依賴的實際路徑。

#### N2：閉迴路接入，通過離線比較後才做

新介面需包含 `track_id`、原始時間戳、frame、預測時間格點、位置、相容速度、品質及
有效期。預測速度應與位置序列一致；對全部使用障礙物未來位置的模組逐一盤點，包括
Predictive CBF 與啟用時的時空代價，避免一處用 LSTM、另一處仍不知情地用 CV。

非同步推論與延遲對齊需實測；歷史不足、過期、非有限值或不可信時回退已驗證的 CV／
既有感知失效處置。保留 Raw-scan Shield 與原有限制，不自動降低安全邊距，也不將 CBF
沿錯誤預測求得可行視為實際無碰撞保證。2D 軌跡只能支援其感測範圍，不能直接宣稱
全手臂高度範圍的 3D 動態避障。

以同場景配對 CV／LSTM／Oracle，分開比較推論負載與預測內容；凍結模型後跑保留場景。
若只有離線誤差下降而閉迴路沒有增益，結論只能是預測改善，不能宣稱導航提升。
交付獨立 `NAV-LSTM` 設定、診斷報告、資料分割、模型與閉迴路結果，不占用 S8-L 語意取物 ID。

### 9.12 參考來源與論文用語

- [Grounded-SAM 官方專案](https://github.com/IDEA-Research/Grounded-Segment-Anything)：文字引導影像偵測／分割。
- [Contact-GraspNet 官方專案](https://github.com/NVlabs/contact_graspnet)：點雲六自由度抓取候選。
- [Where2Act 原論文](https://openaccess.thecvf.com/content/ICCV2021/html/Mo_Where2Act_From_Pixels_to_Actions_for_Articulated_3D_Objects_ICCV_2021_paper.html)：關節式物體可操作區域與動作。
- [VAT-Mart 原論文](https://arxiv.org/abs/2106.14440)：視覺動作軌跡提案。
- [Trajectron++ 原論文](https://arxiv.org/abs/2001.03093)：融合動態與場景資訊的循環模型軌跡預測參考，非已選用模型。

建議用語為「視覺引導之全身受約束預抓取／操作」或「學習式預測輔助的幾何模型預測
控制」。除非學習目標／結構確有物理約束設計，不直接稱 Physics-Informed；使用李群、
QP 或投影也不自動得到閉迴路收斂、硬即時或絕對安全保證。

---

## 十、實驗設計

### 10.1 實驗分層

| 實驗 | 對應階段 | 主要比較 |
|---|---|---|
| E0 模型／時間／安全機制 | S0–S1 | 獨立真值、故障注入、完整幾何與資料有效性 |
| E1 自由空間預抓取 | S1 | A vs B，同 μ；B 的單因素 μ 掃描 |
| E2 跨模擬器 | S2 | 凍結控制器的移植前後差異；致動校準與控制再調參分組 |
| E3 固定底盤接觸 | S3 | 抽屜／門；理想固定抓取與摩擦夾持分組 |
| E4 位姿與循序 | S4 | 固定停放、只看把手可達、整段操作可達 |
| E5 單步全身接觸 | S5 | F／SEQ／B，共同可行與需底盤補償兩類場景 |
| E6 預測控制主比較 | S6 | B vs P；同狀態／限制／觀測／調參預算 |
| E7 保留測試與穩健性 | S7 | 未調參物體、開度、配置與可量測模型誤差 |
| E8 選配延伸 | S8 | 分開研究視覺、順應或語意模組 |

### 10.2 核心評估指標

| 類別 | 指標 |
|---|---|
| 任務 | 完成率、操作時間、最終開度誤差 |
| 導航 | 到達率、導航時間、操作位姿誤差 |
| 末端 | 位置誤差、姿態誤差、抓取約束殘差 |
| 底盤 | 路徑長度、速度與加速度p95、輪速違反率 |
| 手臂 | 關節速度／加速度p95、關節限制違反率、最小操作度 |
| 安全 | 環境碰撞、自碰撞、最小間距、最深穿透 |
| 接觸 | 可用時報告真值／實測力及力矩、估計誤差、滑移、過流／保護停機；不可用填 NA |
| 計算 | 求解與端到端時間分列 p50／p95／p99／觀測最大值、deadline miss、連續逾時與 fallback |
| 執行 | QP 實際執行次數、狀態、約束數／丟列數、QP→安全層→驅動→實際速度各段差異 |

成功率以「一趟獨立任務」為單位；控制週期不能當成獨立樣本。距離模型下界、獨立
碰撞真值、屏障輸入違反與輸出違反分開統計。底盤 m/s 與關節 rad/s 混合時，濾波修改量
採分量呈現或以各軸速度上限正規化；殘差依約束種類保留單位。

### 10.3 成功條件

單趟任務成功至少需同時滿足：

1. 物體達到目標開度容許範圍。
2. 機器人與非目標環境無碰撞。
3. 無自碰撞。
4. 無關節與輪速硬限制違反。
5. 無馬達保護停機或控制器異常終止。
6. 在規定時間內完成、保持達標、安全釋放並退離；若只驗收操作子任務，事前明定窗口。

目標物體上的預期抓取接觸不能與非預期碰撞混為一談，評估程式必須分別標記。

預抓取與物體操作使用不同成功條件。現行預抓取門檻為位置 5 mm、朝向 0.02 rad、
保持 2 s、逾時 60 s；它不等於已抓取或已開門。接觸開度、滑移及力門檻在 S3 開始前
依把手／夾爪與硬體能力凍結，具體規劃值與待確認項見配套文件。

### 10.4 建議消融實驗

| 編號 | 底盤 | 任務與限制求解 | 下游安全檢查 | 比較目的 |
|---|---|---|---|---|
| F | 固定 | 與 B 相同但鎖定底盤三軸 | 相同 | 固定底盤參考 |
| SEQ | 先對準、操作時固定 | 與 F 相同 | 相同 | 操作位姿與循序整合 |
| A | 同動 | 無約束任務解→安全投影 | 相同 | 串接架構診斷，保留負結果 |
| B | 同動 | 單次 QP 納入任務與限制 | 相同 | 正式單步對照 |
| P | 同動 | 多步 W-GMPC 納入任務與限制 | 相同 | 多步預測增益 |

主比較為 B vs P。A vs B 用同權重隔離求解架構；B vs P 同時提供「一致設定」與「相同
調參預算下各自最佳設定」兩組，避免刻意選擇停滯的 B。固定底盤比較另分共同可行集合
與需移動底盤的場景，不能混算成單純演算法優劣。

獨立消融依序為：預測時域（記錄 N、Δt 與 T）、臂形／控制增量權重、反應式安全層、
順應控制。N=1 仍不必然等價 B，須核對成本、離散化及限制。安全層關閉只做隔離模擬／
離線對照，保留量測與中止機制，報告其存在；不可同時更換感知或降低 margin。

### 10.5 泛化驗證原則

- 調參物體與最終測試物體分開。
- 抽屜與櫃門都必須包含未參與調參的尺寸、開度與初始底盤位姿。
- 先凍結測試生成規則、物體分割與 seed 清單；調參完成後鎖住參數，才揭露／執行測試。
- 同一組條件採用配對方式比較。
- 報告樣本數、失敗類型與信賴區間，不只報告平均成功率。

正式主比較建議兩類機構各 30 組配對場景、每組 3 個受控擾動重複，即每方法 180 趟。
這是工作量規劃，非檢定力保證；先用 pilot 的變異估計確認可負擔性。區間以場景／物體為
群集保留配對關係，不把同一場景三次重跑或上千控制週期當成獨立物體。確定性重跑只
驗證可重現性。失敗不從分母刪除；時間需另報所有任務的逾時／未完成與共同成功子集。

---

## 十一、實驗可追溯性

第一階段manifest系統已完成並通過驗收：可記錄Git commit與dirty diff、輸入檔SHA-256、
實際節點的resolved parameters、bag完整性與執行狀態，且歸檔失敗時採fail-closed。第二
階段必須從第一趟實驗開始沿用，不得等實驗結束後再補建紀錄。

每一趟第二階段實驗的manifest需在既有內容上新增：

- 全身URDF/Xacro與mesh hash。
- Lite 6關節限制與控制器參數。
- 物體模型、關節類型與旋量軸。
- $T_{WA,0}$、$T_{AH,0}$與$T_{EG}$。
- 目標關節開度與初始開度。
- 底盤與手臂初始狀態。
- 使用的碰撞幾何與SDF版本。
- W-GMPC、順應控制與安全層resolved parameters。
- Git commit、dirty diff、bag完整性與分析腳本版本。
- 階段進入／退出、首次達標、保持結束、釋放、退離與故障事件時間戳。
- 安全統計保留從動作開始至安全終止的完整窗口；另報預抓取／操作等子窗口。第一階段
  導航的首次抵達窗口可留在導航子結果，不能直接套用第二階段接觸全流程。
- 方法 ID、scene／object／pair／repeat／seed、控制與模型版本、感知來源、量測缺漏原因。
- 模擬器／物理引擎版本、physics step、控制週期、時鐘、RTF、drive gains、effort 上限、
  摩擦與接觸參數、初始狀態誤差、控制硬體與執行負載。
- 夾持模型（理想連接／摩擦）、$T_{HG}$、碰撞允許配對與生效狀態；所有排除項可追溯。

所有圖表與正文數字應由bag或單一機器可讀摘要自動產生，避免手抄常數造成漂移。

---

## 十二、由目前進度起算的時程與決策點

原六個月表未納入 Isaac Sim，也包含已完成的模型／安全介面，故改以 S0–S9 的驗收依賴
安排。下表是工作量規劃，**不是完成日期承諾**。
截止日、每週投入與 Isaac Sim 硬體尚未確認，實際日曆須依執行資料更新。

| 工作 | 建議工作量範圍 | 決策點 |
|---|---|---|
| S0 回歸＋S1 B 基線 | 1–3 週 | 先掃 μ；無解再另輪尺度／臂形處理，不無限重跑單一案例 |
| S2 平台驗證 | 1–3 週 | 匯入準備可提前；凍結 B 後做閉迴路對照，失敗按模型／致動／時序分類 |
| S3 固定底盤接觸 | 2–4 週 | 抽屜先行，再做門；理想抓取與摩擦夾持分層驗收 |
| S4 位姿與循序整合 | 1–2 週 | 完成整段可達性與任務狀態機，不以只摸到把手通過 |
| S5 單步全身接觸 | 1–3 週 | 建立 B 接觸版對照；若改成本，凍結後各方法重跑 |
| S6 多步 W-GMPC | 3–5 週 | 先無障礙，再靜態障礙；檢查是否真的有預測增益 |
| S7 正式測試＋S9 收斂 | 3–4 週 | 凍結後測試；寫作與繪圖管線自 S0 持續累積 |
| S8 選配延伸 | 另估，不占核心承諾 | 核心資料完成後擇一，無餘裕就列未來工作 |

核心串行合計約 12–24 週，另留 2–4 週整合緩衝；上緣會超過六個月，不能假定所有延伸
都能塞入。需依實際截止日決定範圍。優先保留 B vs P、兩類物體、受控安全評估
與失敗數據；先削減語意取物、未知機構估計及無感測器順應等獨立子題。

S1 未到達時可保存負結果，不能稱為成功基線。若 B 在已證明存在可行解的測試仍失敗，
先定位尺度、局部極值、參考偏好或命令鏈問題；不能只為進下一階段就宣稱是單步方法的
固有限制。S6 若 P 無明確改善，報告適用條件與計算代價，不補寫普遍優越性。


---

## 十三、主要風險與降級方案

| 風險 | 影響 | 降級方案 |
|---|---|---|
| 手臂關節力矩資料不可用或雜訊過大 | 無法可靠估計外力 | 使用位置／速度控制完成運動學任務；順應控制列為未來工作 |
| 全身 QP 計算逾時 | 控制週期不穩定 | 縮短時域、向量化、warm start 或先採用單步 QP；減少碰撞列須保留涵蓋驗證，不能靜默截斷 |
| 2D LiDAR與手臂工作高度不重疊 | 伸出底盤包絡後無法完整避障 | 核心階段使用已知模型距離；S8-V 再驗證深度資料，明確限制能力範圍 |
| 腕部深度串流造成運算或散熱負擔 | 控制deadline miss或硬體降頻 | 先做負載量測；必要時降低解析度／頻率，控制驗證先使用已知模型 |
| 抓取幾何誤差過大 | 產生內力或脫落 | 降速、加入誤差界限、重新對準或安全釋放 |
| 操作底盤位姿不可達 | 手臂IK失敗 | 重新選擇候選底盤位姿，不強迫手臂進入奇異區 |
| 安全約束不可行 | 機器人被夾住或無法退離 | 停止任務推進；依接觸／負載選擇保持或受控卸載，確認釋放與退路可行才動作 |
| 夾持／致動模型未校準 | 動畫成功但負載與停距失真 | 理想運動學與動力學試驗分組；補測步階、制動、滑移與接觸反力 |
| B 與 P 同時改權重、幾何或安全層 | 無法歸因預測效果 | 配對凍結與單因素消融，保存設定一致與等調參預算兩種比較 |
| 模型過度複雜導致進度失控 | 無法形成可驗證成果 | 保留固定底盤與循序操作作為最低可交付成果 |

---

## 十四、第二階段最低可交付成果

若完整 W-GMPC 未完成，工程最低可交付成果定義為：

> 機器人能夠從隨機起點導航至經過可達性檢查的操作底盤位姿，在底盤固定的條件下，
> 使用統一 1-DOF 物體介面完成抽屜與櫃門操作；系統具有關節限制、自碰撞檢查、環境碰撞
> 檢查、失敗恢復與可重現實驗紀錄。

此為縮限後的工程交付，不自動滿足「多步全身控制研究」的論文主張。核心研究交付另需
S5–S7 的 B／P 方法與配對證據；若不足，論文題目、研究問題與貢獻同步調整。順應與 AI
本來即為選配，不應把未完成選配和未完成核心方法視為同一種降級。

完整目標則為：

> 在已知或可可靠估計的1-DOF物體模型下，底盤與六軸手臂透過
> $SE(2)\times\mathbb R^6$ W-GMPC同時運動，維持抓取一致性，並在輪速、關節與連桿安全
> 限制內完成prismatic及revolute兩類操作任務。

---

## 十五、預期研究貢獻

若後續實驗成立，本階段可形成以下貢獻：

1. **統一 1-DOF 物體介面：** 以旋量軸統一表示抽屜與櫃門的運動學約束。
2. **移動底盤與手臂的全身幾何預測控制：** 在
   $SE(2)\times\mathbb R^6$ 組態上協調底盤與機械臂，並在 $SE(3)$ 任務空間追蹤把手。
3. **底盤輪系與手臂關節的共同約束：** 將第一階段輪速多面體與手臂關節限制納入同一控制框架。
4. **連桿層級安全約束：** 以全身Jacobian描述非圓形連桿最近點速度，擴展第一階段的近距離安全概念。
5. **可重現的移動操作評估：** 對抽屜與櫃門進行配對、消融及未參與調參場景驗證。

上述項目在實驗完成前均屬於**預期貢獻**，最終論文只保留有程式、bag、統計與可重現實驗
支持的部分。

旋量、MPC 與 CBF-QP 是既有工具；採用同一公式或成功匯入 Isaac Sim 本身不構成方法
新穎性。論文應以本平台上共同限制的具體處理、預測相對單步的增益、可驗證失效邊界
及跨條件證據界定貢獻，並在文獻章逐項對照。此輪審查不代替完整相關研究調查。
