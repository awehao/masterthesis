# 第二階段計畫：通用 1-DOF 關節式物體之全身協同操作

> 平台：四輪全向移動底盤 `omni_bot` ＋ UFACTORY Lite 6 機械手臂  
> 軟體環境：ROS 2 Jazzy ＋ Gazebo Harmonic  
> 文件定位：本文件是**第二階段實作與驗證計畫**，不是既有成果報告。  
> 狀態標記：✅ 已完成、🟡 進行中、⬜ 待實作、🔬 待驗證。

---

## 摘要

第一階段已完成未知動態環境中的移動底盤導航，建立 SE(2) 幾何模型預測控制
（Geometric Model Predictive Control, GMPC）、全時域預測式控制屏障函數
（Control Barrier Function, CBF），以及不依賴障礙物分類與追蹤的 Raw-scan Safety
Shield。第二階段將在此基礎上，加入 UFACTORY Lite 6 機械手臂，研究移動底盤與手臂
在接觸式操作中的全身協同控制。

第二階段的核心任務是操作只有一個運動自由度的關節式物體，例如平移式抽屜與旋轉式
櫃門。系統將以旋量理論統一描述 revolute 與 prismatic 關節，並以

$$
\mathcal Q=SE(2)\times\mathbb R^6\times\mathbb R
$$

表示「全向底盤＋六軸手臂＋一軸目標物體」的廣義組態。末端執行器的操作誤差則定義於
$SE(3)$ 任務空間。實作將採漸進方式：先完成固定底盤的手臂操作，再完成導航與操作的
循序整合，最後才開放底盤與手臂同時運動的 W-GMPC（Whole-Body GMPC）。

本階段不預先宣稱絕對安全或通用泛化。所有結果將限定在感測有效、物體模型可辨識、
控制問題可行及硬體能力足夠的條件下，並以配對實驗、模組消融及未參與調參的測試物體
驗證。

---

## 一、研究目標與範圍

### 1.1 主要研究目標

建立一套可同時控制全向底盤與六軸機械手臂的全身協同控制架構，使機器人能：

1. 導航至關節式物體附近的可操作底盤位姿。
2. 以末端執行器對準並抓取把手。
3. 在維持抓取幾何關係的條件下，操作一個 1-DOF 物體關節。
4. 同時滿足底盤輪速、關節限制、自碰撞及環境碰撞限制。
5. 面對小幅模型與把手位姿誤差時，維持可控且不過載的接觸行為。

### 1.2 研究任務

本階段包含兩類代表性任務：

- **Prismatic joint：抽屜拉取**  
  把手沿固定滑軌方向平移，几何與控制问题相对单纯，作为第一项最小可行任务。
- **Revolute joint：櫃門開啟**  
  把手繞固定鉸鏈軸運動，末端路徑為圓弧，需同時調整位置、姿態與全身可操作空間。

### 1.3 本階段不直接宣稱的內容

以下能力不在尚未驗證前寫成既有成果：

- 不宣稱對任意家具、任意把手或任意 1-DOF 物體皆可零修改操作。
- 不宣稱在感測失效、接觸模型錯誤或物理不可行情境下保證零碰撞。
- 不宣稱無力感測器方法能精準重建完整六維接觸 wrench。
- 不把 AI 模組視為第二階段完成的必要條件。
- 不在固定底盤操作尚未穩定前，直接宣稱已完成全身 W-GMPC。

---

## 二、第一階段可繼承的技術資產

### 2.1 已驗證的導航結果 ✅

三種方法在相同 83 條有效配對路線上的結果如下：

| 方法 | 到達 | 碰撞 | 最深穿透 |
|---|---:|---:|---:|
| GMPC＋CBF＋Shield | 83/83 | 1/83 | 0.1 cm |
| nav2 MPPI | 64/83 | 6/83 | 36.4 cm |
| nav2 RPP | 60/83 | 30/83 | 57.5 cm |

第一階段應以兩個指标分开陈述：

- **总体到达率：** $83/83=100\%$。
- **无碰撞到达率：** $82/83=98.8\%$。

唯一一次接觸的估計穿透深度約為 1 mm，應描述為極淺邊界接觸，而不是宣稱碰撞率為零。

### 2.2 已驗證的底盤限制 ✅

| 物理量 | 實驗使用值 |
|---|---:|
| $v_{x,\max},v_{y,\max}$ | 0.2775 m/s |
| $\dot\varphi_{\max}$ | 1.1327 rad/s |
| $a_{x,\max},a_{y,\max}$ | 6.25 m/s² |
| $\ddot\varphi_{\max}$ | 25.51 rad/s² |

底盤控制器已包含輪速耦合限制：

$$
-r\omega_{\max}\mathbf 1
\leq W\xi_b
\leq r\omega_{\max}\mathbf 1,
$$

其中

$$
\xi_b=\begin{bmatrix}v_x&v_y&\dot\varphi\end{bmatrix}^{\top},
\qquad
W=
\begin{bmatrix}
0&1&L\\
-1&0&L\\
0&-1&L\\
1&0&L
\end{bmatrix}.
$$

第二階段必須保留此限制，因為底盤旋轉與平移會競爭相同的輪速預算。

### 2.3 已驗證的分層安全架構 ✅

第一階段控制鏈為：

```text
GMPC-CBF → velocity smoother → Raw-scan Safety Shield → 底盤
```

- GMPC-CBF：根據追蹤與預測結果提前繞行。
- Raw-scan Safety Shield：直接讀取未經動態分類的 `/scan`，提供近距離命令修正。

此架構可繼續保護底盤，但**不能直接視為手臂安全層**。手臂伸出後，整台機器人不再適合
以固定圓盤描述，因此第二階段必須另外建立連桿層級的碰撞距離與速度約束。

### 2.4 實驗可追溯性基礎 ✅

第一階段已建立實驗 manifest 與單一統計來源，能保存：

- Git commit 與 dirty diff。
- 實際使用的環境變數與 ROS 參數。
- 場景、軌跡、地圖與起訖點檔案及 SHA-256。
- bag 完整性、節點狀態與參數擷取狀態。

第二階段所有操作實驗應沿用相同機制，並額外保存手臂模型、關節限制、物體旋量軸、
抓取轉換與操作初始條件。

---

## 三、目前整合狀態

### 3.1 已完成項目 ✅

- Lite 6 已接合至全向底盤模型。
- 已建立全身 URDF/Xacro 的基本結構。
- 第一階段導航、感知、CBF 與 Shield 程式可作為底盤子系統。

### 3.2 仍需完成的模型驗證 🟡

手臂接到底盤不等於全身模型已可用。以下项目必须逐项确认：

1. `map → odom → base_link → arm_base → end_effector` TF 鏈連續且方向正確。
2. 六個關節的軸方向、零位、上下限及單位正確。
3. collision geometry 與 visual geometry 尺度一致。
4. 收折姿態不會與底盤自碰撞。
5. Gazebo、RViz、MoveIt 與控制器使用相同關節名稱。
6. 底盤移動時，末端在世界座標中的位姿連續且符合正向運動學。
7. 實際 Lite 6 的速度、加速度、負載與控制介面限制已寫入設定。

### 3.3 模型整合驗收標準

- 固定底盤，逐一運動六個關節，RViz、Gazebo與關節回授方向一致。
- 固定手臂關節，平移與旋轉底盤，末端世界位姿與TF计算一致。
- 隨機抽樣至少 100 組合法關節角，URDF FK 與控制程式使用的 FK 誤差低於設定容許值。
- 收折、預抓取及操作姿態皆通過自碰撞檢查。

---

## 四、通用 1-DOF 關節式物體模型

### 4.1 全身組態空間

令底盤位姿為 $X_b\in SE(2)$、手臂關節為 $q_a\in\mathbb R^6$、物體關節量為
$q_d\in\mathbb R$，則全系統組態為

$$
q_{\mathrm{wb}}=(X_b,q_a,q_d)
\in SE(2)\times\mathbb R^6\times\mathbb R.
$$

此系統共有 10 個廣義座標：底盤 3 軸、手臂 6 軸及物體 1 軸。末端執行器位姿
$T_{WE}\in SE(3)$ 是由底盤與手臂組態映射得到的任務空间量，不應将底盘本身写成
$SE(3)$。

### 4.2 單自由度旋量表示

以單位旋量

$$
\hat S_d=
\begin{bmatrix}
\omega_d\\v_d
\end{bmatrix}
\in\mathbb R^6
$$

統一描述物體的一個關節自由度。

#### Revolute joint

若關節軸的單位方向為 $\hat\omega$，軸線通過點 $q$，則空间旋量可写成

$$
\hat S_d=
\begin{bmatrix}
\hat\omega\\q\times\hat\omega
\end{bmatrix}
=
\begin{bmatrix}
\hat\omega\\-\hat\omega\times q
\end{bmatrix}.
$$

此模型用于门或柜门绕铰链转动。

#### Prismatic joint

若滑轨单位方向为 $\hat v$，则

$$
\hat S_d=
\begin{bmatrix}
0\\\hat v
\end{bmatrix}.
$$

此模型用于抽屉沿滑轨平移。

### 4.3 物體與把手位姿

令：

- $T_{WA,0}$：物體關節基座在世界座標中的零位姿。
- $T_{AH,0}$：物體零位時，把手相對關節基座的固定轉換。
- $[\hat S_d]$：旋量的 $4\times4$ 矩陣形式。

則把手在世界座標中的位姿可寫為

$$
T_{WH}(q_d)
=T_{WA,0}\exp\!\left([\hat S_d]q_d\right)T_{AH,0}.
$$

此式同時適用於旋轉與平移關節，差別只在 $\hat S_d$ 的內容。

### 4.4 固定抓取約束

令 $T_{WE}(X_b,q_a)$ 為末端位姿，$T_{EG}$ 為末端工具座標到實際抓取接觸座標的固定
轉換。抓取成功後，应满足

$$
T_{WE}(X_b,q_a)T_{EG}=T_{WH}(q_d).
$$

此等式的意义是：末端抓取点与把手保持同一位姿，而非只让末端位置追随把手。

实际系统需允许小幅误差，因此控制器可最小化 $SE(3)$ 抓取误差，而不一定把此式作为
完全不可松弛的硬等式。

### 4.5 泛化範圍

旋量表示统一了 revolute 与 prismatic 的运动学形式，但不代表更换物体时完全不需要
其他信息。每个新物体至少仍需提供或估计：

- 关节类型与旋量轴 $\hat S_d$。
- 关节零位姿 $T_{WA,0}$。
- 把手固定转换 $T_{AH,0}$。
- 合法关节范围 $q_{d,\min},q_{d,\max}$。
- 抓取方向、可接近区域与碰撞几何。
- 操作阻力或等效摩擦范围。

因此本阶段的“通用”是指**控制器共享同一数学接口与最佳化结构**，不是对任意家具零设定。

---

## 五、W-GMPC 全身協同控制架構

### 5.1 全身控制輸入

定义底盘体速度与手臂关节速度组成的控制向量：

$$
\nu=
\begin{bmatrix}
v_x&v_y&\omega&\dot q_a^{\top}
\end{bmatrix}^{\top}
\in\mathbb R^9.
$$

物体关节速度 $\dot q_d$ 可以作为预测状态或辅助决策变量，但它不是机器人致动器直接
施加的命令。机器人透过末端接触使物体运动。

### 5.2 末端位姿誤差

末端任务定义于 $SE(3)$。采用左不变误差：

$$
e_L=
\log\!\left(
T_{WH,\mathrm{ref}}^{-1}T_{WE}T_{EG}
\right)^{\vee}
\in\mathbb R^6.
$$

此形式在世界座标共同左乘时保持不变。若未来改用右不变误差，应明确写为

$$
e_R=
\log\!\left(
T_{WE}T_{EG}T_{WH,\mathrm{ref}}^{-1}
\right)^{\vee},
$$

并同步调整Jacobian与扰动定义，不能只更换名称。

### 5.3 速度層抓取一致性

在固定抓取下，末端抓取点与物体把手的twist应一致。统一至同一表达座标后，可写成

$$
J_G(X_b,q_a)\nu
=J_H(q_d)\dot q_d.
$$

- $J_G$：全身控制速度到抓取点twist的Jacobian。
- $J_H$：物体关节速度到把手twist的Jacobian。

若两侧twist使用不同座标表达，必须通过Adjoint转换后才能比较。

### 5.4 最佳化目標

W-GMPC可采用下列基本成本：

$$
J=
\sum_{k=0}^{N-1}
\left(
e_k^{\top}Q_e e_k
+\nu_k^{\top}R\nu_k
+\Delta\nu_k^{\top}S\Delta\nu_k
+w_d e_{d,k}^2
\right)
+e_N^{\top}Q_f e_N.
$$

其中：

- $e_k$：末端与把手参考位姿的 $SE(3)$ 误差。
- $e_{d,k}$：物体关节量与目标开度的误差。
- $R$：控制输入大小权重。
- $S$：控制增量平滑权重。
- $Q_f$：终端任务误差权重。

### 5.5 底盤與手臂的權重分配

第二阶段可研究不同的控制增量权重：

$$
S=\operatorname{diag}(S_b,S_a).
$$

- 较大的 $S_b$：减少重型底盘的频繁反转与高频抖动。
- 较小的 $S_a$：允许手臂承担较细微的末端误差修正。

此方法应称为**不对称控制增量权重分配**，不称为“动能分流”，除非成本中实际加入
质量矩阵形成的动能项

$$
\frac12\dot q^{\top}M(q)\dot q.
$$

权重分配的效果必须通过消融实验验证，不能预先宣称能降低能耗或结构共振。

### 5.6 W-GMPC 約束集合

控制器至少应包含：

1. 底盘速度、加速度与轮速耦合限制。
2. 手臂关节位置、速度与加速度限制。
3. 物体关节范围限制。
4. 抓取点运动一致性约束。
5. 手臂自碰撞距离限制。
6. 底盘、手臂与环境的碰撞距离限制。
7. 操作度或奇异点回避项。
8. 必要时的接触力或估计负载限制。

---

## 六、分階段實作策略

### 6.1 階段 A：固定底盤的手臂控制 ⬜

先固定底盘，只验证手臂与物体操作，避免导航误差与全身最佳化同时进入问题。

实施顺序：

1. 关节位置控制。
2. 正向运动学与Jacobian验证。
3. 数值IK或MoveIt末端位姿规划。
4. 预抓取、抓取、释放动作。
5. 固定底盘抽屉拉取。
6. 固定底盘柜门开启。

验收标准：

- 至少5组不同合法初始关节姿态能够到达预抓取位姿。
- 无关节超限、无自碰撞、无环境非预期碰撞。
- 抽屉与柜门任务分别记录成功率、末端误差及最大关节速度。
- 失败必须区分为IK不可达、规划失败、抓取失败、控制超时与碰撞。

### 6.2 階段 B：導航與操作循序整合 ⬜

采用状态机串接第一与第二阶段：

```text
NAVIGATE
  ↓
BASE_ALIGN
  ↓
ARM_PREGRASP
  ↓
GRASP
  ↓
CONSTRAINED_OPERATION
  ↓
RELEASE
  ↓
RETREAT
```

每个状态需定义：

- 进入条件。
- 成功条件。
- timeout。
- 安全停止条件。
- 失败恢复路径。

此阶段底盘与手臂不同时运动。目的是先验证“导航到正确操作位置”与“操作本身”能够稳定
串接。

### 6.3 階段 C：操作底盤位姿選擇 ⬜

导航终点不能只要求靠近物体，还必须位于手臂可操作区域。候选底盘位姿可依据

$$
J_{\mathrm{base}}(X_b)
=w_nJ_{\mathrm{nav}}
+w_rJ_{\mathrm{reach}}
+w_mJ_{\mathrm{manip}}
+w_cJ_{\mathrm{clearance}}
$$

评分，其中：

- $J_{\mathrm{nav}}$：导航成本。
- $J_{\mathrm{reach}}$：把手可达性。
- $J_{\mathrm{manip}}$：手臂操作度与奇异点距离。
- $J_{\mathrm{clearance}}$：底盘与全身碰撞间距。

验收时应报告候选数、可达率、最终操作成功率及因底盘位姿不佳导致的失败比例。

### 6.4 階段 D：全身 W-GMPC ⬜

当前三阶段均稳定后，才开放底盘与手臂同时运动。初期只处理已知物体模型与已知把手
位姿，不同时加入视觉估计误差、动态障碍物与未知接触阻力。

建议逐步增加难度：

1. 无障碍、已知抽屉模型。
2. 无障碍、已知柜门模型。
3. 加入静态环境障碍。
4. 加入把手位姿小扰动。
5. 加入物体关节轴小扰动。
6. 最后才加入导航误差与动态环境。

---

## 七、接觸順應控制規劃

### 7.1 為何需要順應控制

纯运动学轨迹假设铰链轴、滑轨方向及抓取转换完全正确。实际系统存在安装误差、物体
弹性、摩擦与把手位姿误差。如果控制器强制追踪错误轨迹，可能产生侧向内力、马达过流
或抓取脱落。

### 7.2 优先实施顺序

1. 若硬体可用，先以外接六轴力／力矩感测器建立可验证基准。
2. 确认Lite 6是否提供可用且时间同步的关节力矩或马达电流回授。
3. 建立摩擦、重力及传动误差基线。
4. 再评估动量残差观测器是否具有足够可观测性。
5. 最后才将估计外力接入导纳控制。

### 7.3 動量殘差觀測器候選方案 🔬

定义广义动量

$$
p(q,\dot q)=M(q)\dot q.
$$

机器人动力学为

$$
M(q)\ddot q+C(q,\dot q)\dot q+g(q)+\tau_f
=\tau_m+\tau_{\mathrm{ext}}.
$$

动量观测器的目标是估计外部关节力矩 $\hat\tau_{\mathrm{ext}}$，而不是直接宣称得到精确
的六维末端力。若接触集中于末端，可再利用

$$
\tau_{\mathrm{ext}}=J_E(q)^{\top}F_{\mathrm{ext}}
$$

估计

$$
\hat F_{\mathrm{ext}}
=(J_E^{\top})^{\dagger}\hat\tau_{\mathrm{ext}}.
$$

此估计会受到Jacobain条件数、摩擦模型、马达力矩估计及非末端接触影响，必须与真值或
外接感测器比较后才能评价精度。

### 7.4 幾何導納控制候選形式 🔬

可在末端切空间中定义修正量 $\Delta x\in\mathbb R^6$：

$$
M_a\Delta\ddot x+D_a\Delta\dot x+K_a\Delta x
=\hat F_{\mathrm{ext}}-F_{\mathrm{ref}}.
$$

再将修正后的参考转换回 $SE(3)$：

$$
T_{\mathrm{ref,mod}}
=T_{\mathrm{ref}}\exp([\Delta x]).
$$

导纳参数必须限制输出位移、速度与积分量，并设置估计异常时的fallback。是否采用此方案，
取决于前述外力估计是否通过验证。

---

## 八、全身安全層規劃

### 8.1 幾何表示

第二阶段拟将底盘与手臂连杆近似为一组capsule或其他简单凸几何，并计算：

- 连杆与环境距离。
- 连杆之间的自碰撞距离。
- 连杆与目标物体非预期部位的距离。

3D-SDF是候选距离来源之一，但必须先验证更新率、梯度稳定性与场景建图来源。若只有2D
LiDAR，不应直接宣称已经具备完整3D环境防护。

### 8.2 連桿點速度

对第 $i$ 个连杆最近点 $p_i$，其速度由全身Jacobian计算：

$$
\dot p_i=J_{p_i}(q)\nu.
$$

因此全身近距离安全约束应写成

$$
n_i^{\top}J_{p_i}(q)\nu
\leq
\alpha_i\left(d_i-d_{\mathrm{stop},i}\right).
$$

- $n_i$：由机器人最近点指向障碍物的单位法向。
- $d_i$：机器人几何与障碍物之间的表面距离。
- $J_{p_i}$：全身控制速度到连杆最近点速度的Jacobian。

底盘圆盘的

$$
v+\omega\times r_i
$$

只是上述Jacobian形式的刚体特例，不能单独代表机械臂连杆运动。

### 8.3 動態停止距離

候选停止距离沿用第一阶段的结构：

$$
d_{\mathrm{stop},i}
=d_{0,i}
+v_{\mathrm{app},i}\tau_{\mathrm{eff}}
+\frac{v_{\mathrm{app},i}^2}{2a_{\mathrm{brake},i}}
+\varepsilon_i.
$$

但手臂的 $a_{\mathrm{brake},i}$ 不能直接沿用底盘6.25 m/s²，必须由关节减速度限制与
当前Jacobian映射得到或采取保守下界。

### 8.4 求解方式

第一阶段Motzkin逐次投影可作为候选方案，但第二阶段约束同时包含底盘与六个关节，且
Jacobian行不再具有相同尺度。实施前需验证：

- 约束列正规化方式。
- 固定迭代次数下的残余违反。
- 关节盒限制是否会破坏已满足的碰撞约束。
- 多连杆夹击时的不可行情形与降级策略。
- 最坏执行时间，而非仅报告平均时间。

在实测前不写死“0.15 ms”或“刚性保证”。

### 8.5 安全主張的適用範圍

未来若实验支持，最多可主张：

> 对于感测可见、几何模型有效、速度位于机器人可逃逸范围且控制问题可行的障碍物，
> 全身预测控制与反应式安全层维持安全优先的控制行为。系统不宣称对感测失效、模型严重
> 错误或物理不可避免情境提供绝对保证。

---

## 九、感知與 AI 模組定位

### 9.1 最小必要感知

第二阶段核心控制至少需要：

- 物体关节类型。
- 关节轴或滑轨方向。
- 把手位姿。
- 物体合法开度范围。
- 机器人与环境碰撞几何。

初期实验可以使用Gazebo真值或人工标定值，先隔离控制问题。控制稳定后，再逐步换成视觉
估计，不能一开始同时验证感知与控制。

### 9.2 AI 增強模組 ⬜（選配）

可能的后续模块包括：

- 运动亲和性模型：初估关节类型、旋量轴与可抓取区域。
- 学习式残差模型：补偿难以显式建模的摩擦或传动误差。
- 语义模型：提供参数先验或物体类别信息。

这些模块不得直接绕过底层硬限制。其输出应经过范围检查、不确定度判断与fallback，并以
独立消融证明贡献。在第二阶段核心控制尚未完成前，AI不列为关键路径。

---

## 十、實驗設計

### 10.1 實驗分層

#### E1：模型与FK/Jacobian验证

- 固定底盘与固定物体。
- 随机抽样合法关节姿态。
- 比较数值微分、解析Jacobian与模拟真值。

#### E2：固定底盘操作

- 抽屉与柜门分别测试。
- 使用已知物体参数与已知把手位姿。
- 评估手臂控制本身。

#### E3：导航后循序操作

- 随机底盘起点。
- 导航至操作位姿后锁定底盘。
- 评估导航、对准与操作串接成功率。

#### E4：全身协同操作

- 底盘与手臂同时运动。
- 与固定底盘方法进行配对比较。

#### E5：模型误差与泛化

- 关节轴位置扰动。
- 把手位姿扰动。
- 摩擦或阻力变化。
- 未参与调参的物体尺寸及初始开度。

### 10.2 核心評估指標

| 类别 | 指标 |
|---|---|
| 任务 | 完成率、操作时间、最终开度误差 |
| 导航 | 到达率、导航时间、操作位姿误差 |
| 末端 | 位置误差、姿态误差、抓取约束残差 |
| 底盘 | 路径长度、速度与加速度p95、轮速违反率 |
| 手臂 | 关节速度／加速度p95、关节限制违反率、最小操作度 |
| 安全 | 环境碰撞、自碰撞、最小间距、最深穿透 |
| 接触 | 最大估计外力、过流／保护停机次数、抓取脱落次数 |
| 计算 | 求解时间p50／p95／p99、deadline miss率、fallback次数 |

### 10.3 成功條件

单趟任务成功至少需同时满足：

1. 物体达到目标开度容许范围。
2. 机器人与非目标环境无碰撞。
3. 无自碰撞。
4. 无关节与轮速硬限制违反。
5. 无马达保护停机或控制器异常终止。
6. 在规定时间内完成并进入安全释放状态。

目标物体上的预期抓取接触不能与非预期碰撞混为一谈，评估程式必须分别标记。

### 10.4 建議消融實驗

| 编号 | 底盘运动 | 全身GMPC | 连杆安全层 | 顺应控制 | 目的 |
|---|---:|---:|---:|---:|---|
| A | 固定 | ✗ | 基本碰撞检查 | ✗ | 手臂操作基线 |
| B | 循序对准 | ✗ | 基本碰撞检查 | ✗ | 验证操作位姿选择 |
| C | 同时运动 | ✓ | ✗ | ✗ | W-GMPC本体贡献 |
| D | 同时运动 | ✓ | ✓ | ✗ | 连杆安全层贡献 |
| E | 同时运动 | ✓ | ✓ | ✓ | 顺应控制贡献 |

若顺应控制尚未通过外力估计验证，不执行E组，也不把它列入已完成成果。

### 10.5 泛化驗證原則

- 调参物体与最终测试物体分开。
- 抽屉与柜门都必须包含未参与调参的尺寸、开度与初始底盘位姿。
- 参数冻结后才生成最终测试集合。
- 同一组条件采用配对方式比较。
- 报告样本数、失败类型与信赖区间，不只报告平均成功率。

---

## 十一、實驗可追溯性

每一趟第二阶段实验的manifest应新增：

- 全身URDF/Xacro与mesh hash。
- Lite 6关节限制与控制器参数。
- 物体模型、关节类型与旋量轴。
- $T_{WA,0}$、$T_{AH,0}$与$T_{EG}$。
- 目标关节开度与初始开度。
- 底盘与手臂初始状态。
- 使用的碰撞几何与SDF版本。
- W-GMPC、顺应控制与安全层resolved parameters。
- Git commit、dirty diff、bag完整性与分析脚本版本。

所有图表与正文数字应由bag或单一机器可读摘要自动产生，避免手抄常数造成漂移。

---

## 十二、六個月開發時程

### Month 1：全身模型与固定底盘控制

- 完成TF、URDF、关节限制与碰撞模型验证。
- 完成Lite 6 FK、Jacobian与IK测试。
- 建立预抓取、抓取与释放流程。

**里程碑 M1：** 固定底盘下，手臂可重复到达抽屉预抓取位姿，且无关节超限及自碰撞。

### Month 2：固定底盘1-DOF操作

- 完成已知参数抽屉拉取。
- 完成已知参数柜门开启。
- 建立物体旋量接口与操作状态机。

**里程碑 M2：** 固定底盘下完成两类1-DOF任务，并具有可分类的失败记录。

### Month 3：导航与操作串接

- 建立操作底盘位姿候选与评分。
- 串接导航、对准、抓取、操作与释放。
- 建立第二阶段manifest与评估管线。

**里程碑 M3：** 从随机导航起点出发，循序完成至少一种操作任务。

### Month 4：W-GMPC全身协同

- 建立 $SE(2)\times\mathbb R^6$ 全身预测模型。
- 加入底盘轮速与手臂关节限制。
- 加入抓取一致性与操作关节目标。

**里程碑 M4：** 在无外部障碍条件下，底盘与手臂可同时运动完成抽屉或柜门任务。

### Month 5：全身安全与顺应候选方案

- 建立连杆capsule距离与Jacobian约束。
- 验证自碰撞与环境碰撞安全层。
- 评估关节力矩资料品质与动量观测器可行性。
- 只有外力估计通过验证后，才接入导纳控制。

**里程碑 M5：** 完成全身安全层消融；顺应控制依验证结果决定是否纳入。

### Month 6：泛化测试与论文整理

- 冻结参数。
- 执行未参与调参的抽屉与柜门测试。
- 完成配对消融、失败分类与统计分析。
- 整理论文方法、限制与实验结果。

**里程碑 M6：** 形成可复现的第二阶段实验资料与论文结果，不以单一成功展示取代统计验证。

---

## 十三、主要風險與降級方案

| 风险 | 影响 | 降级方案 |
|---|---|---|
| 手臂关节力矩资料不可用或噪声过大 | 无法可靠估计外力 | 使用位置／速度控制完成运动学任务；顺应控制列为未来工作 |
| 全身QP计算超时 | 控制周期不稳定 | 缩短时域、减少碰撞点、分层求解或先采用速度层QP |
| 3D环境感知不足 | 连杆无法完整避障 | 初期使用已知场景SDF或Gazebo真值，明确限制，不假装未知环境能力 |
| 抓取几何误差过大 | 产生内力或脱落 | 降速、加入误差界限、重新对准或安全释放 |
| 操作底盘位姿不可达 | 手臂IK失败 | 重新选择候选底盘位姿，不强迫手臂进入奇异区 |
| 安全约束不可行 | 机器人被夹住或无法退离 | 停止操作、释放物体、退回预操作姿态并重新规划 |
| 模型过度复杂导致进度失控 | 无法形成可验证成果 | 保留固定底盘与循序操作作为最低可交付成果 |

---

## 十四、第二階段最低可交付成果

若完整W-GMPC、无力感测顺应与AI模块无法全部在时程内完成，第二阶段最低可交付成果定义为：

> 机器人能够从随机起点导航至经过可达性检查的操作底盘位姿，在底盘固定的条件下，
> 使用统一1-DOF物体接口完成抽屉与柜门操作；系统具有关节限制、自碰撞检查、环境碰撞
> 检查、失败恢复与可复现实验纪录。

完整目标则为：

> 在已知或可可靠估计的1-DOF物体模型下，底盘与六轴手臂透过
> $SE(2)\times\mathbb R^6$ W-GMPC同时运动，维持抓取一致性，并在轮速、关节与连杆安全
> 限制内完成prismatic及revolute两类操作任务。

---

## 十五、預期研究貢獻

若后续实验成立，本阶段可形成以下贡献：

1. **统一1-DOF物体接口：** 以旋量轴统一表示抽屉与柜门的运动学约束。
2. **移动底盘与手臂的全身几何预测控制：** 在
   $SE(2)\times\mathbb R^6$ 组态上协调底盘与机械臂，并在 $SE(3)$ 任务空间追踪把手。
3. **底盘轮系与手臂关节的共同约束：** 将第一阶段轮速多面体与手臂关节限制纳入同一控制框架。
4. **连杆层级安全约束：** 以全身Jacobian描述非圆形连杆最近点速度，扩展第一阶段的近距离安全概念。
5. **可复现的移动操作评估：** 对抽屉与柜门进行配对、消融及未参与调参场景验证。

上述项目在实验完成前均属于**预期贡献**，最终论文只保留有程式、bag、统计与可复现实验
支持的部分。
