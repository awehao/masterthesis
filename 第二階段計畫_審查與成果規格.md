# 第二階段計畫：審查、階段驗收與成果圖表規格

> 更新：2026-09-09。配套主文件：[第二階段計畫.md](第二階段計畫.md)。
> 本文件是計畫與審查結果，不是實驗完成報告；F 為規劃的圖、T 為規劃的表，ID 不代表檔案已生成。
> 依據：完整主計畫、第二階段進度紀錄（尤其 §10.11–10.17）、障礙實驗紀錄及現行預抓取／掃描程式。
> 本輪只修訂文件，未更動控制器、未執行模擬，也不覆寫正在累積的 `evaluation/results/mu_sweep/`。

## 1. 整體判斷：主軸可延續，但要補齊比較與接觸兩段

目前工作正在建立「機器人有手臂伸出時，仍能讓底盤與手臂協調移動、受限制並在資料失效時停止」
的控制基礎。這能銜接到開抽屜／門，但**預抓取到達、抓住把手、帶動物體、完成安全釋放**是四個
不同成果，須逐一驗證。

建議核心研究問題：在共享物體模型、硬體限制、感知與安全介面的條件下，多步全身預測能否比
整合式單步 QP 更早調整底盤路徑與臂形，提高 1-DOF 操作的完成能力？同時量化安全、接觸與計算代價。
若單步已足夠，這也是有效結果；預測的效益可能只在長行程、接近限位或須提前轉位的場景出現。

| 層級 | 要交付什麼 | 不以什麼替代 |
|---|---|---|
| 工程可用 | 模型／命令／安全／接觸鏈接通，可重跑 | 單張漂亮模擬畫面 |
| 控制有效 | 機器人真的把物體開到指定開度 | 末端沿預定直線／圓弧移動 |
| 研究比較 | B vs P 的配對完成率、限制、接觸與計算結果 | 只與已知停滯的 A 比較 |
| 外推邊界 | 保留物體、參數誤差、跨平台子集與失敗區域 | 換模擬器即代表實機可用 |

### 1.1 審查發現與修正優先序

P0：會影響實驗效度或任務能否成立；P1：正式論文前必須明確；P2：依時間選配。

| 優先 | 原計畫問題／證據 | 修正與必需證據 |
|---|---|---|
| P0 | 原 §3 仍寫 12 點無消費端、模型驗收待做；§10.17 已有全身閉迴路 | 更新完成狀態；模型 hash 分版本，歷史值不混算 |
| P0 | 原消融只有固定／循序／MPC，缺少 B | 加入相同限制的單步 QP；A 留作架構診斷 |
| P0 | 把 `μ_post` 說成末端姿態權重 | 分開「臂形偏好」與「末端朝向」；保存成本方程及參數對照 |
| P0 | 原 W-GMPC 有成本，未完整定義預測與物體如何受驅動 | 補每步狀態轉移、抓取耦合、首步執行、低階追蹤與物體被動性 |
| P0 | 預抓取直接接到拉門，缺夾持與接觸模式 | 增 S3；理想連接與摩擦抓取分開，物體不得直接受位置命令 |
| P0 | 全域正間距可能阻止手指真正接觸把手 | 只對指定接觸配對、指定狀態允許接觸；其他門板／櫃體仍受保護 |
| P0 | 停止驗收可能被讀成故障全程安全 | 分開故障→偵測→歸零→實際停止；重測停距，正確標示命令動態覆寫 |
| P0 | 所有第二階段安全數據沿用首次抵達截斷 | 保留至保持、釋放、退離／中止；子任務另有窗口 |
| P0 | `v_prev` 不同、輸入凍結停距與輸出驗算問題 | S1 補命令回授與時間配對；凍結列殘差和最終速度重算值分列 |
| P1 | 原六個月未含 Isaac Sim，且順序與現況不同 | 以 S0–S9 排程；S2 做有明確退出條件的平台驗證 |
| P1 | $T_{EG}$、$T_{HG}$、twist 排列／座標混用風險 | 統一抓取座標，核對 `[角;線]`／`[線;角]` 與 log Jacobian |
| P1 | m、rad、臂形、加速度與 jerk 混成單一標量 | 按物理量分軸、正規化，保留指令與實際值；不將位移叫能耗 |
| P1 | 只在關閉時可抓取，不一定能完成整段開度 | 候選底盤位姿先做全路徑可達性與門板掃掠檢查 |
| P1 | 把 MPC／旋量／移植本身列為新穎性 | 限定具體方法改進與證據；另做相關工作比較，不先宣稱首創 |
| P2 | AI、無感測器順應、語意取物、動態障礙全部展開 | 選配分支各自有進入條件；核心未凍結前不綁進主成功條件 |

### 1.2 截至本次審查的進度快照

| 項目 | 已知狀態 | 本輪判讀 |
|---|---|---|
| FK／Jacobian／模型方向 | 有獨立參考驗收，新車高也有回歸 | 維護並補版本索引，不從零重做 |
| 全連桿安全幾何 | 已由舊 12 點升級，包含底盤與完整帶 | 保留涵蓋條件；幾何正確不等於全部動態安全已證明 |
| reason 7 | +365 ms 出現 reason 7；+417 ms 命令零；其前底盤走 36.0 mm | 故障停止功能已有案例證據；實機制動未驗證 |
| reason 8 | 137/138 週期為 reason 8；命令全零、自始未動 | 截斷拒絕通過個案驗收 |
| A／B，有障礙預抓取 | A 終誤差 225.9 mm；B μ=1 終誤差 180.7 mm，兩者未達標 | 架構改善證據，非成功基線；長度／週期數不同，不直接比較原始違反計數 |
| 空約束＋限位裕度 | 紀錄已修：硬錯誤、0.02 rad 內縮 | 正式回歸記版本；卡限底層原因仍未解 |
| μ 掃描 | 腳本已存在；資料目錄有未追蹤內容 | 不將正在產生的內容當成驗收結論 |
| 接觸／W-GMPC／Isaac Sim | 尚無本輪可確認的完成證據 | 按待驗收規劃 |

臂形總位移另有口徑差異：§10.17 比較表的 1.399 rad 與掃描摘要重算的 2.483 rad
不能直接互換。正式圖表須明定是終點差、逐軸累積變化量或採樣窗口；本文件不替原資料猜定義。

## 2. 階段依賴與方法名稱

```text
S0 模型／量測回歸 → S1 自由空間 B 基線 → S2 Isaac Sim 介面／致動驗證
                            │                         │
                            └──────→ S3 固定底盤接觸 ←┘
                                          ↓
                             S4 操作位姿／循序整合
                                          ↓
                             S5 單步全身接觸 B
                                          ↓
                             S6 多步預測 P 比較
                                          ↓
                             S7 保留測試 → S9 論文交付
                                          └→ S8 選配延伸
```

S2 的匯入／介面準備可現在開始，閉迴路對照須有 S1 的凍結參考。S3 選已驗證可用的平台
執行；S2 若受阻，不必阻止 Gazebo 接觸試驗。S9 的圖表與寫作隨各階段累積。

| 方法 | 定義 | 使用位置 |
|---|---|---|
| F | 底盤固定；手臂任務與安全限制同時求解 | S3、S5 |
| SEQ | 導航／底盤對準後固定操作 | S4、S5 |
| A | 任務求解後再投影 | S1 架構診斷；可保留失敗 |
| B | 同次 QP 納入任務、臂形、硬體與幾何限制 | S1 自由空間版、S5 接觸版 |
| P | 多步全身預測，只執行首步，再以回授重解 | S6 主方法 |

B 的自由空間版與接觸版分別凍結版本；不能把 S1 預抓取成功直接算作 S5 接觸成功。

## 3. 全階段共用的驗收與資料規則

### 3.1 哪些門檻已存在，哪些只是提案

| 項目 | 數值／規則 | 性質 |
|---|---|---|
| 預抓取位置／朝向 | 5 mm／0.02 rad（約 1.15°） | 現行程式預設；正式凍結須記 resolved params |
| 預抓取保持／逾時 | 2 s／60 s | 現行程式預設 |
| 關節邊界內縮 | 0.02 rad | 紀錄中的修補值；不等同含完整制動動態的安全證明 |
| 模型 FK 回歸 | 位置 ≤1 mm、朝向 ≤0.1°，按來源列出最大與 p95 | 新建議工程門檻；若量測解析度不足，先修量測 |
| Jacobian 回歸 | 在已定義尺度下相對誤差 ≤10⁻⁵；近零欄另報絕對誤差 | 新建議；差分步長掃描避免數值消去 |
| 抽屜／門終開度 | 5 mm／1°，達標保持 2 s | S3 pilot 建議值；須由行程／把手幾何驗證後凍結 |
| 接觸任務逾時 | 操作段 60 s；端到端依導航距離另定上限 | S3/S4 建議，不能執行後延長救回成功 |
| 可重現工程通過 | 每類選定標準案例 5/5 完成，再做多初始條件 pilot | 工程進階門檻，非泛化成功率證據 |
| 自碰撞／環境 margin | 依合法配對、幾何誤差與動態條件凍結 | 不全域套單一 5 mm；不放寬 margin 讓某方法過關 |
| 接觸力／力矩／滑移 | 由感測能力、夾持尺寸與硬體規格決定 | 尚待確認；沒有可信來源不能填看似合理的 N 值 |
| 計算截止 | 如使用 20 Hz，名義週期為 50 ms | 記錄實際控制頻率；端到端包含資料處理及發布，非只計 OSQP |

上述門檻是進度規劃，不能事後覆蓋歷史結果。更改門檻必須產生新評估版本並對所有比較方法一致重算。

### 3.2 成功、安全與窗口分開

每趟至少有 `task_reached`、`safe_success`、`abort_reason`、`failure_stage`。成功須同時達標、
維持要求的抓取、無非預期接觸、無硬限制違反，並按指定流程終止。目標已開但途中碰撞，屬到達而
非安全成功；安全停滯也不是任務成功。

記錄 `t_start`、`t_pregrasp`、`t_grasp`、`t_open_first`、`t_hold_end`、`t_release`、`t_retreat_end`
及 `t_abort`。全流程安全統計從開始到最後停止／退離，另切導航、預抓取、接觸操作等子窗口。
失敗任務的終值與全窗口最小距離仍需保留。

故障注入另記 `t_inject → t_detect → t_zero_cmd → t_stopped`，各段計算實際路徑長與最大關節
變化量，不以中位速度乘時間代替積分。`t_stopped` 用依靜止雜訊凍結的線速／角速／關節速度
門檻，持續 0.2 s 才判定；同時記錄取樣間隔與時間不確定度。

### 3.3 統一指標定義

| 指標 | 定義／注意事項 |
|---|---|
| TCP 位置誤差 | $\|p_G-p_G^\ast\|$，圖用 mm |
| TCP 朝向誤差 | $\|\log(R_G^{\ast\top}R_G)^\vee\|$，圖用 °；近 0／180° 都需數值測試 |
| 抓取殘差 | 比較機器人側與物體側抓取座標；位置 mm、朝向 ° 分圖，非任務開度誤差 |
| 開度誤差／進度 | 原單位 m 或 rad；跨物體另報 $(q_d-q_{d,0})/(q_d^\ast-q_{d,0})$，非零目標行程 |
| 安全距離 | 模型下界、獨立幾何參考／物理接觸真值分列；缺真值不寫成零碰撞 |
| 速度約束殘差 | 屏障 m/s、角速度列 rad/s 等分種類；raw 與正規化殘差不可混合 |
| 濾波修改量 | 每軸 $\nu_{safe}-\nu_{QP}$ 及 $\|D_{vmax}^{-1}(\nu_{safe}-\nu_{QP})\|_2$ |
| 命令配對 | 依 command sequence／時間戳對齊；「最近一筆」不保證是同一週期，無法配對標 missing |
| 關節裕度 | 每軸對原安全界及內縮控制界的最小距離 rad，兩者分列 |
| 各軸運動量 | 底盤路徑 m、yaw 累積絕對變化 rad、各關節 $\sum\lvert q_{j,k}-q_{j,k-1}\rvert$；不叫能耗 |
| 能量（選配） | 有可信 effort／速度時才算機械功或功率；電能須有電壓電流，不能互稱 |
| 接觸 | 力 N、力矩 N·m 分列，記 frame／濾波／取樣率；估計、模擬真值與實機量測分列 |
| 即時性 | 求解、端到端 wall time、狀態 age、控制間隔分列；RTF 與 simulation time 另報 |

### 3.4 場景、配對與統計

開發先每類 5 組場景 × 3 次擾動試驗；正式 B vs P 建議每類 30 組 × 3 次，共 180 趟／方法。
以 pilot 確認變異、時間和所需精度後才凍結正式數量，不能把 180 當成統計效力保證。
兩類各至少 3 個未調參的幾何設定是起始建議；若只是同個 CAD 改尺寸，稱參數外插／內插測試，
不稱任意新家具泛化。物體為統計層級時也要揭露只有多少獨立物體。

所有方法共享 `pair_id`、目標、初始真值、誤差注入、感知資料來源及外部障礙時間腳本。
隨機交錯方法執行順序以降低負載漂移；動態障礙按相同絕對時間腳本重播，不能要求不同控制器
經歷完全相同的相對遭遇。確定性重跑是重現性檢查，不擴充環境樣本數。

主要報告安全成功率與配對差值 95% 區間，以場景為群集重抽、保留同一配對的所有方法；
若研究主張跨物體，需按物體層級處理且承認物體數少的限制。單組二元率可另報 Wilson 區間。
碰撞 0/n 仍有不確定度，不寫成風險為零。多組探索性比較須標示，正式主要比較預先指定。

逾時是未完成，不把其 60 s 當成實際完成時間。時間報告全體到達曲線及共同成功子集的配對
時間差；被碰撞／異常中止的失敗不是任意當作獨立刪失。所有啟動試驗保留，重置失敗另列
流程失敗且不混入「控制器已接手」分母，事先定義兩個分母。

## 4. S0：模型、座標與量測回歸

**目的：** 後續差異確實來自控制方法，不是兩套機器人模型或時間戳。

工作：核對修正後的車高／底盤 collision／手臂限位／夾爪；比對獨立 FK、Jacobian、模擬連桿
位姿；分別驗證收折、全零、預抓取與近限位構形。環境與自碰撞使用獨立檢查途徑，排除配對要
有機構依據，不能只因距離長期很小就自動排除。

樣本：沿用 200 組運動學、300 組自碰撞隨機測試作版本回歸，增加具名姿態、近奇異與近限位
案例；隨機通過不是全構形證明。關節 6 軸與底盤 x/y/yaw 分別做低速步階與停止，量命令、接受
命令與實際回授。歷史已完成項只有在模型變更或缺必要欄位時補跑。

退出：新模型通過 §3 門檻、碰撞清單有明確界線、單一模型／frame／joint-name 來源與時間基準。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F0-1 | 模型正視／側視疊圖 | visual、collision、LiDAR 平面、手臂工作高度、地面與主要 frame；尺寸 m |
| F0-2 | FK／Jacobian 誤差分布 | x：測試類別或差分步長；y：誤差（log 軸），位置／朝向／正規化 Jacobian 分面；畫門檻 |
| F0-3 | 命令—回授步階圖 | x：s；y：底盤 m/s／rad/s、關節 rad/s 分面；標延遲與停止時刻 |
| T0-1 | 模型回歸表 | model hash、來源、n、位置／朝向最大及 p95、Jacobian 誤差、最小自碰撞距離、結論 |
| T0-2 | 介面與限制表 | joint/frame/topic、單位、頻率、來源、上下限、裕度、手冊／量測標記、timestamp clock |

## 5. S1：安全機制收斂與單步 B 基線（目前所在）

**目的：** 證明 B 能在已知靜態障礙與硬體限制下完成預抓取，並把其不成功的邊界記清楚。

順序：

1. 保存目前 A／B μ=1 的歷史結果與版本；不把起始誤差、時長不同的兩趟稱為完全相同閉迴路。
2. 使用現成 `sweep_mu.py` 掃 `1, 0.3, 0.1, 0.03, 0`，其餘固定。μ=0 仍保留朝向任務及阻尼。
3. 若無合格解，檢查目標／可行性、臂形偏好與位置／朝向尺度；另開一輪固定 μ 調整尺度或權重。
4. 本輪掃描完成後，獨立修補 `cmd_out → v_prev`、輸出時間配對與 solver 執行診斷；先後各自保存
   版本，再以修補後版本重跑候選 μ 和 A/B 比較，不跨修補直接歸因 μ 效果。
5. 回歸空約束、QP 未收斂、限位內縮、完整帶溢出與資料過期。空障礙場景也應有明確的「有效且
   無近障礙」表示及硬體限制，不與無資料混為一談。
6. 擴成至少 5 組預先定義的可行場景：無障礙、底盤停放點受阻、手臂近障礙、較大轉向、近限位
   但可解；先做 3 次擾動試驗。另留不可行案例驗證停止，不計入可行到達率但報告其結果。

S1 工程退出：凍結候選在指定標準案例 5/5 安全完成，多場景 pilot 已完成並報告全部失敗，
QP 確實執行且輸出限制驗算通過。零故障時的 2 s 保持需確認沒有反覆跨越門檻。
這允許開始 S2 閉迴路移植，不等同證明所有場景可收斂。

故障試驗：reason 7／8 至少在 3 種移動狀態各 3 次，補 watchdog、JointState／TF 過期與空約束。
先單獨注入，核對沒有別的條件搶先；正常延遲、故障反應與制動三者分列。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F1-1 | A/B 同案例軌跡圖 | x-y 底盤路徑（m）＋TCP 三維路徑；障礙輪廓、目標、等時間姿態、停滯點 |
| F1-2 | 任務誤差與 μ 掃描 | 時間—位置 mm／朝向 °；另以 μ 為 x 的終誤差／完成率分面，μ=0 用類別軸；均畫達標線 |
| F1-3 | 命令鏈與安全作用 | x：s；y：各軸命令與正規化修改量、輸入／輸出殘差分面；標 QP 狀態、活躍列數 |
| F1-4 | 故障停止時序 | x：注入後 ms；y：reason、data age、命令、實際速度／累積位移分面；四事件垂線 |
| F1-5 | 配對改善與代價 | 每 pair 的 A/B 終誤差連線；位置／朝向分面；全程最小裕度與關節運動量另面，不混軸 |
| T1-1 | μ 掃描表 | μ、n、完成/總數、成功時間、位置／朝向終值及峰值、最小模型與獨立距離、關節裕度、QP 狀態 |
| T1-2 | 安全機制驗收表 | fault、移動模式、n、注入→偵測／歸零／停住的中位與最大、各段位移、恢復策略、通過/失敗 |
| T1-3 | 架構與濾波表 | method、model/controller hash、窗口、有效配對週期、輸入違反數/分母、輸出違反、修改量 p50/p95/max、各軸運動量 |

尾段修改量明定為最後幾秒或最後幾筆，不混用「200 週期後」與另一窗口。不要把目前案例的
「安全停住」寫成「安全完成」。F1-1 可展示失敗，F1-2 與 T1-1 決定是否成立到達基線。

## 6. S2：Isaac Sim 移植與致動校準

**目的：** 先確認相同控制輸入在新平台的意義，再用跨平台差異研究模型敏感度。

現在可做：確認可用硬體、選定並記錄 Isaac Sim／ROS 2 版本、匯入 URDF/USD、核對 frame／joint
映射與場景。閉迴路正式開始條件是 S1 有凍結的成功案例與失敗案例各一組。

分兩層驗證：

1. 介面等價：同 FK、單位、命令方向、控制週期與已知模型距離來源；用開迴路重播檢查致動差異。
2. 動力學敏感度：記錄質量／慣量、有限 effort、drive gains、摩擦／接觸設定，重新量步階、制動
   與受載追蹤。每次只變更一類設定，先凍結高階 B，之後需要再調控制器時另列 tuned-transfer 組。

底盤明確選擇：若仍以 body velocity 直接驅動，跨平台結果只支持運動學／軟體介面；若要研究
輪速耦合、牽引與真實停止，需建立全向輪驅動與有效接觸模型，驗證側移／旋轉／斜行。手臂不能
用逐幀強設關節位置完成受力試驗。不要直接照其他機器人的教程把 effort 上限設成無限。

抽屜／門接觸主要平台可在此決定。為防同時除錯感知，先仍用已知場景幾何。
控制器凍結不代表可拒絕校準模擬器；致動校準參數與研究控制參數分別記錄。

退出：T0 回歸在 Isaac Sim 通過；標準預抓取安全完成；reason 7／8 重測；有限致動模型
如尚未通過，清楚停留在介面移植層，不能據此宣稱真實煞車或 sim-to-real。
官方文件也將 gain、joint limits 與 collision 一致性列為獨立測試事項。
[Isaac Sim Gain Tuner](https://docs.isaacsim.omniverse.nvidia.com/latest/robot_setup/ext_isaacsim_robot_setup_gain_tuner.html)

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F2-1 | 同視角／同構形跨平台圖 | Gazebo、Isaac Sim 與模型骨架、collision 疊圖；標模型版本與比例尺 |
| F2-2 | 步階與制動對照 | x：s；y：要求／接受／實際速度、停止後位移；相同輸入與初速分面 |
| F2-3 | 凍結 B 跨平台軌跡與誤差 | 同場景 x-y／TCP 路徑；位置、朝向與最小裕度隨時間；再調參組獨立顏色 |
| T2-1 | 平台設定與差異表 | version、step、控制Hz、RTF、gains、effort、慣量、摩擦、接觸、相同/差異/未知 |
| T2-2 | 跨平台配對結果 | n、成功/總數、追蹤 RMSE、停止延遲／距離、碰撞、最小裕度、deadline miss、未校準項 |

## 7. S3：固定底盤，透過接觸完成 1-DOF 操作

**目的：** 證明統一物體介面能帶動被動抽屜與門；讓抓取與接觸問題先有可用答案。

抽屜先從已知滑軌、低阻力、易抓把手做起，再做已知鉸鏈的門。每類至少有短／中／長的可達
開度；固定底盤基線先選整段可行的目標，過長不可達行程另列為 S5 的能力邊界。

先用理想固定抓取連接檢驗旋量路徑與全身／物體運動一致性，再換有有限摩擦與夾持能力的接觸。
兩組都讓物體關節被動運動，不直接把 $q_d$ 寫成目標。理想連接可支持運動學控制主張，但不能
支持抓取成功率、抗滑移或精確接觸力主張。

需實作：接近→對準→閉爪→接觸確認→小位移帶動→正式開啟→保持→釋放→退離。接觸確認不能只讀
`reached_goal`，至少用相對位姿穩定與被動物體跟隨；若有力資料再加入接觸量測。
只對手指／把手等指定接觸配對設允許關係，且綁定狀態，不關閉整個目標櫃體的防撞。

物體幾何隨 $q_d$ 更新，抽屜側板／前板與門板掃掠都在評估中。物體可能回彈／關閉時，安全
恢復先考慮保持或受控卸載，不一律張爪倒退。沒有力感測不等於可宣稱「不過載」；先限於受控
低阻力、速度與已知模型，S8-C 再處理可驗證的力控制。

退出：兩類標準物體各 5/5 通過指定模式的操作與釋放，且每類 5 場景×3 次 pilot 完成；
報告可達失敗、抓取失敗、滑移、過載、碰撞、逾時。接觸模式與量測缺口一併寫在成果標題。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F3-1 | 抽屜／門操作連續畫面 | 4–6 個等間隔或具名事件時刻；標實測 q_d、時間、把手與抓取座標 |
| F3-2 | 開度追蹤 | x：s；y：抽屜 mm、門 ° 分面；目標與容許帶、抓取/釋放事件 |
| F3-3 | 抓取相對位姿與反力 | x：s；y：位置殘差 mm、朝向 °、力 N／力矩 N·m、滑移分面；無力資料不畫零線冒充量測 |
| F3-4 | 機構統一介面驗證 | 實測把手路徑與旋量預測路徑疊圖；旁圖為正規化開度—路徑誤差 |
| T3-1 | 物體／抓取設定 | ID、類型、軸、零位、尺寸、開度、質量/慣量、阻力、T_HG、接觸模式、目標是否直接致動（應否） |
| T3-2 | 固定底盤操作結果 | 類型、模式、n、成功/總數、開度終誤差、時間、抓取殘差、接觸峰值/NA、滑移、各失敗類型 |

## 8. S4：操作底盤位姿選擇與循序整合

**目的：** 導航到的位置能支援整段操作，並確認每段控制的交接。

在把手附近建立 x-y-yaw 候選網格，記候選範圍、解析度與計算預算。對整段開度取樣做 IK、
碰撞、關節／操作度裕度篩檢；通過取樣只代表此解析度下可行，緊貼障礙處加密或做連續檢查。
再以導航路長與操作裕度排序。比較固定人工停放、只檢查初始把手可達、整段可行性三種。

以同導航起點／物體成對測試，先各類 5 起點×3 次；記錄狀態機轉移與唯一命令擁有者。
分別報條件操作成功率與端到端成功率，避免導航失敗被刪掉。

退出：各狀態有成功、逾時與故障路徑，標準端到端任務可重現；底盤位姿策略比較全部保留。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F4-1 | 底盤操作可行域 | x/y：m；依 yaw 分面；顏色：可完成的最大開度或最小裕度；標被選位置 |
| F4-2 | 端到端場景／路徑 | 導航軌跡、停放姿態、TCP 操作路徑與門板掃掠範圍 |
| F4-3 | 狀態時間圖 | x：s；y：任務/命令所有權；正常與失敗案例各一，標 timeout、保持與卸載 |
| T4-1 | 位姿策略比較 | 策略、候選數、篩選耗時、初始可達率、全路徑可行率、完成率、失敗原因 |
| T4-2 | 端到端階段統計 | 階段、進入數、通過數、逾時、失敗、安全中止、耗時 p50/p95；全部起始任務分母 |

## 9. S5：單步全身接觸操作 B

**目的：** 建立值得與 P 比較的單步接觸控制器，測量底盤自由度的實際作用。

將 S3 的被動物體與抓取一致性納入 B；對所有方法使用一致的安全幾何與限制。分兩類場景：
共同可行（F／SEQ／B 都可解）比較效率與接觸；長行程／有限臂展／需側移的場景比較能力範圍。
兩類不可混在同一平均值中宣稱控制品質提升。

初期仍用已知靜態場景與已知阻力。每類物體至少 5 場景×3 次，包含開度增加時接近關節限位
和門板逐步侵占空間。B 的增益、臂形偏好、物體速度限制與接觸設定在調參集合完成後凍結。

退出：至少一個需底盤配合的案例證明安全完成，兩類都有已記錄 B 結果、全部失敗與計算成本；
這才是「單步全身接觸基線」，不是用底盤多動幾秒判定。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F5-1 | F/SEQ/B 操作軌跡 | 相同視角與物體軌跡；底盤路徑、手臂等開度姿態；標不可達/停止位置 |
| F5-2 | 協調與限制隨開度變化 | x：正規化開度；y：底盤 x/y/yaw、各關節、關節裕度、抓取殘差分面 |
| F5-3 | 能力邊界 | x：要求開度／行程；y：完成率；依初始距離或障礙配置分面，保留安全失敗 |
| T5-1 | 單步接觸基線比較 | 方法、場景類、n、完成/總數、時間、開度誤差、最小距離、接觸/NA、底盤/各關節運動量、QP/fallback |

## 10. S6：多步 W-GMPC 的核心研究比較

**目的：** 在 B 已能工作的基礎上，測試多步預測是否改善提前轉位、限位回避與完成能力。

先補主計畫 §5.7 的離散模型、抓取 log 誤差／Jacobian 數值驗證、物體耦合、每步碰撞約束、
終端成本、warm start／不可行處理。確認預測的 $q_d$ 不會與實際被動物體脫離。低階不能跟上
速度時，先有追蹤誤差界或簡化致動模型，不以理想積分聲稱實際預測準確。

場景按研究機制設計，調參前列出幾何範圍：直線易例；開啟後段會接近限位的長抽屜；門板
掃掠要求提前移位；底盤位置受限但仍有全身可行路徑。對難例用離線高預算規劃／幾何搜尋提供
可行性參考，其代價與限制亦記錄，不將單次 IK 成功當成接觸全路徑證明。

比較：B 與 P 同感知、初始狀態、參考、物理模型、安全限制、控制頻率與硬體。給各方法相同
調參預算與同一開發集；先報一致設定，再報各自調好的設定。外部安全層相同，並記錄其修改量。
P 若只依靠外部安全層避障而預測中沒看到障礙，不能主張已驗證預測避障的效果。

時域先試 N={1,5,10,20}，以 Δt=0.05 s 為起始設計即 T={0.05,0.25,0.5,1.0} s。
數值須配合實際任務時間尺度與計算預算，不是既定最佳設定；N=1 要逐項核對與 B 的差別。
另外單獨測控制增量權重；預測時域、取樣間隔、姿態／臂形權重不能同輪一起換。

退出：P 在兩類受控任務有可重現結果，完成 B/P 配對 pilot、時域消融與端到端計時；
「P 勝出」不是進入 S7 的人為條件，無增益與失敗範圍也要正式測試。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F6-1 | 預測提前行動圖 | 在相同幾個開度/時刻畫預測與實際底盤/臂形、障礙；標 B 晚轉位與 P 提前轉位，若無此現象如實呈現 |
| F6-2 | B/P 配對任務與安全曲線 | x：時間或開度（明列）；y：開度誤差、抓取殘差、關節裕度、最小距離、接觸分面 |
| F6-3 | 時域—成效—成本 | x：N 或 T；y：安全完成率、時間、solve/e2e p99、deadline miss 分面；標 50 ms 線 |
| F6-4 | 權重分配消融 | x：固定的 S_b/S_a 候選；y：底盤加速度/反轉次數、手臂速度、任務誤差與接觸；不標成能耗 |
| T6-1 | 主方法公平設定表 | B/P 成本、限制、感知、Δt/N、終端項、調參次數、算力、求解器容許值、fallback、差異理由 |
| T6-2 | 預測效益配對表 | 配置、n、安全成功率差與區間、共同成功時間差、碰撞/限位、抓取/力、修改量、p99、觀測最大與連續逾時 |

## 11. S7：參數凍結、保留物體與穩健性測試

**目的：** 檢查效果能否離開原本調好的那個場景，且知道何時應拒絕或停止。

正式集依 §3.4 執行。先做單因素敏感度，建議起始層級：把手位置偏差 0/5/10/20 mm、朝向
0/1/3/5°、機構軸方向 0/1/3/5°；revolute 軸位置另掃 0/5/10/20 mm。實際層級在看正式
結果前依感測／模型能力凍結。估計誤差只改控制器拿到的模型，真實幾何保持不變；摩擦試驗則
改物理模型並凍結控制器估計，才能分清模型錯誤與場景改變。

延遲按 0/1/2/4 個控制週期增加，另做跨過 stale 門檻的故障測試；需區分「舊時間戳資料」與
「舊資料偽裝新時間戳」，後者不受普通 age watchdog 保護。擾動的 frame、方向、分布和 seed
要記錄，不用只挑單一有利方向。

先各自測單因素，再選少數事前指定組合，不做不可負擔的全因子組合。動態障礙留到相對速度／
感知更新已驗證後，否則本階段限制於靜態環境。跨平台只重跑代表性保留子集並說明選樣規則。

退出：正式資料集、分析版本與全部失敗凍結；結果不足時縮小主張，不回頭調正式集再稱保留測試。

| ID | 圖／表內容 | 軸／欄位及用途 |
|---|---|---|
| F7-1 | 正式主結果圖 | 各物體類型下 B/P 安全成功率與 95% 區間；旁圖配對差值與 n |
| F7-2 | 穩健性曲線／熱圖 | x：單一誤差強度；y：成功率或抓取/開度誤差；少數預先指定雙因素才用熱圖；標停止區 |
| F7-3 | 失敗分類與代表案例 | 各方法失敗比例堆疊（含分母）；配一個停滯/滑移/不可行案例的時序，不能只放成功影片 |
| T7-1 | 資料分割與覆蓋表 | train/dev/test 物體 ID、幾何範圍、場景數、擾動、repeat/seed、hash、是否曾調參 |
| T7-2 | 正式核心數據 | 方法×機構×場景類：n、到達率、安全成功率、配對差與區間、時間/逾時、距離、接觸、限制、計算 |
| T7-3 | 失敗與能力界線 | failure stage/type、次數/分母、首發原因、是否安全停止、恢復結果、參數範圍、限制主張 |

## 12. S8：選配研究分支，各自交付

核心資料未完成時先保留介面。三個分支可擇一，不把「學習、力估計、語意抓取」打包成同一
必要里程碑，也不因視覺信心高就放寬安全限制。

| 分支 | 進入條件與實驗 | 成果圖 | 數據表 |
|---|---|---|---|
| S8-V 視覺／機構估計 | 已有時間同步深度與誤差真值；真值參數→單視角→多視角→探測分別比較；新增相機負載測試 | F8-V：x 軸位姿/軸誤差，y 控制成功率；另畫機構混淆矩陣、信心—錯誤/拒絕率曲線 | T8-V：位置/朝向/軸誤差、n、接受/拒絕/誤接受、端到端成功、延遲與負載 |
| S8-C 接觸順應 | 有可信力真值或外接感測器；先驗證殘差估計，移動底盤引入的動態也需處理；再比較順應開/關 | F8-C：時間—真值/估計力與力矩、抓取誤差；不同模型誤差下峰值接觸與成功率 | T8-C：bias/RMSE、頻寬/延遲、姿態與負載、峰值力/力矩、滑移、成功率、控制參數 |
| S8-L 語意取物 | 開抽屜已穩定且有獨立抓取流程；已知物品位姿 vs 視覺選擇→抓取分開 | F8-L：語意選擇、定位、抓取三段結果與失敗例；物品取出軌跡 | T8-L：目標選對率、條件抓取率、端到端取出率、錯選/不可達/掉落 |

只有 S8-V 通過，才有根據討論視覺來源下的表現；S8-C 未通過，不寫精確無感測器 wrench 或
順應安全；S8-L 完成才擴展至「開抽屜並取指定物品」。

## 13. S9：論文、圖表與可重現交付

每階段至少交一份可重跑設定、原始紀錄、機器可讀摘要、圖表產生程式、簡短結果說明。
圖表依既有 repository 評估管線整合；以下是規劃格式，不表示本輪已建立這些產物：

```text
evaluation/results/phase2/<stage>/<run_id>/
  manifest.json       # 程式、模型、平台、參數、窗口、pair/seed
  raw/                # bag／原始 JSON／必要的物理接觸與真值
  metrics.json        # 單趟摘要；NA 與原因明列
  events.csv          # 狀態轉換與故障時間
evaluation/results/phase2/reports/<report_id>/
  aggregate.csv       # 每 run 一列，保留失敗
  figures/            # 每圖 PDF + PNG + 對應繪圖數據 CSV
  tables/             # CSV + 論文用 Markdown/LaTeX
  report.md           # 結論、限制、指令、分析版本、run ID
```

圖檔建議 `F6-3_horizon_tradeoff.pdf` 等固定 ID；規劃階段不填假數據。線圖和研究示意圖用可
編輯／可重現繪圖工具輸出；畫面截圖、影片作輔助。論文圖標清單位、容許線、樣本數、窗口、
感知來源、接觸模式、模型版本與不確定度。曲線平均不得把成功/失敗時間扭成同一路徑；若對齊
正規化開度，揭露未完成者在哪裡停止，不外插填滿。

小樣本圖保留個別點；同類方法固定配色與線型。每個主要比較至少保留成功、中位表現與失敗
案例，選例規則先訂，不只選最漂亮的一趟。raw hash、分析版號與圖表數字能互相追溯。

| ID | 內容 | 交付要求 |
|---|---|---|
| F9-1 | 論文總架構／訊號圖 | 觀測→狀態與幾何→B/P→安全檢查→致動→回授；標物體被動運動、故障與接觸模式 |
| T9-1 | 主張—證據索引 | 每項主張、適用條件、比較方法、圖表 ID、run ID、分析版、限制；未支持主張刪除或移未來工作 |

論文正文可從完整成果庫精選約 10–12 組圖、5–7 張表，其餘放附錄：模型與架構、A/B 診斷、
故障停止、兩類接觸操作、位姿可行域、B/P 提前動作、時域代價、正式成功率與失敗邊界。
每階段圖表都要先產出，正文是否全部收錄留到結果收斂再決定。

## 14. 下一個實驗階段與待定條件

近期按以下順序做即可：S1 μ 掃描 → 必要時另輪臂形／任務尺度處理 → 命令回授與安全修補
回歸 → 凍結 B 預抓取參考 → S2 平台對照 → S3 抓取與被動物體操作。現在的工作會變成
F1-1～5、T1-1～3，之後自然支援 B/P 比較與跨平台診斷，不會只是一次性除錯。

仍需在實際排程／接觸試驗前確認：論文截止日與每週投入、Isaac Sim 可用 GPU/記憶體與版本、
是否預計做實機、夾爪實際配置、力感測／力矩回授可用性、接觸阻力與負載規格。這些不阻礙
目前的文件與 S1 規劃，但會影響 S2/S3/S8 的範圍。本輪未為它們假設硬體已具備。

## 15. 審查來源與技術核對

本地進度與程式是「目前做到了什麼」的主要依據：

- [第二階段進度紀錄](第二階段進度紀錄.md)：模型、全連桿幾何、故障注入與 §10.17 的現況。
- [有障礙全身實驗紀錄](evaluation/results/obstacle_wholebody_20260909.md)：A/B 及 reason 7/8。
- [全身預抓取程式](evaluation/wholebody_pregrasp.py)：μ 臂形項、QP 成本、v_prev、成功門檻。
- [停滯求解器比較](evaluation/compare_stall_solvers.py)、[μ 掃描](evaluation/sweep_mu.py)：同約束對照與現有試驗流程。
- [全身安全濾波器](src/ammr_wholebody_mpc/ammr_wholebody_mpc/wholebody_safety_filter.py)：速度凍結停距、幾何列與限制集合。

技術邊界另對照原始／官方資料（查閱 2026-09-09；本輪未做完整文獻新穎性審查）：

- [Modern Robotics：Body Jacobian](https://modernrobotics.northwestern.edu/nu-gm-book-resource/5-1-2-body-jacobian/)：body／space twist 與 Adjoint 轉換；主計畫據此要求座標、排列與參考點一起核對。
- [Ames 等：Control Barrier Function Based Quadratic Programs for Safety Critical Systems](https://arxiv.org/abs/1609.06408)：CBF-QP 的模型／可行性框架；每步線性殘差不自動構成整套系統安全證明。
- [Isaac Sim：Tuning Joint Drive Gains](https://docs.isaacsim.omniverse.nvidia.com/5.1.0/robot_setup_tutorials/joint_tuning.html)：致動增益、速度追蹤與力限制需按機器人設定；這是版本示例，非指定本專案使用版本。
- [Isaac Sim：Gain Tuner](https://docs.isaacsim.omniverse.nvidia.com/latest/robot_setup/ext_isaacsim_robot_setup_gain_tuner.html)：關節限制、致動與碰撞的一致性驗證；正式實驗應鎖定對應版本文件。
