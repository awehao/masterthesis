# 第二階段進度紀錄

> 對應 `第二階段計畫.md`。本檔只記錄**已執行的工作與實測結果**，不含規劃。
> 每項結果都附可重跑的指令。最後更新：2026-09-02

---

## 摘要

Month 1 的模型層工作完成。全身 9-DOF 運動學模組建立並通過**三個獨立參考**的驗證
（手寫 FK、KDL、gz 物理引擎），自碰撞檢查完成，URDF 已與實機幾何對齊。

§3.3 四項驗收：**全部完成**。

| 驗收項 | 狀態 | 結果 |
|---|---|---|
| URDF FK 與控制程式 FK 一致 | ✅ | 最大誤差 $5.55\times10^{-16}$（n=200） |
| 自碰撞檢查 | ✅ | 合法關節盒內 0/300，但需訂最小餘裕 |
| 關節方向與零位一致 | ✅ | 逐關節 TF vs FK，最大誤差 $6.8\times10^{-11}$ m |
| 底盤移動時末端位姿一致 | ✅ | 四種運動，最大誤差 $8.2\times10^{-7}$ m |
| （額外）gz 真值連桿位姿 vs 模型 | ✅ | link1／3／6，最大誤差 $4.0\times10^{-7}$ m |

---

## 一、模型層：與實機幾何對齊

### 1.1 採用參考模型的掛載點

`base_link → link_base` 設為 `xyz="0.11 0 0.28"`，與
`my_mobile_manipulator/urdf/fake_mobile_manipulator_modified.urdf:168` 逐字相同。

`chassis_frame.stl` 頂面實測為 $z=0.280$ m，與手臂基座高度一致 —— 手臂直接鎖在底盤
頂板上，**沒有額外支架**，因此不需要建模支撐結構，也不新增 LiDAR 遮蔽。

先前自行設計的置中桅杆（`arm_mount.xacro`）已移除。原設計把四根支柱藏在 `scan_relay`
已遮蔽的 45/135/225/315° 扇區；盤子前移 0.11 m 後這個條件失效（遮蔽扇區與盤子底面
不再相交，解判別式為負），任何支柱都會落在未遮蔽方位，其中兩根正對前方 ±20°。

### 1.2 關節限位

改為直接呼叫 `lite6_urdf` 並逐關節傳值，因為 `lite6_robot` 只提供布林 `limited`。

| 關節 | 實機安全限位 | 說明 |
|---|---|---|
| joint1／4／6 | $-168.20^\circ \sim +168.20^\circ$ | |
| joint2 | $-140.00^\circ \sim +140.00^\circ$ | |
| joint3 | $-3.50^\circ \sim +168.20^\circ$ | 只縮上限 |
| joint5 | $-114.00^\circ \sim +114.00^\circ$ | |

與參考檔逐位元相符。`limited="true"` 只能到 $\pm168.2^\circ$（$=\pm0.99\pi$），而那正是
參考檔中**被註解掉**的那一行；實際生效的值再緊 $0.174533$ rad（10°），是手動加的邊界。

**尚未對齊**：速度六軸皆 3.14 rad/s（手冊值，顯然未逐關節實測）、加速度、600 g 負載。

### 1.3 夾爪與腕部相機

- 原廠夾爪已接入 gz 並實測可動：指令 0.0089 → `reached_goal: true`，兩指對稱 ±0.009
  （mimic 有效）。控制器改用 `parallel_gripper_action_controller`。
- 腕部 D435i 已加入，9 個 `camera_*` frame 與參考檔相符。照參考的
  `use_gazebo_camera=false`，**只有幾何與 TF，未掛 gz 感測器**，因此目前沒有深度資料流。
- `d435i_urdf` 自行宣告 `link_eef`，故 `link_eef` 為條件式宣告，避免重複 link。

### 1.4 新增檔案

| 檔案 | 用途 |
|---|---|
| `urdf/omni_bot_wholebody.urdf.xacro` | 9-DOF 運動學模型（虛擬平面底盤關節） |
| `urdf/chassis_body.xacro` | 底盤本體＋輪子，兩個模型共用 |
| `urdf/arm_detect_points.xacro` | 12 個連桿偵測座標系，兩個模型共用 |
| `launch/omni_bot_wholebody.launch.py` | 檢視／驅動全身模型（Foxglove） |
| `ammr_wholebody_mpc/arm_detection_points.py` | 12 點最近障礙物向量（ROS 1 移植） |
| `ammr_wholebody_mpc/wholebody_kinematics.py` | 控制程式的 FK／Jacobian |

`chassis_body.xacro` 的抽取以展開結果 diff 證明**逐位元不變**。
`use_arm=false` 展開與 8/13 基準 **0 行差異**，第一階段 benchmark 未受影響。

---

## 二、全身運動學模組

`ammr_wholebody_mpc/wholebody_kinematics.py`

廣義座標 $q=[x,y,\theta,j_1..j_6] \in SE(2)\times\mathbb R^6$，對應計畫 §4.1。
底盤三個自由度是**鏈上的真實 URDF 關節**，因此單一 Jacobian 同時涵蓋底盤與手臂。

幾何全部在建構時從 URDF 讀取，**沒有任何硬編偏移** —— 改掛載點或換夾爪後，控制器
不會默默對著一台不存在的機器人規劃。

```bash
xacro src/my_omnibot_description/urdf/omni_bot_wholebody.urdf.xacro > /tmp/wb.urdf
python3 evaluation/verify_wholebody_kinematics.py /tmp/wb.urdf --n 200
```

---

## 三、§3.3 驗收結果

### 3.1 FK 與 Jacobian（已完成）

```
FK vs 獨立參考實作        n=200   最大誤差 5.551e-16   ✓
Jacobian vs 數值微分       n=200   最大誤差 3.645e-10   ✓
rank 中位 6/6   最小 6   冗餘 3 DOF   降秩組態 0/200
底盤三欄 = 平面基底                              ✓
```

**用兩個參考而非一個**：數值微分只能證明 Jacobian 與 FK 彼此一致；若 FK 本身讀錯
URDF，兩者會一起錯而測試照樣通過。因此另寫一份**不共用任何程式路徑**的 FK（RPY 以
$R_zR_yR_x$ 明確相乘、旋轉用軸角分量公式而非 Rodrigues）。兩者差 $5.55\times10^{-16}$，
證明 URDF 的解讀本身正確。

**冗餘 3 DOF 在 200 組隨機組態中全部成立，無降秩** —— W-GMPC 的零空間穩定存在。

順帶確認：`base_theta` 限位為 $\pm3600^\circ$。那是為了讓 revolute 關節不繞行而設的
技術值，**不是實機限位**，論文不可如此陳述。

### 3.2 自碰撞（已完成）

```bash
python3 evaluation/verify_self_collision.py /tmp/wb.urdf --n 300
```

```
12 個碰撞幾何   53 對非相鄰組合
合法關節盒隨機取樣 300 組   自碰撞 0/300   全域最小 +0.0003 m
```

必須把兩類分開，否則統計量會被機構本身主導：

**結構性接近**（間距幾乎不隨姿態變化）

| 配對 | 中位 | std |
|---|---|---|
| link4 – link6 | +0.0101 m | 0.0019 |
| link5 – link_eef | +0.0207 m | 0.0000 |
| link_base – link2 | +0.0461 m | 0.0010 |

Lite 6 腕部三連桿同心套疊，`link4–link6` 在任何姿態都差 10 mm。這是機構設計，不是風險。

**姿態相依**（規劃時真正要檢查的）

| 配對 | 最小 | 中位 | std |
|---|---|---|---|
| link_base – link5 | +0.0003 m | +0.2569 | 0.1372 |
| link2 – uflite_finger1 | +0.0006 m | +0.2542 | 0.0619 |
| link1 – link4 | +0.0007 m | +0.1901 | 0.0667 |

中位數 19–34 cm，**但最小值全部掉到 0.3–0.9 mm**。大部分姿態寬鬆，少數擦邊。

**具名姿態**

| 姿態 | 最小間距 | 配對 |
|---|---|---|
| all-zeros | +0.0089 m | link_base – link5 |
| tuck（行駛姿態） | **+0.0011 m** | link1 – link_eef |

**行駛姿態的自碰撞餘裕只有 1.1 mm。** 這比預期窄得多，加入夾爪後尤其如此。

#### 結論與建議

規劃器不能只檢查關節限位。0/300 沒有真的穿透，但 sub-millimetre 的擦邊代表：
IK 解算或追蹤誤差只要 1 mm 就會真的碰到。

建議 §6.1 的「無自碰撞」驗收改為**明確最小餘裕 ≥ 5 mm**（姿態相依最小值的十倍以上，
且遠小於中位數 250 mm，不會過度限制工作空間）。行駛姿態的 1.1 mm 應重新選定。

#### 工具設計上的三次誤判

三次都是**幾何正確、語意無意義**的距離，記錄下來避免重犯：

1. 第一次跑出「100% 自碰撞」—— 夾爪兩指在 `finger_joint=0`（閉合端）本來就相接。
2. 第二次 `link6–uflite_gripper_link` 固定 0.0011 —— 兩者之間全是 fixed joint，同一剛體。
3. 第三次 `link4–link6` 主導 87/120 —— 結構性接近，需以變異數分離。

工具現在自動排除：相鄰連桿、同剛體群（union-find）、設計上接觸的配對，並以
$\mathrm{std} < 0.002$ m 判定結構性。

### 3.3 模擬器中的驗證（已完成）

```bash
ros2 launch my_omnibot_description omni_bot_arm.launch.py \
    use_arm:=true add_gripper:=true gui:=false foxglove:=false
python3 evaluation/verify_model_in_sim.py
```

「方向一致」不以目視認定 —— 軸向取負或底盤變換組合順序錯誤，在畫面上都看起來完全
正常。三項檢查都是數值的，而且**參考來源各不相同**。

**[A] 逐關節方向與零位** —— 逐一運動六關節，把**回授的**關節值代入模型 FK，與
`robot_state_publisher`（KDL）發布的 TF 比對。

| 關節 | 指令增量 | 回授 | 位置誤差 |
|---|---|---|---|
| tuck | — | — | $3.1\times10^{-13}$ m |
| joint1 | $+0.35$ | $+0.3500$ | $4.0\times10^{-12}$ m |
| joint2 | $+0.35$ | $+0.2680$ | $2.8\times10^{-11}$ m |
| joint3 | $+0.35$ | $+0.4390$ | $3.4\times10^{-11}$ m |
| joint4 | $+0.35$ | $+0.3500$ | $1.3\times10^{-11}$ m |
| joint5 | $-0.35$ | $+1.3290$ | $6.8\times10^{-11}$ m |
| joint6 | $+0.35$ | $+0.3500$ | $2.1\times10^{-11}$ m |

回授值為 tuck 起始值加上增量，六軸皆精確到達。

**[B] 底盤運動時的末端位姿** —— 手臂固定於 tuck，底盤前進／側移／旋轉／斜向＋旋轉，
檢查 $T_{\text{world}\to\text{EE}}=T_{\text{world}\to\text{base}}\circ T_{\text{base}\to\text{EE}}$。
四種運動最大誤差 $8.2\times10^{-7}$ m。

**[C] gz 真值連桿位姿 vs 模型** —— A 與 B 都以 TF 為參考，而 TF 由
`robot_state_publisher` 從**同一份 URDF** 算出。那是有效的獨立實作檢查（KDL 對我們的
實作），能抓出 URDF 解讀錯誤，但**抓不出模擬器裡的機器人與 URDF 不符** —— 兩邊都不會
知道。因此另外讀 gz 的 `/world/<w>/pose/info` 真值連桿位姿比對：

| 連桿 | gz 真值（model 相對） | 位置誤差 |
|---|---|---|
| link1 | $(+0.1100,\ +0.0000,\ +0.5735)$ | $3.4\times10^{-7}$ m |
| link3 | $(+0.0936,\ +0.0000,\ +0.7730)$ | $2.3\times10^{-7}$ m |
| link6 | $(+0.1557,\ -0.0000,\ +0.5596)$ | $4.0\times10^{-7}$ m |

gz 會把 fixed joint 連桿合併（例如 `link6_fixed_joint_lump__link_eef_visual_1`），
因此只能比對真正的動態連桿；`link_tcp` 在 gz 中不獨立存在。

**三個參考來源**：獨立手寫 FK（§3.1）、KDL（A、B）、gz 物理引擎（C）。三者一致，
模型才算可用。

---

## 四、幾何限制（影響後續設計）

### 4.1 收折餘裕 3.7 cm

以實際碰撞網格量測（非關節原點）：

| 項目 | 水平半徑 |
|---|---|
| 手臂＋夾爪收折最大包絡 | 0.263 m |
| 第一階段底盤碰撞圓柱 | 0.300 m |
| **餘裕** | **+0.037 m** |

因掛載點前移 0.11 m。**手臂外伸不到 4 cm 就超出 2D 安全層的認知範圍**，故連桿層安全
是階段 D 的前置條件，不是附加功能。

### 4.2 2D 感知與手臂工作高度不重疊

統一以 `base_link` 表示（`base_footprint` 低 0.05 m）：

```
2D LiDAR 掃描平面   z ≈ 0.2275 m
手臂連桿（收折）    z = 0.280 – 0.772 m
場景障礙物高度      至 1.0 m
```

雷射看不到手臂會撞到的東西。腕部 D435i 尚未掛感測器，因此目前**沒有任何 3D 距離
來源**，Month 1 必須先決定走已知模型／gz 真值或啟用深度感知。

### 4.3 `arm_detection_points` 已可用但無消費端

12 個偵測點已發布指向最近障礙物表面的向量（$d_i$ 與 $n_i$ 可直接取得），全身 Jacobian
已驗證，故 §8.2 的

$$n_i^\top J_{p_i}(q)\,\nu \le \alpha_i\!\left(d_i - d_{\mathrm{stop},i}\right)$$

不是從零開始。但目前障礙物來源是**已知模型／模擬資訊，不等同真實 3D 感知**。

---

## 五、動態 LiDAR Self-filter（第 1 項，已完成）

`ammr_wholebody_mpc/arm_scan_self_filter.py`

### 5.1 問題

掃描面在 `base_link` $z=0.2152$。手臂多數時候在其上方，但合法關節盒中有 **9.5%** 的
組態會伸到 $z=0.070$ —— 進入掃描面。此時雷射看到自己的手臂，下游全部把它當障礙物。

`scan_relay` 處理不了：它遮蔽的是**固定方位**（底盤四根支柱的 45／135／225／315°），
而手臂會動，沒有任何靜態扇區能涵蓋。

### 5.2 驗證結果

以三組真正進入掃描面的姿態測試（由 4000 組隨機取樣中挑出），底盤先開到牆面
0.503 m 處，使真實回波落在手臂附近：

| 姿態 | `/scan_base` | `/scan` | 移除 | 最近自體回波 | 最近保留 | 誤刪 |
|---|---|---|---|---|---|---|
| tuck | 233 | 233 | 0 | — | 0.503 m | — |
| low0 | 233 | 214 | **19** | 0.213 m | 0.503 m | **0** |
| low1 | 233 | 218 | **15** | 0.296 m | 0.503 m | **0** |
| low2 | 232 | 191 | **41** | **0.120 m** | 0.503 m | **0** |

- 三種低姿態皆成功移除 **15–41 個**手臂自體回波。
- 最近自體回波 **0.120 m**，確實落在 Raw-scan Shield 的 `emergency_d = 0.15 m`
  緊急距離之內 —— 不濾除就會對自己的夾爪緊急煞車。
- **外部牆面誤刪為 0**，三個姿態皆然。

### 5.3 為何用實際網格截面而非 capsule

先擬合過 capsule：`link_base` 需要半徑 **0.123 m** 才能包住實際水平範圍僅
**0.094 m** 的連桿，**過度覆蓋約 30%**。

過度覆蓋是危險的方向 —— 遮罩比手臂寬就會刪掉真實回波，而離手肘 3 cm 的障礙物正是
最需要看見的那一個。因此改為逐點測試：只有落在手臂**實際截面** `tolerance = 0.03 m`
以內的回波才移除。tolerance 僅涵蓋測距雜訊與網格取樣，調大它就是真障礙物開始消失的
起點。

### 5.4 濾波後的區域是觀測盲區，不是已知自由空間

必須區分「濾掉」與「遮住」：

| 姿態 | 被手臂遮擋而變短 | 完全消失 | 未受影響 | 受影響扇區 | 濾波器誤刪 |
|---|---|---|---|---|---|
| low0 | 19 | 0 | 213 | 18.1° | 0 |
| low1 | 15 | 0 | 217 | 14.0° | 0 |
| low2 | 41 | 0 | 191 | 40.1° | 0 |

那些方位在 `/scan_base` 中量到的距離**本來就比基準短** —— 手臂擋在牆前面。移除自體
回波後，該方位變成**無資料**，而不是恢復成牆的距離。

物理上這是正確的：**看不穿自己的手臂**。但代表協調同動時會出現 **14–40° 的移動盲區**，
且盲區跟著手臂走。這是第一階段沒有的失效模式。

> **這 14–40° 必須標記為「觀測盲區」，不得因自體回波被移除就視為自由空間。**
> 第 2 項的距離介面與第 3 項的安全約束都必須遵守這一點。

### 5.5 佈署位置與診斷

以 launch remap 插入，消費端完全不變：

```
/scan_raw → scan_relay → /scan_base → arm_scan_self_filter → /scan
```

`use_arm:=false` 時濾波器不啟動，`scan_relay` 照舊直接發布 `/scan`，第一階段鏈路
逐位元不變。

`/scan_self_filter/diag`（7 欄）可監測**過度遮罩、關節資料過期與移除結果**：

| 欄 | 內容 |
|---|---|
| 0 | cycle |
| 1 | 掃描面帶內的手臂點數 |
| 2 | 移除的回波數 |
| 3 | 最近**保留**回波距離 |
| 4 | 最近**移除**回波距離 |
| 5 | 關節資料過期旗標 |
| 6 | 總回波數 |

第 3、4 欄若交叉（移除的比保留的遠），代表 tolerance 過大。關節資料過期時濾波器
**直接放行不過濾** —— 未過濾的掃描過度保守，錯誤過濾的掃描是盲的。

---

## 六、連桿 3D 距離介面（第 2 項，已完成）

`ammr_wholebody_mpc/arm_link_distance.py`

### 6.1 介面規格

`/arm_link_distance/points`，`sensor_msgs/PointCloud2`，12 點 × 10 欄：

| 欄 | 內容 |
|---|---|
| `x y z` | $p_i$，連桿偵測點位置 |
| `nx ny nz` | $n_i$，單位向量，由 $p_i$ 指向最近表面 |
| `d` | $d_i$，到最近**表面**的距離 |
| `status` | 0 = OK、2 = STALE、3 = NODATA |
| `age` | 該筆障礙物位姿的資料年齡（$t_i$） |
| `occluded` | 0／1，該方向是否為觀測盲區 |

選 `PointCloud2` 而非兩個平行 topic：單一原子訊息，帶 header（frame＋stamp），
不會像兩個 topic 那樣失去同步，Foxglove 也能直接顯示。

報告座標系預設 **`odom`**，不是 `base_link`：底盤一移動 `base_link` 就非慣性，
約束會需要額外項，而那是沒人會記得加的。下游 Jacobian 必須用同一座標系。

`/arm_link_distance/diag`（7 欄）：點數、OK、occluded、stale、nodata、最差年齡、最小距離。

### 6.2 `status` 與 `occluded` 必須分開

第一版把遮蔽折進 `status`，結果手臂擋住雷射時 12 個點全被標成 UNKNOWN，
**連有效的距離一起丟掉**。

那是把兩件不同的事混為一談：

- **`status` 是關於資料本身** —— $d_i$ 能不能用。
- **`occluded` 是關於可觀測性** —— 該方向現在看不見，可能藏著沒建模的東西。

本階段的距離來源是**已知場景模型**，而模型不需要視線：一個已建模的箱子，不論手臂
擋不擋得住雷射，它的距離都是有效的。分開之後，第 3 項可正常使用 $d_i$，同時額外
拒絕把盲區方向當成淨空。

### 6.3 不安全的預設也已修正

原本「**從未收到**遮蔽資料」等同於「沒有遮蔽」，等於在 self-filter 未啟動時默默假設
淨空。改為 `require_occlusion_feed`（預設 `true`）：沒收到就是盲區。

### 6.4 驗收結果

```bash
python3 evaluation/verify_link_distance.py
```

**[1][2] 距離、法向量、座標系與時間戳** —— 與獨立自 world SDF 重算的場景幾何比對，
$p_i$ 與 TF 比對（12 點，45 個靜態障礙物）：

| 量 | 最大誤差 |
|---|---|
| $\Delta d$ | $6.0\times10^{-8}$ m |
| $\Delta n$ | $3.1\times10^{-8}$ |
| $\Delta p$ vs TF | $4.8\times10^{-8}$ m |

**[3] 盲區標記**

| 姿態 | 遮蔽 bearing | status OK | occluded | min_d |
|---|---|---|---|---|
| tuck | 0 | 12 | 0 | 1.286 m |
| low0 | 19 | 12 | 0 | 1.291 m |
| low1 | 15 | 12 | 0 | 1.010 m |
| low2 | 41 | 12 | **12** | 1.218 m |

**[4] 資料逾時可辨識**

```
遮蔽資料新鮮:  status OK=12  occluded=0
資料停止:      status OK=12  occluded=12   ← 逾時全部轉為盲區
恢復:          status OK=12  occluded=0
```

### 6.5 距離來源的性質

目前為 **gz 真值／已知場景模型**（`random_room.sdf` 的 45 個靜態障礙物，位姿由 SDF
直接給定，不需 pose topic，故永不 stale）。

這是**建模輸入，不是感知**。它存在的目的是驗證介面與約束本身；在接上經過驗證的深度
感知之前，不得據此宣稱任何「未知 3D 環境」能力。

---

## 七、連桿速度安全約束（第 3 項，已完成）

> **狀態：連桿速度安全約束已完成離線核心、ROS 節點及模擬器在環驗證。**
> 單關節、六關節與底盤 3-DOF 皆符合約束，`STALE` 命令可歸零，距離資料失效時可降速，
> 測試過程無碰撞。
>
> **已知限制：端到端延遲 p95 最高 52.2 ms，略高於 50 ms 控制週期。**
> 進入高速全身協調運動前仍須改善，或納入停止距離計算。

`ammr_wholebody_mpc/wholebody_safety_filter.py`（離線核心）

### 7.1 約束形式

對每個偵測點：

$$n_i^\top J_{p_i}(q)\,\nu \;\le\; \alpha_i\left(d_i - d_{\mathrm{stop},i}\right)$$

$$d_{\mathrm{stop},i}=d_0+v_{\mathrm{app},i}\tau
+\frac{v_{\mathrm{app},i}^{2}}{2a_{\mathrm{brake}}}+\varepsilon$$

該列**只**限制單一點朝單一表面的接近速度。切向與遠離運動的
$n^\top J\nu \le 0$，任何非負右手邊都自動滿足 —— 會擋住它們的濾波器是壞的。

$d_{\mathrm{stop}}$ 含速度項，故約束對 $\nu$ 非線性。與第一階段相同，由**輸入指令**
求值一次後固定，每週期解線性可行性問題；殘餘違反**事後量測**而非假設為零。

同時納入：關節速度盒、關節位置限位（單步內不得越限）。

### 7.2 降級規則

| 情況 | 處理 |
|---|---|
| `status = OK` | 直接使用 $d_i$ |
| `status = STALE` | $d_i$ 依資料年齡與障礙物可能移動速度**縮減**，並對整體指令限速 |
| `status = NODATA` | 該點無距離可約束，只能全域限速 |
| `occluded = 1` | **保留**已知模型的約束列（模型不需視線），**另加**朝盲區運動的更緊上限 |

### 7.3 離線驗收結果

```bash
python3 evaluation/verify_link_velocity_constraint.py <expanded.urdf>
```

| # | 檢查 | 結果 |
|---|---|---|
| 1 | 點速度：解析 vs FK 有限差分 | 最大誤差 $1.41\times10^{-10}$ m/s ✓ |
| 2 | 接近受限 | 0.2000 → 0.1400，正好貼上限 ✓ |
| 3 | 切向不受阻 | 輸出逐位元等於輸入 ✓ |
| 4 | 遠離不受阻 | 輸出逐位元等於輸入 ✓ |
| 5 | $d<d_{\mathrm{stop}}$ 退離 | $d=0.02$，輸出 $-0.2200$ m/s（主動退離）✓ |
| 6 | STALE／NODATA 降級 | 基座 0.25 → 0.05 m/s；OK 時不降級 ✓ |
| 7 | `occluded=1` | 37 → 38 列，接近 0.14 → 0.03，未丟棄有效距離 ✓ |
| 8 | 關節限位＋屏障同時成立 | joint2 停在 +140.00°，接近仍受限 ✓ |

第 3、4 項是關鍵的**負面測試** —— 一個把所有運動都擋掉的濾波器同樣會通過「接近受限」。

### 7.4 殘差、執行時間與降級率

```
執行   中位 2.26 ms   p95 3.67 ms   最壞 4.90 ms    （20 Hz 預算 50 ms，佔 10%）
殘差   中位 2.6e-18   p95 1.4e-05   最大 5.9e-04 m/s
未解決 0/300
```

固定迭代**不保證**零殘差（投影到一列可能重新破壞另一列），因此量測而非假設。

降級率依資料難度分解，確認並非濾波器過度保守：

| 測試資料 | 降級率 | 未解決 |
|---|---|---|
| 含穿透（0.03–1.2 m） | 20.0% | 0% |
| 接近但未穿透（0.15–1.2 m） | 1.5% | 0% |
| 一般距離（0.40–1.2 m） | **0.0%** | 0% |

12 點同時隨機方向、距離低至 3 cm 時，多列要求朝互相對立的方向退離，**完整約束集本身無解**。

### 7.5 過程中修正

驗收腳本原本顯示的「上限」用的是零速 $d_{\mathrm{stop}}=d_0+\varepsilon$，
在 0.20 m/s 下應為 0.130 而非 0.080，上限 0.14 而非 0.24。檢查本身仍通過
（$0.14\le0.24$），但**報表會讓約束看起來比實際寬鬆**，已修正為顯示實際右手邊。

### 7.6 尚待完成的在環驗證

1. 包成 ROS 節點，接收原始 9-DOF 速度命令與 `/arm_link_distance/points`。
2. 單關節在環：接近、切向、遠離、穿透退離。
3. 六關節在環：多點同時約束、關節限位、自碰撞。
4. 加入底盤 3-DOF：全身 Jacobian、座標系與輪速限制。
5. `STALE`／`NODATA`／`occluded` 及節點停止時的 fail-safe。
6. 記錄端到端延遲、控制週期、最大殘差、降級率、最小間距與是否碰撞。

### 7.7 ROS 節點與在環驗收（已完成）

`ammr_wholebody_mpc/wholebody_safety_node.py`

```
/wholebody_safety/cmd_in    Float64MultiArray, 9   原始 [vx vy wz q̇1..q̇6]
/arm_link_distance/points   PointCloud2            p_i, n_i, d_i, status, occluded
/joint_states               JointState             六個手臂關節
TF report_frame → base_link                        q 的底盤部分
   ↓
/wholebody_safety/cmd_out   Float64MultiArray, 9   濾波後
/wholebody_safety/diag      Float32MultiArray, 15  本週期發生什麼
```

節點必須明確指定 `wholebody_urdf` 參數。`/robot_description` 是**模擬器**的機器人，
其底盤為浮動剛體、沒有 `base_x/base_y/base_theta` 關節，無法建出 9-DOF Jacobian；
全身模型才把那三個宣告為真實關節。

所有缺資料的情況一律朝**停止**失效：無指令／指令逾時 → 輸出零；距離資料逾時 →
全部視為 `NODATA`（由濾波器降級為限速）；關節資料逾時 → 輸出零；TF 缺失 → 底盤
自由度歸零、手臂仍受濾波。**不過濾直接放行**會讓安全層在輸入壞掉的當下悄悄消失。

```bash
python3 evaluation/verify_safety_in_loop.py
```

| 階段 | 檢查 | 結果 |
|---|---|---|
| 2 | 單關節：接近／遠離 | 約束未作用時輸出＝輸入 ✓ |
| 3 | 六關節：多列同時、關節限位 | 48 列，關節全在限位內 ✓ |
| 4 | 底盤 3-DOF：全身 Jacobian、輪速限制 | 基座速度 ≤ 0.2775 m/s ✓ |
| 5 | `STALE` 指令 | 輸出歸零 ✓ |
| 5 | 距離來源停止 | 12 點轉 `NODATA`，限速 0.050 m/s ✓ |
| 6 | 碰撞 | 最小間距皆 > 0 ✓ |

### 7.8 端到端量測

| 項目 | 六關節 | 底盤 3-DOF |
|---|---|---|
| 端到端延遲 中位 | 16.5 ms | 32.9 ms |
| 端到端延遲 p95 | 39.5 ms | **52.2 ms** |
| 節點自身耗時 p95 | 3.52 ms | 3.99 ms |
| 最差殘差 | $+6.9\times10^{-18}$ | $+1.4\times10^{-17}$ |
| 降級率 | 0.0% | 0.0% |
| 最小間距 | 0.112 m | 0.086 m |

**全測試最差殘差 $+1.4\times10^{-17}$**（浮點零，視為滿足）。

殘差定義為**左式減右式**，$\le 0$ 即滿足，因此「最差」是**最接近零**的值，不是最負的。
階段 3 曾出現 $-1.39\times10^{-1}$ —— 那是**安全餘裕**，不是最大殘差；把它列為
「殘差最大」會讓讀者以為約束幾乎被違反，實際上它是被滿足得最寬鬆的一次。

節點自身只佔控制週期 8%，延遲主要來自 `/arm_link_distance` 的 30 Hz 發布週期與 DDS 傳遞。

### 7.9 過程中修正（三項，兩項是測試錯誤）

**節點載錯模型** —— 從 `/robot_description` 載入 gz 的浮動基座模型，缺三個虛擬關節。
改為明確指定全身模型檔。

**兩個節點搶同一組 topic** —— `kill` 只終止 `ros2 run` 外殼，python 子行程仍存活。
結果 `cmd_out` 來自新節點（正確）、`diag` 來自舊節點（`reason=no-kinematics`），
診斷完全誤導。**今天第三次踩到同類問題**（另兩次為 `robot_state_publisher` 與
`pgrep` 自我比對）。

**測試未持續送指令** —— 節點 `max_cmd_age = 0.25 s`，而測試送一次就等 0.6 s，
讀到的是**逾時歸零**的輸出；階段 3 也在指令停止後才讀 diag。
**節點行為自始正確** —— 停止送指令就歸零正是 fail-safe 應有的動作；是測試在量逾時
機制而非濾波器。已改為串流指令，並只在 `reason = ok` 的週期取樣診斷。

### 7.10 測試規範（因上述問題新增）

驗收腳本啟動時會檢查 `cmd_out`、`diag`、`points` 三個 topic **各只有一個 publisher**，
否則直接拒絕測量並報錯。清理節點時必須確認 python 子行程已終止，而非只殺 `ros2 run`
外殼，否則診斷資料會跨執行污染。

---

## 八、實機限制集合（第 4 項，進行中）

### 8.1 已完成：靜態重力力矩模型

`ammr_wholebody_mpc/arm_payload_limits.py`

$$\tau(q)=-\sum_j J_{v,\mathrm{com}_j}(q)^\top m_j\,g
\;-\; J_{v,\mathrm{tcp}}(q)^\top m_{\mathrm{payload}}\,g$$

Jacobian 取在各連桿的**質心**而非原點 —— 兩者在 link2 上相差達 18 cm，用原點會低估
承載最重的那幾個關節。

由手臂關節承載的連桿共 9 個（link1–link6、夾爪本體與兩指），總質量 **6.205 kg**。

力矩對負載是**仿射**的，故拆成 $\tau_0(q)+m\,\tau_{\mathrm{unit}}(q)$ 後，
任何負載問題都變成算術；原本的二分法每個取樣要 40 次 Jacobian，整個工作空間掃描跑不完。

### 8.2 驗收結果

```bash
python3 evaluation/verify_payload_limits.py <expanded.urdf>
```

| # | 檢查 | 結果 |
|---|---|---|
| 1 | 力矩 vs 位能數值微分 $dV/dq$ | 最大誤差 $1.13\times10^{-8}$ N·m ✓ |
| 2 | 水平伸至最遠（TCP 0.588 m） | joint2 = 14.99 N·m，與 $mgr/2$ 同量級 ✓ |
| 3 | 負載單調性 | 利用率 30.0% → 41.5%（0 → 1.0 kg）✓ |
| 4 | 額定 0.6 kg 的工作空間代價 | 合法關節盒內 100% 可行 |

**位能微分是完全獨立的路徑** —— Jacobian 轉置組裝若有符號或座標系錯誤，兩者不會相符。

### 8.3 結論（限定範圍）

> 靜態重力力矩模型經位能微分驗證，最大誤差為 $1.13\times10^{-8}$ N·m。
> **在目前 URDF 慣性參數與 50 N·m 關節力矩上限設定下，0.6 kg 負載不構成靜態重力
> 力矩瓶頸。此結果不代表機械臂可超額承載。**

模型算出「最差姿態仍可靜態承載 6.12 kg、中位 12.33 kg」**只是數學外推，不能當成
可承載能力**，理由如下：

- **50 N·m 的性質未知** —— 可能是峰值、驅動器設定或 URDF 佔位值，不一定是可長時間
  使用的**連續**力矩。
- **原廠額定同時考量多項因素** —— 末端質心位置、腕部力矩、溫升、壽命與重複精度，
  這些都不在 URDF 裡。
- **本模型只有靜態項** —— 加速產生的慣性力矩尚未納入。

因此**不能推論「600 g 的瓶頸不在力矩」**。能說的只有：在這組參數下，靜態重力項不會
先觸發限制。

### 8.4 後續處理方式

**任務層硬限制**

$$m_{\mathrm{payload}}\le 0.6\ \mathrm{kg}$$

不允許規劃器以模型結果放寬額定負載。

**控制層動態力矩檢查**

$$\tau=M(q)\ddot q+C(q,\dot q)\dot q+g(q)+J^\top F_{\mathrm{ext}}$$

用於限制加速與高動態姿態，**不取代** 0.6 kg 額定限制。

### 8.5 動態模型建立前必須先驗證的事

1. URDF 各連桿質量、質心與慣量是否可靠。
2. 逆動力學與另一套獨立實作比對。
3. 零速度時是否退化成已驗證的重力模型。
4. 功率與力矩是否隨負載、速度及加速度合理變化。
5. **不把 19.98 rad/s² 同時施加於所有關節** —— 應測試實際可達軌跡。

### 8.7 資料來源核對與落差（2026-09-04）

取得 **UFACTORY Lite 6 Hardware Manual V2.6.0** 後逐項核對。

**已由手冊確認**（Preface）

| 量 | 手冊值 |
|---|---|
| 關節範圍 | J1 ±360°、J2 ±150°、J3 −3.5°~300°、J4 ±360°、J5 ±124°、J6 ±360° |
| 關節速度 | 0～180 °/s |
| 關節加速度 | 0～1145 °/s² |
| Jerk | 0～28647 °/s³ |

**手冊中不存在，來源待查證**

| 量 | 實際來源 | 狀態 |
|---|---|---|
| `EFFORT_NM = [50,50,32,32,32,20]` N·m | UFACTORY 發布的 `xarm_description` URDF `<limit effort=…>` | 物理意義（峰值／連續／模擬用）**未經硬體文件確認** |
| 額定負載 0.6 kg | 廣為引用，但無官方文件佐證 | **待查證** |

搜遍全文 1159 行：手冊的 Technical Specifications 只列自重 8 kg、最大關節速度
180 °/s、末端速度 500 mm/s、重複精度 ±0.5 mm、功耗 150 W、工作範圍與法蘭規格 ——
**沒有 payload，也沒有關節力矩表**。全文唯一的 N·m 是底座螺栓鎖固扭矩 20 N·m，與
關節力矩無關。

**已修正的錯誤引用**

`arm_limits.py` 原本標示來源為 *User Manual V2.0.0* 的 Table 1.1／1.2，並宣稱 effort
"matches the datasheet"。實際文件是 *Hardware Manual V2.6.0*，版本與文件名皆不同；
datasheet 從未被查閱。兩處已移除而非改寫 —— **查不到的引用比沒有引用更糟**。
該檔目前**沒有任何程式 import**，故那些「手冊值」從未被實際強制執行。

### 8.8 修正後的結論

> 在 UFACTORY 發布的 URDF 慣性參數與 effort 欄位設定下，0.6 kg 負載連同手冊加速度
> 上限，**在已測軌跡中**未觸發模型力矩限制。URDF effort 是否代表峰值、連續或模擬用
> 限制，Hardware Manual V2.6.0 並未說明。

### 8.9 夾爪與負載定義的未決問題

在取得官方定義前，以下皆**不可寫成確定值**：

- 若 payload 定義**包含末端工具**，夾爪 0.25 kg 可能使可抓物體額度只剩 **0.35 kg**。
- 若 payload 指**法蘭外部**允許負載，仍需確認是否包含夾爪及其質心偏移。
- **0.6 kg 與 0.35 kg 兩者都不能當成已確認規格。**

處理方式：0.6 kg 保留為**任務層硬限制**，不允許規劃器以模型結果放寬；
**不得**把 URDF 的 effort 欄位提升為已驗證的實機力矩上限。

後續須查 UFACTORY 官方產品規格、datasheet，或直接向原廠確認。

### 8.6 尚未完成

- 關節速度六軸皆 3.14 rad/s（＝手冊 180 °/s，已確認），但 URDF 未逐關節區分，
  且**未實機量測**。
- 加速度 1145 °/s²（手冊已確認）；URDF 無此欄位，尚未納入控制器約束。
- 關節力矩上限與額定負載的來源缺口見 §8.7。

---

## 九、實機限制接入控制路徑（第 5A 項，已完成）

### 9.1 為何需要這一步

`arm_limits.py` 收錄了經手冊核對的運動包絡，卻**沒有任何程式 import 它**。
一個只存在於文件裡的限制不構成任何約束，因此 5A 的重點不是「寫下限制」，而是
**證明每一項限制真的改變了輸出**。

### 9.2 兩組位置限位並存

| 來源 | joint1 範圍 | 用途 |
|---|---|---|
| Hardware Manual V2.6.0（已驗證） | ±360° | 原廠**外包絡** |
| 參考模型／URDF | ±168.2° | **實機安全限位**，控制以此規劃 |

實機限位在每一軸都更緊，且**收緊量不一致**：joint3 只縮上限，其餘兩端等量內縮。
以外包絡規劃會產生模擬正常、實機控制器拒絕的軌跡，因此兩組必須並存而非互相覆蓋。

### 9.3 優先序

```
位置／速度／加速度   硬限制，永不放寬（對應馬達轉速與力矩）
jerk                 軟限制，可被安全屏障覆寫
屏障                 需要煞車或退離時覆寫 jerk，並記錄 safety_override
```

### 9.4 jerk 與加速度何者作用，取決於前一輪加速度

兩個盒**中心不同**：

$$\Delta v_k \in a_{k-1}\Delta t \pm j_{\max}\Delta t^2
\qquad\text{（jerk）}$$

$$\Delta v_k \in \pm\, a_{\max}\Delta t
\qquad\text{（加速度）}$$

在 20 Hz 且**前一輪加速度為零**時，$j_{\max}\Delta t^2 = 1.25$ rad/s 大於
$a_{\max}\Delta t = 0.999$ rad/s，因此**初始階躍由加速度限制主導，無法用來辨識
jerk 是否實作**。為了單獨驗證 jerk，測試改用 $\Delta t = 0.02$ s。

**在非零歷史加速度及加速度反向時，jerk 於 20 Hz 仍可能作用**。實測（$\Delta t=0.05$）：

| $a_{k-1}$ | jerk 盒 | 加速度盒 | 輸出 | 下界由誰決定 |
|---|---|---|---|---|
| 0.00 | [−0.2500, +2.2500] | [+0.0008, +1.9992] | +0.0008 | 加速度 |
| 10.00 | [+0.2500, +2.7500] | [+0.0008, +1.9992] | +0.2500 | **jerk** |
| 19.98 | [+0.7490, +3.2490] | [+0.0008, +1.9992] | +0.7490 | **jerk** |

煞車與反向正是 jerk 在真實控制頻率下作用的地方。

### 9.5 離線驗收

| 檢查 | 結果 |
|---|---|
| 位置限位投影（六軸） | 皆停在上限不越界 ✓ |
| 速度裁切 | 10.00 → 3.1416 rad/s（手冊 180°/s）✓ |
| 加速度裁切 | 3.0000 → 0.9992 rad/s ✓ |
| 綁定實機限位而非手冊包絡 | 越界後輸出歸零 ✓ |
| jerk 階躍（dt=0.02） | 0.3997 → 0.2000 ✓ |
| jerk 反向 | 499.7 ≤ 500.0 rad/s³ ✓ |
| jerk 變動 Δt | 0.02／0.05／0.10 皆用實際經過時間 ✓ |
| jerk 重啟初始化 | 無 `a_prev` 時不套用 ✓ |
| 安全覆寫 | 穿透時 override=True，遠處 False ✓ |

### 9.6 ROS 在環接線（關鍵修正）

離線驗證通過時，節點呼叫的仍是

```python
filter_velocity(self.K, q, v_in, pts, self.cfg)
```

**沒有傳 `v_prev`、`a_prev` 或實際 `dt`** —— 也就是說加速度與 jerk 在實際 ROS 路徑上
**完全沒有生效**，`safety_override` 也從未進入診斷。離線正確性對此毫無保證。

已修正：節點保存**實際輸出**歷史（非輸入）、由相鄰輸出計算 $a_{k-1}$、傳入時鐘量測的
實際 $\Delta t$、失效時清除歷史、`safety_override` 加入診斷（15 → 18 欄）。

| 在環檢查 | 結果 |
|---|---|
| 歷史保存 | `has_history` 100% ✓ |
| 階躍受限 | 輸出 `[0.9992, 1.9984, 2.9976, 3.0, …]`，每步 $a_{\max}\Delta t$ ✓ |
| 實際 dt | 中位 50.0 ms ✓ |
| 逾時清除並重新爬升 | `has_history=0`，恢復後自 0.9992 重爬 ✓ |
| `safety_override` 進診斷 | 18 欄，靜止時 0/20 未濫用 ✓ |

### 9.7 過程中的兩個錯誤

**`d.data` 未被修改** —— 註解已更新為 18 欄但 list 沒動，比對字串與實際換行不符而
**靜默未套用**。診斷顯示 15 欄時一度誤判為節點未重建。

**`v_prev` 初始為 `None` 讓第一個週期逃脫約束** —— 輸出直接跳到全速，而該值又成為
歷史，於是**爬升從未發生**，後續每個週期都看到「本來就在全速」。

修正的關鍵是區分兩件事：**`v_prev = 0` 不是捏造而是事實**（節點啟動時機器人靜止，
清除歷史時節點正在輸出零）；**`a_prev` 需要兩筆輸出**，捏造零會無理由夾死第一個
真實指令，故仍保持 `None`。

### 9.8 一項不可據以宣稱的量測

在環量到的 dt 抖動為 **0.0 ms**，因為節點使用 `use_sim_time`，gz 時鐘規律步進。
**模擬中量不到真實週期抖動**，不可據此宣稱時序穩定；須待實機或關閉 sim time。

### 9.9 安全語意修正（審查後補）

兩處在缺少輸入時產生「看起來有把握」的輸出，均已改為 fail-safe：

| 情況 | 舊行為 | 修正後 |
|---|---|---|
| `no-TF`（取不到底盤位姿） | 只把底盤速度歸零，**手臂繼續濾波** | 9-DOF 命令**全部歸零**（`reason = 6`） |
| 遮蔽來源或 LiDAR TF 失效 | `occluded = 0`，全部視為可觀測 | `occluded = 100%`，**未知不得解讀為淨空** |
| `points` 不存在 | 仍呼叫濾波器求解 | **拒絕求解並進入 fail-safe**（`reason = 5`） |

**為何「固定底盤、繼續濾手臂」是錯的答案而不是降級**：偵測點在 report frame，而
$J_{p_i}(q)$ 的手臂欄位**隨底盤朝向旋轉**。缺少底盤位姿時 $q_{[:3]}=0$ 等於假設底盤
位於原點且朝 0°；底盤實際在 $\theta=90^\circ$ 時，整組 $n_i^\top J$ 在錯誤座標系中
求值 —— **約束看起來被滿足，方向卻是錯的**。固定底盤並不會修復手臂那些列。

**為何 TF 失效必須讓遮蔽轉為未知**：`_occluded()` 本身一直 fail-closed，漏洞在於它
**只在感測器 TF 可得時才被呼叫**。不知道感測器在哪，不等於知道沒有東西擋住它。

驗收：

```
[A] TF 正常  reason=ok  輸出範數 1.0000            ✓
    diag 18 欄（safety_override / dt_actual_ms / has_history）
[B] 正常 occluded 0% → 來源停止 100% → 恢復 0%     ✓
```

---

## 十、固定底盤預抓取控制（第 5B 項）

> **狀態：5B 首次整合基線失敗。到達率 1/8。**
> 整條鏈已接通（IK → 受限軌跡 → 連桿安全層 → 限制集合），但尚未達成驗收。

`ammr_wholebody_mpc/arm_pregrasp.py`、`evaluation/verify_pregrasp.py`

### 10.1 設定

| 項目 | 值 |
|---|---|
| 目標箱體 | `obs_0`，中心 (16.27, 14.25, 0.60)，尺寸 1.05×1.4×1.2 |
| 底盤停放 | (17.35, 14.25, −180°)，離面 0.55 m，**固定不動** |
| 預抓取位姿 | (16.920, 14.250, 0.550)，離面 0.12 m |
| 肩部到目標 | 0.321 m（臂展約 0.44 m） |

底盤位姿不是原點：5B 固定底盤，但沒有規定它停在哪。第一次嘗試把底盤留在原點，
目標距離 1.9 m 而臂展只有 0.44 m，**八組規劃全部失敗，原因與 IK、軌跡、安全層無關**。

離面距離由**量測的可達集合**決定而非猜測：位置＋姿態 IK 自 0.25 m 起收斂，
0.20 m 失敗（手臂無法折得那麼緊又讓工具指向該面）。

### 10.2 首次整合基線結果

```
起始 2:  末端誤差    1.2 mm    0.63°     ← 到達
起始 6:  末端誤差   13.0 mm    6.68°
起始 3:  末端誤差  358.0 mm   55.36°
起始 5:  末端誤差  382.5 mm  109.20°
（另 4 組於規劃階段因自碰撞餘裕不足被拒絕）

到達率 1/8   最小環境間距 0.0909 m   最小自碰撞間距 0.0073 m
違反率（211 步）位置 0   速度 0   加速度 0
fallback 118   unresolved 3   safety_override 115
到達後末 20 步最大關節變化 0.3222°  ★ 有殘餘運動
```

### 10.3 定位 +2.01 殘差：最後手段歸零違反了它自己的硬限制

首次執行的最大殘差為 **+2.01**，遠非浮點誤差。拆分後來源明確：

| 類別 | 修正前 | 修正後 |
|---|---|---|
| 屏障 | +2.84e−01 | +4.22e−02 |
| 關節位置 | +0.00e+00 | +3.45e−05 |
| **速度／加速度盒** | **+2.01e+00** | **+0.00e+00** |
| jerk | +7.68e−02 | +2.22e−16 |

**根因**：濾波器的最後手段是 `v = 0`。但加速度盒以 $v_{k-1}$ 為中心，
$v_{k-1}pprox 3$ rad/s、$a_{\max}\Delta t = 0.999$ 時，**零落在盒外 2.0** ——
濾波器違反了它自己宣稱為硬限制的那一項，殘差 $3.0-0.999pprox 2.01$ 正好吻合。

**修正**：最後手段改為**以硬體能力全力煞車**，即把零投影到加速度盒上：

$$v_{	ext{stop}} = \mathrm{clip}\left(0,\; v_{k-1}-a_{\max}\Delta t,\; v_{k-1}+a_{\max}\Delta t\right)$$

零看起來安全，但它不是可達的答案；可行的停止是本週期全力減速、數個週期內歸零。

修正後：加速度違反 **9 → 0**，速度／加速度盒殘差 **+2.01 → +0.000**，
unresolved **10 → 3**，到達率 **0/8 → 1/8**。

### 10.4 其餘兩項修正

**`a_brake = 1.0 m/s²` 是佔位值。** 計畫 §8.3 早已載明手臂的煞車能力須由關節減速度
經 Jacobian 映射。現改為逐點計算，對關節加速度盒為精確上界：

$$a_{\text{along},i} = \sum_k \left|(n_i^\top J_{p_i})_k\right|\, a_{\max,k}$$

實測 10.4 m/s²，非 1.0。`safety_override` 由 90 降至 63。

**兩個限位來源漂移 $3.9\times10^{-6}$ rad。** URDF 存參考模型精確值 `2.443457075`，
而 `LITE6_SAFE` 由 `140.0°` 換算得 `2.4434609528`。濾波器夾到較寬者、測試以較緊者
判定，於是誤判越限。改為**直接以弧度儲存精確值**，兩者差異現為 0.000e+00。

**IK 只試一個種子。** 6-DOF 手臂到 6-DOF 位姿存在離散解分支，最接近起始姿態的那支
常是把手臂折入自身的那支。加入多重種子後規劃成功率 2/8 → 4/8。

### 10.5 尚未解決

| 問題 | 現況 |
|---|---|
| 屏障殘差 | ~~+4.22e−02，仍未恆滿足~~ ✅ 已解決，見 §10.8 |
| unresolved 週期 | ~~3~~ ✅ 0，見 §10.8 |
| 自碰撞規劃失敗 | 4/8，餘裕 0.0016 < 0.005 |
| 到達後殘餘運動 | 末 20 步 0.3222°，未穩定保持 |

**在屏障殘差確認恆為滿足之前，不得宣稱起始 3、5 是「被安全層擋住」** —— 目前同樣
可能是求解器未收斂。

### 10.8 定位 +4.22e−02：獨立可行性檢查（2026-09-04）

先分辨兩件本質不同的事，而不是直接加大投影次數：這 3 個週期是**存在可行解但投影器
沒找到**，還是**屏障與硬體限制物理上不可同時滿足**。

工具：`evaluation/replay_unresolved.py`。位元級重播驗收迴圈（同種子、同 8 組起始、
同場景），對每個 unresolved 週期存下完整快照 —— $q$、$v_{\mathrm{prev}}$、
$a_{\mathrm{prev}}$、$v_{\mathrm{in}}$、$\Delta t$、每一列 $A,b$ 及其類別與 row ID、
每點的 $d_i,n_i,d_{\mathrm{stop},i}$、fallback 前後輸出、各類最大殘差
（`evaluation/results/unresolved_snapshots.json`）。

#### 獨立求解器判定

同一組列交給 **scipy HiGHS**（與濾波器的逐次投影不共用任何程式碼或演算法），
解最小違反量 LP：

$$\min s \quad \text{s.t.} \quad A_{\mathrm{barrier}}v \le b_{\mathrm{barrier}}+s,
\quad A_{\mathrm{hardware}}v \le b_{\mathrm{hardware}}, \quad s \ge 0$$

屏障問兩次：fallback 實際針對的不是原始 $b$，而是放寬後的
$b' = \max(b,0)$（「不再接近」而非「以屏障要求的速率退出」），殘差也是對 $b'$ 量的。
只測原始 $b$ 會回答一個濾波器沒在問的問題。

| 週期 | 硬體單獨 | 屏障(原)+硬體 | 屏障(放寬)+硬體 | +jerk |
|---|---|---|---|---|
| 起始2 步11 | 0 | **0** | **0** | **0** |
| 起始2 步13 | 0 | **0** | **0** | **0** |
| 起始2 步16 | 0 | **0** | **0** | **0** |

$s^\ast = 0$ 全數成立，且 LP 解經獨立代回驗證（屏障最大殘差 −2.05e−01、−2.12e−01、
−1.68e−01，皆為餘裕）。**結論：可行。屬求解器執行問題，不是物理衝突。**

#### 三個缺陷

| # | 缺陷 | 證據 |
|---|---|---|
| D1 | **最後手段煞車覆蓋了更好的解** | 輸出 $=\operatorname{clip}(0,v_{\mathrm{prev}}\pm a_{\max}\Delta t)$ 逐位元相符，其屏障殘差 **+4.22e−02**，而它丟棄的 30 次投影結果只有 **+8.0e−03** |
| D2 | **迭代預算不足兩個數量級** | 完整硬集合在 $\lambda=1$ 下需 **361 / 390 / 422** 次收斂，預算只有 8 + 30；30 次時殘差 ~1e−02，恰好越過 1e−3 門檻而誤觸最後手段 |
| D3 | **`safety_override` 誤報** | 含 jerk 與去 jerk 的殘差完全相同（改善 +0.000e+00），jerk 從未擋路；`r2 <= resid` 讓「不變差」也記為 override，211 週期中誤報 **115** 次 |

D1 是產生那個數字的直接原因：關節空間的全力煞車不等於任務空間停止接近，兩者差一個
Jacobian，煞車可以把點推得更深。**最後手段不得使情況變差。**

#### 修正

| 項目 | 舊 | 新 | 依據 |
|---|---|---|---|
| 投影步長 | $\lambda = 1$ | $\lambda = 1.7$（Agmon–Motzkin 過鬆弛） | 收斂次數 361→**12**、390→**16**、422→**15** |
| `proj_iters` | 8 | 60 | 66×9 問題實測：30 次 0.095 ms、500 次 1.16 ms，對 50 ms 週期可忽略 |
| `fallback_iters` | 30 | 200 | 同上 |
| 最後手段 | 直接取原始煞車 | 以煞車為**起點**再投影，並在三個候選中取殘差最小者 | 煞車後投影殘差 **+9.9e−10** |
| override 條件 | `r2 <= resid` | `r2 < resid - 1e-9` | 須真正改善才算覆寫 |
| `resid_velbox` / `resid_accbox` | 兩者都回報速度盒數字 | 直接量 $\max(\lvert v\rvert - v_{\max})$ 與 $\max(\lvert v-v_{\mathrm{prev}}\rvert - a_{\max}\Delta t)$ | `_box_rows` 把兩者摺成同一列，列殘差無法歸屬 |

過鬆弛不是單調的，一步可能跳到比前一點更差的位置，因此 `_project` 改為回傳**歷程中
最佳**的迭代點，而非最後一點。

#### 修正後（同 8 組起始，未更換樣本）

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 最大約束殘差 | +4.22e−02 | **+0.00e+00** |
| 屏障殘差 | +4.217e−02 | **−1.567e−05**（餘裕） |
| unresolved | 3 | **0** |
| fallback | 118 | **0** |
| safety_override | 115 | **0** |
| 位置／速度／加速度違反 | 0 / 0 / 0 | 0 / 0 / 0 |
| 到達率 | 1/8 | 2/8 |
| TCP 位置誤差（達成者） | 1.22 mm | 中位 1.12 mm，最大 2.06 mm |
| 到達後末 20 步關節變化 | 0.3222° | 0.1997° |

#### 起始 3、5 的歸因（§10.5 保留條件解除）

§10.5 記載「在屏障殘差確認恆為滿足之前，不得宣稱起始 3、5 是被安全層擋住」。
殘差現已恆為滿足，該歸因可以成立，並已獨立查證：

- `plan.q_goal` 的 FK 誤差為 **0.0 mm / 0.01°** —— IK 正確，不是規劃失敗。
- 未經濾波的**參考路徑**最小環境間距：起始 3 為 **0.0793 m**、起始 5 為 **0.0389 m**，
  而零速 $d_{\mathrm{stop}} = d_0 + \varepsilon = 0.08$ m。

即關節空間 min-jerk 直線內插雖然兩端點都無碰撞，中途仍穿進障礙物，屏障正確地擋住。
**這是路徑問題，不是濾波器問題**，屬 §10.7 第 5 步（法向低速接近）與路徑規劃的範圍。

#### 尚未處理：加速度感知的位置制動邊界

目前的位置列只禁止**下一步**越界，不保證「以最大可達減速度仍能在剩餘行程內停住」。
本次 3 個週期經檢查全部滿足 $\dot q_k^2 \le 2a_{\max,k}d_{\mathrm{limit},k}$，
**因此尚未觸發**，但這不代表約束存在。高速全身同動前需補上該提前制動邊界，
屏障距離亦應以相同概念建立可停止集合。

### 10.9 預抓取距離掃描（第 4 步，2026-09-05）

§10.7 第 3 步完成後解除封鎖。`verify_pregrasp.py` 加入 `--stand`，**同 8 組起始姿態、
同 seed，未更換樣本**。

底盤隨 standoff 等量後退（`base_off = 0.55 + (stand − 0.12)`）。只加大 standoff 而
不動底盤，會把預抓取點推向**肩部**而非遠離箱子：0.20 m 的 standoff 只剩 0.241 m 臂展，
落回 IK 不收斂的區間（實測 0.25 m 以外才收斂、0.20 m 失敗），重跑會失敗於與安全層
無關的理由。固定「肩部到目標 0.321 m」才能讓兩個問題分開。

| standoff | 底盤離面 | 到達率 | 規劃失敗 | 最大殘差 | fallback | 到達後殘餘運動 |
|---|---|---|---|---|---|---|
| 0.12 m | 0.55 m | 2/8 | 4 | +0.00e+00 | 0 | 0.2237° |
| 0.16 m | 0.59 m | 4/8 | 3 | +0.00e+00 | 0 | 0.1660° |
| **0.20 m** | **0.63 m** | **5/8** | **3** | **+9.50e−10** | **1** | **0.2237°** |
| 0.24 m | 0.67 m | 5/8 | 3 | +0.00e+00 | 1 | 0.0808° |

0.20 m 時起始 3 與 5 從「被屏障擋住」（624.3 / 621.7 mm）變成到達（0.6 / 1.7 mm），
起始 7 也能規劃。位置／速度／加速度違反全程 0。**§10.6 的方向得到證實：衝突不是
不可解的安全／任務衝突，加大 standoff 即可。**

剩餘 3 個規劃失敗全部是自碰撞餘裕（0.0016–0.0019 m < 0.005 m 門檻），與 standoff
無關，屬 §3.3 記載的 tuck 姿態 1.1 mm 間距問題。

### 10.10 偵測點涵蓋不足（掃描過程中發現）

| standoff | 連桿最小間距 | 偵測點最小距離 | 落差 |
|---|---|---|---|
| 0.12 m | 0.0914 | 0.1287 | **0.0416** |
| 0.16 m | 0.0373 | 0.0710 | **0.0416** |
| 0.20 m | 0.0773 | 0.1110 | **0.0416** |
| 0.24 m | 0.1173 | 0.1510 | **0.0418** |

落差在四個 standoff 下**完全一致**，不是雜訊。0.16 m 時連桿最小間距掉到 **0.0373 m**，
遠低於零速 $d_{\mathrm{stop}} = d_0 + \varepsilon = 0.08$ m，**而屏障回報滿足**
（殘差 +0.00e+00）。

原因是屏障只約束 **12 個偵測點**，連桿的其餘幾何不在任何約束裡：

$$0.08 - 0.042 = 0.038 \approx 0.0373 \;(\text{實測})$$

量測每個連桿幾何超出最近偵測點的最大距離（60 組隨機合法姿態）：

| 連桿 | 超出量 | 連桿 | 超出量 |
|---|---|---|---|
| **link_base** | **0.1062 m** | uflite_finger1/2 | 0.0587 m |
| link4 | 0.0823 m | link6 | 0.0575 m |
| link2 | 0.0655 m | link5 | 0.0570 m |
| link1 | 0.0602 m | uflite_gripper_link | 0.0461 m |
| link3 | 0.0599 m | | |

**最大 0.1062 m > 零速 $d_{\mathrm{stop}}$ 0.08 m。屏障可以完全滿足，而連桿實際上
已經接觸障礙物。** 5B 這幾條軌跡只吃到 0.042 m 是運氣，不是保證。

這與第一階段底盤的 $r_{\mathrm{eff},i} = r_i + m_i$ 是同一類問題 —— 底盤版本做了，
連桿版本沒做。

#### 建議修法（尚未實作，需決策）

每個偵測點帶自己的涵蓋半徑 $\rho_i$，屏障列改為

$$n_i^\top J_{p_i}(q)\,\nu \;\le\; \alpha_i\bigl(d_i - \rho_i - d_{\mathrm{stop},i}\bigr)$$

$\rho_i$ 由該偵測點所屬連桿的幾何量出，而非猜測。**這會使屏障顯著保守**：
以 link_base 的 0.1062 m 計，零速有效站距從 0.08 m 變成 0.186 m，
0.12 m 的預抓取距離將直接不可行 —— 這正是 §10.9 掃描顯示要走向 0.20 m 以上的另一個理由。

替代或並行做法：**增加偵測點密度**，把最大超出量壓到可接受範圍，代價是每週期的
約束列數上升（目前 12 點 → 12～24 列）。

**在此修正之前，不得宣稱「連桿層級安全約束保證不碰撞」** —— 目前保證的是
「12 個偵測點不碰撞」，兩者相差最多 0.106 m。

### 10.11 全連桿幾何安全表示（2026-09-06）

§10.10 的修法決策：**不採用「12 點各加 $\rho_i$」**。

$\rho_i$ 只修距離，不修速度。距離場是 1-Lipschitz，所以
$d_{\mathrm{link}} \ge d(p_i) - \rho_i$ 是不等式而非估計；但同一剛體上另一點的速度是

$$v_x = v_{p_i} + \omega_i \times (x - p_i)$$

膨脹距離卻仍用 $J_{p_i}$，等於假設整條連桿以代表點的速度接近，在旋轉主導時不成立。

改為**在真正最近的連桿表面點上建立屏障列**。

#### 表示法

`ammr_wholebody_mpc/arm_link_geometry.py`：

1. 每條連桿的 collision mesh 做面積加權表面取樣，再以**最遠點取樣（FPS）**選點 ——
   FPS 對給定點數最小化涵蓋半徑，而涵蓋半徑正是屏障要扣掉的量。
2. 點數**逐連桿**由二分搜尋決定，使各自的 $\rho \le$ 目標值。固定點數不合適：
   fingers 20 點就到 7 mm，link_base 要 320 點才 13 mm。
3. 每週期對每條連桿找出離障礙物最近的 $k$ 個取樣點，在該點取 $d^\ast, n^\ast, J_{p^\ast}$。
4. 列為

$$n_i^{\ast\top} J_{p_i^\ast}(q)\,\nu \;\le\; \alpha_i\bigl(d_i^\ast - \rho_{\mathrm{sample}} - d_{\mathrm{stop},i}\bigr)$$

`wholebody_kinematics.jacobian(q, link, offset)` 原本為 COM Jacobian 而寫的 `offset`
參數正好提供連桿上任意點的 Jacobian，運動學不需修改。

$\rho$ 目標 15 mm 時：

| 連桿 | 點數 | $\rho$ | 連桿 | 點數 | $\rho$ |
|---|---|---|---|---|---|
| link_base | 238 | 14.96 mm | link5 | 85 | 14.95 mm |
| link4 | 221 | 14.99 mm | uflite_gripper_link | 54 | 14.96 mm |
| link2 | 194 | 14.96 mm | link6 | 41 | 14.71 mm |
| link1 | 144 | 14.96 mm | uflite_finger1 | 8 | 11.80 mm |
| link3 | 134 | 14.97 mm | uflite_finger2 | 8 | 12.62 mm |

合計 1127 點，最大 $\rho$ **14.99 mm**（原 12 點的缺口是 106 mm）。

#### 驗收（`evaluation/verify_link_geometry.py`，80 組隨機姿態／障礙物方向）

參考取樣自身 $\rho_{\mathrm{ref}} = 4.21$ mm。**首次執行時參考只有 9.22 mm 而最小裕度
4.53 mm，比參考自身的不確定度還小，該測試在最緊處無法下結論**；加密後才成立。

**距離** $d^\ast - \rho \le d_{\mathrm{true}}$：

| | |
|---|---|
| 違反 | **0 / 800** |
| 最小裕度 | +3.88 mm |
| 中位裕度 | +14.17 mm |

最緊那一筆的 3.88 mm 仍略小於 $\rho_{\mathrm{ref}} = 4.21$ mm，**該單點嚴格來說仍在
測不準範圍內**；其餘皆有明確裕度。

**舊 12 點，同樣姿態**：$d_{\mathrm{true}} - d_{\mathrm{detect}}$ 有 **948 / 960 為負**
（樂觀），中位 −55.4 mm，**最樂觀 −238.0 mm**。

**速度**（真實接近速率 − 該連桿所有列中最嚴格者），$k = 6$：

| 真實距離 | 樂觀比例 | 最大樂觀量 |
|---|---|---|
| > 20 mm | 26 / 662（3.9%） | +16.52 mm/s |
| 5–20 mm | **0 / 22** | 從不樂觀 |
| ≤ 5 mm | 13 / 116 | +498.56 mm/s |

$k$ 的效果（400 列）：

| $k$ | 樂觀比例 | 最大 | p95 |
|---|---|---|---|
| 1 | 44.8% | +2435 mm/s | +45.68 |
| 3 | 11.2% | +76.63 | +5.31 |
| **6** | **4.9%** | +498.56 | **+0.00** |

**速度的界沒有被證明，只被實測。** 取樣中距離最小的點與真正最近點可能相隔甚遠
（$\rho$ 只約束距離誤差，不約束兩點的間隔），同一剛體上兩點的速度差上界是
$|\omega|\cdot|x-s|$，用的是連桿長度而非 $\rho$。取 $k$ 個最近點是為了讓真正最近點的
鄰域也被約束住，實測有效但仍非嚴格保證。

殘餘樂觀集中在 $d \le 5$ mm，該處 $n = v/d$ 本身無定義，而零速
$d_{\mathrm{stop}} = 0.08$ m 早已要求全速退出。

#### 成本

| | 最近點搜尋 | 濾波 | 合計 |
|---|---|---|---|
| $k=1$（10 列） | 1.49 ms | 2.13 ms | 3.62 ms |
| $k=3$（30 列） | 1.52 ms | 6.42 ms | 7.94 ms |
| **$k=6$（60 列）** | **1.57 ms** | **12.82 ms** | **14.39 ms** |
| 舊 12 點 | — | 2.06 ms | 2.06 ms |

最近點搜尋原為 **13.73 ms**（逐點 Python 呼叫 `_closest_local`，3 障礙 × 1127 點），
向量化後 **1.57 ms**，數值逐位相同（距離最大差 0.000e+00）。

控制週期 50 ms，$k=6$ 佔 29%。**但端到端 p95 原本已是 52.2 ms**，接入在環前必須先處理。

#### 重掃 standoff（0.20 m 不再成立）

同 8 組起始姿態、同 seed。

| standoff | 12 點 到達率 | 12 點 最小間距 | **連桿** 到達率 | **連桿** 最小間距 |
|---|---|---|---|---|
| 0.16 m | 4/8 | 0.0384 | 3/8 | 0.0446 |
| 0.20 m | 5/8 | 0.0773 | **4/8** | 0.0796 |
| 0.24 m | 5/8 | 0.1173 | 4/8 | 0.1173 |
| 0.28 m | — | — | **5/8** | 0.1573 |

誠實的屏障使 0.20 m 的到達率由 5/8 降為 4/8；**5/8 需要 0.28 m**。四個 standoff 下
殘差皆為 +0.00e+00，fallback 0，unresolved 0，override 0，位置／速度／加速度違反 0。

**§10.9 對 0.20 m 的結論撤回。** 它是在已知樂觀的屏障上得到的。

#### 尚未處理

- 最緊處的距離裕度（3.88 mm）仍在參考取樣解析度內，需更密的參考或解析距離場才能定案。
- 速度樂觀量在 $d>20$ mm 仍有 +16.5 mm/s。可折入 $\rho$：$\Delta\rho = \varepsilon_v/\alpha
  = 0.0165/2.0 \approx 8$ mm，代價是再保守 8 mm。
- 端到端 p95 52.2 ms 加上本層的 14.4 ms。
- 自碰撞規劃失敗 3/8 與 standoff 無關，屬 §3.3 的 tuck 姿態間距問題。

### 10.12 專用測試場景、單一來源初始姿態、致動自檢（2026-09-07～08）

§10.11 之後的問題是：認證取樣的安全表示已離線驗證，但從未在真實 ROS 迴路裡驅動過
一支手臂。這一節記錄把它接上模擬硬體之前所需的場景與前置驗證。

#### 專用測試環境

不沿用導航世界，另建 `src/ammr_bringup/worlds/arm_barrier_test.sdf`：地面加單一箱體，
位於 (0.74, 0, 0.60)，尺寸 0.40 × 1.00 × 1.20 m。距離刻意取得容易觀察，本輪不沿用
§10.9 的 standoff 數值來定案。

手臂改用 `velocity_controllers/JointGroupVelocityController`
（`config/lite6_velocity_controllers.yaml`），**僅用於本模擬測試**，不動原有位置控制
設定。`urdf/omni_bot.urdf.xacro` 新增 `controllers_yaml` 參數以切換，預設仍是原本的
`lite6_controllers.yaml`。

安全解算輸出 9 個分量（底盤 3 ＋手臂 6）。固定底盤時**必須在求解中**把底盤三個速度
限為零，而不是只在輸出端丟棄——濾波器新增 `fix_base`，於 `_box_rows` 將 `vmax[:3]`
設為 0。丟棄輸出會讓屏障誤以為底盤可以幫忙讓開。

命令路徑拆成兩段獨立程序：

- `evaluation/arm_vel_adapter.py`：9 → 6，關節順序**取自控制器自己的 `joints` 參數**
  而非寫死；不重送過期命令。
- `evaluation/arm_vel_gate.py`：`/lite6_vel_controller/commands` 的**唯一發布端**，
  單調 wall-clock 逾時 150 ms。不假設速度控制器在訊息中斷後會自動停車。

#### 初始姿態：自碰撞與單一來源

原本的 TUCK 姿態同時存在於兩處（`omni_bot_wholebody.launch.py` 與
`lite6_ros2_control_gz.xacro`），數值各自維護。以 `evaluation/mesh_distance.py` 做
三角面對三角面的精確距離檢查後發現：

**TUCK 實際上處於自碰撞**——`link1 ↔ uflite_gripper_link` 與 `link1 ↔ uflite_finger2`
距離 0.000 mm 且三角面相交（finger1 為 1.588 mm）。先前取樣點雲報出 1–3 mm，是因為
取樣點從未落在相交的那幾片三角面上。**取樣間距不是距離的下界**，這與 §10.11 對 $\rho$
的認證是同一件事的兩面。

全零姿態通過全部 44 組（排除相鄰連桿後）非排除配對，最緊為
`link_base ↔ link5` 的 **8.792 mm**。但全零位於碰撞邊界上且**方向不對稱**：joint3 往
負向 0.05 rad 內即相交，往正向 +0.05 rad 為 ≥ 20 mm，+0.45 rad 為 88 mm。

初始角度改為單一來源 `config/arm_initial_pose.yaml`（joint1..6 全零）。

**後續修正（2026-09-08）：單一來源當時只做了一半。** 生成／視覺化讀了這個檔，但評估腳本
各自寫死了另一組起始角度——`run_wholebody_demo.sh`、`pregrasp_demo.py --start`、
`wholebody_pregrasp.py --posture` 三處，正是這個檔案要防的那個失效。已把三組具名姿態全部
放進同一個檔（`spawn` / `test_start` / `pregrasp_reference`），Python 端由
`evaluation/arm_poses.py` 讀取，xacro 與 launch 仍直接索引 `joint1..joint6`，因此加入巢狀鍵
不影響展開（已驗證：生成姿態仍為全零 × 7）。

**為什麼執行起點是 joint3 = +0.30 而不是全零。** 兩個理由，取樣自碰撞（53 配對）：

| joint3 | −0.05 | −0.02 | 0.00 | +0.05 | +0.30 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 最小間距 | 3.11 mm | 4.28 mm | **9.09 mm** | 19.66 mm | 19.66 mm |

全零本身通過，但**站在斜坡上**：往負向 0.02 rad 間距就掉一半；+0.05 之後平掉在 19.66 mm
（最緊配對換成 link4 ↔ link6，與 joint3 無關）。真正決定 0.30 而非 0.05 的是**限位餘裕**：
joint3 安全範圍 [−0.0611, +2.9356] 是單邊的，全零離下限僅 0.0611 rad，而執行時的保護門檻在
下限 + 0.05 = −0.0111，只剩 **0.011 rad**。解算器把 joint3 壓低一點整趟就中止——全身的前兩次
正是如此。+0.30 有 0.361 rad 餘裕，且是致動自檢已驅動並量測過的角度。
`lite6_ros2_control_gz.xacro` 以 `xacro.load_yaml` 讀入，
`omni_bot_wholebody.launch.py` 以 `yaml.safe_load` 讀同一檔。展開後的 URDF 含
`initial_value` 0.0 共 7 處，且 "tuck" 字串為 0 次。

#### 致動自檢

`evaluation/verify_actuation.py` 四案，詳見
`evaluation/results/actuation_selftest_20260908.md`。因全零往負向即相交，測試軌跡改為
先正向到 +0.30 rad、再反向回 +0.05 rad，全程停在正向側。

| 案例 | 結束 joint3 | 故障→實際停止 | 致動／停止功能 |
| --- | ---: | ---: | --- |
| 正向至 +0.30 | +0.29706 | 6.5 ms | 通過 |
| 反向回 +0.05 | +0.05284 | 6.6 ms | 通過 |
| 停止發布，watchdog 介入 | +0.14944 | 160.6 ms | 通過 |
| 零命令保持 | +0.14944 | — | 通過 |

#### 峰值減速度 21.43 rad/s² 的判讀（重要修正）

初步結論寫成「21.43 > 設定 19.98 rad/s²，加速度限制未通過」。**此判讀不成立。**

本機 `/joint_states` 實測 142.9 Hz，時間戳解析度 1 ms，樣本間隔 6／7／8 ms。減速度以
相鄰兩則 JointState 差分估計；速度若在**一個樣本內**歸零，估計值恆等於
$v_{peak}/\Delta t$，是取樣率的性質而非關節的性質。以命令 0.15 rad/s：
$0.15/0.006 = 25.00$、$0.15/0.007 = 21.43$、$0.15/0.008 = 18.75$ rad/s²。量到的 21.43
與 $0.15/0.007$ 完全吻合。它是**兩筆速度樣本之間的平均差分值**，不是已解析出的瞬時
峰值；設定上限 19.98 恰好夾在兩個可能讀數之間，**本次量測在原理上無法判定**是否超限。
提高起始速度只能改善辨識能力，不能直接保證可判定，本輪也不為了量測而提高測試速度。

結構上的原因更關鍵：閘門逾時送出的是零命令**階躍**，直接寫入 gz 速度介面，這條路徑上
沒有任何環節施加加速度限制。因此兩項結論必須分開陳述：

- 正常控制路徑的加速度由濾波器加速度盒 $|v - v_{prev}| \le a_{max}\,dt$ 約束。
- **緊急停止路徑刻意不受加速度限制約束**；送零是停止請求，優先於加速度限制。這是設計
  選擇，不是量測失敗。使用者先前的提醒——「watchdog 送零是停止請求，不能直接算成加速度
  限制通過」——在兩個方向上都成立：也不能算成不通過。

正式措辭：

> 停止功能通過；停止過程是否符合加速度上限，現有量測無法判定。緊急歸零路徑未施加加速度
> 限制。

**階躍歸零不等於整條鏈已經 fail-safe。** 逾時直接送零作為目前模擬測試的停止策略是合理
的，但同一個故障風險也適用於閘門本身：若 watchdog 在送出零之前就失效，控制器仍可能保持
最後的非零命令。本輪能說的是「**watchdog 存活時，能處理上游停止發布**」，不涵蓋
watchdog 自身失效。先記錄，不重做整套停止機制。

`verify_actuation.py` 已修改為一併輸出降速橫跨的樣本間隔數與可分辨下限，讀數落在下限
時標示「無法判定」。閘門是否改為 $a_{max}\Delta t$ 斜降至零屬安全路徑設計決策，**未
實作**：斜降需 watchdog 在故障期間持續計算並發布，若其本身失效於斜降中途，手臂會保留
最後一個非零命令；階躍歸零則是失效安全的。

#### 本輪不能宣稱的事

- 本輪未獨立重新量測全連桿幾何間距，**不能宣稱測試全程 ≥ 20 mm**。
- 四案通過的是致動與停止**功能**，不是安全屏障閉迴路驗收。
- `gui:=false` 配置下出現控制參考更新、模擬時鐘前進（RTF 1.0）、控制器 active 且介面
  已宣告，但實際關節位移僅 3e-9 rad；切換 `gui:=true` 後同一測試軌跡可執行。底層原因
  尚未定位，僅記錄為本次環境中的觀察。
- 底盤 collision 與 visual 不一致（圓柱 z 0.10–0.28 對 mesh z 0.00–0.28），已發現，
  **未修**。
- §3.3 的自碰撞結果應標註為「取樣檢查結果」，須以本節的三角面工具重算。§10.9 的
  「21 mm」是搜尋估計值，不是全域最大值。


### 10.13 近障礙閉迴路屏障驗證（2026-09-08）

完整結果 `evaluation/results/barrier_engagement_20260908.md`，原始資料
`evaluation/results/barrier_probe_3phase.json`（458 週期）。這裡只記結論與過程中發現的
缺陷。

**範圍限定**：已知靜態場景、固定底盤。**不是**感知驗證（障礙來自世界檔，
`require_occlusion_feed:=false`，沒有 self-filter），**不是**全身 GMPC 驗證（輸入由 probe
以阻尼最小平方產生），**不是**自碰撞驗證。

#### 靜置與對齊

無 probe 時 20 s：六軸位移 0.00 µrad，控制器命令 958 則全為零。whole-body FK 的 TCP 世界
座標與 Gazebo 的 TF 逐位吻合；獨立上界線上值與離線重算同為 262.0 mm。

#### 接近（TCP 目標 50 mm/s，起始姿態由 `--home 0 0 0.30 0 0 0` 伺服到位）

模型下界 min(d−ρ) 由 207.0 mm 降到 **89.3 mm 後停住**；輸入違反屏障 **106/153** 週期
（最大 +53.02 mm/s），**輸出違反 0/153**（最大 −0.13 mm/s）；作用列數最大 48；命令 |max|
在作用期間由 0.2091 削到 0.1688 rad/s（19%）；**實測 TCP 速度（TF 差分，非命令）由 45.6
降到 35.3 mm/s**。理論停距 $d_0 + v\tau + v^2/2a + \varepsilon \approx 88$ mm 與 89 mm 平台
一致。

快照（輸入違反 → 輸出滿足 → 實測速度下降的第一個週期）：+20.88 → −0.559 mm/s，7 列作用，
實測 TCP 38.8 mm/s，作用連桿為夾爪與兩指，法向全為 [1,0,0]。

#### 切向與後退

切向（+y，維持在箱體前面範圍內）與後退（−x）**各 0 列被違反，命令未被修改**，實測 TCP
47.9 / 48.6 mm/s。**此處不作歸因**：兩者都沒有被修改，沒有需要歸因的對象；只能說這組軌跡
上未出現非接近方向被誤擋，不等於排除了任何特定機制。

#### 兩條界線的證據力不對稱

`bound_violation = 下界 − 上界` 轉正能證明距離模型或資料對齊有問題；**非正不能反過來證明
下界正確**，夾擠中間仍有空隙。下界的可信度仍來自 §10.11 的涵蓋半徑認證及其適用條件，
這張圖只是線上交叉檢查。

另有一項必須扣除的量測偏差：兩者取樣率不同（下界約 60 Hz、上界 3 Hz），**運動中逐週期相比
不成立**。上界保留 `age` 秒期間連桿以 v 移動，最多落後 `v·age`；後退時原始訊號出現乾淨鋸齒，
峰值 +9.95 mm ≈ 48 mm/s × 0.21 s。扣掉 `實測 TCP 速度 × 陳舊時間` 後，458 週期中為正的 0 個。**但這只是誤差分析，不是嚴格的
時間補償界**：最近點可能在任何一根連桿上，而不同連桿的線速度不等於 TCP 速度。正式措辭為
「**非同步比較，補償後未觀察到正值**」；嚴格核對留待離線以**同一份關節姿態與障礙物狀態**
同時計算兩條界線，不為此擴充線上功能。`clearance_ub_age` 已一併發布與記錄。

#### 過程中發現並修正的三個缺陷

1. **實測 TCP 速度原本是假的**：以 wall-clock 差分 TF 查詢，量到的是 probe 轉 executor 的
   頻率而非手臂速度（相鄰查詢回傳同一份快取 → 0，累積後跳成 300 mm/s）。改用 transform
   自己的時間戳差分，並在整個週期持續 spin。修正後 48.6／50.0 mm/s。
2. **開迴路後退不是回到起始姿態的方法**：TCP 越過肩部後阻尼逆解把 joint1 繞過去，三次復位
   後手臂在 joint1 = 2.50 rad 背對箱體，此時最近的是**固定底座**，回報間距成為手臂動作改變
   不了的常數 354.3 mm。改為 `--home` 伺服到指定關節角，命令一樣經 cmd_in。
3. **`ros2 run` 的孫程序在收拾時活了下來**：包裝器被送訊號、真正的節點沒有。三次重啟累積出
   **六個約 80% CPU 的孤兒節點**，全部仍在同樣 topic 上發布 → load average 19、
   `/joint_states` 出現 337 ms 空隙、每個安全 topic 有四個發布者。**在此之前的所有屏障數字
   已作廢並在乾淨圖上重跑**。堆疊改為每個子程序自成 process group、以群組收訊號並在結束時
   查核殘留。清理後 `/joint_states` 126.8 Hz、間隔中位 7.0 ms、p99 7.5 ms、最大 16.3 ms，
   超過 300 ms 的空隙 0 個。

#### 尚未處理

- 未做故障注入（TF／JointState／距離資料／命令／安全節點／轉接器各斷一次）。
- 切向僅測 +y；未測 −y 與非軸向。
- 89 mm 平台只是單一接近方向、單一速度的一組觀測。

### 10.14 預抓取動作交付（Gazebo 實際執行，2026-09-08）

完整記錄 `evaluation/results/pregrasp_demo_20260908.md`，影片
`evaluation/results/pregrasp_demo.mp4`，逐週期資料 `evaluation/results/pregrasp_demo.json`。

底盤停放 (−0.15, 0, 0.05) 固定；起始姿態 [0, 0, 0.30, 0, 0, 0] 由 `--home` 伺服到位；
預抓取目標 world (0.300, 0, 0.550)，離箱面 0.240 m，工具 +z 指向箱面。底盤不在原點是刻意的：
留在原點時預抓取點只離肩部 0.204 m，落在 IK 不收斂的區域，所有計畫都會因為與安全層無關的
理由失敗。`base_x/base_y/base_theta` 在 whole-body 模型裡是真關節，停放底盤是改 q 不是改模型。

**離線**（未通過就不進 Gazebo）：IK 殘差位置 0.00 mm、姿態 0.00°；整段路徑 41 點檢查——關節
位置在安全限位內、速度峰值 3.142 rad/s（限 3.14）、加速度峰值 9.896 rad/s²（限 19.98）、
自間距最小 10.22 mm、對箱體間距最小 244.0 mm。自間距是**取樣篩檢不是證明**，排除清單沿用
自碰撞驗收那一份，沒有為了讓數字好看移除任何配對。

**線上**（路徑被追蹤而非回放，命令全程經屏障）：153 週期 7.86 s；關節總行程 2.585 rad、最大
單軸 1.634 rad；TCP 峰值 65.7 mm/s，最後 10 週期 0.00 mm/s；追蹤誤差峰值 158.2 mrad、最終
0.002 mrad。**屏障輸入違反 0/153、輸出違反 0/153、作用列數 0**，模型下界全程 ≥ 230.2 mm。

**到達（由 TF 量測，非由計畫宣告）**：位置誤差 **0.0011 mm**，姿態誤差 **0.0000°**，關節最大
誤差 0.0017 mrad。

屏障這一趟完全沒介入是預期的：0.24 m 標稱距離讓最近連桿點全程 ≥ 230 mm，遠高於 §10.13 量到
的 89 mm 停住點。**因此這趟不是屏障的測試**，是預抓取動作本身的交付。

#### 螢幕錄影不可用（重要）

前兩次用 ffmpeg x11grab 抓 Gazebo 視窗所在的**螢幕區域**。x11grab 抓的是該區域合成後的畫面而
非視窗內容，只要有別的視窗蓋在前面就錄到那個視窗——**兩次都錄到使用者的私人瀏覽器頁面**，
其中一次含學號、姓名與住址。檔案已刪除，經確認**未進入任何 git 提交、未推上遠端**。改為在
世界檔放 `demo_camera` 相機感測器，橋接 `/demo_cam` 後由 `evaluation/record_gz_camera.py`
逐幀寫入 ffmpeg。相機感測器只可能看到模擬場景，這是結構上的保證。日後需要畫面一律走這條路。

#### `arm_barrier_test.launch.py` 少了 `GZ_SIM_SYSTEM_PLUGIN_PATH`

`gz_ros2_control-system` 只從這個路徑載入。此 launch 一直缺它，能跑是因為最初啟動的 shell 已
export 過；從乾淨環境啟動時 plugin 被靜默略過、controller_manager 不出現、spawner 重試到放棄，
且沒有控制器抓住關節時手臂會在重力下垮下。已比照 `omni_bot_arm.launch.py` 補上。

#### 尚未處理

- 只跑一組起始姿態；原本八組要在此之後跑，**不換樣本**（第一組 TUCK 已知處於實際自碰撞，
  §10.12，該事實不因本次交付改變）。
- 本趟屏障未介入，不能據此對屏障作任何宣稱。
- 自間距 10.22 mm 是取樣篩檢；整段路徑的精確三角面距離未做。

### 10.15 全身協調預抓取（2026-09-08）

目標改為：**世界座標中固定夾爪目標位姿，底盤停在哪裡是輸出不是輸入**，由同一個解算同時
決定底盤三自由度與手臂六軸。固定底盤那次（§10.14）保留為基礎驗證。

#### 命令路徑：一則訊息，一個閘門決策

底盤與手臂必須一起執行，否則下游各自裁切後，原本那組全身解就不能再宣稱滿足約束。因此：

- 轉接器把 `cmd_out` 的九個分量**合成一則** `/wb_vel_cmd`（底盤已由世界座標旋轉到車體座標，
  純旋轉、可逆、不改變大小），不再分成兩則。
- 閘門成為 `/lite6_vel_controller/commands` 與 `/cmd_vel` 的**唯一發布端**，同一 tick
  同時送出或同時歸零。半個全身命令不是比較小的命令，是**另一個**命令。
- 拒收也是全有全無。
- `base_tf_bridge.py` 從模擬器里程計（真值導出）發布 live `world → base_footprint`。底盤會動
  之後，靜態 TF 會讓濾波器以為機器人還在原地，每一條屏障列都建在錯的地方而殘差看起來仍健康。
  用真值位姿代表**本測試不涉及定位**。

#### 解算形式：加權堆疊最小平方，不是零空間投影

$$\min_v \; \lVert Jv-e\rVert^2 \;+\; \mu^2\lVert v_{arm}-k_p(q_{pref}-q_{arm})\rVert^2
\;+\; \lambda^2\lVert v/w\rVert^2$$

先試零空間投影，**不可行，原因值得記錄**：投影項不含底盤分量，能偏置手臂卻永遠停不了車。
任務於是自行用底盤滿足自己——底盤開到 $x=+0.39$ 而目標在 $x=+0.30$，衝過頭，手臂只好往後
折進自己。

改為加權項後，姿態與任務在**全部九欄**上競爭。手臂被壓在選定構形附近，工具相對底盤的偏移
因而固定，任務要滿足就只能讓底盤停在唯一能把工具送到目標的位置——**停車距離是同一個解算的
輸出，不是誰輸入的數字**。

#### 一個符號錯誤造成全部症狀

`e_r` 寫成 `-rot_error(R_cur, R_des)`，而 `solve_ik` 用的是不帶負號的同一函式。負號把工具
**推離**目標方向，而且不是單純失敗：它收斂到 180° 的**對蹠點**，那裡梯度為零，解算器就停在
那裡。位置誤差仍到 2.8 mm，所以**所有以位置為基礎的檢查都看起來健康**，同時工具指向反方向、
底盤越過目標 0.45 m、手臂折進自己。修正後全部症狀一次消失。

#### 離線 rollout（純運動學，尚未進 Gazebo）

起點：底盤 $x=-0.70$、手臂 $[0,0,0.30,0,0,0]$；目標 world $(0.30, 0, 0.55)$，工具 +z 指向箱面。
六組增益全數收斂，結果幾乎一致：

| 量 | 結果 |
| --- | --- |
| 收斂時間 | 8.95 s |
| **底盤停放（輸出）** | $x=-0.155$，全程單調不衝過頭（最遠即終點） |
| 最終臂形 | $[0, 0.6518, 0.5886, 0, -1.6339, 0]$ |
| joint3 最小值 | $+0.305$（安全下限 $-0.061$） |
| 位置／姿態誤差 | 4.9 mm／0.008° |
| **底盤與手臂同時運動** | 3.75 s |

底盤自行停在 $-0.155$，與 §10.14 人工算出的 $-0.15$ 相符，但那裡是輸入、這裡是輸出。

採用預設增益 `base_weight=3, mu_post=1.0, kp_post=1.2, k_limit=1.5`——它給出**最長的同動時間**。

#### Gazebo 實跑結果（已完成）

完整記錄 `evaluation/results/wholebody_pregrasp_20260908.md`，資料
`evaluation/results/wholebody_pregrasp.json`（207 週期），重跑步驟
`evaluation/run_wholebody_demo.sh`。四項驗收全部取自 `/joint_states` 與 `/odom` 實測值，
速度由相鄰實測位姿差分，不使用命令值也不使用計畫軌跡。

| 驗收項 | 結果 |
| --- | --- |
| 底盤與手臂**同時運動** | 累計 **3.50 s**，最長連續 **3.18 s**（66/206 週期） |
| 到達 | 位置 698 → **0.55 mm**；姿態 72.3° → **0.001°** |
| 停穩 | 最後 20 週期 底盤 0.00248 m/s、手臂 0.00001 rad/s |
| **底盤停放（輸出）** | x = **−0.1505**（離線預測 −0.155，無人輸入） |
| 關節位置越界／速度超限／底盤速度超限 | **0 / 0 / 0** |
| 對箱體 | 模型下界最小 231.0 mm；獨立上界最小 243.3 mm |
| 屏障 | 輸入違反 0、輸出違反 0（全程未介入） |
| 自碰撞（取樣，53 配對） | 最小 **10.37 mm**（link4 ↔ link6） |

姿態在前 3 s 由手臂處理完，底盤同時把整體位移帶過去，最後一起減速停穩。屏障全程未介入
（最近處離箱體 231 mm，遠高於 §10.13 的 89 mm 停住點），**本輪對屏障不作任何宣稱**。

#### 另一個預設值錯誤：構形參考

文件寫參考構形是 §10.14 檢查過的預抓取臂形，程式卻預設為**起始姿態**。構形正則項於是拉向「維持
折起來」、任務拉向箱子，解算停在兩梯度抵銷處：位置誤差 131 mm、姿態 22°、所有速度為零、
屏障全程未介入，只留下一個逾時。已改為預設檢查過的構形，並加入**停滯偵測**——命令連續低於
門檻但誤差仍大時直接報「收斂到非目標的駐點」，不再偽裝成逾時。

另：teleport 會讓 `/joint_states` 停頓數百 ms，緊接著歸位會觸發自身 300 ms 新鮮度保護，手臂
被留在上一輪結束處並**悄悄成為下一輪起始姿態**。`run_wholebody_demo.sh reset` 已改為先等
資料穩定再歸位。

#### 機器關機事件

機器在 13:24 於 gz 與堆疊執行中（load ≈ 10）關機，停機 10 小時後於 23:31 開機；關機前最後的
kernel 訊息是 ACPI 熱框架錯誤（`\_TZ.ETMD`、`_SB.IETM._OSC`）。**不能據此斷定是過熱**，但
符合使用者一再提醒的風險。之後的執行由使用者親自啟動模擬，全程監看 load 與
`/joint_states` 間隔。

#### 追加：起始姿態改為全零（使用者指示）與三個缺陷

詳見 `evaluation/results/wholebody_pregrasp_20260908.md` 的追加段。摘要：

**全零起點可行。** joint3 全程最小 **+0.0002**（保護門檻 −0.0111），限位避讓項撐住了；位置
808 → **0.7 mm**、姿態 89.5° → **0.000°**、底盤仍停在 **−0.1505**、同動累計 **3.40 s**
（最長連續 2.97 s）。代價是**整趟最緊的自碰撞點就是起點**（取樣 9.34 mm，對比 joint3 = 0.30
起點的 10.37 mm）——這是全零固有的性質，不是可調掉的。

**缺陷一：資料逾時門檻壓在模擬器真實停頓上。** 獨立監看程序（只訂閱、不做事）量到
`/joint_states` 141.7 Hz／中位 7.0／p99 8.0 ms，另一窗口卻有 **332 ms** 停頓；`/odom`
29.3 Hz／中位 34.0 ms，最大 **309.6 ms**。300 ms 門檻正坐在這個停頓分布裡，於是在真實但無害
的打嗝上中止執行。門檻改為可設定的 `--max-data-age`，預設 **0.50 s**。**這不是調鬆安全檢查**
——它的作用是「測試放棄資料」，真正的安全停止是閘門的 150 ms 單調 watchdog，獨立程序、完全
未動。此值屬於這台模擬器，上硬體要調回並重新量測。過程中我先誤判為節點 executor 被餓到而改用
ReentrantCallbackGroup ＋ MultiThreadedExecutor，**那是修錯地方**（獨立程序也看到同樣停頓），
該修改保留但不是原因。

**缺陷二：底盤速度用錯時間差。** 先前報「超限 2 週期、峰值 0.3239 m/s」是量測錯誤——把 odom
位置變化除以控制迴圈 dt（53 ms），而 odom 自己走 34 ms。改用 odom 時間戳後最大 **0.2700 m/s、
超限 0**，與命令端完全一致。與先前 TCP 速度那個同類：**用接收端時鐘差分帶自己時間戳的資料**。

**缺陷三：輪子懸空 0.05 m（未修）。** 模型靠四顆看不見的支撐球站著。實測 TF：輪圈
`rim_*_link` 在 world z = 0.100、半徑 0.05 → 最低點 **+0.050**；支撐球 `support_*` 在
world z = 0.050、半徑 0.05 → 最低點 **0.000**。原因在 `omni_bot.urdf.xacro:73`：球的關節原點
是 `${-wheel_r}` 而半徑也是 `${wheel_r}`，最低點落在 base_link 下方 0.10，輪圈最低點只在下方
0.05——**支撐球比輪子低整整一個輪半徑**，與生成高度無關。**未修**，因為修了車身降 0.05 m，
肩部跟著降，預抓取目標相對肩部改變，IK 解／構形參考／底盤停放全部要重算。改法為該行改成
`xyz="${x} ${y} 0"`，同時生成高度 `-z` 由 0.05 改 0。與先前記錄的底盤 collision／visual 不
一致是相關但不同的問題。

#### 尚未處理

- 單一目標、單一起點；未掃描不同目標方位與起始位置。
- 屏障未介入，本輪對屏障無任何宣稱。
- 自碰撞為取樣篩檢；未做整段路徑的精確三角面距離。
- 底盤 yaw 全程幾乎為零（目標在正前方），含轉向的情形未測。
- **輪子懸空未修**（見上）。

### 10.16 模型高度修正與狀態資料過期驗證（2026-09-08）

完整記錄 `evaluation/results/model_height_and_staleness_20260908.md`。
**§10.13–10.15 的數值屬於舊模型版本，保留但不可與本節混算。**

#### 我先前三個幾何說法的更正

1. **collision／visual 比較混了座標系。** 統一到 base_link：visual mesh z ∈ [0.000, 0.280]、
   collision 圓柱 z ∈ [0.050, 0.230]，半徑皆 0.300。正確說法是**圓柱上下各短 0.05 m**，不是
   先前寫的「0.10–0.28 對 0.00–0.28」。此項未修。
2. **接地面是從動滾子，不是輪轂。** 滾子中心距輪軸 0.0396、`roller_omni_wheel.stl` 半寬
   0.0090–0.0100 ⇒ 有效外半徑 **0.0486–0.0496**，與宣告的 `wheel_r = 0.05` 相符。我先前拿輪轂
   視覺半徑 0.0285 推導，「懸空 0.05 m」的結論對但**推導用錯半徑**，數字碰巧吻合。
3. **支撐球比輪子低整整一個輪半徑。** 關節原點 `${-wheel_r}` 配上半徑 `${wheel_r}`，球最低點在
   base_link 下方 0.10 而輪子接地線在 0.05，所以 gz 把模型撐在球上、輪子必然懸空，與生成高度
   無關。改為 `xyz="${x} ${y} 0"`。

**修正只改離地高度，不改變接觸模型**：輪圈與滾輪仍純視覺、接地仍是四顆零摩擦球、底盤仍由
`VelocityControl` 驅動。可驗證運動學與命令限制，**不能**據此宣稱打滑、牽引力或真實煞車。

**四處一起改**（舊值互相吻合，補償是隱形的，只改一處不會報錯只會讓控制器留在舊座標系）：
支撐球原點 −0.05 → 0；五個 launch 的 `-z` 0.05 → 0.0；`MODEL_Z` 0.05 → 0.0；
`--spawn-z` 與 `base_tf_bridge --z` 0.05 → 0.0。

**實測（新模型）**：`base_footprint` 世界高度 **0.0000**、支撐球最低點 **0.0000**、72 顆滾子
視覺網格最低點 **+1.45 mm**（修正前約 +51 mm）。模型 FK 對 Gazebo TF 六個 frame
**最大差 0.0000 mm**；全零自碰撞篩檢不變（9.09 mm）。

#### `pregrasp_reference` 必須重算

新模型原封不動重跑，停滯偵測報出「位置誤差仍 **47.4 mm**、姿態 0.46°，收斂到非目標的駐點」。
原因是該值為**舊車高**的 IK 輸出，車身降 0.05 m 後臂形已放不到目標，構形正則項與任務項互相抵銷。
重算後 [0, 0.6518, 0.5886, 0, −1.634, 0] → **[0, 0.3641, 0.5263, 0, −1.4086, 0]**，IK 殘差
位置 0.000 mm、姿態 0.000°。

#### 新模型回歸（同動預抓取，全零起點）

同動累計 **3.40 s**（最長連續 2.87 s）；位置 816 → **0.53 mm**、姿態 89.5° → **0.000°**；
底盤停放仍為 **−0.1505**；關節位置越界 0、關節速度超限 0（最大 1.2724）、底盤速度超限
**0/224 段**（最大 0.2505，限 0.2775，以 odom 自己的時間戳差分）；joint3 最小 +0.0003；
環境下界 231.0 mm；屏障 0 違反；自碰撞取樣最小 **9.36 mm**（起點本身）。

#### watchdog 不能替代狀態新鮮度檢查（更正先前說法）

我曾說「把 `max-data-age` 由 0.30 放寬到 0.50 s 不影響安全，因為真正的安全停止是閘門的 150 ms
watchdog」。**這是錯的。** 閘門只看「多久沒收到命令」，看不到那些命令用了多舊的狀態。

鏈路上實際的檢查：命令→閘門 150 ms／安全節點 250 ms；JointState→安全節點 300 ms；
距離點雲→300 ms（**限速降級，不是停止**）；底盤 TF→**本次新增 300 ms**。`max_tf_age` 之前
**不存在**：`_base_q()` 取「最新可得」的 TF，發布端停掉後會無限期回傳最後一筆，不丟例外。

**故障注入實測**（命令全程 20 Hz 持續發布）：

| 故障 | 自 feed 停止到**命令歸零** | 安全節點 reason |
| --- | ---: | --- |
| JointState 停 | **392 ms** | 關節過期 |
| 底盤 TF 停 | **319 ms** | 無/過期 TF |
| /odom 停 | **435 ms** | 無/過期 TF |

三種故障底盤與手臂**命令**都同時歸零。**這是命令歸零時間，不是實際停止時間**；命令歸零後機器
多久真的停下、期間走了多遠，本輪未量。

**這些時間都超過停距公式用的 τ = 0.15 s（兩到三倍）**，而 τ 是**正常控制路徑**的反應時間。
**不能再說 τ = 0.15 s 已涵蓋資料失效情境**：正常控制延遲（0.15 s）與故障反應（0.32–0.44 s）
必須分開記錄，而「命令歸零→實際停止」與「故障期間位移」兩項**未量**。要宣稱故障期間也安全，
須補量這兩項並以故障反應時間重算停距。本輪不作此宣稱。**反事實**：把 `max_tf_age` 設為 1000 s 重現修正前行為，同一
故障下 **TF 年齡達 7937 ms、鏈路從未歸零**，reason 全程 `ok`，機器人持續執行用近 8 秒前底盤
位姿算出的命令，閘門全程未察覺。

測試本身也修了兩個缺陷：注入原本用同步 subprocess 阻塞約 1.5 s，期間測試自己不發命令而量到假的
「命令過期」；原本用 SIGKILL 觸發堆疊 fail-fast 導致整個堆疊收掉，改為 **SIGSTOP 凍結／SIGCONT
恢復**。

#### 底盤碰撞幾何對齊（含一個比圓柱嚴重得多的發現）

**`base_link` 從來就不在 `ARM_LINKS` 裡**：屏障約束十個手臂連桿，**車體本身沒有任何幾何約束**。
底盤固定時看不出來；底盤進入解算後，車體可以開進障礙物而每條屏障殘差都回報滿足。三件事一起改
才能讓 Gazebo／距離計算／安全檢查用同一版幾何：

1. **collision 圓柱 length 0.18 → 0.28。** 視覺 mesh 在 base_link 為 z ∈ [0.000, 0.280] 且
   **兩端都到滿半徑 0.300**（底盤 0.000–0.080、頂板 0.260–0.280），中間僅 0.180–0.200 是細柱
   （r 0.110）。原圓柱只涵蓋 0.050–0.230，**2688 個頂點有 1386 個（51.6%）在碰撞體外**。細柱段
   被填滿是保守方向，兩端沒涵蓋不是。
2. **屏障讀不懂 primitive。** `link_collision_tris` 只處理 `<mesh>`，所以把 `base_link` 加進去會
   **靜默無效**。新增 `_primitive_tris`，並以 `1/cos(π/seg)` 放大成**外接**多邊形——內接會讓算出的
   距離偏大，正是安全模型不能偏的方向（seg=32 代價 0.5% 半徑）。
3. **兩份 link 清單各自實作必然分家。** `arm_link_names` 自己也判斷「有無 mesh collision」，底盤
   進了取樣器卻沒進名單，啟動時撞上斷言。**若兩份長度剛好相同，斷言不會響，每列都會接到錯的
   連桿。** 已改為由 `link_collision_tris` 導出。

認證取樣新增 `base_link` 1200 點、ρ **28.12 mm**（撞到取樣上限，車體表面積約 1.09 m²；ρ 偏大是
保守方向）。

**列數上限 60 → 100**：把上限暫放 400 量得各連桿最大 band 為 base_link **90**、link2 77、
link4 62…，總列數最大 392。100 高於觀測最大值，但這是**單一情境的觀測，不是全姿態證明**，所以
丟列數照常每週期發布。另外 `barrier_stack.py` 自帶一份預設 60 會**覆蓋節點的值**，導致節點改成
100 後完全沒生效——已改為不傳就不覆蓋。**在 launcher 重複一次預設值，就是多一個地方會錯。**

耗時代價：solve 由（純手臂 179 列）p50 10.85 / p95 11.75 / max 13.18 ms 升到（含底盤 392 列）
**p50 12.13 / p95 26.75 / max 32.62 ms**；實際 dt p50 50.0 / max 51.0 ms，**無 deadline miss**，
但 50 ms 預算餘裕明顯變薄，列為已知風險。

**新幾何回歸（同一案）**：位置 816 → **0.54 mm**、姿態 89.5° → **0.000°**、底盤停放 −0.1505、
同動 3.29 s、底盤速度超限 0、屏障違反 0、模型下界最小 230.5 mm、丟列 0。

#### 距離資料過期：限速降級，不是停止

條件：JointState 與 TF 正常、命令持續發布、**只凍結 `arm_link_distance`**。

第一次測看不出變化，因為命令 0.05 **剛好等於** `nodata_speed_cap`。改以 0.20 m/s、0.4 rad/s 重測：

| | 故障前 | 凍結中（6 s） | 恢復後 |
| --- | ---: | ---: | --- |
| 手臂命令中位 | 0.4000 | **0.0306** | 12 ms 內回復非零 |
| 底盤命令中位 | 0.0135 | **0.0465** | — |
| NODATA 列數中位 | 0 | **274** | 0 |
| reason | ok | **ok（全程未變）** | ok |
| 命令歸零 | — | **從未歸零** | — |

**這不能算安全。** 障礙物距離完全未知時 0.05 m/s 仍足以撞上：本次 6 秒中斷，底盤以中位
0.0465 m/s 前進約可走 **0.28 m**。且 `reason` 全程 `ok`，**唯一可見訊號是 `n_nodata`**。降級期間
是否應改為停止、或至少讓 reason 反映降級，屬設計決策，本輪未改。

#### 尚未處理

- 「命令歸零」不是「實際停止」；命令歸零後的停止時間與位移**未量**。
- 故障反應 0.32–0.44 s 為 τ = 0.15 s 的兩到三倍，停距公式未以故障反應時間重算。
- 距離資料過期只降級不停止，且 reason 不反映。
- 列數上限 100 高於單一情境觀測最大值，非全姿態證明；底盤 ρ 28.12 mm 受取樣上限所限。
- 本輪屏障全程未介入（最近 230 mm），對屏障作用無任何宣稱。

### 10.17 有障礙的同動預抓取（2026-09-09）

完整記錄 `evaluation/results/obstacle_wholebody_20260909.md`。依既定驗收表落在**第三格**：
**安全停住，但無法到達——保護有效，任務完成能力不足**。但底盤確實**繞了 226 mm 讓開**，
只是停在離目標 225.9 mm 處。

#### 先收掉的兩個缺口

**距離資料整批過期改為停止（reason 7）。** 原本的限速降級**不是更保守**：實測 6 秒中斷期間底盤
命令中位由 0.0135 升到 **0.0465 m/s**，機器人在不知道任何障礙距離之後反而走得更快，因為
`nodata_speed_cap = 0.05` 比任務當下要求的還鬆。障礙距離未知時沒有任何速度站得住腳。
（更正：先前「0.0465 × 6 ≈ 0.28 m」只是風險量級示意，**中位數乘時間不是實測位移**；測試已改為
分別量命令歸零與實際停止及其間位移，新策略下尚未重跑。）

**帶被截斷改為報錯並停止（reason 8）。** `max_rows_per_link` 預設改為 **0 ＝ 不截斷**，完整帶照
發；若設了上限且咬到，距離節點在**雲中**送出 `STATUS_OVERFLOW` 一列（走消費端會讀的通道），
安全節點輸出零。涵蓋論證是對整條帶成立的，留下最近 N 條再宣稱涵蓋並不成立。實測完整帶 361 列、
丟列 0。

#### 障礙位置是算出來的

**「擋住直線中段」不存在。** 路徑 0.55 m、底盤有效半徑 0.328 m（0.300 ＋ ρ 28 mm），兩端淨空圓
沿整條路徑重疊；要只擋中段需路徑 ≥ 2 × 0.328 = 0.656 m。要求起點模型間距 ≥ 30 mm 時最深侵入僅
**8.1 mm**（雜訊），≥ 35 mm 則無解。第一次擺的位置起點僅 4.8 mm，執行時被 20 mm 中止門檻擋下。

改為**擋住底盤原本想停的位置**（問題更利）。`obs_1` 中心 (−0.225, −0.320)、尺寸
0.16 × 0.20 × 0.60：起點 **+124.0 mm**、名義停放點 (−0.15, 0) **−108.1 mm**（被擋）、
偏移 (−0.15, +0.15) +41.9 mm、+0.20 +91.9 mm。十二個 +y 偏移位姿離線**全部**通過 IK 與路徑檢查
——所以「到不了」是協調問題，不是任務不可能。目標箱 `obs_0` **未移動**；障礙寫在世界檔，距離節點
由同一份檔案產生規格。

#### 實測

底盤 (−0.700, 0.000) → (−0.150, **+0.226**)；**輸入違反 1105/1154 週期**、最大 resid_in
**+58.80 mm/s**、**輸出違反 0**、最大同時作用 **83 列**；模型下界最小 **110.5 mm**（無接觸）；
姿態 90.0° → **0.00°**；位置 816.9 → **225.9 mm**（未到達）；作用期間命令 |max| 0.1000 → **0**。

TCP 停在 (0.30, **+0.2259**, 0.55)，誤差**完全在 y**（−225.9 mm，x/z 皆 0），數值等於底盤側移量。

#### 為什麼停住：任務控制器只指向被擋住的方向

停滯時 `cmd_in` = 底盤 [0, −0.1, 0]、手臂 **[0,0,0,0,0,0]**。**手臂命令在進入屏障之前就是零**
——不是屏障停掉手臂，是解算器從未要求手臂做任何事；屏障只擋掉底盤那個 −y 要求。

而手臂做得到：從停滯的底盤位姿解 IK **成功、整段路徑通過**，需要臂形
[−0.8615, 0.7031, 1.1004, −0.9013, −1.3161, 0.3083]，與現況差最大 **0.901 rad**，底盤完全不必
再動。

機制是三件事疊起來：任務誤差全在 −y；加權最小平方把它全派給底盤（`base_weight = 3` 較便宜）而
底盤被擋；構形正則項殘差 **0.00001 rad** 已滿足，正把手臂釘在原形狀。**權重是偏好，不是備援。**
**沒有調低 margin 讓它通過。**

#### 撤回：「單步做不到，要靠 W-GMPC 看未來」

本節初稿有這句，**證據不支持，已撤回**。離線對照
（`evaluation/compare_stall_solvers.py`，資料 `stall_solver_compare.json`）在停滯快照上重建
屏障列（290 條屏障、326 條總約束），以同一狀態、同一約束集合求解兩次，指標為末端位置誤差的
方向導數 $\mathrm{d}|e|/\mathrm{d}t$（負值＝誤差縮小）：

| 求解方式 | 姿態權重 | 底盤 (vx, vy, ωz) | 手臂 \|max\| | d\|e\|/dt | 違反 |
| --- | ---: | --- | ---: | ---: | ---: |
| **A** 任務求解 → 投影（現行） | 1.0 | (0, 0, 0) | 0.000 | **−0.0025 mm/s** | 0 |
| **B** 單次約束式求解 | **1.0（同上）** | (0.000, −0.041, −0.038) | 0.013 | **−54.83 mm/s** | 0 |
| B | 0.1 | (0.003, −0.038, −0.189) | 0.159 | −90.49 mm/s | 0 |
| B | 0.0 | (0.004, −0.037, −0.205) | 0.191 | **−96.59 mm/s** | 0 |

**在完全相同的姿態權重下** A 停滯、B 取得 −54.83 mm/s，兩者滿足同一組約束。**主要成因是架構，
不是姿態權重，也不是缺少多步預測。** 把姿態降為次要目標再多拿 −96.59 mm/s，是次要因素。

**機制**：兩階段**各自都最佳化了自己的目標**（第一階段最小化任務誤差、不含約束；第二階段最小化
與第一階段答案的距離、不含任務），但**這個組合不等價於「在安全可行集合內最佳化原任務目標」**
——那才是想要的東西。無約束最佳解指向被擋方向時，離它最近的可行點可以幾乎為零，即使存在能大幅
推進任務的可行速度。B 在 μ=1.0 找到的動作含**底盤旋轉** −0.038 rad/s；**投影並非不能產生旋轉**，
它的目標是貼近原命令、沒有直接獎勵旋轉帶來的任務進展，本次沒選到不代表永遠不會選。

**這個對照不能推論的事**：只證明停滯當下**存在可行下降方向**且單次約束求解找得到，**反覆施用
是否收斂未證明**；μ ≤ 0.1 才有較大手臂速度**是此快照下的觀察，非通用必要條件**；也**不能**推論
「所有單步方法都不會卡」或「W-GMPC 一定能解」——**若 W-GMPC 沿用同樣強的構形偏好、或預測問題
沒納入障礙約束，會卡在同一個地方**。

#### W-GMPC 的研究問題（改寫）

> **在障礙與硬體限制下，透過多步全身預測，提前調整底盤路徑與手臂構形，改善任務完成能力。**

不是「只有 W-GMPC 才能讓手臂接手」。

#### 順帶修掉的潛在缺陷

`pregrasp_demo.target_pose` 原以「z 中心最低的箱子」選目標，加入第二個障礙後**靜默選錯**（底盤
障礙比目標箱矮），使離線檢查改瞄障礙物，對一個實際只差 226 mm 的執行報出 1006 mm。已改為按
名稱選（預設 `obs_0`）。所有 run 都以 `--target` 明確指定座標，執行結果不受影響。

#### reason 7／8 驗收（完成）

命令全程 20 Hz 持續發布。**reason 7（距離整批過期）**：注入 t=4.000 → reason 轉 7 於 **+365 ms**
→ **命令歸零 +417 ms**（期間底盤走 **36.0 mm**、最大關節 **55.6 mrad**）→ **實際停止**於歸零後
**+52 ms**（期間底盤 **0.00 mm**）。故障後手臂／底盤命令中位皆 **0.0000**（故障前 0.1500／0.1000）
——是**停止**不是降級；恢復後 110 ms 命令再度非零；故障後只出現 reason 0 與 7。

兩點限定：命令歸零時底盤仍 0.0589 m/s，是 reason 7 生效前一兩個週期 NODATA 列先觸發 0.05 上限
所致（與觀測一致，機制未逐週期證明）；**「命令歸零 → 實際停止 0.00 mm」是 gz `VelocityControl`
的性質**（速度直接寫入、無減速動力學），**實機上不會是零，不可外推**。

**reason 8（帶截斷）**：`max_rows_per_link=20` 必然咬到時，reason **8 佔 137/138 週期**，命令
全程 0.0000，機器人自始至終不動。

#### B 接進閉迴路（μ = 1.0，其他一律不動）

`--solver qp`：任務、碰撞、硬體限制進同一個 QP（OSQP），姿態仍為次要目標且**權重不變**；d_stop
線性化點沿用兩階段會交給濾波器的同一個 v_in，兩者約束集合相同；下游濾波器保留為最後檢查。

| | **A** 任務求解→投影 | **B** 單次約束求解 |
| --- | ---: | ---: |
| 位置誤差 | 816.9 → **225.9 mm** | 815.4 → **180.7 mm** |
| 姿態誤差 | 90.0 → **0.001°** | 89.5 → **2.723°** |
| 底盤終點 | (−0.150, +0.226) yaw **0.00°** | (−0.150, +0.211) yaw **−4.88°** |
| 手臂總位移 | ≈ 0 | **1.399 rad** |
| 屏障輸入違反 | **1105/1154** | **5/326** |
| 結果 | 停滯 | **停滯** |

**B 改善但未到達**：位置近 **45.2 mm**，用上 A 完全沒用的**底盤旋轉**與實質手臂動作——正是離線
對照預期的兩件事；代價是姿態誤差 0.001° → 2.723°（任務向量以 kp_p = kp_r = 1.0 把位置與姿態
混在一起，B 拿姿態換位置）。

**下游濾波器對 B 的修改**：前 20 週期中位 0.05455（最大 0.99920，發生在**第 0 週期**，濾波器
`v_prev` 由零起算、加速度盒壓成全零），之後迅速收斂，**200 週期後中位 2.2 × 10⁻⁴**——QP 的答案
幾乎就是濾波器會接受的答案，對比 A 是每週期被大幅修改。殘留差異已定位：**兩邊 `v_prev` 不同**
（QP 用驅動端上次發布、濾波器用自己上次輸出），修法是把 cmd_out 回授給 QP，本輪未改。

**成立**：架構診斷正確，同權重下改善 45.2 mm、啟用底盤旋轉與手臂、下游修正降到 2.2 × 10⁻⁴。
**不成立**：**B 在 μ = 1.0 下未完成任務**，還不能宣稱已有「整合安全約束的單步全身控制」基線。
下一個受控步驟是**只改 μ**，其餘不動；姿態誤差變差需一併處理。

#### 過程中發現的兩件事

**QP 曾整趟沒執行。** 第一次 B 與 A 逐位相同，因為 `self.link_names` 在 `__init__` 被後面一行清成
空 list，每列都通不過連桿索引檢查、約束集合為空、QP 分支被跳過，而執行**照常回報成功**。已修。

**關節被推到限位後回不來。** joint2 被推到 URDF 上限 2.443457075 後，以 −0.30 rad/s 命令 8 秒，
控制器確實收到（529 則）但位置紋風不動、回報速度恆 **0.00000**；同樣命令的 joint3 動了
−0.5874 rad——**只有觸限的關節卡住，不是整條手臂凍結**。只能重啟模擬脫困，底層原因未定位。
濾波器的關節限位列 `b = max(0, (hi − q)/dt)` 允許剛好走到 hi，與此行為合起來形成陷阱。

#### 正式基線前的兩項必要修補（已完成）

**一、QP 約束集合意外為空 → fail closed。** 原本 `_constraints` 在拿不到可用列時回傳 None，
QP 分支就落回無約束解，而**執行照常回報成功**（第一次 B 與 A 逐位相同即是此故）。現在改為丟出
`RuntimeError`：沒有運動學、沒有距離資料、連桿名單為空、沒有任何一列可用、約束集合為空、QP
未收斂——**任何一種都中止該趟並先停車**，不再有靜默回退。已用兩種情況驗證會硬錯誤。

**二、關節上限加入保守裕度。** `_joint_limit_rows` 的界改為 `(hi − m − q)/dt` 與
`(q − lo − m)/dt`，`SafetyConfig.joint_limit_margin = 0.02 rad`，**禁止命令剛好抵達 hi**。
已在限位裕度內的關節不會被困住：朝限位方向的界夾為 0，反方向仍完全開放。**底層卡限原因獨立
追查，尚未定位。**

#### μ 掃描結果（只改 μ，kp_p／kp_r 不動）

`evaluation/sweep_mu.py`，資料 `evaluation/results/mu_sweep/`。

| μ | 完成 | 秒 | 位置終 mm | 姿態終 ° | 屏障違反 | 濾波差尾中位 | 最小間距 mm | 關節裕度 rad | 底盤 m | 關節 rad |
| ---: | :-: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1.0 | 否 | 22.5 | 180.7 | 2.72 | 3 | 5.8e−4 | 95.9 | 0.0617 | 0.779 | 2.493 |
| 0.3 | 否 | 18.1 | 122.9 | 2.54 | 3 | 1.9e−3 | 95.1 | 0.0615 | 0.731 | 3.155 |
| 0.1 | 否 | 54.3 | 24.6 | 0.63 | 0 | 7.8e−4 | 94.4 | 0.0615 | 0.765 | 4.234 |
| **0.03** | **是** | **12.2** | **2.3** | **0.06** | 3 | 5.9e−3 | 88.8 | 0.0618 | 0.697 | 4.314 |
| **0.0** | **是** | **12.0** | **0.5** | **0.00** | 2 | 4.9e−3 | 88.2 | 0.0618 | 0.673 | 3.882 |

**單調，且在實測的 μ = 0.03 與 0 完成**（只有這兩點量過，**不能推論整個 μ ≤ 0.03 區間**）：位置終值 180.7 → 122.9 → 24.6 → 2.3 → 0.5 mm。μ = 0.03 與 0 在
**12 s** 內到達（位置 2.3／0.5 mm、姿態 0.06／0.00°）。

**μ = 0.1 是真的停滯**：t = 17.1 s 停在 24.6 mm，到 54.3 s 讀數一字不差（37 秒），最後才被一次
QP `maximum iterations reached` 以 fail closed 中止——停滯在先、求解失敗在後。

**姿態沒有被犧牲**：μ = 1.0 的 2.72° 是**停滯的症狀不是取捨**，μ = 0 時姿態終值 0.000°。
**因此不需另開一輪動 kp_p／kp_r。**

**安全全程保持**：最小間距 88.2–95.9 mm（無接觸），最小關節限位裕度 0.0615–0.0619 rad（遠高於
新加的 0.02），屏障輸出違反全為 0。**工作分配隨 μ 移轉**：關節總位移 2.493 → 4.314 rad，底盤
位移維持 0.67–0.78 m，底盤**轉角由 A 的 0° 變成 −47°**。**下游濾波器修改**：**尾段**中位 5.8e−4 至 5.9e−3——此數只描述尾段中位，**不代表全程修改都很
小**（μ=1.0 那趟最大 0.99920，在第 0 週期）。基線**必須包含實際使用的下游濾波器**。

**QP**（eps 1e−6／50000 迭代／polish）：μ = 1.0／0.3／0.03／0 全部 `solved`，迭代峰值
4100–25900；μ = 0.1 有 1 次 `solved inaccurate`。先前 8000 迭代 ＠ eps 1e−7 會在多個 μ 上失敗，
**那是數值問題，不可算到 μ 頭上**，故先以離線量測換掉設定再重跑。

#### 本輪成立：整合安全約束的單步全身控制（本案例）

同一個 QP 同時處理任務、碰撞與硬體限制，在**實測的 μ = 0.03 與 0** 下繞過障礙並到達預抓取位姿**
（12 s、位置 ≤ 2.3 mm、姿態 ≤ 0.06°），全程無接觸、無關節限位違反、下游濾波器僅作 10⁻³ 量級
修正。這是後續 W-GMPC 的公平對照基線：多步預測的貢獻要與此比較，而不是與架構分離的失敗比較。

**仍不能宣稱**：單一障礙位置／目標／起點；μ = 0.03 與 0 幾乎等於關閉構形正則項，長時間運行是否漂到不良
構形未測；μ = 0.1 的停滯機制未單獨分析；仍是靜態已知場景，不涉感知。

#### （原）μ 掃描計畫

`evaluation/sweep_mu.py`：**只掃 μ**，其餘一律不動；**不在同輪改 kp_p／kp_r**，否則改善無法歸因
給 μ。姿態誤差作為掃描的共同評估指標一併記錄。每個點都以相同方式復位（底盤 teleport 同一點、
手臂 home 同一構形），逐點記錄六項：

完成與否／完成時間；位置與姿態的終值與峰值；屏障輸入違反數；QP 與濾波器輸出差異（全程中位、
尾段中位、最大）；最小安全裕度與最小關節限位裕度；各自由度總位移（底盤位移與轉角、逐關節）。

以上一輪 μ=1.0 的資料試算，六項均可取得：未完成、21.3 s、位置終 180.7／峰 815.4 mm、姿態終
2.72／峰 89.46°、輸入違反 5、濾波差尾段中位 5.86 × 10⁻⁴、最小間距 96.0 mm、最小關節裕度
0.0616 rad、底盤 0.771 m／0.095 rad、關節總位移 2.483 rad。

若沒有任何 μ 同時達成任務、安全與可接受姿態，再另開一輪只調位置／姿態尺度或權重。

#### 尚未處理

- 僅一個障礙位置、一個目標。
- **關節觸限後無法退出的底層原因未定位**（已加裕度迴避，非解決）。
- 屏障有效性宣稱僅限本案例的靜態已知場景，不涉感知。

### 10.18 基線凍結與重複測試（2026-09-09）

完整記錄 `evaluation/results/baseline_B_frozen_20260909.md`。**主要基線 μ = 0.03，補充對照 μ = 0。**

#### 凍結

程式版本 git `6ef269be65ee`；控制器 `wholebody_pregrasp.py --solver qp`；OSQP
`eps = 1e-6 / max_iter = 50000 / polish`；`base_weight 3.0`、`kp_p = kp_r = 1.0`、`damping 0.06`、
`kp_post 1.2`、`k_limit 1.5`；容差 `tol_p 5 mm / tol_r 0.02 rad / settle 2 s`；場景
`arm_barrier_test.sdf`（目標 `obs_0`、底盤障礙 `obs_1`）；起點底盤 (−0.70,0,0)、手臂 `test_start`
全零；**下游濾波器保留於迴路中，是基線的一部分，比較時不得略去**。原始紀錄在
`results/mu_sweep/` 與 `results/baseline_repeat/`。

**用詞更正**：μ 加權的是**關節構形正則項**（把手臂拉向 `q_pref` 並避開限位），**不是末端姿態
追蹤**；末端姿態由任務項的 `kp_r` 處理。μ = 0.03 與 0 幾乎等於**關閉構形正則項**。

#### 重複測試（相同復位，3 次）

**3/3 完成**；完成時間 **12.194 ± 0.012 s**、位置終值 **2.250 ± 0.020 mm**、末端姿態終值
**0.059 ± 0.002°**、最小安全間距 **88.817 ± 0.001 mm**。重現性良好。

#### 計時：QP 不是瓶頸，迴圈結構才是

| 量 | p50 | p95 | max |
| --- | ---: | ---: | ---: |
| QP 求解 | 2.06 ms | 5.91 ms | 15.58 ms |
| 整體 `solve()`（重建約束＋QP） | 10.18 ms | 14.62 ms | 24.69 ms |
| **實際發布間隔** | **61.19 ms** | **65.37 ms** | **74.86 ms** |

標稱週期 50 ms，**超時比例 100%**。歸因明確：驅動迴圈**先 drain 滿一個標稱週期再做事**，實際
週期恆為「50 ms ＋ 工作量（中位約 11 ms）」。**超時來自 harness 的迴圈結構，不是 QP**——QP p95
5.9 ms、整體求解 p95 14.6 ms 都塞得進 50 ms。**因此不能宣稱滿足即時控制**；修法在迴圈不在求解器。
**迭代峰值不是排程指標**，不能用來論證超時與否。

#### 舊求解器設定的失敗：另列為數值診斷

`eps = 1e-7 / max_iter = 8000 / polish=False` 時 μ = 1.0/0.3/0.1 皆以「QP 未收斂」被 fail closed
中止；離線在同一狀態比較設定後確認為**數值問題**（同樣問題在 `eps = 1e-6` 下最多 2575 次迭代
收斂）。**該批失敗不構成 μ 的性質**，與掃描結論分開記錄。新設定下的掃描仍是單變數實驗。

#### 後續比較規則

W-GMPC 以**本基線（B, μ = 0.03）為主要比較對象**，A（任務求解 → 投影）保留為**架構分離的消融
對照**。比較須統一：任務容差、硬體與碰撞限制、**實際使用的下游濾波器**、計算預算。

#### 排程修正版（凍結為後續比較與移植用的版本）

凍結版迴圈先 drain 滿一個標稱週期再做事，週期恆為「50 ms ＋ 工作量」。修正版改為**固定截止
時間**：先做事、再 drain **剩餘**時間；錯過時槽則**記錄並跳過**，不補發——補發會把數個週期的
速度連續送上線，那是另一條軌跡。`--sched legacy` 可重現凍結版行為。

**加速度盒的 dt 一併修正**：`|v − v_prev| ≤ a_max·dt` 原本用標稱 50 ms 而實際週期 61 ms，
**比硬體真正允許的緊 18%**（保守但錯誤）；改用實測發布間隔，與下游安全節點本就採用的實測時間
一致。

| 量 | legacy | **deadline** |
| --- | ---: | ---: |
| 完成 | 3/3 | **3/3** |
| 完成時間 s | 12.194 ± 0.012 | 12.134 ± 0.023 |
| 位置終值 mm | 2.250 ± 0.020 | 2.293 ± 0.008 |
| 末端姿態 ° | 0.059 ± 0.002 | 0.055 ± 0.001 |
| 最小間距 mm | 88.817 ± 0.001 | 88.816 ± 0.000 |
| **發布間隔 p50** | **61.19 ms** | **50.0 ms** |
| p95 / max | 65.37 / 74.86 | **52.6 / 61.8 ms** |
| **錯過截止／跳槽** | 結構上恆超時 | **0 / 0** |
| `cfg_dt` | 固定 50（實際 61） | 實測 p50 50.0 |

**本次三次測試未觀察到任務與安全指標的實質退化**（位置差 43 µm、間距差 1 µm）；三次樣本不足以
排除更小或更罕見的影響。**驅動端固定截止一次都沒錯過**；彙總的
5.5% 是「發布間隔 > 52.5 ms」的抖動定義，243 週期中 13–14 個超過 52.5 ms、0–1 個超過 60 ms。
**可報告的是 20 Hz 排程在本樣本的實測達成情況，不是硬即時保證。**

#### Isaac Sim 移植準備（不需 Isaac 的部分，已完成）

`evaluation/results/isaac_port_checklist.md`：介面清單**由執行中系統實測**（topic 型別、關節
名稱與順序取自控制器本身、frame 的 world z），並列出四關驗收表——**關節命令 → 底盤命令 →
停止行為 → 同案例閉迴路**，每關的判準與 Gazebo 基線數值都寫進去。特別標明不可沿用的三項：
Gazebo 的停止距離（`VelocityControl` 直接寫速度）、`max_data_age = 0.5 s`（依 Gazebo 的停頓分布
量出）、輪地接觸模型（Gazebo 是零摩擦球＋純視覺輪）。**Isaac Sim 尚未安裝於本機**，屬環境待辦。

#### 已知限制（保留，不阻擋 Isaac Sim 移植）

- 成功的是**實測的 μ = 0.03 與 0 兩點**，不能推論整個 μ ≤ 0.03 區間。
- 單一障礙位置／目標／起點；靜態已知場景，不涉感知。
- ~~控制週期 100% 超時~~ **已修**（固定截止時間排程；實測中位 50.0 ms、無錯過截止）。
  仍非硬即時保證。
- μ = 0.1 的停滯機制未單獨分析。
- **關節精確觸及 URDF 上限後無法退出**，底層未定位，已加 0.02 rad 裕度迴避。
- 下游濾波器修改量僅**尾段**中位可述（5.8e−4 至 5.9e−3），全程最大曾達 0.99920（第 0 週期）。
- Gazebo 以 `VelocityControl` 直接寫入速度，**停止距離不可沿用至 Isaac Sim 或實機**。

### 10.19 底盤輸入輸出重測與指標來源核對（2026-09-09）

完整記錄 `evaluation/results/metric_source_audit.md`，資料
`evaluation/results/gz_base_stop_{zero,silence}.json`（含逐筆命令與實測軌跡，供 Isaac 重跑
相同輸入）。

#### 撤回一項解釋

先前寫「`VelocityControl` 直接寫速度、沒有減速動力學，所以停止距離必為零」。**該推論撤回。**
原本量到的零位移仍是觀測結果，但**原因與量測解析度都要重新確認**。

#### 一個使測試無效的環境問題

橋接未啟動期間 gz 端 `/cmd_vel` 顯示 **No publishers**，ROS 命令從未送達，底盤跑的是更早以
`gz topic` 原生發布的殘留命令。**該期間所有量測作廢**（含「追蹤率約 30%」「零命令無反應」
「停止發布無反應」），不納入比較。本輪先以速度變更驗證送達：命令 0.05 / 0.15 / 0.00 →
實測 0.0500 / 0.1500 / 0.0000，逐一跟隨。

#### 命令與實際運動的分離（實測）

| 工況 | 命令 | 實測 | 追蹤率 |
| --- | ---: | ---: | ---: |
| 開闊直線（y = 3.0，無接觸） | 0.100 m/s | **0.1000 m/s** | **100.0%** |
| 擦過 `obs_1`（有接觸） | 0.100 m/s | 0.031–0.054 m/s | **31–54%** |

**無接觸時命令與實測一致；有接觸時顯著分離。** 100.0% 是本次量測精度下的追蹤率，支持「此無接觸
工況下速度維持在命令值」，**不足以單獨證明每個物理步的覆寫機制**。先前非單調的速度曲線
（53%→31%→35%）是底盤擦過 `obs_1` 的接觸阻力，不是致動特性。

#### 停止：zero 與 silence

| | zero（發布零命令） | silence（停止發布） |
| --- | --- | --- |
| 零命令 → 首次判定低於門檻 | **34 ms** | **4 s 觀測窗內未低於門檻** |
| 該區間 odom 淨位移 | 0.00 mm（最後非零命令起算 2.40 mm） | 持續以 0.1 m/s 前進 |

明確送出零命令能使底盤停止；停止發布後**在 4 秒觀測窗內未觀察到命令逾時或減速**（更長時間未
測，不寫成「沒有逾時機制」）。**這不能直接驗證 `arm_vel_gate`**：第二階段全身堆疊中
`arm_vel_gate` 是 `/cmd_vel` 唯一發布端；第一階段導航中則由 `gmpc_node` 發布，該節點在無計畫、
到達容差內或 TF 失敗時自行發零——**但那是正常執行時的保護分支，不是 watchdog**：節點卡住或退出
後即失效，而 `VelocityControl` 會保持最後命令。第一階段底盤路徑**未涵蓋發布端本身失效**，此情境
列入 Isaac 的停止驗收。

**實際停止時間與距離仍受取樣限制**：`/odom` 設定 30 Hz，較高頻真值 `/world/.../pose/info` 約
58 Hz，均不足以解析毫秒級停止過程。**毫秒級停止時間保留為未解析的限制。**

#### 重複位姿樣本：先前判讀已更正

先前寫「28% 重複樣本會製造假的零速度」，**核對後不成立**。重複幾乎全部發生在**車已停住之後**
（zero 模式：零命令前 0.0%、零命令後 99.1%；**確定仍在運動的區間僅 1.7%**），且時間戳照常前進。
這是停車的正常表現，反而支持 zero 模式確實停住。停止門檻不再由「最小觀測步進」自動推導——
**最小步進不是量化解析度**（同批資料最小 2.400 mm、中位 3.400 mm）。

#### 指標來源核對：發現兩處必須更正

**一、進度報告的「jerk p95」其實是命令加速度的 p95。** `analyze_new_TEST.py` 計算
`accel = diff(cmd_vec)/dt` 後取 `p95(|accel|)`——**一階差分是加速度不是 jerk**（程式自己的說明
也寫 accel），且取自 **`/cmd_vel`（命令）**而非實際運動。**數值本身沒錯，錯的是名稱與被賦予的
意義。** 建議改稱**「命令加速度 p95」**；若要 jerk 須另算二階差分並標明命令或運動。**不需重跑
實驗，原始 bag 仍在，重算即可。**

**二、`smooth_*` 與 `jerk_*` 都來自命令**，與「實際運動平滑度」不是同一件事。無接觸時兩者接近，
有接觸時分離（本輪實測 31%）。凡把這些數字說成「機器人運動有多平滑」的敘述都應改為
**「命令有多平滑」**。

其餘指標來源正確：間距／碰撞取自 `/model/<name>/pose`（**實測**）；

> **2026-09-09 更正（見 §10.23）**：上面把 `path_length_m`、`tracking_rmse_m` 歸為
> 「取自 `/odom`（實測）」**不正確**。`/odom` 由 `omni_drive_controller` 產生，
> 它只訂閱 `/cmd_vel` 並積分，是**命令積分得到的里程計估計，不是獨立實測**。
> 這兩項指標的來源必須重新標明為「命令積分」。歷史數字保留不動；
> Isaac 量到的 +7.6% 只適用於該次 Isaac 試驗，**不能拿來修正 Gazebo 的歷史結果**。第二階段的 TCP 速度、底盤速度、關節速度皆取自**各自 feed 的
時間戳**差分（**實測**）。

**安全計算本身作用在命令上**，這是設計而非疏漏：速度盒、加速度盒、jerk 盒都限制「要求硬體做
什麼」。**屏障是唯一混合項**——矩陣由實測姿態建立、不等式作用於命令速度，因此它**保證命令滿足
約束，不保證實際運動滿足**；後者需另外量測（見上方接觸工況的 31%）。

### 10.6 決策：加大預抓取距離＋低速接近，不降低全域 margin

$d_{\text{stop}}$ 在零速時為 0.08 m，小於預抓取距離 0.12 m；衝突只在以 0.3 m/s
接近時出現，速度下降後 $d_{\text{stop}}$ 隨之下降。**因此這不是不可解的安全／任務
衝突**，方向為：

1. 預抓取距離增至約 **0.20 m**。
2. 接近目標時限制**法向**接近速度。
3. **不降低全域 $d_0$。**

真正接觸把手時需要**任務階段化安全語意**，而非放寬所有障礙物：

| 階段 | 語意 |
|---|---|
| Transit／Pre-grasp | 所有表面均禁止接觸，保留完整安全距離 |
| Guarded approach | 僅指定把手接觸區允許低速接近；其他連桿與櫃體維持完整約束 |
| Contact／Manipulation | 目標把手改由接觸速度、力或位移限制管理；非目標表面仍由安全層保護 |

### 10.7 後續順序

1. ~~記為首次整合基線~~ ✅
2. ~~拆分各類約束殘差，定位 +2.01~~ ✅
3. ~~消除屏障殘差與 unresolved 週期~~ ✅ §10.8：獨立 LP 判定為可行，屬求解器
   執行問題；修 3 個缺陷後殘差 +0.00e+00、unresolved 0、fallback 0
4. ~~預抓取距離改為 0.20 m~~ ✅ §10.9：到達率 2/8 → 5/8，殘差維持 0
5. 加入法向低速接近策略（**擋住**：須先完成 §10.11 的殘餘項並重新定案 standoff）
6. **以相同 8 組起始姿態重跑，不得更換樣本**
7. 之後才設計把手的 guarded-contact 模式

---

## 十一、下一步

1. ~~§3.3 模型驗收~~ ✅
2. ~~動態 LiDAR Self-filter~~ ✅（第 1 項）
3. ~~連桿 3D 距離介面~~ ✅（第 2 項）
4. ~~連桿速度安全約束~~ ✅（第 3 項）—— 離線核心＋ROS 節點＋在環驗證皆完成
5. **實機限制集合**（第 4 項，進行中）：靜態重力力矩模型已完成並驗證；
   下一步為動態逆動力學。速度／加速度仍為手冊值。
6. **固定底盤預抓取控制**（第 5B 項）—— 首次整合基線失敗，到達率 1/8，見 §10。

**全身同動前仍須完成（不阻擋 5B）**：底盤輪速耦合、header 時間戳、正式整合 launch、
端到端延遲 p95 52.2 ms。

**待辦（不阻擋目前進度）**：端到端延遲 p95 52.2 ms 略高於 50 ms 控制週期，
進入高速全身同動前須改善或納入停止距離。

### 10.20 Isaac Sim 移植 Gate 0／Gate 1：純底盤幾何與直接速度控制（2026-09-09）

完整記錄 `evaluation/results/isaac_gate0_20260909.md`、`isaac_gate1_20260909.md`，
`isaac_port_checklist.md` 第六節已回填實測；腳本 `evaluation/isaac_base_check.py`。

**Gate 0（幾何、根部、關節清單）通過。** base_link 碰撞圓柱直徑 0.6000 m、高 0.2800 m，
與 URDF 差 0.00 mm。整機視覺 AABB 與碰撞 AABB 分開比較：視覺 z +0.0009…+0.3300、
碰撞 z 0…+0.3750（相機碰撞盒比視覺頂高 45 mm）。所有視覺件的 x/y 半寬都不超過底盤圓柱
半徑 0.300（滾輪最遠 0.264），沒有輪子或感測器造成的尺寸誤判。articulation root 唯一，
位於 `/World/omni_bot/Geometry/base_footprint`，`num_dof = 0`（底盤唯一自由度是浮動基座）。
USD 84 剛體／83 fixed joint 對 URDF 93 links／92 joints，差的 9 個是無幾何的相機座標框被
匯入器併除——這些 frame 在 USD 裡沒有 prim，之後接相機不能沿用 URDF 的 frame 名稱。

**匯入過程有四處會靜默出錯**，都已在腳本內修掉並記錄：變體選擇懸空（根層選 `"physx"`，
變體集只有 `none`/`physics`，物理整組沒被組合，表現為「找不到 articulation」）、
link 子樹 `instanceable` 導致 `stage.Traverse()` 不下探、碰撞幾何 `purpose="guide"` 被預設
bbox cache 濾掉、以及 `<gazebo>` 標籤完全不被讀取。

**Gate 1（直接速度控制）通過，但先修正了一個會讓結論反過來的前置問題。** URDF 的支撐球零摩擦
寫在 `<gazebo><mu1>0.0</mu1></gazebo>` 裡，importer 不讀，PhysX 退回預設摩擦，追蹤率只有
36.8%／37.4%／20.6%。在 Isaac 端補上 mu=0 材質（支撐球與地面都要綁，預設組合模式取平均）後，
前進／橫移／旋轉追蹤率皆為 **100.0%**，與 Gazebo 一致。

`apply`（`hold`／`once`，速度狀態的施加方式）與 `stop`（`zero`／`silence`，結束方式）已分成
兩個獨立維度，`silence` 段完全不呼叫 API：

| apply | stop | 前進 | 橫移 | 旋轉 | 結束後中位速度 | 4 s 位移 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| hold | zero | 100.0% | 100.0% | 100.0% | 0.001 mm/s | 0.00 mm |
| hold | silence | 100.0% | 100.0% | 100.0% | 100.017 mm/s | 398.08 mm |
| once | zero | 100.0% | 100.0% | 75.0% | 0.000 mm/s | 0.00 mm |
| once | silence | 100.0% | 100.0% | 74.9% | 99.909 mm/s | 397.64 mm |

三點結論：（一）**目前這條直接速度施加路徑，在停止呼叫後 4 秒內未自動停止**，滑行 398 mm。
本輪只測了 `set_linear_velocity`／`set_angular_velocity` 這一條，不能泛稱 Isaac 所有控制介面
都沒有命令逾時；Gazebo 在同窗內亦未觀察到逾時或減速。就此路徑而言 fail-safe 只能靠控制鏈主動送零；（二）送零在兩種施加方式下都停得住，殘餘
≤0.001 mm/s，但本輪沒有量停止過程本身的時間常數；（三）`once` 只影響旋轉，平移在 mu=0 下
寫一次就維持 100%，旋轉則依實測 exp(−0.0387·t) 衰減。USD 上沒有寫入任何 damping 屬性，
擬合衰減率 0.0387 s⁻¹ **疑似與預設角阻尼有關**，但在讀到有效阻尼值或做阻尼對照前不宜確定歸因。

**下一步**：接上第一階段控制器，在 Isaac 重跑原任務。

### 10.21 指標更名：命令加速度 p95（2026-09-09）

依 §10.19 的來源核對結果修正名稱，**計算本身未改、不需重跑實驗**。

- `jerk_{vx,vy,wz}` → **`cmd_accel_p95_{vx,vy,wz}`**。它是 `/cmd_vel` 的**一階**差分取 p95 絕對值，
  單位 **m/s²（vx、vy）與 rad/s²（wz）**。數值與第四次進度報告一致，只是名稱與單位標註更正。
- 另立**真正的 jerk**：`cmd_jerk_p95_{vx,vy,wz}`，`/cmd_vel` 的**二階**差分，
  單位 **m/s³、rad/s³**。這是新指標，第四次進度報告的數字裡從來沒有它，
  尚未在任何實驗上量測。
- `smooth_{vx,vy,wz}` 的說明改為**命令平滑度**，不代表實際運動平滑度（接觸工況下實測差 31%）。
- 既有 CSV 仍是舊表頭 `jerk_vx`，意義完全相同；`summarise_overnight.py`、
  `summarise_shield100.py` 改為兩種欄名都接受，**歷史資料不必重算也不會失效**。

改動：`evaluation/analyze.py`、`analyze_new_TEST.py`、`summarise_overnight.py`、
`summarise_shield100.py`、`plot.py`、`plot_3way.py`。

### 10.22 Isaac 接上第一階段控制器：端到端流程確認（2026-09-09）

完整記錄 `evaluation/results/isaac_controller_port_20260909.md`。
新增 `evaluation/isaac_bench_sim.py`（取代 `gz sim` 與 `ros_gz_bridge`）、
`isaac_common.py`（Gate 0／1 找到的匯入修正集中一處，避免日後分歧）、
`run_one_trial_isaac.sh`、`src/ammr_bringup/launch/isaac_dynamic.launch.py`。
Nav2、AMCL、GMPC、`omni_drive_controller`、`scan_relay`、`dynamic_obstacle_driver`
全部沿用第四次進度報告的同一份節點，只換模擬器。

**設定明列**：物理步 10 ms、RTF 節流至 1.0、GMPC 20 Hz（控制器參數未改）、
速度每物理步重寫（`hold`）、`/cmd_vel` 車體 twist 依 yaw 轉世界軸、
輪與地面綁定 mu=0、命令逾時關閉。前三項都照抄世界檔宣告值，不是自選：
不節流可跑 4.5×，但那樣控制器每個模擬秒拿到的 CPU 與 Gazebo 不同。
掃描增量 0.017501894 取自實錄 gz 掃描，非推算。

**致動核對（`ammr_base` 有 4 個可動輪關節，與 Gate 1 全 fixed 的 `omni_bot` 不同車）**：
平移 99.9%、旋轉 100.0%。自由空間直線命令 0.200 m/s 實測 0.1993–0.2023，
碰到 `obs_44` 後被擋下是真實碰撞。

**端到端通過**：`gmpc_cbf` seed 0 跑 150 s，由 (0,0) 導航至 (14.63, 13.79)，
路徑 23.83 m，topic 齊全，**RTF 1.000**（27000 步只落後 11 步）。

**兩項發現，必須先處理再談實驗組：**

一、**`/odom` 不是狀態資料，是命令推算。** `omni_drive_controller` 只訂閱 `/cmd_vel`
並 dead-reckoning 出 `/odom`。Gazebo 沒有機器人真值可對照，這件事一直沒被量到；
Isaac 新增 `/model/ammr_base/pose` 真值後實測：位置差中位 119.7 mm、p95 185.7 mm，
250 s 那趟 `/odom` 路徑長度**高估 +7.6%**（49.22 對真值 45.72 m）。
§10.19 把 `path_length_m`、`tracking_rmse_m` 記為「取自 `/odom`（實測）」**需要更正**。
依「任務結果與實際路徑使用狀態資料」的要求，Isaac 的任務結果應改用真值位姿，
命令平滑度仍取自 `/cmd_vel`。

二、**250 s 的原始案例與基線結果不同，原因未定。** t≈170–180 s 後真值與 AMCL 大幅分歧，
真值停在 (10.38, 0.29) 並在 t=180 後倒退，AMCL 卻報 (15.79, 16.44)。
速度比中位 0.997（各時段皆然）只支持「**大部分樣本**追蹤良好」，
**不能排除故障時段本身的執行問題**；p05＝0.35 也不能單獨證明發生接觸，
需要接觸紀錄或幾何證據對上時間戳才算數。
**在與 Gazebo 跑同一案例對照之前，不能說移植重現了基線，也不能說 Isaac 較差。**

### 10.23 最小配對診斷：Gazebo 對 Isaac 同一案例（2026-09-09）

完整記錄 `evaluation/results/paired_divergence_20260909.md`。
工具 `evaluation/diagnose_divergence.py`；Gazebo 端新增機器人真值
（`ammr_base.urdf.xacro` 加 `PosePublisher`，**純輸出**，與動態障礙物同一外掛，
發布 `/model/ammr_base/pose`，與 Isaac 同 topic）。`run_one_trial_gz.sh` 為
`run_one_trial.sh` 加上該 bridge 的版本。CPU 全程 ≤79 °C。

**`/odom` 的正式更正。** 它是**命令積分得到的里程計估計，不是獨立實測**；
§10.19 的來源分類已更正。歷史數字保留，Isaac 的 +7.6% 不外推到 Gazebo 歷史結果。
而且配對診斷顯示**這個 +7.6% 完全來自分歧之後**：分歧前 `/odom` 對真值的中位差
是 Gazebo 4.2 mm、Isaac 0.6 mm。命令積分在定位正常時準到毫米級，
它真正的問題是**沒有獨立量測可以察覺定位已經壞掉**。

**依序檢查的結果**：`map` 與模擬器 world 在兩邊都可視為同一座標系
（前 60 s |amcl−truth| 中位 66.6 mm／0.6 mm）；雷射對真值幾何逐束比對，
中位殘差 Gazebo −0.2 mm、Isaac +0.5 mm，**Isaac 的 PhysX raycast 通過了同一項檢查**；
動態障礙位姿間隔兩邊都是 50.0 ms。這三項排除。

**接觸：尚未排除。** 以底盤 0.4×0.4 的內接 0.200 m 判定，Gazebo 0/4142、
Isaac 0/5856 個樣本侵入，**但這只排除了該判準能確認的接觸**；角點可及 0.283 m，
落在兩半徑之間的樣本（62 與 586 個）**仍可能是角接觸**。
先前用速度比 p05＝0.35 推測「期間確有接觸」的講法不成立，撤回；
反過來標成「既非接觸」同樣沒有依據。

**首次分歧：兩個模擬器都會分歧。** truth vs `/odom`：Gazebo t=195.37 s、Isaac t=164.76 s；
truth vs AMCL：Gazebo t=199.32 s、Isaac t=161.76 s。map→odom 修正量最大
Gazebo 3.46 m／10.82°，Isaac 31.02 m／175.33°。

**機制線索（未定案）**：以真值算「比地圖預期更短的回波」比例，分歧前兩邊幾乎相同
（10.1% 對 10.5%），Isaac 分歧後升到 49.4%。Isaac 首次分歧當下最近物體是
`dyn_obs_2` 0.261 m 且**不在地圖內**，同刻未建圖回波 66%；Gazebo 首次分歧當下
最近三個物體全在地圖內，全程最大 26%。這只是對上時間戳的線索，
尚未量 AMCL 內部匹配品質。

**本輪配對的限制：兩趟不是同一個情境實例。** `dynamic_obstacle_driver` 是依回授位姿
ping-pong 的閉迴路且無種子，相位由啟動時刻決定；實測同一 sim 時間下兩趟障礙物
位置差 0.11–2.43 m。**因此結果差異不能歸因於模擬器。** 要做真正的配對，
須先讓障礙物相位可重現（改以模擬時間推算位置），那是功能變更，另記。

**未解釋**：Isaac 真值/命令速度比 p05＝0.35。命令跳變暫態的解釋不成立
（低比值樣本命令變化中位 0.0070，其餘 0.0113 m/s），但**角接觸仍未排除**。
另外，命令積分誤差變大**不一定由 AMCL 分歧造成**——兩者可能互相影響或有共同原因；
本輪只記錄先後關係，且兩個模擬器的先後次序相反（Gazebo odom 先、Isaac AMCL 先），
這本身就說明不能推定因果方向。`isaac_bench_sim.py` 已加記
速度狀態，下次重跑可判斷是位置差分的量測假象還是執行不足。

**實驗組維持不開。**

### 10.24 情境 v2：可重現障礙軌跡下的配對（2026-09-09）

完整記錄 `evaluation/results/paired_v2_20260909.md`。**未調整 AMCL 或控制器任何參數。**

**變更**：`dynamic_obstacle_driver` 保留回授式 ping-pong 為 `legacy`，新增 `scheduled`——
位置由模擬時間計算，相位零點由 `/case_start` 決定（收到即重設）。
設定 `config/dynamic_trajectories_v2.yaml`，兩個模擬器共用，固定起點／端點／速度／
初始方向／初始相位。行為差異明列：legacy 在距端點 0.20 m 折返，scheduled 正好在端點折返，
每端多掃 0.20 m，折返處為 2×speed 的速度不連續。

**量了實際軌跡才發現的三個問題**（都會讓情境無效，已修）：`/case_start` 的 DDS discovery
race 導致障礙物整趟不動；`/case_start` 發五次使相位重設五次、零點不明確；
Isaac 主迴圈每步只 `spin_once` 一次而流量剛好也是每秒 100 則，回呼飢餓使命令延遲，
障礙物在折返點落後排程最多 320 mm。三趟分別標記 `INVALID_no_case_start__`、
`SUPERSEDED_multireset__`、`SUPERSEDED_spinlag__`。

**對齊已用資料確認**：兩趟實際軌跡差 中位 1.5–2.9 mm、p95 3.2–7.7 mm、最大 60.2 mm
（legacy 同一比較為 110–2430 mm）。

**問題一：同一障礙軌跡下定位分歧是否仍出現。** Gazebo **不再分歧**
（truth vs odom 最大 0.036 m、vs AMCL 最大 0.225 m，皆未越過 0.3 m）；
Isaac **仍分歧**（AMCL t=159.36、odom t=165.61，與 v1 的 161.8／164.8 幾乎相同）。
map→odom 最大修正 Gazebo 0.80 m／3.85°，Isaac 15.67 m／73.87°。
座標系與雷射幾何兩邊依舊乾淨（中位殘差 +0.1／−0.0 mm）。
**但仍不足以歸因於模擬器**：只固定了障礙物，機器人路徑仍是湧現的，且各只有一趟；
可以說的是「情境不同」這個競爭解釋已被排除。

**問題二：低速度比是否也出現在速度狀態。** 是。速度狀態比值 p05 0.0617、
低於 0.6 的樣本 803/3265，位置差分為 0.0561 與 798/3264，其中 776 個樣本重合。
**不是位置差分的量測假象。** 且與間距強烈相關：低比值樣本最近障礙間距中位 0.253 m
（非低比值為 0.579 m），其中 **99.6% 低於外接半徑 0.283 m、0.0% 低於內接半徑 0.200 m**。
全程最小間距 0.240 m 落在兩半徑之間，與角部抵住障礙物相符。
**這是關聯與幾何一致性，不是接觸的直接證明**（無接觸力紀錄），
但它解釋了內接圓判準為何始終找不到東西：機器人中心從未靠那麼近。

**界線**：接觸仍未由直接紀錄證實；命令積分誤差與 AMCL 分歧的因果方向仍未確定
（本輪 Isaac 是 AMCL 先、odom 後）；各只有一趟。
**下一步**應加入接觸的直接紀錄，把角接觸由關聯升級為證據。
硬體：CPU 峰值 Gazebo 79 °C、Isaac 87 °C，低於 92 °C 中止線但逐輪上升。

**實驗組仍暫停。**

### 10.25 任務身分更正：§10.22–§10.24 移植的不是第四次報告的任務（2026-09-09）

**§10.22–§10.24 的 Isaac 移植與配對診斷，做在 `random_room_dynamic` ＋ `ammr_base` ＋
`run_one_trial.sh` 上。第四次進度報告的原任務不是這個。**

報告寫得很清楚：第三次報告用 `random_room_dynamic`（20×20、4 個移動體、**固定起訖點**），
第四次**新增 `bigarena`**（8 隔間、交錯門洞、10 個移動體、7 個未知靜物、**隨機起訖點**，
報告 §一、§95、§99）。執行方式也不同：

| | §10.22–24 做的 | 第四次報告的 |
| --- | --- | --- |
| 腳本 | `run_one_trial.sh` | `run_omnibot_dynamic.sh` |
| 機器人 | `ammr_base` | **`omni_bot`** |
| 世界／地圖 | `random_room_dynamic` | **`bigarena`**（`BIGARENA=1`） |
| 軌跡 | `dynamic_trajectories.yaml` | **`TRAJ=bigarena_traffic`** |
| 起訖點 | 固定 (0,0)→(17,17) | **`POSES_CSV` 依 seed 取列**（`bigarena_poses*.csv`，每檔 30 列） |
| 方法 | `gmpc_cbf` | `gmpc_scan` / `gmpc_scan_nosm` / `gmpc_truth` / `gmpc_nocbf` / `mppi` / `rpp` |

**原因**：我沒有先核對第四次報告與原始執行腳本，就依 `gazebo_dynamic.launch.py`
往下推進，那其實是更早期的設定。

**關於 seed 的更正**：`run_one_trial.sh` 的 `SEED` **只是 bag 檔名標籤**，不做任何隨機化
（腳本自身註解寫 `deterministic phase randomisation of obstacles is TODO`）。
真正的 seed 有兩個，都不在那支腳本裡：`run_omnibot_dynamic.sh` 的 `POSES_CSV` 依 seed
取一列起訖點；`generate_bigarena.py --seed`（預設 11）產生場景本身，報告 §654 說可逐位元重現。

**處理**：§10.22–§10.24 的紀錄與 bag 全部保留，並在檔頭標記為
「`random_room_dynamic + ammr_base` 移植診斷」，**不納入第四次報告的重現結果**。
通用結論（匯入修正、零摩擦、雷射 raycast 驗證、RTF 節流、回呼飢餓、可重現障礙相位、
`/odom` 是命令積分而非實測）可沿用，**但都要在正確任務上重新驗證**。

**重建三步**：（一）確認任務身分；（二）Isaac 開 GUI 顯示正確場景與機器人並核對幾何；
（三）對齊 `omni_bot_dynamic.launch.py` 的感知鏈與命令路徑，跑單一方法、單一起訖點。
`scheduled` 障礙模式**先不取代** `bigarena_traffic` 的原行為，需要時另立情境版本。

### 10.26 Isaac：bigarena ＋ 帶臂 omni_bot，seed 1 完整單案例（2026-09-09）

完整記錄 `evaluation/results/isaac_bigarena_seed1_20260909.md`。**n=1，是帶臂平台在 Isaac
的單案例完成驗證，不是第四次報告純底盤結果的重現，也尚未與 Gazebo 對照。**

**設定已凍結**：Isaac headless ＋ `limit_cpu_threads=8`、`bigarena`、`omni_bot.urdf.xacro`
（`use_arm/add_gripper/add_arm_camera` 皆 true）、手臂六軸全 0 位置驅動保持、`gmpc_scan`、
`TRAJ=bigarena_traffic`、`POSES_CSV` seed 1 (17.10,14.80)→(8.12,0.48)、
`omni_bot_dynamic.launch.py` ＋ `NO_GZ=1`、88 °C 中止線。

**結果**：停止原因 **`goal_reached_truth`**，任務 **60.3 s 模擬 / 79 s 牆鐘**，
**RTF 0.811（未維持即時）**，真值終點誤差 **0.249 m**（容差 0.25，餘裕 1 mm），
真值路徑 18.29 m，`/plan` 64 筆，非零命令 45.6%，
`|truth−odom|` 中位 0.0 mm、`|truth−amcl|` 中位 **39.5 mm**／p95 107.8 mm，
手臂保持誤差最大 **0.737 mrad**，|roll|max 0.00000°、|pitch|max 0.00005°，
溫度平均 77.4 °C、峰值 **80 °C** 未觸線。

**三處界定**：（一）`goal_reached_truth` 是**測試器**的判定，**GMPC 並未自行判定完成**——
停止前六個週期控制器自算距目標 0.345→0.290 m，全程最小 0.290 m，從未進入自身
`goal_tolerance_xy = 0.20`；（二）0.400 m 是機器人**中心**到**移動體表面**的淨距，
**只涵蓋 10 個移動體**，未量牆面與靜物，不可延伸為全場無碰撞；
（三）RTF 0.811 表示模擬比即時慢約 19%，模擬時間與牆鐘時間必須分列。

**與前一趟的差距是我比較錯了量，不是效能改善。** 前一趟報的 140.7 s 是 bag 的**總模擬時間**
（含目標前約 77 s 待機），任務時間其實約 63 s 模擬／81 s 牆鐘，與本趟 60.3 s／79 s 幾乎相同。
前一趟是在距目標 0.27 m、仍在逼近時被錄製牆鐘逾時切斷。
**該趟 bag 已在重跑前被我刪除**，上述任務時間為由時間戳與 RTF **重建的估計**，
「再一兩秒就會抵達」亦僅為推估；該趟**不再視為可重新驗證的配對資料**。

**本輪修正的流程缺陷**：計時拆分（錄製涵蓋啟動；任務時限改模擬時間、自收到 `/goal_pose` 起算；
牆鐘逾時另設）、`/goal_pose` 重複發布不重設起點（本趟 5 則只採第一則）、
停止時分別保存 `at_trigger`（到達判定用）與 `after_stop`（僅描述停機）、
停機順序改為先歸零步進再保存再清理。

**下一步**：Gazebo 同案例對照，需沿用同一帶臂模型、手臂姿態、seed 與完成判準，
並處理兩項差異——雷射近似（Isaac 穿過四根柱子，遮罩後 `/scan` 可比、`/scan_raw` 不可比）
與計算時間條件（RTF 不同則每模擬秒的計算時間不同）。

### 10.27 更正：§10.26 的到達判定與 GUI 溫度資料點（2026-09-09）

**一、`goal_tolerance_xy` 實際是 0.30，不是 0.20。** §10.26 寫「控制器從未進入自身容差、
未宣告完成」是錯的。0.20 只是 `gmpc_node.py` 的 `declare_parameter` 預設值；
launch 載入 `ammr_wholebody_mpc/config/gmpc_params.yaml`，其中 `goal_tolerance_xy: 0.30`。
控制器最小距目標 0.290 m **< 0.300 m，已進入自身容差並自行停車**。
凍結那趟是**兩套判準都成立**：控制器 0.290 m（TF 位姿、容差 0.300）、
測試器 0.249 m（真值、容差 0.25），兩者相差 41 mm，與 `|truth−amcl|` 中位 39.5 mm 同量級。
成因：我只查程式碼預設值，沒有追參數檔覆寫。

**二、GUI 溫度的兩個資料點作廢。** 先前報「Base experience 1600×900 在 92 °C 線下
峰值 89 °C、577 筆未觸線」**不成立**：（a）該趟實際載入的是 `isaacsim.exp.full.kit`，
`EXPERIENCE` 傳遞未生效，所以「Base 1600×900」這個組合根本沒測到；
（b）Isaac 在 40.8 秒即因收到訊號退出，那 577 筆多為它退出後取樣器繼續記錄，
**不能當作 GUI 持續運行的證據**。溫度取樣器未隨 Isaac 結束，是腳本缺陷。

**三、Isaac 未以獨立 session 啟動**，與腳本同屬一個 process group，因此會被外部指令的
生命週期波及（該趟 `停止原因 signal`、`LIMIT_HIT` 0 次、峰值 89 °C 未達 92 °C 上限）。

目前仍成立的 GUI 溫度資料點只有一個：**Base experience 960×540，平均 76.9 °C、峰值 81 °C，
於 88 °C 線下跑完 180 s 任務時限**（該趟 `task_timeout`，真值 0.274 m 未進 0.25 m 容差，
RTF 0.529）。

### 10.28 評測判準改版與兩套指標（2026-09-10）

完整內容見 `evaluation/results/isaac_bigarena_seed1_20260909.md` 附錄。

**一、測試器判準改為真值 ≤0.30 m（v2），控制器參數不動。** 0.25 m 是我自己挑的，
沒有先核對原報告——原報告的到達判準是 `analyze_new_TEST.py` 的
`GOAL_TOLERANCE_M = 0.30`（量 `/odom`）；`goal_watcher.py --tol 0.25` 只是提早收尾用，
不是評測判準。**v1（0.25 m）的設定與結果保留、未覆寫。**
這只對齊距離門檻，**不是完整評測定義**（路徑長度、間距、碰撞、命令平滑度未處理）。

**二、兩套指標並列**：原報告指標（`/odom` ≤0.30 m）與真值驗證（真值 ≤0.30 m），
到達時間皆自**第一個 `/plan`** 起算，命名 `arrival_time_s`；
任務逾時另用 `task_elapsed_sim`，自**收到 `/goal_pose`** 起算，兩者分開命名不混用。

**三、三趟既有資料重算，全部符合 0.30 m 判準**（分列，非同設定重複）：
凍結基線 headless 59.4 s；GUI Base 960×540 57.9 s；GUI Base 1600×900 57.2 s
（`/odom` 與真值到達時刻差 <0.01 s）。
**不可由 59.4→57.2 s 宣稱 GUI 較快**——三趟 experience、解析度、RTF、障礙相位皆不同。
**各組設定不同且各僅一趟，無法將到達時間差歸因於 GUI 或解析度。**
**兩套指標一致不是獨立交叉驗證**：`/odom` 由 Isaac 真值導出里程計產生，與真值同源。

**四、車頭 = `base_footprint` 的 +x**。手臂底座 `link_base` 相對 `base_footprint` 是
(0.110, 0, **0.330**)；先前寫的 0.280 是相對 `base_link`（後者高於 `base_footprint` 0.050 m）。
底盤相機 (0.2606, 0.0175, 0.3625) 也在 +x 側。不影響 +x 車頭的結論。

### 10.29 底盤相機 RGB 啟用（新設定版本，2026-09-10）

完整記錄 `evaluation/results/isaac_base_camera_20260910.md`。
**「啟用相機」為新設定版本，先前無相機影像輸出的凍結結果保留未動。**
相機只用於觀看與記錄，未接入導航；深度未開。

**對齊與方向**：相機對齊 `base_camera_color_optical_frame`，以 `camera_axes='ros'` 交付位姿，
機體座標（相對 `base_footprint`）(0.2606, 0.0325, 0.3625)。三軸以彩色探針定量驗證：
紅（正前）u=319.5 對預測 320.0；綠（左 0.3 m）u=191.2 對 193.5；藍（上 0.3 m）v=110.9 對 113.5，
誤差 0.5–2.6 px → 無鏡像、無上下顛倒。同高探針不足以排除上下顛倒，藍球為此補上。

**兩個設定版本**：規格版 640×480 @10 Hz（忠於 URDF `base_camera_rgbd`）；
觀看版 **1600×900 @30 Hz、JPEG、關閉 raw**。觀看版為 16:9，垂直視角與內參都與規格版不同，
**不可與凍結結果混用**。

**幀率問題是「卡」不是「慢」**：640×480 @10 Hz 幀間隔雙峰（110–130 ms 88.0%、
150–200 ms 11.6%，無一幀落在設定的 100 ms）。逐步計時歸因：超過 dt 的 403 步中 400 步是算圖步，
算圖步中位 20.19 ms、p95 80.48、最大 162.96，而**相機自身只佔整趟 2.5%**。
三項改動（JPEG 移到背景執行緒、關閉 4 MB 原始影像、相機與算圖同節拍）後，
牆鐘幀率由 **4.3 Hz 提升到 19–23 Hz**；1600×900 為 19.3 Hz、p90 間隔 58 ms（最穩定），
JPEG 平均 35.4 KiB、零丟棄、CPU 峰值 83 °C 未觸線。

**本輪修掉四個缺陷**：（一）`DomeLight` 只在 GUI 分支建立，headless 算圖全黑；
（二）相機內參用 USD 預設，視角 23.7° 而非模型宣告的 87.0°；
（三）**到達判準硬編碼 0.25——v2（0.30）只寫進紀錄文件、沒進程式碼**，
已改為 `--arrive-tol` 參數並於啟動印出，短距案例驗證 `goal_reached_truth` t=3.11 s、0.286 m；
（四）`camera_info` 寫在 `publish_image` 內部，關閉 raw 就一併關掉，整趟未發布內參，已解耦。

**尚未量到**：Foxglove 端實際顯示幀率（只量到 ROS 發布端）；`camera_info` 修正後未重跑驗證；
算圖步為何不穩未查明（GPU 僅 1–14%、50 °C，有餘裕未被利用）。

**附帶發現：機器人側向移動、車頭不朝行進方向。** `|vy|>|vx|` 佔 80.0%、`|wz|` 中位 0.0015 rad/s、
全程 yaw 僅變 −4.3°、**行進方向與車頭夾角中位 124.3°**。成因是控制器參數而非模擬器：
`Q_y: 0.0`（isotropic）、`R_vx = R_vy = 2.0`、`vy_max` 與 `vx_max` 同級，代價函數無朝向項。
第四次報告為純底盤無相機，車頭方向不影響指標，故此前未顯現。**未改動控制器任何參數。**

### 10.30 bigarena 靜態朝向驗證，以及模擬時間記帳缺陷（2026-09-10）

> **本節前半的所有時間數值已被 §10.31 取代。** 該批在時間基準缺陷下取得，
> 「秒」與「每秒」的量不可引用；角度、比例、次數亦因閉迴路行為改變而與
> 修正後不同。**保留為歷史紀錄，不作為基線，也不以統一倍率換算成有效基線。**

**這不是動態避障實驗。** 障礙物照常生成、佔位、出現在 `/scan`，但不下 `cmd_vel`
（新增 `NO_TRAFFIC=1`）。目的只有驗證 `gmpc_scan_heading` 的朝向目標在真實 Nav2
`/plan` 下的行為。完整紀錄見
[`evaluation/results/heading_probe_bigarena_20260910.md`](evaluation/results/heading_probe_bigarena_20260910.md)。

**選點**：起點 (5.00, 10.50)、初始 yaw 0°、三條 3.0 m 通道（0°/90°/180°）。選點同時
要求地圖距離變換與 `bigarena.sdf` 真值碰撞幾何都通過，因為兩者本身不一致——
`known_obs_13` 在地圖上佔的格子小於它的真值碰撞體。三條通道的地圖淨距 0.900–1.050 m、
真值淨距 0.900–1.000 m，皆遠大於 `inflation_radius` 0.35。**「淨距」指中心線到障礙表面，
未扣除機器人半徑**（底盤碰撞圓柱 r = 0.300）。

**結果**：三趟皆 `goal_reached_truth`，距目標 0.288–0.297 m。前進佔 98.9–100%，
倒退 0%，側移主導 0–1.6%（三者不互斥）。累計絕對轉角 38.2°/112.5°/214.1°。
朝向限速後單步跳動三趟都被壓在 2.86°（= 1.0 rad/s × 0.05 s），限速確實作用；
**重規劃並未造成朝向跳動**（重規劃當下 1.31–3.20°，小於全程原始跳動最大值
5.77–7.02°）。末段 0.6 m 內累計轉角 1.99–7.75°，未見接近目標時反覆轉頭。

**新增檢查**：`stillness_check.py` 在發目標前實測 3 s，逐樣本算最大瞬時速度
（不是頭尾位移，否則來回移動會看起來像靜止）。三趟移動體位移皆 0.0 mm。
第一次嘗試被此門擋下且未發目標，原因是我的檢查程式以 RELIABLE QoS 訂閱
BEST_EFFORT 的 `/odom`，收不到資料——門擋對了，錯的是訂閱端。

**重大缺陷（影響所有 Isaac bigarena 結果，含 §10.26 的 seed 1 凍結紀錄）**：
`isaac_bigarena_sim.py` 以 `World(rendering_dt = physics_dt * 4)` 建立世界，帶 render 的
步進推進 4 個物理子步，主迴圈卻只記帳 1 步，而 `/clock` 用的就是這個記帳值。
`render_hz=12`、`physics_dt=0.01` → 每 8 步 render 一次，實際／記帳 = 1.375。

反推驗證：以 `dyaw / cmd_wz` 求每個 log 視窗的實際物理時間，得到的值精確地是
**0.0800 s 或 0.0500 s**（比值 1.6 或 1.0），與「視窗含不含 render」一致。三項獨立佐證：
(一) 正前趟真值均速 0.2959 m/s 超過命令每軸上限 0.2775，除以 1.375 得 0.215，與
`v_nominal 0.22` 相符；(二) 真值淨轉角/∫cmd_wz dt 與真值位移/∫cmd dt 是**同一個**比值
（1.373–1.390），代表時間尺度而非控制問題；(三) 真值 |wz| 最大 1.8110 rad/s 看似超過
`wz_max` 1.1327，而 1.1327 × 1.6 = 1.8123——**機器人從未超過命令角速度**。

受影響：任務時間、RTF、速度、角速度、角加速度等所有「每秒」量。
不受影響：角度、比例、次數、距離、位置。**尚未修正**，本輪三趟刻意維持設定不變。

**兩處措辭更正**：
1. `goal_sim_t` 比控制器實際開始動作晚 0.84–1.60 s（模擬器 ROS callback 在物理迴圈內
   處理），任務時間改以首次非零 `/cmd_vel` 起算。
2. 我一度把輪速需求在 5.550 精確飽和寫成「解耦箱型約束下 QP 視為可行、實際輪子做不到」。
   **錯誤**：`gmpc_node` 的 `wheel_coupling` 預設 True 且未被覆寫，QP 本就帶四輪耦合約束，
   飽和正是約束生效，超出量 1.7e-4 rad/s 屬求解器容差。已撤回。


### 10.31 模擬時間基準修正與三方向重跑（2026-09-10）

完整驗收見 [`evaluation/results/isaac_time_basis_fix_20260910.md`](evaluation/results/isaac_time_basis_fix_20260910.md)，
重跑結果見 [`evaluation/results/heading_probe_bigarena_20260910.md`](evaluation/results/heading_probe_bigarena_20260910.md)
的「修正後重跑」一節。

**缺陷**：`World(rendering_dt = physics_dt × 4)` 下，帶 `render=True` 的
`world.step()` 推進 4 個物理子步，主迴圈只記帳 1 步，而 `/clock` 正是由該記帳值
發布。倍率 = `(re_ − 1 + m) / re_`，**逐趟不同**（實測 1.300 / 1.368–1.378 /
1.600 / 1.999，相機 tick 會額外觸發 render 而拉高）。這不只是報表問題：控制器、
Nav2、逾時判斷與加速度限制全部跑在這份 `/clock` 上。

**一次錯誤的中間結論已更正**：我曾以同一行程掃描 `rendering_dt` 並得出「它不是
原因」。日誌顯示 `SimulationContext is already initialized. Constructor
parameters are ignored on subsequent calls`——第二個設定未生效。改為每設定獨立
行程並回讀 `get_physics_dt()` / `get_rendering_dt()` 後，結論相反。

**修正**：(一) `rendering_dt = physics_dt`，啟動時回讀比對，不符即中止；
(二) 模擬時間一律由 `world.current_time` 讀取，`/clock` 在步進**之後**以該值
發布；(三) scan / pose / odom / render / camera 的節奏改以模擬時間到期判定，
不再用迴圈次數；(四) 新增記錄 `physics_dt`、`rendering_dt`、`time_skew_max_s`
——這三項先前完全未被記錄。

**三層驗收**：(一) 獨立無接觸場景 12 組設定，`rendering_dt = physics_dt` 時四種
render 頻率（含不渲染）倍率全為 1.0000（原為 1.150 / 1.375 / 2.005）；命令在
**當次** `world.step()` 生效，位移恰為一個物理步。(二) 真實模擬器三種 render
頻率，迴圈/物理時鐘偏差 ≈2 µs、位移倍率 0.99947。(三) `/clock` 本身與位姿
時戳差 **0.000000 s**，依 `/clock` 算出的速度 0.19989 m/s（命令 0.20）。

**重跑**（控制參數完全未動，三趟皆 `goal_reached_truth`，距目標 0.292–0.299 m）：
`arrival_time_s`（自第一個 `/plan` 起算）12.69 / 13.55 / 14.90 s。真值 `|wz|`
最大 1.132 ≤ `wz_max` 1.1327，**不再超過命令上限**；前一批的 1.811 = 1.1327 × 1.6
是時鐘假影。角加速度三趟全程無樣本超過 `az_max` 25.51（後方趟命令端最大值恰為
25.510，是飽和非超限）。

**與舊值比較須先對齊起算點**：舊表的「任務時間」起算於首次非零 `/cmd_vel`，
對應新表的 `motion_elapsed_s` 而非 `arrival_time_s`。對齊後 ×1.375 與實測差
0.01–0.19 s，**僅能作量級參考**——兩批閉迴路軌跡本就不同（正前趟淨轉角
26.1° → 20.4°，左側累計轉角 112.5° → 120.4°）。

**`goal_sim_t` 正名為「模擬器收到目標的時間」**，不代表發布時間：後方趟它
（98.11 s）晚於第一個 `/plan`（97.48 s）與運動開始（97.54 s）。要讓任務逾時自
發布起算，需另記可核對的發布事件並確認與該次 `/plan` 屬同一目標，尚未實作。

**待釐清**：左側趟累計/淨轉角 = 2.00。淨轉角只是終末 yaw 減起始 yaw、**不是
路徑所需轉角**，所以這不代表轉了兩倍必要角度，只表示存在明顯反向轉動。
下一步做離線對齊分析（原始朝向、限速後朝向、實際 yaw、角速度、重規劃事件
同軸比對），先分清是參考折返還是追蹤過衝，再決定是否修改。

**兩個過程失誤**：批次進行中編輯了 `run_bigarena_isaac.sh`，bash 依位元組偏移
續讀腳本，正在跑的 seed 103 可能讀到錯位內容，已主動中止並標記；另有一次以
字串比對終止程序時比對到自己的 shell（exit 144），已改為顯式 PID。


### 10.32 左側趟朝向鏈離線對齊：反轉來自參考本身（2026-09-10）

完整分析見 [`evaluation/results/heading_left_alignment_20260910.md`](evaluation/results/heading_left_alignment_20260910.md)。
腳本 `evaluation/align_heading_left.py`，逐點序列 `evaluation/results/heading_left_alignment.csv`。
**本輪未修改任何控制邏輯或參數。**

§10.31 留下的問題是左側趟累計/淨轉角 = 2.00 的反向轉動來自何處。把原始朝向
`raw`、限速後 `ref`、控制器看到的 yaw、真值 yaw、角速度與重規劃事件放在同一條
模擬時間軸上，結論是**朝向參考本身折返，不是控制器追蹤過衝**：

- 真值 yaw 的 30.04° 反轉中 27.28°（91%）集中在 t = 88.68–94.78 s；同期
  `raw` 與 `ref` 各變化 −22.70°，**參考自己往回轉**。
- 該區段追蹤誤差 `|ref − yaw|` p50 4.15°、p95 6.72°、max 7.17°——機器人是
  跟著參考走，不是追過頭。
- 全程 270 筆中限速實際改變參考的只有 **4 筆（1.5%）**。也就是 `ref ≈ raw`，
  **限速在本趟幾乎沒有作用**。§10.30／§10.31 的「限速後單步跳動被壓在 2.86°」
  只證明設了上界，不證明它在起作用，措辭據此收準。
- `|ref − yaw|` p95 55.93°、max 90.15° 是**初始條件**：`_desired_yaw` 首週期
  直接令 `_yaw_ref = raw`、不受限速，參考一步跳到 90°，車體以有限角速度追上，
  轉向段結束即收斂。
- 重規劃不是成因：17 次重規劃當下 `raw`／`ref` 跳動最大 0.89°。

**成因（離線補查後更正，未重跑）**：以 `/plan` 與 `/odometry/filtered` 重建前視點
（`evaluation/lookahead_probe.py`），重建弦角對照記錄的 `raw`，|差| p50 0.12°、
p95 0.86°、max 2.26°，重建可採信。

我原先寫的「前視點退化到路徑末端、弦長縮短放大側向偏差」**不成立，在此更正**：
前視點落在路徑末端最早出現在 t = 91.46 s（占 30.4% 樣本），弦長在 t ≤ 91.0 s
之前穩定在 1.17–1.24 m，而反轉起點是 t = 88.68 s。反轉開始時弦長仍是完整的
1.2 m，弦長縮短只在 t ≈ 91.5 之後才附加作用，「1.54 m 與 1.2 m 同量級」這個
論據不成立。

**實際成因是機器人偏離「起點→目標」直線（x = 5.000），Nav2 重規劃成一條把它
拉回目標的斜線，朝向參考忠實跟隨該斜線。** 偏離量由 0.000 單調增至 −0.349 m
（EKF；真值 −0.269 m），`raw` 同期由 90.00° 降至 57.44°，相關係數 **r = +0.720**
（n = 270）。t = 92.01 的計畫實際幾何為 (4.65, 12.55) 斜向 (5.00, 13.50)，
沿其取 1.2 m 前視點，弦角自然落在 57–75°。

**另一項更正**：我中途量過「側偏 ≈ ±0.04 m」並據此排除側向偏移——那是相對
**當下計畫**的橫向偏差，而計畫起點就是機器人本身，依構造恆為零，不具鑑別力。
有意義的是相對「起點→目標」直線的偏離量。

機器人為何累積該側向偏離（參考視窗不含朝向、控制器可用側移換取位置誤差），
是另一個問題，本輪未追查。

**因此「累計/淨 = 2.00」的正確讀法**：淨轉角只是終末 yaw 減起始 yaw、不是路徑
所需轉角，2.00 不代表轉了兩倍必要角度。實際組成為轉向段 +89.6°、末段跟隨弦角
−27.1°，兩段各有幾何理由，**未發現多餘的來回擺動**。

**尚未實作**：若要縮小末段回轉，可考慮剩餘路徑短於前視距離時凍結朝向參考、
改以路徑切線取代弦角、或末段權重衰減。三者都會改變控制行為，需各自單獨驗證，
不與時間基準修正混在同一輪。
