# 第五次進度報告：從底盤預測導航到全身協同與接觸操作

整理日期：2026-09-10；**修訂 2026-09-13（新增第 9–11 節：Isaac 固定底座的接觸操作）**。
範圍：以《第四次進度報告》的已報內容為起點，整理截至目前的新增實作與驗證；第四次已有的 bigarena、輪速耦合、表面點感知與 Raw-scan Shield 僅作回顧。
本稿依現有實作與實驗紀錄整理，未為撰稿重新執行模擬。實驗採用最新有效版本；較早結果如經後續修正，不混入現行基線。

## 1. 本次報告主軸

> 第四次報告完成純底盤的預測導航與近距離安全保護。本期將系統延伸至底盤與六軸手臂，建立 9 自由度全身運動學、連桿幾何安全約束與控制介面，並在 Gazebo 完成有障礙的協同預抓取測試。同時建立 Isaac Sim 導航介面、相機輸出與時間基準驗證，作為後續跨模擬器測試的基礎。

本期已建立的全身控制基線是**整合安全約束的單步 QP**；全身多步預測控制 W-GMPC 尚待開發與比較。

修訂補充（9/12–9/13）：在 Isaac 中以**固定底座**完成了預抓取之後的**接觸操作** —— 對一個沒有驅動的被動抽屜，由手臂帶動完成 200 mm 全行程的「拉開→保持→釋放→退出」。抓取關係分兩版驗證且**不混稱**：理想固定連接版已完成 200 mm；摩擦夾持版完成靜態雙側夾持與受控拉動約 17 mm，尚未達 20 mm。**底盤在這些測試中由外部固定支撐，不代表輪子靠地面摩擦能承受相同負載。**

## 2. 第四次到本次的差異

| 面向 | 第四次報告 | 第五次新增進度 |
|---|---|---|
| 控制對象 | 全向底盤 3 自由度 | 底盤 3＋手臂 6，共 9 自由度 |
| 任務 | 導航至底盤目標位置 | 底盤與手臂協同到達末端預抓取位姿 |
| 幾何安全 | 底盤圓盤、2D 雷射回波 | 隨關節構形更新的連桿表面距離與速度約束 |
| 自體感測干擾 | 固定支柱遮罩 | 動態移除手臂進入掃描平面所產生的自體回波 |
| 控制限制 | 底盤速度、加速度與輪系限制 | 加入手臂關節位置、速度、加速度與 jerk 處理 |
| 全身任務解算 | 尚未建立 | 任務與安全限制整合至單一 QP，完成局部任務基線 |
| 模擬環境 | Gazebo | Gazebo 全身任務驗證＋Isaac Sim 初步導航移植 |
| 任務終點 | 到達預抓取位姿 | **接觸操作**：帶動被動抽屜完成 200 mm 全行程（固定底座、理想固定連接） |
| 抓取關係 | 未建模 | 分兩版：**理想固定連接**（已完成 200 mm）／**摩擦夾持**（受控拉動約 17 mm，進行中） |

## 3. 全身模型與運動學完成

整合 omni_bot、Lite 6、夾爪與腕部相機座標系，建立單一全身運動鏈：

\[
q=[x,y,\theta,q_1,\ldots,q_6]^\top.
\]

由 URDF 讀取幾何並建立 FK 與 Jacobian，使底盤移動、旋轉及手臂關節運動都能共同影響末端位姿。運動學模型與 Gazebo 模型分工：`omni_bot_wholebody.urdf.xacro` 供全身解算；`omni_bot.urdf.xacro` 供模擬。

既有驗收中，200 組組態的 FK 對獨立實作最大誤差為 5.55×10⁻¹⁶，Jacobian 對數值微分最大誤差為 3.65×10⁻¹⁰；並完成關節方向、零位與底盤移動下末端位姿的模擬交叉檢查。這些是模型一致性驗證，不能解讀為實機定位精度。

本期也修正支撐球高度造成的車身抬高 5 cm 問題，同步更新控制模型與預抓取參考姿態。後續主要結果採高度修正後的版本。

依據：[第二階段進度紀錄](第二階段進度紀錄.md) §1–3；[模型高度修正](evaluation/results/model_height_and_staleness_20260908.md)。

## 4. 感測與連桿安全層建立

**動態 LiDAR 自體濾波。** 依手臂當下姿態與網格截面，移除掃到自身手臂的回波。三組低姿態測試移除 15–41 個自體回波，測試中的外部牆面誤刪為 0。被手臂遮住的方向仍是未知區域，不能當成自由空間。

**連桿幾何距離。** 原本 12 個固定偵測點可能漏掉連桿突出部位，改為以碰撞網格表面取樣、涵蓋半徑及鄰近表面點建立約束；速度 Jacobian 也取在對應表面點。後續加入完整距離帶選取及截斷檢查，避免只保留部分列後仍假設涵蓋完整。

**安全速度約束。** 限制連桿朝向障礙表面的接近速度，停止距離考量基礎間距、延遲與制動項；共同處理底盤與關節速度。已進行接近、切向、退離與資料失效測試。

**限制實際接入 ROS 控制路徑。** 保存前次輸出及實際時間間隔，使關節位置、速度、加速度與 jerk 處理在線上生效；jerk 可因安全需要被覆寫。距離資料整批過期或安全約束資料截斷時，命令歸零。

目前連桿障礙距離主要來自已知世界模型／模擬資訊；腕部相機的幾何與 TF 已建立，真實深度感知尚未接入這條控制鏈。

依據：[第二階段進度紀錄](第二階段進度紀錄.md) §5–9、§10.11、§10.17；[有障礙同動預抓取](evaluation/results/obstacle_wholebody_20260909.md)。

## 5. 預抓取由固定底盤推進到全身同動

先完成固定底盤的手臂預抓取，再開放底盤與手臂共同調整，使機器人以移動底盤配合關節運動到達指定 TCP 位置與工具朝向。

高度修正後、全零手臂起始姿態的同動測試中，末端位置誤差由約 816 mm 收斂至 **0.53 mm**，末端姿態誤差約 **0.000°**；底盤與手臂同時動作累計 **3.40 s**。該趟未量到關節位置越界、關節速度超限或底盤速度超限。

這項結果證明全身命令路徑與末端追蹤已接通。任務目前到達的是**預抓取位姿**；抓住物體、搬運與接觸力控制尚未完成。

依據：[模型高度修正後的同動回歸](evaluation/results/model_height_and_staleness_20260908.md)。

## 6. 本期主要發現：安全停住仍可能無法完成任務

在原本底盤想停的位置放置障礙後，原架構先解任務速度，再由安全層修正。測試中底盤側移約 226 mm 讓開障礙，但末端停在距目標 **225.9 mm** 的位置；安全層輸出違反為 0，任務仍未完成。

停滯時，任務解算器持續要求底盤往被障礙擋住的方向移動，手臂輸入已為零；安全層將該底盤速度削為零。另以 IK 確認，底盤留在該位置時，手臂仍有可行動作到達目標。

將任務與安全約束放入**同一次 QP 求解**後，在相同停滯狀態、相同約束與末端姿態權重下，可得到縮小末端誤差的方向。後續閉迴路測試再調整關節構形正則權重 μ，在實測 μ=0.03 與 μ=0 兩個設定下完成任務。

因此，本案例支持的結論是：**在安全可行範圍內直接最佳化任務，有助於底盤與手臂重新分配動作。** 離線同狀態對照支持架構差異；完整到達結果同時涉及構形權重調整，不能把所有改善只歸因於架構。這個案例也不足以主張必須使用多步 MPC 才能解決停滯。

依據：[有障礙同動預抓取與求解器對照](evaluation/results/obstacle_wholebody_20260909.md)。

## 7. 已凍結的全身控制基線與成果

採用單步 QP、μ=0.03、保留實際下游安全濾波器，並以固定截止時間排程作為後續比較版本。場景為已知靜態障礙、同一起點與同一目標，Gazebo 重複 3 次。

| 指標 | 排程修正版結果 |
|---|---:|
| 任務完成 | **3/3** |
| 完成時間 | **12.134 ± 0.023 s** |
| 末端位置誤差 | **2.293 ± 0.008 mm** |
| 末端姿態誤差 | **0.055 ± 0.001°** |
| 最小安全間距（模型量測） | **約 88.816 mm** |
| QP 求解時間 p95 | **6.1 ms** |
| 命令發布間隔中位／p95 | **50.0／52.6 ms** |
| 驅動端錯過截止／跳過時槽 | **0／0** |

原先迴圈固定等待 50 ms 後才運算，使實際發布間隔中位達 61.19 ms；改為工作完成後只等待剩餘時間，恢復至約 20 Hz。發布仍有抖動，不能宣稱硬即時保證。

這是單一案例的重複性結果；3/3 不代表跨場景成功率，也不是一般性安全保證。

依據：[全身控制基線凍結紀錄](evaluation/results/baseline_B_frozen_20260909.md)「排程修正版」。

## 8. Isaac Sim 初步移植與驗證

建立 Isaac Sim 的模型、掃描、里程計、TF 與速度命令介面，接上第一階段導航鏈；已執行帶臂平台的 bigarena 單案例。該導航測試中手臂保持固定姿態，尚不代表 Gazebo 的全身預抓取已移植完成。

底盤 RGB 相機已可輸出影像，並以彩色探針檢查光學座標方向。影像目前供觀看與記錄，尚未用於導航或物件辨識。

本期發現並修正物理步進與 `/clock` 記帳不一致：統一 `rendering_dt` 與 `physics_dt`，改由物理世界時間發布時鐘及安排感測節奏。修正後 `/clock` 與位姿時戳一致，命令 0.20 m/s 的測試量得約 0.19989 m/s。

**修正後有效的三方向導航測試**：固定障礙物，起點相同、目標分別在前方／左側／後方 3 m。

| 目標方向 | 到達時間，自第一個 `/plan` 起算 | 終點位置誤差 |
|---|---:|---:|
| 前方 0° | 12.69 s | 0.292 m |
| 左側 90° | 13.55 s | 0.292 m |
| 後方 180° | 14.90 s | 0.299 m |

三趟均符合真值距離 ≤0.30 m 的到達判準。這是靜態場景的朝向行為驗證；修正前 bigarena 動態導航的時間、RTF、速度及相機幀率等數字不作現行基線，仍須重新測試。

最新左側案例分析顯示，末段反向轉頭主要跟隨朝向參考的變化：機器人偏離起點至目標的直線後，Nav2 路徑斜向連回目標，前視朝向隨之改變。前視距離縮短不足以解釋反轉的起始；側偏如何累積仍待追查。本輪僅分析，未修改控制參數。

依據：[時間基準修正](evaluation/results/isaac_time_basis_fix_20260910.md)、[三方向修正後重跑](evaluation/results/heading_probe_bigarena_20260910.md)、[最新朝向對齊分析](evaluation/results/heading_left_alignment_20260910.md)、[底盤相機紀錄](evaluation/results/isaac_base_camera_20260910.md)。

## 9. Isaac 固定底座的接觸操作：被動抽屜

預抓取之後的下一步是**真的去動一個東西**。本期在 Isaac 中建立一個被動抽屜櫃，
以**固定底座**的手臂完成「拉開 → 保持 → 釋放 → 退出」的完整操作。

**為什麼另建資產。** 場景中既有的 `known_obs_12` 是實心靜態方塊，沒有把手、
沒有活動自由度；把它當成「已有的操作機構」會讓「開抽屜」變成沒有機構的宣稱。
新資產是一個有滑動自由度與可夾持把手的櫃體：抽屜為單一剛體，沿滑軌
（`UsdPhysics.PrismaticJoint`）平移，**驅動的 stiffness／damping／maxForce／target
全部寫成 0 並讀回驗證** —— 抽屜不接收任何位置或速度命令，開度只能由手臂帶動。
把手是 ⌀10 mm 橫桿，尺寸由夾爪幾何與行程需求反推，不是挑好看的。

**兩種抓取關係分版驗證，不混稱。**

| 版本 | 意義 |
|---|---|
| 理想固定連接 | 在夾持時刻建立夾爪—抽屜固定關節，代表「抓握不會失效」的理想上限 |
| 摩擦夾持 | 只有接觸與摩擦，兩指閉合夾住把手；抓握能力由實際接觸決定 |

### 9.1 過程中量到的問題：垂直反作用力隨開度成長

以理想固定連接執行時，手腕傳遞力的**垂直分量隨開度線性成長**
（0–97 mm 區間斜率 **+0.2599 N/mm**，相關係數 +0.9996），而**沿滑軌方向的分量
全程只有 0.1 N 量級** —— 成長的那部分不是拉抽屜的力。未處理時，該趟在開度
**97.2 mm、|F| 30.73 N** 觸發力中止，200 mm 未完成。

一系列排除實驗顯示：取消抽屜重力、取消手臂重力都不改變這個成長；
**移除固定連接後成長完全消失**（斜率 −0.0000 N/mm）。再以無連接試驗量手臂
**實際走出的路徑**，發現它相對理想滑軌有隨行程成長的垂直偏離。

### 9.2 修法：沿路徑的前饋致動補償 b(s)

把一次性偏置改為**路徑相依**的補償，完全由已量到的偏離反解，不加力回授：

\[
q_{\text{cmd,new}}(s)=q_{\text{cmd,old}}(s)+\Delta q_{\text{ff}}(s),\qquad
\Delta q_{\text{ff}}(\text{拉開首點})=0 .
\]

**校準必須涵蓋全行程，不能外推。** 實測偏離**不是線性**、會飽和：由 0–76 mm
線性外推到 198 mm 給 2.201 mm，實測只有 1.120 mm（**1.96 倍**）。若照外推施加，
會多補約 1.08 mm —— 與要消除的偏離本身同量級。兩趟獨立校準在重疊區的差異
最大 0.0043 mm，校準可重現。

補償後的無連接驗證：垂直偏離最大 **1.1371 → 0.0302 mm（−97.3 %）**，
姿態誤差最大 **0.1127° → 0.0025°**，沿軸行程只差 0.025 mm（配時未受影響）。

### 9.3 結果：200 mm 完整操作完成

| 相位 | 樣本 | 開度 起→末 |
|---|---|---|
| engage | 200 | 0.000 → 0.000 mm |
| **pull** | 834 | −0.001 → **198.422 mm** |
| **hold** | 200 | 198.453 → 198.730 mm |
| release / retreat / settle | 1769 | 198.731 mm（無變化） |

* **最終開度 198.731 mm，誤差 −1.269 mm**
* **到位早於解除連接 0.620 s** —— 是連接仍在時的受控到達，不是釋放後的滑行
* 解除連接後開度變化 **0.0000 mm**（記錄窗 17.7 s、解析度 0.01 s）
* **超過 30 N 的樣本 0 / 4467**；力峰值 11.65 N 出現在 postengage，不在拉開段
* 底盤漂移 0.00000 mm；1209/1209 命令套用、0 拒收；監看無失效

補償前後對照（同一條命令、同一目標）：

| \|F\| 對開度擬合 | 斜率 |
|---|---|
| 未補償 | **+0.2599 N/mm**（於 97.2 mm 中止） |
| 全行程補償 | **−0.0039 N/mm**（走完 198.4 mm） |

### 9.4 這個結果沒有證明的事

* **只跑一趟，未重複**；兩趟之間基準與參考都改變，不是單變數對照
* 補償表取自**無連接**試驗，成功**不等於**已辨識出那個 0.26 N/mm 負載的物理來源
* 適用條件是「同模型、固定底座、同路徑與配時、同增益」，
  **是本案例的致動校準，不是通用方法**
* **底盤由匯入器的固定關節外部支撐**；不代表輪子靠地面摩擦能承受相同負載，
  移動底盤的操作要另外驗證
* 停止原因 `sim_limit` 是**程序停止原因**，不是任務成功的判準；
  完成依據是到位時刻與逐相位證據

## 10. 摩擦夾持（進行中）

理想固定連接證明了「路徑與負載可行」，但那不是真的夾住。摩擦夾持版**只改抓取
關係**，其餘幾何、行程、容差與停止處置沿用。

**先補齊模型本身。** 查核發現案例規格中的摩擦係數、手指接觸中止門檻等欄位
**都沒有接進模擬**：全場沒有任何接觸摩擦材質（URDF 中唯一的 μ 是刻意設為 0 的
無摩擦球輪），12 N 手指中止也不存在。補上之後逐一讀回驗證：靜／動摩擦
**分開明訂**各 0.8（同值是建模假設，不是量測值）、混合規則 `min`、綁到
**3 個碰撞形狀**（把手橫桿與兩片指墊）；手指 drive 出力上限由**執行期**讀回
確認為 5.0 N／指。

**閉合前必須先對中。** 三層核對（設計目標位姿／實測關節角 FK／Isaac 實際位姿）
顯示：**設計位姿完全對中（0.0000 mm），命令也對中（+0.0011 mm），但實際保持
姿態偏 1.4376 mm** —— 是穩態關節追蹤誤差（0.002150 rad，幾乎全在 joint2）。
以命令層的經驗修正一次補上後，中心偏移 **−1.4376 → −0.0021 mm**、
兩側指墊間隙差 **−2.1408 → −0.0031 mm**。

**目前進度**：

| 項目 | 結果 |
|---|---|
| 靜態雙側夾持 | **通過** —— 連續 2.0 s，兩指接觸率 100 %，把手相對夾爪位移 0.0450 mm |
| 受控拉動 | **15.785 mm**（原配時）／**17.117 mm**（拉動配時放慢一倍） |
| 20 mm | **未完成**（最佳 17.118 / 20 mm） |

停止由**事前定義的滑脫判準**觸發（相對拉動起點 2.0 mm），力與溫度保護未觸發。
方向分解顯示滑脫**100 % 沿滑軌方向**（沿閉合方向與第三軸皆 0.0 %），
不是橫向跑偏或失去包覆。放慢配時只改善拉動起始的暫態，
**單位滑脫率反而略升**（0.06128 → 0.07213 mm/mm），因此不再繼續降速。

依裁決，摩擦診斷在此設停止點，保留現有成果與未解限制。

## 11. 成果展示影片

兩類影片，性質不同，**不混稱**：

| 類型 | 作法 | 用途 |
|---|---|---|
| 姿態重演（3 支） | 讀已完成趟次的 log，逐幀把關節角與抽屜開度**設定**回場景後取像 | 展示已驗證的預抓取與 20 mm 操作 |
| **執行時錄影**（1 支） | 在**這一趟真正執行的當下**逐物理步取像 | 200 mm 完整操作 |

影像全部來自**模擬器內的相機感測器**，**全程未做桌面擷取**。姿態重演每幀設定後
都讀回核對（關節最大差 0.194 mrad、開度最大差 0.00048 mm），
證明畫面代表該趟實際量到的狀態而非另一次模擬。

執行時錄影版的說明另存 README，影片本身不含任何文字。

## 12. 下一階段工作

**優先序已調整**：接觸操作在固定底座下已取得 200 mm 成果，但**底盤與手臂的協同
尚未在 Isaac 接通**。下一步先打通主線能力，再用接觸條件檢驗它。

1. **把 Gazebo 已驗收的全身同動接到 Isaac 執行端。** 這是移植，不是重新研究
   控制器。已盤點既有資產：9 欄 Jacobian、全身 resolved-motion rate control、
   9 維安全濾波（無資料一律輸出零）、格式轉接器都已存在且在 Gazebo 驗收過
   （底盤與手臂同時運動累計 3.50 s、位置誤差 697.5 → 0.8 mm）。
   **核心缺口**：目前沒有任何 Isaac 模擬能同時接受底盤速度與手臂關節命令 ——
   導航版手臂固定、操作版底盤固定。將另立新檔，既有三支一律不動。
   驗收依序為：底盤單獨小動作 → 手臂單獨小動作 → 同步小動作 → 自由空間全身預抓取。
2. **協同成立後，做理想固定連接下的短行程協同拉抽屜。** 與固定底盤採相同抓取
   條件比較。**不可直接解除底盤固定後沿用原命令**，也不可假設固定底座的 b(s)
   仍有效 —— 該補償的適用條件明載「固定底座」。
3. **再回接摩擦夾持**，屆時才問：協同是否讓滑動更少、負載更低、可完成行程更長。
   摩擦工作因此直接服務研究問題，而不是孤立的夾爪調參。
4. 以凍結的單步 QP 為主要基線，發展全身多步預測控制 W-GMPC，檢驗多步預測在
   動態障礙、提早改變構形與任務完成率上的增益。
5. 擴充障礙位置、起始姿態與目標測試，分別量到達率、安全間距、控制平滑度與運算時間。
6. 接入腕部深度與物件位姿估計；目前抽屜位姿為已知世界模型，尚未由感知取得。
7. 實機驗證致動響應、延遲與制動能力；負載與力矩能力仍須核對可信硬體資料。

## 13. 投影片安排建議

| 頁 | 標題 | 該頁要傳達的事 |
|---|---|---|
| 1 | 第四次回顧與本次目標 | 從底盤導航延伸到全身預抓取；可沿用現有第一階段回顧圖 |
| 2 | 9 自由度模型與運動學 | 單一運動鏈、FK／Jacobian 驗證、模型高度對齊 |
| 3 | 感測與全連桿安全 | 動態自體濾波、連桿表面點、速度限制與失效處理 |
| 4 | 底盤與手臂同動 | 展示同動軌跡與末端誤差收斂 |
| 5 | 有障礙時的停滯問題 | 安全層擋住底盤後，原解算沒有讓手臂接手 |
| 6 | 任務與安全約束整合 QP | 同狀態對照、構形權重的角色與任務完成結果 |
| 7 | 重複測試與控制週期 | 3 次結果、2.29 mm 誤差、88.8 mm 模型間距、20 Hz 排程 |
| 8 | Isaac Sim 與相機介面 | 初步導航移植、時鐘修正、有效的三方向測試 |
| 9 | 被動抽屜與兩種抓取關係 | 為何另建資產；理想固定連接 vs 摩擦夾持分版驗證 |
| 10 | 垂直負載成長與 b(s) 補償 | 0.26 N/mm 的成長、排除實驗、路徑相依前饋；**外推會多補一倍** |
| 11 | 200 mm 完整操作 | 198.731 mm、到位早於釋放、0/4467 超過 30 N；影片（執行時錄影） |
| 12 | 摩擦夾持進度 | 材質與保護補實、對中修正 1.44 mm、靜態雙側夾持、受控拉動 17 mm |
| 13 | 尚待完成與下一步 | **Isaac 全身同動移植**、協同拉抽屜、回接摩擦；W-GMPC 與實機 |

可直接使用的結尾講稿：

> 本期完成了從純底盤導航到全身協同預抓取的第一個閉迴路。除了建立九自由度模型與連桿安全約束，也透過有障礙案例發現，安全停住不等於能完成任務。將任務與約束整合求解並調整構形權重後，目前的單步 QP 基線在相同案例三次測試皆完成，末端位置誤差約二點三毫米。下一步會以這個基線為比較對象，評估全身多步預測控制在更複雜與動態情境下的效益。
