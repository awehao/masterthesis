# 第五次進度報告講稿

依據新版《0917 (1).pdf》的 10 頁內容撰寫。各頁以可直接口述的段落呈現，公式的符號、意義與用途融入講述，不另設公式解說區。影片播放時間可依現場報告節奏調整。

## 第 1 頁｜動態環境下移動機械臂預測導航與受約束操作

老師好，我是陳文顥，今天報告的題目是「動態環境下移動機械臂預測導航與受約束操作」。這次的進度，主要是把先前以底盤導航為主的系統，往底盤與手臂共同運作的方向推進。我會介紹全身機器人的模型與運動學、Isaac 模擬平台的控制介面移植，以及底盤朝向和定點抽屜操作的成果。

這次有兩個比較具體的進展。第一個是，底盤和手臂已經可以在 Isaac 裡由同一個求解器產生命令，透過實際狀態回授，完成自由空間中的末端到位。第二個是，在固定底盤和理想固定連接的條件下，已經完成約兩百毫米的抽屜開啟，包含後續的保持、釋放和退出。不過，這兩項目前是分別驗證的能力，還不是從導航到抓取、再到移動操作的完整任務。

## 第 2 頁｜報告大綱

今天的報告分成三個部分。首先是進度回顧，簡單說明整體研究目標，以及先前已經建立的導航、感知和安全控制架構。接著是本次進度，依序介紹全身模型、Isaac 閉迴路、底盤朝向設計，以及抽屜操作。最後說明未來如何把這些能力串起來，並從目前每次求解當下速度的控制方式，進一步發展成能提前規劃底盤與手臂動作的全身預測控制。

## 第 3 頁｜研究目標

這個研究希望處理的，不只是機器人從一個位置移動到另一個位置，而是移動過程中還需要操作環境的任務。以上面的情境來說，使用者要求機器人去拿抽屜裡的筆，機器人就必須先前往書房；如果途中遇到關閉的門，需要把門打開；移動時還要避開未知的靜態或動態障礙物；最後抵達抽屜，完成開啟、取物和返回。這張圖是整體研究希望串接的應用流程，並不是目前已經全部完成的成果。

因此，我把工作分成兩個階段。第一階段處理導航避障，重點是讓底盤在動態環境中移動到指定位置。這裡的 SE(2)，可以理解成平面上的位置與朝向，也就是底盤的 x、y 和旋轉角度。GMPC 會利用模型預測接下來的運動，再配合預測式控制障壁函數，也就是 CBF，把障礙物相關的安全限制放入控制問題。同時，還需要障礙物追蹤與定位回授，讓控制器知道機器人在哪裡，以及周圍物體如何移動。

第二階段則把手臂加入，處理底盤與末端的協同運動。這時候，問題不只是末端能不能到達某個點，還要考慮物件允許怎麼動。例如門受旋轉軸限制，把手通常沿圓弧移動；抽屜受滑軌限制，主要沿固定方向直線移動。所以，控制器不能任意指定末端路徑，而要配合物件的運動約束，同時考慮關節極限、避障，以及手臂是否處於容易操作的姿態。

這次工作的定位，就是先把第二階段需要的模型、模擬執行與操作案例建立起來。全身多步預測控制仍是後續要發展的部分，目前的全身到位使用單步 QP，不能直接把它稱為已完成的全身 GMPC。

## 第 4 頁｜進度回顧

這一頁整理的是先前第一階段的導航架構。可以先看中間這條主要控制路徑：使用者給定目標點之後，Nav2 利用環境地圖規劃一條繞過已知靜態障礙物的參考路徑，再交給 GMPC 決定底盤接下來的速度。因此，Nav2 主要回答「大致要走哪裡」，GMPC 則根據當下狀態與未來預測，回答「現在應該怎麼移動」。

左下方是感知與定位的資料來源。雷射掃描經過障礙物處理、群集與追蹤，提供障礙物的位置和速度估計；定位端則結合地圖、雷射與里程計，提供底盤位姿。這些資料送進控制器之後，預測式 CBF 會把預測到的接近風險轉成約束，讓路徑追蹤和避障限制在同一個最佳化問題裡一起處理，而不是先算一個追蹤命令，再完全不考慮它與障礙物之間的關係。

GMPC 除了希望接近參考路徑，也會考慮速度增量的平滑。這裡的速度增量，就是這次命令和上次命令的差。例如，前一刻速度是每秒零點一公尺，下一刻突然改成反方向，就會有很大的命令變化；平滑項會讓最佳化在追蹤目標的同時，盡量避免這類不必要的急變。不過，平滑是最佳化中的偏好，不等於只靠這一項就能保證加速度永遠不超限，所以輪速與加速度仍需要各自的限制。

右側則是速度平滑器與近距離安全層。近距離安全層直接使用原始雷射資料，限制朝障礙物接近的速度，不必完全依賴前面對物體動靜態的分類。最後命令送到底盤，底盤的運動再透過里程計回傳，形成閉迴路。這一頁的重點是，先前已經建立導航、感知、定位與安全處理的完整資料流；本次是在這個基礎上，加入手臂與新的 Isaac 執行端，而不是把導航系統全部重新做一次。

## 第 5 頁｜全身機器人模型與運動學建立

這一頁先說明機器人模型如何從底盤擴充成移動機械臂。底盤是全向移動平台，在平面上可以沿 x 和 y 方向平移，也可以改變朝向 θ，所以有三個自由度。上方的 Lite 6 手臂有六個關節，合起來就是九個全身運動自由度。投影片上的 q＝[x、y、θ、q₁ 到 q₆]ᵀ，就是把底盤位置、底盤朝向和六個手臂關節角，放進同一個狀態向量；右上角的 T 表示轉置，也就是把它寫成直向的欄向量。

這裡的九個自由度，是用來描述底盤和手臂如何把末端送到目標。夾爪開合雖然也有關節，但不包含在這九維的全身運動求解裡；夾爪是另外處理的操作動作。另外，模型也加入工具中心點 TCP 和腕部相機座標系，讓末端目標、工具方向和後續可能使用的相機資料，都有明確的座標基準。不過，建立相機座標系不等於已經完成視覺辨識或視覺控制。

接著是正向運動學。它回答的問題是：當底盤停在某個位置、朝向某個方向，而且六個關節角都已知時，夾爪末端在世界座標中會在哪裡、朝向哪裡。我從 URDF 讀取連桿尺寸、關節軸向和各段固定變換，再沿著底盤到手臂、最後到 TCP 的運動鏈計算。這樣，後面判斷是否到達目標時，就不必只相信控制器自己回報的誤差，也能用實際量到的關節角和底盤位姿重新計算。

Jacobian 則是在這個模型上，描述速度之間的關係。正向運動學是從「現在的姿態」算出末端位姿，Jacobian 是從「底盤和關節正在怎麼動」算出末端的線速度與角速度。這個全身 Jacobian 有九個輸入欄位，分別對應底盤三個速度和手臂六個關節速度；末端則以三個方向的線速度和三個方向的角速度表示，因此形成六乘九的速度映射。

這個映射的重要性在於，同一個末端動作，可能由底盤、手臂，或者兩者共同完成。例如末端要向前移，可以讓底盤向前，也可以讓手臂伸出去。求解器就是利用這個關係，配合權重和限制，選擇底盤與手臂各自要動多少。不過，九個自由度多於末端六維需求，不代表任何姿態都一定可達，實際上仍受到關節限位、奇異位形與碰撞條件限制。這一頁完成的是後續控制共用的幾何與速度模型。

## 第 6 頁｜Isaac 移植與全身閉迴路驗證

模型建立之後，下一步是讓算出來的命令真的能在 Isaac 裡執行。先前底盤與手臂同動的驗證是在 Gazebo，這次的重點是承接既有的全身求解方法，接通 Isaac 的命令和回授介面。這裡使用的是單步全身 QP，也就是每個控制週期根據當下位姿誤差，求出一組底盤和手臂速度，再等下一次回授更新；它還不是同時規劃未來多個時間步的全身預測控制。

可以沿著下方流程圖由左往右看。首先給定 TCP 的目標位置與姿態，全身 QP 透過前一頁的運動學模型求解九維速度。這組速度經過安全濾波和執行端限制，再交給 Isaac。九個分量使用同一則訊息傳遞，而且在同一個物理步使用同一份命令快照，目的是避免底盤執行新命令、手臂卻還在使用上一筆命令，造成原本協同求出的動作被拆開。

底盤與手臂的執行方式不同。底盤這邊施加速度；手臂則把關節速度轉成位置設定點。投影片上的公式 q_cmd,k+1＝q_cmd,k＋q̇_arm,k Δt_physics，就是說下一步的關節目標位置，等於目前目標位置，加上這一步希望的關節速度乘上物理時間步長。這裡的 q_cmd 是六個手臂關節的位置設定點，不是前一頁包含底盤的九維狀態；q̇_arm 是六個關節速度；Δt_physics 則是模擬器真正推進一次物理計算的時間。

例如，某個關節要求每秒零點零五弧度，而物理步長是零點零一秒，這一步就把位置設定點增加零點零零零五弧度。持續累加，就把速度命令轉成位置驅動能使用的軌跡。這裡特別使用物理時間，而不是程式在電腦上花掉的牆鐘時間，避免算繪變慢時，積分距離也跟著改變。同時，設定點並不等於實際關節位置，驅動仍可能有追蹤落後，所以驗收必須另外看實際運動。

回授則從 Isaac 發布底盤里程計、關節狀態和 TF，再送回求解器與安全層。也就是說，機器人不是只播放一段預先寫好的動作，而是持續根據執行後的狀態重新計算命令。保護方面保留速度與輪級命令限制，以及接收端逾時處置；如果命令中斷，手臂停止積分並保持設定點，底盤依停止策略處理。但停止積分不代表關節當下的實際速度會瞬間變成零，因此先前也另外量測了停止後的位移與收斂。

右側影片展示的是這條閉迴路在自由空間的到位結果，目標標記只用來幫助觀看，不是要接觸的物體。驗收不只看畫面，也用 Isaac 的 TCP 位姿和實測關節狀態重算的正向運動學交叉核對，並確認位置與完整姿態誤差能持續符合條件。這次已經驗證底盤與手臂有連續同動，且能完成指定的自由空間到位。不過，目前的成果是受限配置下的閉迴路驗證，不是原先凍結 B 基線，也不是 WGMPC；長時間保持，以及下游命令修改後的完整避碰保證，仍不能由這次到位結果直接推出。

## 第 7 頁｜底盤朝向目標設計與驗證

這一頁回到導航階段，處理的是全向底盤的車頭朝向。全向底盤和一般車輛不同，即使車頭不轉，也能側移或倒退，所以「沿著路徑到達」和「車頭朝著前進方向」是兩件事。原本若沒有對朝向提出明確要求，控制器可能用大量側移或倒退完成任務。因此，這次加入一個根據路徑建立的朝向參考，讓車頭有方向可以追蹤。

第一步是沿著 Nav2 的路徑選取前視點，也就是位於路徑前方一段距離的位置。投影片把它寫成 x_L、y_L，而 x、y 是機器人當下的位置。公式 θ_raw＝atan2(y_L−y，x_L−x)，就是先算機器人指向前視點的向量，再把它轉成方向角。atan2 會同時考慮 x 和 y 的正負，因此能正確區分方向所在的象限。這個角度是「機器人看向路徑前方」的方向，不是直接拿實際速度方向來當目標，也不一定等於路徑在某一點的切線方向。

第二步是避免這個參考突然跳變。因為前視點會隨機器人移動而更新，在轉角或路徑改變時，直接算出的 θ_raw 可能一下子改很多。所以下面的限制式，是把這一次和上一次參考朝向的角度差，經過 wrap 處理後取絕對值，限制在 Ω_ref 乘上 Δt 以內。其中 Ω_ref 是允許的參考角度變化速率，Δt 是相鄰更新的時間差。

wrap 的意思，是把角度差換成跨越正負一百八十度時仍連續的最短角度差。例如車頭參考從正一百七十九度變成負一百七十九度，實際只差兩度，不應該當成要旋轉三百五十八度。這個限制處理的是「參考方向變得多快」，不是直接保證底盤實際角速度等於這個值；真正的角速度還要由最佳化和執行限制共同決定。

最後，將朝向誤差加入既有 GMPC 的目標函數，讓最佳化一起考慮位置追蹤、朝向追蹤和動作平滑。朝向權重越大，代表在可行的解裡，控制器越重視車頭接近參考方向，但它不是另外加上一個命令，強迫機器人無論如何都要先轉頭。原有避障與速度限制仍需要遵守。朝向權重會改變所選擇的動作，可能間接改變路徑和遭遇障礙物的方式，所以也不能只因為加了朝向，就直接宣稱安全性提高。

右邊的對比是左側 OFF、右側 ON，兩邊按照各自任務開始事件對齊，並以相同的三倍速播放。這支影片是根據原始紀錄進行的狀態重演，不是原趟次的相機實錄。以展示的這組案例來說，車頭與實際移動方向的夾角中位數，從約一百二十七度降到約八度，表示開啟朝向功能後，車頭與行進方向更一致；代價是累計轉角增加，到達時間也略長。兩趟路徑長度很接近，但沒有逐點重疊比較，不能說兩條路徑完全一樣。較大批次的間距評估也沒有取得一致變化方向的顯著證據，所以這一頁的主要成果是朝向行為改善，不是安全性提升的證明。

## 第 8 頁｜定點抽屜操作

這一頁展示的是固定底盤下的抽屜操作。在說明方法之前，先界定這個成果的條件：底盤由固定關節支撐，夾爪與抽屜使用理想固定連接，因此這一版驗證的是手臂能否依照滑軌方向帶動被動抽屜，並完成拉開、保持、解除連接和退出。它不是依靠兩指摩擦夾持完成的兩百毫米操作，也不能直接當成移動底盤開抽屜的成果。

右上方示意圖先把操作問題轉成幾何問題。抽屜只能沿滑軌方向移動，所以不需要替抽屜設計任意的三維路徑，而是用一個開度參數 s 描述進度。圖中的 s＝0 是關閉位置，s＝兩百毫米是目標拉開位置；帶帽子的 a 是滑軌方向的單位向量，只描述方向、不帶長度。因此，p_D(s)＝p_D(0)＋s â 的意思，就是抽屜參考點的位置，等於關閉時的位置，再加上沿滑軌方向移動的距離。p_D 是三維位置向量，下標 D 表示抽屜，計算時 s 與位置使用一致的長度單位，例如把兩百毫米寫成零點二公尺。

知道抽屜應該在哪裡之後，還要把它轉成夾爪應該在哪裡。理想固定連接建立時，會記錄夾爪和抽屜之間的相對位姿；沿著滑軌推進抽屜目標，再利用這個固定關係，就可以得到夾爪每個位置的目標位姿。由於這裡是直線滑軌，參考路徑中的夾爪方向保持不變，主要改變的是位置。這也就是圖上那條夾爪直線目標路徑的來源，而不是隨意在空間裡畫一條直線。

接著，下方的 q_geo(s)＝IK(上標 W 的 T_G,des(s))，表示把每個開度對應的夾爪目標位姿交給逆運動學，求出手臂的關節角。這裡 W 表示世界座標系，G 表示夾爪，des 表示期望值；T 不只包含位置，也包含姿態，所以 IK 解的是夾爪要到哪裡、同時要朝哪個方向。q_geo 則是幾何上對應的六個手臂關節角，因為這個案例的底盤固定，不需要再把底盤三個自由度一起求解。

逆運動學通常不只一組解，所以不是每個路徑點各算一組角度就結束，還要維持相鄰解的連續性，避免前一點和下一點突然跳到不同的手臂姿態。得到連續關節路徑後，再安排時間，讓它符合速度、加速度和關節限位，也檢查沿途的取樣幾何間距。因此，s 描述的是沿滑軌走了多遠，而 s 隨時間如何變化，才決定抽屜拉得多快。這裡的幾何檢查仍是離散取樣，不是連續時間內絕對不碰撞的證明。

實際執行還有一個投影片上沒有另外展開、但成功案例有使用的步驟，就是命令校準。純粹由 IK 算出的幾何路徑，不一定等於物理模擬裡真正走出來的路徑，因為位置驅動會有保持與追蹤誤差。先前量到的偏差雖然只有次毫米到毫米量級，但在剛性連接下，仍可能造成不必要的反作用力。因此，我先用沒有連接抽屜的試驗量出實際路徑偏差，再建立沿行程的前饋修正，加到原本的關節命令上。這是同一模型、路徑、配時與增益下的校準，不是已建立通用的重力補償或力回授控制。

最後，這個固定底盤、理想固定連接的案例，已經把抽屜拉到約一百九十八點七毫米，距離兩百毫米目標約差一點三毫米，並在解除連接之前完成到位保持，之後才釋放和退出。影片是執行當下錄製的，不是把紀錄中的位置重新擺回場景的姿態重演。這一頁最主要的成果，是把「物件的運動限制」轉成「末端目標位姿」，再透過逆運動學、配時和致動校準，完成一段可驗證的受約束操作；真正的摩擦抓取與底盤同步移動，則仍要分別驗證，不能由這個結果直接延伸宣稱完成。

## 第 9 頁｜未來工作

接下來第一個方向，是把目前分開驗證的導航與操作串起來。現在已經有底盤導航、自由空間的底盤手臂閉迴路，以及固定底盤的抽屜操作，但它們還不是一個自動接續的完整流程。後續需要讓機器人先移動到操作區，再確認停放的位置和朝向是否足夠準確、底盤是否已經穩定，最後才交接給手臂開始操作。如果還沒有對準，就應該先調整，而不是直接把導航到達視為手臂可以開始的條件。

第二個方向，是發展全身預測控制。白話來說，就是不只決定底盤和手臂「現在這一步要怎麼動」，還要提前考慮「接下來這一段一起怎麼動比較合適」。目前全身 QP 是根據當下狀態求出一組速度；未來則希望利用模型，預測多個時間步的底盤位置、手臂姿態與末端路徑，在滿足限制的前提下，一起規劃這段動作。每次只執行規劃中的第一步，收到新狀態後再重新計算，持續修正預測和實際之間的差異。

例如在拉開抽屜時，如果一直固定底盤，手臂可能越拉越接近不適合操作的姿態；全身預測控制就可以提前考慮，是否需要讓底盤配合後退，讓手臂維持較合適的工作範圍。開門時也類似，需要同時考慮把手的圓弧路徑、底盤如何移動，以及周圍空間是否足夠。不過，這些是後續要驗證的能力，不能只因為改成多步最佳化，就保證操作一定更順暢或安全。下一階段會以現在的單步控制和既有案例作為比較基礎，量化到達、平滑程度與限制滿足情形，再逐步擴展到移動中的操作。

## 第 10 頁｜結語

以上是這次的進度報告。這次主要建立了底盤與手臂共用的九自由度模型，完成 Isaac 的全身控制與回授介面，驗證自由空間到位；另外也完成路徑前視朝向的設計，以及固定底盤、理想固定連接下約兩百毫米的抽屜操作。接下來會把導航與操作串接起來，並進一步發展能提前規劃底盤和手臂動作的全身預測控制。以上，謝謝老師。
