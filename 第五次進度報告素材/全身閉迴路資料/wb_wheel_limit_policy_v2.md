# 完整 9 維命令的輪級限制修改策略 v2

> **v1 未用於任何趟次即被取代**，無回溯問題。v1 有兩處錯誤：
> 1. 把底盤分量的座標系寫成 `report_frame`。**錯** —— 執行端收到的
>    `/wb_vel_cmd` 已由 adapter 轉成**本體座標**，W 必須作用在本體速度。
> 2. 逾時沿用「完整 9 維插值」與「手臂設定點凍結」**互相矛盾**：
>    `u_out = (1−λ)·u_prev`，λ<1 且前一筆手臂速度非零時，
>    輸出手臂速度仍非零，設定點會繼續積分。

**執行版本 E2**。E1（`isaac_wholebody_port_20260913.md` §9.4b）與其既有結果
**保持不變**，不在原地改動。

適用命令：`[vx, vy, wz, dq1..dq6]`。

**底盤三分量在「底盤本體座標」**，不是 `report_frame`、也不是世界座標。
安全層在 `report_frame` 解算，`arm_vel_adapter` 轉成本體座標後發
`/wb_vel_cmd`，執行端再轉回世界座標才呼叫速度 API。
本策略作用在**執行端收到的本體命令**上：

    r·ω = W · ξ_body

`u_prev` 必須保存為**上一物理步真正套用的本體命令**。
yaw ≈ 0 時本體與 report frame 碰巧一致，**不可據此混稱**；
離線測試含 yaw = 0°／40°／90° 的對照。

---

## 0. 這份規格要解決的問題

E1 的 `WHEEL_LIMIT_IMPLEMENTED = False`，pregrasp 由程式常數無條件禁止。
要解除那個禁止，**不能只把旗標翻成 True** —— 輪級限制會**修改命令**，
而被修改後的命令**不再是任何求解器的輸出**，上游據原命令算出的
全身安全性（距離約束、耦合運動）**不會自動保留**。

因此本規格先固定「命令怎麼被修改」，再談驗證。

---

## 1. 每個物理步的處理順序（固定）

| 步 | 動作 | 失敗處置 |
|---|---|---|
| 1 | 結構檢查（長度 9、全有限） | **整筆拒收**（E1 行為，不變） |
| 2 | 模式檢查（不符模式的分量） | **整筆拒收**（E1 行為，不變） |
| 3 | 手臂速率檢查 `|dqi| ≤ arm_rate_max` | **整筆拒收**（E1 行為，不變） |
| 4 | **輪級限制** | 見 §2；前提不成立時進入停止模式 |
| 5 | 套用：底盤速度 ＝ 限制後的底盤分量；手臂設定點 `+= 限制後的 dq × dt` | — |

手臂速率仍用**整筆拒收**，不改成裁切 —— 裁切之後執行的就不是任何求解器
產生的命令。輪級限制則**必須**能修改，否則無法保證輪級合規。
兩者的差別在 §2 的「同一個 λ」上被處理。

---

## 2. 修改策略：**同一個 λ 施加於完整 9 維增量**

設前一筆**實際套用**的命令為 `u_prev`（9 維），本步請求為 `u_req`。

    u_out = u_prev + λ · (u_req − u_prev),    λ ∈ [0, 1]

λ 由**底盤**分量的輪級約束決定（§3），但**施加於全部 9 個分量**。

### 為何是同一個 λ

只縮底盤、不動手臂，會讓底盤與手臂的相對比例被改掉 ——
上游求解出的耦合運動方向就沒了。同一個 λ 讓 `u_out` 落在
**完整 9 維空間中** `u_prev` 到 `u_req` 的線段上。

**保留的是 `u_req − u_prev` 這個增量的方向**，
**不是**一般情況下請求命令本身的底盤／手臂比例 ——
只有 `u_prev = 0` 時兩者才恰好一致。

### 這**不能**宣稱什麼

* `u_out` **不是**求解器的輸出。上游的距離約束是對 `u_req` 算的，
  不是對 `u_out`。**不得宣稱上游全身安全性不變。**
* 保留的是**增量方向**，不是任何安全裕度。
* λ < 1 時必須在 log 標記 `modified = True` 與限制原因。

### 手臂限制為何仍然成立

`u_prev` 與 `u_req` 的手臂分量都已通過步驟 3（`≤ arm_rate_max`）。
凸組合不會超出兩端點的絕對值上界，故 `u_out` 的手臂分量
**必然** `≤ arm_rate_max`。這是可離線證明的性質，列為測試項。

---

## 3. λ 的計算

輪速映射（列序與 `gmpc` 的耦合約束一致，L = 0.245 m 取自 URDF
`base_link_rim_*_joint` 的 ±0.245）：

    r·ω = W · u_base,    W = [[0, 1, L], [−1, 0, L], [0, −1, L], [1, 0, L]]

    b = W · u_prev,base
    d = W · (u_req,base − u_prev,base)

限制（**正常模式同時滿足兩者**，沿用 `design_wheel_limit_chain.md` 的裁決）：

| 約束 | 式 | 參數 |
|---|---|---|
| 輪速 | `|b + λd| ≤ w_lim` | `w_lim = r · ω_max` = 0.05 × 5.55 = **0.2775 m/s** |
| 輪加速度 | `|λd| ≤ a_lim` | `a_lim = r · α_max · dt` = 0.05 × 125.0 × dt |

λ 取所有列的最小可行值，再截到 `[0, 1]`。

**dt 用物理步長**（Isaac `physics_dt`，本階段 0.01 s），
不是牆鐘、不是發布間隔 —— 執行端是每個物理步套用一次。

### 前提與失敗處置

正常模式的前提是 **`u_prev` 本身符合輪速限制**。此時交集必非空（λ=0 即 `u_prev`）。

前提不成立（`|b| > w_lim`）→ 回傳 `prev_infeasible`，**不偷偷換基準**。
呼叫端進入 `stop_unverified` 模式：底盤送零，**並明確標記該步不在
輪級加速度保證之內**。這與 `design_wheel_limit_chain.md`
「直接送零不納入輪級合規保證」一致。

`dt ≤ 0` 或 `dt > dt_max` → 同樣進入 `stop_unverified`，
**不得**把中斷期間當成加速度預算。

---

## 4. 逾時：**完整 9 維插值的例外**

沿用 §2 的插值會與「逾時凍結設定點」**直接矛盾**：

    u_out = u_prev + λ·(0 − u_prev) = (1 − λ)·u_prev

λ < 1 且前一筆手臂速度非零時，**輸出的手臂速度仍然非零，設定點會繼續積分**。

    例：u_prev 底盤 0.25 m/s、joint2 0.05 rad/s，單步預算 0.0625 m/s
        → λ = 0.25，v_out = 0.1875 m/s，dq2_out = 0.0375 rad/s（設定點還在動）

因此**正常與逾時明確分開**：

| 模式 | 底盤 | 手臂 | 耦合方向 |
|---|---|---|---|
| **正常** | 共用 λ 的 9 維增量插值 | 同一個 λ | 保留（指增量方向） |
| **逾時** | 經輪級限制**減速** | **速率立即設為 0**，設定點停止積分（E1 語意） | **不保留**，`coupling_preserved = False` |
| **stop_unverified** | 直接送零 | 零 | 不適用；**不在加速度保證內** |

逾時模式必須記錄**最後真正套用的整筆命令**，且**不得宣稱保留耦合方向**。

### 與 E1 的差異，以及它在本階段看不出來

E1：命令過期 → 底盤**瞬間歸零**、手臂設定點凍結。
E2：底盤經限制器減速、手臂設定點**一樣立即**凍結。

| 輪速 | 單步預算 0.0625 m/s | 減速步數 |
|---|---|---|
| **0.03 m/s（先前趟次幅度）** | 已足夠 | **1 步 —— 與 E1 無法區分** |
| 0.25 m/s | 不足 | 4 步 |

所以先前的斷訊趟次**不能**用來證明 E2 的減速行為。
要驗證必須讓輪速超過單步預算，或讓正常模式的 λ 真的小於 1。

## 5. 記錄（每個物理步）

| 欄位 | 意義 |
|---|---|
| `u_req` (9) | 修改**前**的命令 |
| `u_out` (9) | 修改**後**、實際套用的命令 |
| `u_prev` (9) | **前一筆實際套用**的命令（λ 的基準） |
| `lam` | 縮放係數 |
| `modified` | λ < 1 |
| `reason` | `ok` / `wheel_speed` / `wheel_accel` / `both` / `prev_infeasible` / `dt_invalid` |
| `dt` | 本步物理步長 |
| `wheel_speed_max` | `max|W·u_out,base|` |
| `wheel_accel_max` | `max|W·(u_out,base − u_prev,base)| / dt` |
| `mode` | `normal` / `timeout` / `stop_unverified` |
| `coupling_preserved` | 逾時為 False |

---

## 6. pregrasp 仍然禁止

實作輪級限制**不足以**解除 pregrasp 禁止。還缺：

1. 上游安全性在「命令被修改後」的重新論證 —— §2 明說目前**沒有**
2. 停止掃掠範圍的避碰保證 —— 見停止預算撤回書，目前**沒有**
3. 接觸情境的判準 —— 目前的工程判準只涵蓋低速自由空間

因此 E2 的 `WHEEL_LIMIT_IMPLEMENTED` 即使為 True，
**pregrasp 仍由另一個獨立條件禁止**，不由單一旗標決定。

---

## 7. 驗收：不得只跑 λ 全為 1 的剖面

先前趟次的幅度（0.03 m/s）**根本不會觸發限制**。
要宣告限制器驗收完成，趟次必須**實際觸發**限制（λ < 1 的樣本數 > 0）。

**不得為了觸發限制而提高既有速度界限。** 現行低速介面界限
`lin_mps = 0.05`、`ang_rps = 0.2` 保持不變 ——
改的是**剖面形狀**（階躍與反向，而非斜坡），不是幅度上限。
