# 影片說明：Isaac 全身閉迴路 — 自由空間到位（**含目標標記**）

檔案：`Isaac全身閉迴路_自由空間到位_含目標標記.mp4`
1920×1080、**8.0 s**、320 幀、1.4 MB。

這是**推薦在報告播放的版本**。同目錄的
`Isaac全身閉迴路_自由空間到位.mp4`（33 s、無標記）為早先版本，保留不動。

---

## 性質

**模擬器內相機在趟次執行當下錄下的畫面** —— 不是桌面螢幕錄影、不是姿態重演。
畫面內**沒有任何文字**：無片頭、字幕、數值、時間戳或浮水印。

## 目標標記（本版新增）

* 半透明目標點（半徑 20 mm）＋ 短三軸（長 80 mm）表示**目標姿態**
* 姿態定義與判準同一組：工具 +z → 世界 +x、工具 +x → 世界 +z
* **純視覺：無碰撞體、無剛體**。執行端建立後自我查核，
  驗到 `UsdPhysics.CollisionAPI` 或 `RigidBodyAPI` 即中止
* 不會被距離節點當成障礙物 —— 該節點的障礙物來自自己的 `obstacles` 參數，
  **不掃 stage**
* 只在錄影時建立；不錄影的趟次場景完全不受影響

## 後製

只有兩項，都不改內容：

1. **時間裁切**：錄影窗為 sim 10.0–55.0，本片取 **sim 10.03 – 25.98**
   （到達於 sim **18.00**，其後保持約 8 s）
2. **播放加速**：以 20 fps 錄、40 fps 播 = **2 倍速**

## 這一趟的數字

趟次 `wb_solver_iso_rec_142105`，**23/23 項事前判準通過**。

| 量 | 值 |
|---|---|
| 到達（連續符合條件之始） | sim 18.00 s |
| 連續符合到達條件的時間 | 34.36 s |
| TCP 位置誤差（全程最小，Isaac `link_tcp`） | 1.610 mm |
| 同上，實測 FK 重算 | 1.595 mm |
| 兩來源一致 | 0.220 mm |
| 完整三維姿態誤差（最小） | 0.00283 rad |
| 實測連續同動 | 4.64 s |
| **同動占任務時間** | **94.1 %**（任務 4.93 s，首個非零命令 sim 13.08 → 到達 18.00；只有底盤 5.7 %、只有手臂 0.2 %） |
| 底盤線速度 max | 0.035268 m/s |

> **更正（2026-09-14）**：本表先前寫「77.2 %（任務 6.0 s）」。
> 當時的算法把區間起點**寫死在 sim 12 s**，不是任務實際開始（首個非零命令 sim 13.08）；
> 多算進的 1.08 s 兩者都沒動，壓低了占比、也把任務長度標錯。以正確起點重算為 **94.1 %**。
> 同一錯誤也影響其他趟：驗收趟 `100502` 原報 23.4 %、重算 **93.4 %**。

> **這一支是展示全身同時運動的較佳版本。** 同目錄的長距離版
> （`Isaac全身閉迴路_長距離到位.mp4`）同動只占 **17.0 %**（原報 13.7 %，同一起點錯誤），
> 主要是「手臂先擺姿勢、底盤長程接送」。
> 需要**直式、全車可見**的版本，見 `Isaac全身閉迴路_自由空間到位_直式.mp4`（另一趟錄影）。

## 與報告引用趟次的關係

**報告 §12 引用的驗收趟次是 `wb_solver_iso_100502`**（未錄影）。
本影片來自**另一趟錄影趟**，配置相同（目標、門檻、增益、執行界限、安全鏈未改），
差別只有：開了模擬器內相機、加了純視覺目標標記。
**該趟自身也 23/23 通過。**

兩趟數字略有差異 —— **這是趟間差異，不是同一趟的兩種說法**。引用時請指明哪一趟。

## 三條界線（與報告一致）

1. **最小誤差不是最終精度。** 表中為全程最小值；到達後仍有長尾漂移，
   **長時間保持尚未驗收**。
2. **速度限制沒有被取消。** 關節位置限位、速度框、加速度框、jerk 框
   與執行端最終檢查全程在線；命令拒收 0、執行端失效無。
3. **不是 B 基線、不是 WGMPC。** 修改後命令的上游全身安全性未重新論證、
   停止掃掠範圍無避碰保證、接觸操作不在本趟範圍。
   本片呈現的是**自由空間到位**，不是整個 S2、不是接觸操作。

## 報告播放時的一句口頭說明

> 模擬器內相機的執行實錄，2 倍速；橘色點與三軸是 TCP 的目標位姿，
> 純視覺標記、不參與物理與感測。底盤與手臂同時動，把夾爪送到該位姿。
