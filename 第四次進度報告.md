# 第四次進度報告

**動態環境下移動機械臂之預測導航與受約束操作**
主軸:SE(2) 幾何 MPC + 全時域預測式控制屏障函數(GMPC + CBF)

撰寫時間:2026-08-07
平台:ROS 2 Jazzy + gz Sim Harmonic,omni_bot 全向底盤 + 2D LiDAR

---

## 摘要

> **第三次報告的結束狀態**:`omni_bot` 平台移植完成,真實 LiDAR 感知取代真值障礙物,在 `random_room_dynamic`(20×20 m、4 個移動體、**固定起訖點**)上以 N=40 取得成功率 98%、動態碰撞 0。

本期工作分五層:

| 層 | 內容 |
|---|---|
| **場景** | 建立 `bigarena` 泛化測試場景:8 隔間、交錯門洞、10 個移動體、7 個未知靜物、**隨機起訖點** |
| **硬體** | 由輪系 Jacobian 導入實際運動限制,取代沿用的估計值 |
| **控制** | 輪速耦合多面體、位姿來源修正 |
| **感知** | 表面點表示、淨位移動靜分流;並找出其適用邊界 |
| **安全** | Raw-scan Safety Shield —— 不依賴分類鏈的最後一層 |

**本期的主要發現不是某個參數調對了,而是一個架構缺口:**

$$\text{感測可見} \;\nRightarrow\; \text{進入 CBF}$$

感知與控制之間有一連串硬閘門(cluster → track → age → 瞬時速度 → 淨位移),任一關擋掉,該障礙物就**完全沒有約束**。逐一調整門檻只能關掉個案,因為所有門檻都建立在同一個有偏的質心速度估計上(§5)。Raw-scan Safety Shield 是對此的架構回應:它只要求「有有效回波就不得繼續逼近」,不需要知道物體是什麼、在不在動。

**新增的量測基礎設施同樣重要**:本期量到同路線同設定的趟間差異達 0.043–0.479 m,**大於設定之間的效應(0.11 m)**。這使得多數單項調整在統計上無法證明,也是本期把論證重心放在**機制證據**(每趟數萬個控制週期)而非碰撞次數的原因。

## 〇、硬體參數修正(2026-08-08)

取得底盤的實測硬體參數後,發現先前的運動限制與硬體不符,且鏈上有兩處覆蓋使部分
實驗實際上沒有生效。

### 由輪系推導的真實上限

4 個全向輪,$r = 0.05$ m、$L = 0.245$ m、$\omega_{\max} = 5.55$ rad/s(最大效率點)、
$\alpha_{\max} = 125$ rad/s²。底盤 Jacobian:

$$v_x = \frac{r}{2}(\omega_d - \omega_b),\qquad
v_y = \frac{r}{2}(\omega_a - \omega_c),\qquad
\dot\phi = \frac{r}{4L}\sum_i \omega_i$$

兩個平移軸用**不同的輪對**,因此獨立飽和;旋轉共用四輪,與平移競爭。

| 量 | 推導 | 值 |
|---|---|---|
| $v_{x,\max} = v_{y,\max}$ | $r\,\omega_{\max}$ | **0.2775 m/s** |
| $v_{\max}$(斜向) | $\sqrt2 \times 0.2775$ | 0.3924 m/s |
| $\dot\phi_{\max}$ | $\dfrac{r}{L}\omega_{\max}$ | **1.1327 rad/s** |
| $a_{x,\max} = a_{y,\max}$ | $r\,\alpha_{\max}$ | **6.25 m/s²** |
| $\ddot\phi_{\max}$ | $\dfrac{r}{L}\alpha_{\max}$ | **25.51 rad/s²** |

### 先前設定的三個問題

| | 先前 | 硬體 | 後果 |
|---|---|---|---|
| $v_x$ | ±0.35 | **±0.2775** | **超出 26%**,所有以此計算的逃逸速度與可行性邊界皆偏樂觀 |
| $a_x/a_y/a_z$ | 0.8/0.6/1.2 | **6.25/6.25/25.51** | 使用了硬體 1/8 的加速度能力 |
| smoother `min_velocity` | −0.20(x 軸) | ±0.2775 | GMPC 輸出經此節點才到底盤,**後退一直被夾在 0.20** |

後兩項都是「下游覆蓋上游」:`gmpc_params.yaml` 寫 1.5/1.0/2.0,但 launch 的
`cbf_overrides` 在其後套用、預設 0.8/0.6/1.2;而 `velocity_smoother` 又在 GMPC
之後夾住命令。因此:

- **「階段 4:提高加速度使碰撞 1→0」的結論作廢** —— 加速度從未改變
- **「`vx_min` −0.20 → −0.35」的實驗作廢** —— smoother 夾在 −0.20,輪端沒有變化

### 修正後的可行性邊界

$\alpha = 0.5$、$r_{\text{eff}} = 0.90$、$v_{\text{esc}} = 0.2775$:

| $v_{\text{obs}}$ | $d^*$ | 淨脫離速度 |
|---|---|---|
| 0.10 | 0.61 m | 0.178 m/s |
| **0.18(場景上限)** | **0.73 m** | **0.098 m/s** |
| 0.22 | 0.79 m | 0.058 m/s |
| **0.28** | **0.91 m** | **≤ 0 —— 無法脫離** |

場景的移動體上限 0.18 m/s 距離不可行邊界(0.2775)比先前以為的更近:淨脫離速度
只有 0.098 m/s,而非先前用 $v_{\text{esc}} = 0.35$ 算出的 0.17。

### 三個方法現已統一

GMPC、MPPI 與各自的 `velocity_smoother` 全部設為硬體上限。RPP 的 $v_y$ 維持 0
—— 純追蹤法本身不產生側移指令,那是演算法特性而非人為限制,但比較時必須註明。

**所有先前批次因此不可與後續比較**,效能數字需重跑。

---

## 一、場景:`bigarena`

### 1.1 設計

| | `random_room`(第三次) | `bigarena`(新增) |
|---|---|---|
| 格局 | 開放房間 | **8 個隔間 + 交錯門洞(2.0–2.5 m)** |
| 已知障礙 | 牆 | 牆 + **18 個已知箱體** |
| 未知靜物 | 3 | **7** |
| 移動體 | 4 | **10** |
| 移動體速度 | 0.15–0.30 m/s | **0.10–0.18 m/s** |
| 起訖點 | 固定 (0,0)→(17,17) | **隨機取樣** |
| 典型路徑 | 38 m | 20 m |
| 物理步長 | 0.001 s | **0.01 s**(53 個碰撞體下 RTF 0.27 → 可用) |

由 `generate_bigarena.py` 產生。障礙物以拒絕取樣放置,並用 BFS 在 inflation 0.45 下驗證連通性;移動體的來回路線以「兩體前推 600 s 不重疊」的檢查保證彼此不卡死。

**設計原則**:不刻意把障礙物擠在路線上。8 個隔間提供多條替代路徑,使「選哪條路」成為真正的決策而非形式;移動體做長距離穿越而非針對機器人,遭遇由時序自然產生。

### 1.2 移動體速度上限的依據

由 CBF 的可行性邊界決定(見 §3.5),不是任意設定。逃逸速度 $v_{\text{esc}} = |v_{x,\min}|$
即底盤單軸上限;當 $v_{\text{obs}} \ge v_{\text{esc}}$ 時機器人無法拉開距離,約束在
keep-out 內處處不可行。

以硬體真值 $v_{\text{esc}} = 0.2775$ 逐一檢核十個移動體。判準取「從 $d^*$ 再拉開
0.1 m 所需時間」,必須小於預測時域 1.0 s:

$$t_{0.1} = \frac{0.1}{v_{\text{esc}} - v_{\text{obs}}}$$

| 移動體 | 速度 | 半徑 | $d^*$ | $t_{0.1}$ |
|---|---|---|---|---|
| dyn_obs_3 | 0.18 → **0.14** | 0.15 | 0.87 | 1.0 s → **0.73 s** |
| dyn_obs_9 | 0.16 → **0.14** | 0.20 | 0.89 | 0.9 s → **0.73 s** |
| dyn_obs_0 | 0.15 → **0.14** | 0.25 | 0.92 | 0.8 s → **0.73 s** |
| 其餘七個 | 0.10–0.14(未改) | 0.15–0.30 | 0.75–0.93 | 0.6–0.7 s |

原先最快的三個,$t_{0.1}$ 為 0.8–1.0 s,等於或超過整個預測時域 —— **正面遭遇一旦
進入 $d^*$ 內,在時域中無解**,這與 $\alpha$、margin、平滑度或線性化都無關,是輪徑
與轉速決定的物理下限。原設定的 0.18 上限是以錯誤的 $v_{\text{esc}} = 0.20$ 推得
(見 §0),以硬體真值重推後本就應調整。

**只調三個、七個不動**:場景的擁擠度、路徑封閉率與遭遇頻率不變,改變的僅是那些
在時域內原本就無解的遭遇。中位移動體速度由 0.125 變為 0.13,幾乎不動。

### 1.3 隨機起訖點

`random_poses.py` 改為光柵化 **world SDF** 而非佔據地圖取樣 —— 原本用地圖取樣時,未知靜物不在地圖上,30 個起點中有 6 個生成在障礙物內或其上。

**測試集組成**:三次獨立抽樣各 30 條(A/B/C),合計 90 條(89 條有效)。三組的負間距趟數為 8/30、2/29、13/30,卡方檢定 $\chi^2 = 10.23$, $p = 0.006$ —— **三組不同質**,因此:

- 跨配置比較**必須用同一組路線**,否則路線難度會被誤認為配置效果
- 報告數字必須合併多組,且說明抽樣方式
- 後續改用單一抽樣的 300 條檔案,避免分組不同質的問題

---

## 二、與第三次報告的異動

### 2.1 刪除

| 項目 | 第三次 | 現在 |
|---|---|---|
| `Q_yaw` 參數 | 7.0 | **參數移除**,寫死 0 |
| `ref_yaw_lookahead` | 有 | **移除** |
| `ref_yaw_rate_max` | 有 | **移除** |
| `_limit_ref_yaw_rate()` | 有 | **移除** |
| `_interp_xy()` | 有 | **移除**(唯一使用者是 lookahead 弦) |
| `YAW_TRACK` 環境開關 | 有 | **移除** |

### 2.2 新增

**控制層**:`cbf_prune_range` / `cbf_near_steps` / `cbf_far_stride`(約束剪枝)、`margin_mode` 及其四個參數(速度相關 keep-out,**預設關閉**)、`cbf_vel_margin_gain`(接近率相關 margin,**預設 0**)。

**感知層**:表面點表示(`surf_pts_max` / `surf_pts_range` / `surf_pts_min_sep`)、淨位移分流(`static_window_s` / `min_net_speed`)、`young_track_range`、`static_keep_velocity`(**預設關閉**)。

**感測層**:`MASK_HW` 環境開關。

**場景**:`bigarena.sdf` + `generate_bigarena.py`;`gmpc_nocbf` 消融方法;baseline launch 支援 `BIGARENA`/`TRAJ`/`SPAWN_X`/`SPAWN_Y` 及三個 NVIDIA EGL 變數。

**評測**:`analyze_trials.py`、`loc_error.py`、`coverage.py`、`guard_first_trial.sh`。

### 2.3 參數變更

| 層 | 參數 | 第三次 | 現在 |
|---|---|---|---|
| 車體 | gz 碰撞幾何 | 0.45×0.45 方盒 | **圓柱 r = 0.300** |
| | 速度上限 $v_x$/$v_y$/$\dot\phi$ | 0.20 / 0.25 / 0.80 | **0.2775 / 0.2775 / 1.1327**(硬體推導,§0) |
| | 加速度上限 | 0.8 / 0.6 / 1.2 | **6.25 / 6.25 / 25.51**(硬體推導,§0) |
| 感測 | 遮罩半寬 | 15°(遮 120/360 束) | **10°(遮 80 束)** |
| 定位 | `pose0_rejection_threshold` | 2.5 | **1.0e9(關閉)** |
| 感知 | `max_cluster_radius` | 0.60 | **1.20** |
| | `static_window_s` | 0.8 | **2.0** |
| | `min_net_speed` | 0.0(關閉) | **0.05** |
| 規劃 | `inflation` | 0.45 | **0.70** |
| | `replan_period` | 3.0 s | **1.0 s** |
| | costmap 來源 | `/scan` | **`/scan_filtered`** |
| CBF | `cbf_alpha` | 3.0 | **0.5** |
| | 動態 margin | 0.38 | **0.60** |
| | 靜態 margin | 0.33 | **0.38** |

| 控制 | 位姿來源 | TF 合成 `map→base_footprint` | **直接訂閱 EKF 的 `/odometry/filtered`** |
| 安全 | — | 無 | **Raw-scan Safety Shield(`SHIELD=1`)** |

**未變**:horizon 20 × 0.05 s、控制頻率 20 Hz、`Q_xy` 15、`R` 2/2/1、`S` 15/15/8、`Qf_mult` 5、AMCL beam-skip 組態、EKF 融合架構。

**位姿來源為何要改**:`lookup_transform(map, base_footprint, Time())` 取「最新可得」,而 tf2 不保證 `map→odom` 與 `odom→base_footprint` 來自**同一時刻**。某一段延遲時,合成位姿會錯掉機器人在該期間的位移。量測顯示這是**偶發**而非系統性 —— 多數趟次 TF 與 EKF 的中位差為 0.000 m,但個別趟次中位 0.176、峰值 **0.826 m**,而同期 EKF 對真值的誤差僅 0.039 m。定位是好的,錯的是控制器取用它的方式。改為直接訂閱後,TF 保留作為後備與診斷參照。

---

## 三、新增機制的原理

### 3.1 移除航向參考

求解器回傳參考旋量加修正量:

$$u^\* = \xi_{\text{ref}}(0) + \delta\xi^\*(0)$$

參考視野由路徑弧長取樣,參考旋量取自相對位姿的對數映射:

$$\xi_{\text{ref}}(k) = \frac{1}{\Delta t}\log\!\left(X_{\text{ref}}(k)^{-1} X_{\text{ref}}(k{+}1)\right) \in \mathfrak{se}(2)$$

第三分量即參考航向的角速度。**`Q_yaw` 只懲罰航向誤差,不影響前饋** —— 令 `Q_yaw = 0` 並不能阻止旋轉被送到底盤。

**做法**:令所有取樣點的航向等於機器人當下航向 $\theta_{\text{now}}$:

$$X_{\text{ref}}(k) = \begin{bmatrix} R(\theta_{\text{now}}) & p_{\text{ref}}(k) \\ 0 & 1\end{bmatrix}
\Longrightarrow
\xi_{\text{ref}}(k) = \frac{1}{\Delta t} R(\theta_{\text{now}})^{\!\top}\!\left(p_{\text{ref}}(k{+}1) - p_{\text{ref}}(k)\right),\quad \xi_{\text{ref},\theta} \equiv 0$$

連續兩個參考位姿只差平移,角速度前饋恆為零。$w_z$ 仍是自由輸入,QP 需要時可以轉。

**為何三種航向來源都不可用**(半徑 3 m 弧、$v_{\text{nom}} = 0.22$):

| 來源 | 前饋 $\xi_{\theta}(0)$ | 全視野 $\max|\xi_\theta|$ |
|---|---|---|
| 路徑切線 | 0.000 | 0.158 |
| 1.2 m lookahead 弦 | 0.100 | 0.101 |
| **無航向參考** | **0.000** | **0.000** |

SmacPlanner2D 自帶朝向亦不可用:8 趟、541 條 `/plan`、113536 個路徑區段實測,隱含角速度中位 0.067、p99 1.356、**最大 18.85 rad/s**(上限 1.13),1.4% 的區段超過 $w_{z,\max}$,走完一條路徑累計需轉**中位 448°、最大 5805°**(幾何上只需 90–180°)。成因是 Smac 平滑器輸出間距 0.05–0.15 m,該尺度下微小側向抖動產生巨大朝向變化。

**效果**(K → M,同 24 條路線配對):

| | 碰撞 | 到達 | 間距中位 | 旋轉中位 | solve>50ms |
|---|---|---|---|---|---|
| 有航向參考 | 9 | 23/24 | +0.064 | **2387°** | 1.3% |
| **無航向參考** | **1** | **24/24** | **+0.167** | **138°** | **0.0%** |

### 3.2 全時域 CBF 與約束剪枝

障礙物以表面點表示,等速外插預測:

$$o_i(k) = o_i(0) + k\,\Delta t\, v_i$$

障礙函數採平方形式(避免開根號的奇異性):

$$h_i(k) = \lVert p(k) - o_i(k)\rVert^2 - r_{\text{eff},i}^2, \qquad r_{\text{eff},i} = r_i + m_i$$

離散 CBF 條件 $\dot h + \alpha h \ge 0$ 對 $\delta\xi_k$ 線性化。令 $d_k = p(k) - o_i(k)$、$R_k$ 為第 $k$ 步參考姿態:

$$2 d_k^{\!\top} R_k\,\delta\xi_k + \varepsilon_k \;\ge\; 2 d_k^{\!\top} v_i - \alpha h_i(k) - 2 d_k^{\!\top} R_k\, \xi_{\text{ref}}(k)$$

$\varepsilon_k$ 為每步的鬆弛變數。$k = 0$ 的懲罰放大 $\times 100$(近似硬約束但保持可行);靜態點的鬆弛另外放大 `cbf_static_slack_scale` 倍,使被夾在移動體與牆之間時優先讓出動態餘裕。

**Gain scheduling**(連續斜坡,非開關):

$$\text{danger} = \max\!\left(0,\; 1 - \frac{\max(0, h_{\min})}{h_{\text{thresh}}}\right),\quad
Q_{\text{eff}} = \left[1 - (1 - \kappa_Q)\,\text{danger}\right] Q,\quad
w_{\varepsilon,\text{eff}} = \left[1 + (\kappa_\varepsilon - 1)\,\text{danger}\right] w_\varepsilon$$

$h_{\text{thresh}} = 0.4$、$\kappa_Q = 0.2$、$\kappa_\varepsilon = 20$。$h = h_{\text{thresh}}$ 時 danger $= 0$,權重恰為標稱值,**無不連續點**。

**剪枝**。約束列數為 $n_{\text{obs}} \times N$,而 $n_{\text{obs}}$ 是表面點數(實測中位 8、峰值 40,即 160–800 列),OSQP 的 p99 求解時間曾達 425 ms。兩道篩選:

$$\mathcal{K} = \{k \le k_{\text{near}}\} \cup \{k : (k - k_{\text{near}}) \bmod s = 0\},\qquad k_{\text{near}} = 6,\ s = 2$$

$$\text{保留 } i \iff \lVert p(0) - o_i(0)\rVert \le d_{\text{prune}} + N\Delta t\,(v_{\max} + \lVert v_i \rVert)$$

依據:$d_{\text{prune}} = 1.2$ m 外的點在 1 s 內不可能被違反;時域後半是等速外插,精度本來就低。單元測試:14 點 × 20 步 = 280 列 → **38 列(降 86%)**,`h_now` 每點仍完整計算,關閉時與原本逐元素相同。實測 solve > 50 ms 從 2.34% 降至 0.38%,無避障退步。

### 3.3 表面點表示

不對群集擬合形狀。取本週期的雷射回波,依距離排序,以 $\delta_{\min} = 0.20$ m 稀疏化後取最近 $n_{\max} = 4$ 個:

$$S_i = \left\{q \in P_i \;:\; \lVert q - p_{\text{robot}}\rVert \le 2.5,\ \min_{q' \in S_i} \lVert q - q'\rVert \ge \delta_{\min}\right\},\qquad |S_i| \le n_{\max}$$

**原理**:雷射回波本身就是表面上的點,是唯一實際觀測到的量。擬合形狀等於加入假設 —— 對長寬比超過約 2:1 的物體,擬合誤差大於它消除的偏差。稀疏化的理由:相距 2 cm 的兩個回波承載同一個約束,不稀疏化會讓 $n_{\max}$ 用在同一小塊表面,其餘表面失去約束。

軌跡本週期無回波時(被coast過)退回追蹤中心,即舊行為。

### 3.4 淨位移動/靜分流

瞬時 KF 速度會被遮擋與質心滑動灌大(靜止圓柱上實測到 2 m/s)。改用時間窗內的淨位移:

$$\bar v_i = \frac{\lVert p_i(t) - p_i(t - T_w)\rVert}{T_w},\qquad T_w = 2.0\ \text{s}$$

判定為移動體需**同時**滿足:$\text{age} \ge 3$ 幀、瞬時速度過遲滯閘門、$\bar v_i \ge 0.05$ m/s。靜止物體原地抖動 → $\bar v \approx 0$;移動體持續累積位移。

速度閘門採遲滯:進入需達 `min_track_speed`,退出需低於 `release_track_speed`,並再保持 `track_release_frames` 個週期。單一門檻在雜訊估計上會使軌跡每數幀切換一次,而每次切換都會不連續地改變 CBF 的約束集 —— 實測一趟中障礙物數量變動 395 次,且在變動後 0.2 s 內加速度飽和的比例為 26%(其餘時間 10%)。

$T_w$ 由 0.8 s 提高到 2.0 s 的理由:未知圓柱的觀測質心隨可見弧變化而移動,位移量以物體半徑為界;短窗會把它讀成淨位移,2 s 窗使真實移動體的累積位移遠超過該界。

### 3.5 CBF 可行性邊界

輸入飽和下,CBF 約束並非處處可滿足。正面接近的最壞情況:

$$2d\,(v_{\text{esc}} - v_{\text{obs}}) + \alpha\,(d^2 - r_{\text{eff}}^2) \ge 0$$

解得最小可維持中心距:

$$d^\* = \frac{-(v_{\text{esc}} - v_{\text{obs}}) + \sqrt{(v_{\text{esc}} - v_{\text{obs}})^2 + \alpha^2 r_{\text{eff}}^2}}{\alpha}$$

$v_{\text{esc}} = |v_{x,\min}|$。$v_{\text{obs}} \ge v_{\text{esc}}$ 時機器人無法拉開距離,只能提前繞開,而所需預警時間超過 1 s 的時域。

$\alpha = 0.5$、$r_{\text{eff}} = 0.90$、**硬體真值 $v_{\text{esc}} = 0.2775$** 時:

| $v_{\text{obs}}$ | $d^\*$ | 淨脫離速度 |
|---|---|---|
| 0.10 | 0.61 m | 0.178 m/s |
| **0.18(場景上限)** | **0.73 m** | **0.098 m/s** |
| 0.22 | 0.79 m | 0.058 m/s |
| **0.2775** | **0.91 m** | **0 —— 分界線** |

分界線落在 $v_{\text{esc}} = 0.2775$ m/s,即底盤的單軸速度上限。場景速度上限 0.18
在可行側,但淨脫離速度僅 0.098 m/s —— **只有底盤能力的三分之一用於脫離**,其餘被
移動體本身的速度抵銷。這是場景難度的量化依據。

**先前此處的數字是錯的**:曾以 $v_{\text{esc}} = 0.20$ 列表(0.18 → 0.86 m),又
在改用 −0.35 後宣稱 $d^\*$ 大幅下降。兩者都不成立 —— 0.35 超出硬體,而 −0.35 被
`velocity_smoother` 夾在 −0.20,從未到達輪端(見 §0)。

### 3.6 車體幾何與精確符號距離

**車體**由視覺網格量得為圓盤:每 30° 扇區的最大半徑均為 0.300 m(差異 < 5%)。gz 碰撞體同步改為 $r = 0.300$ 圓柱。

**符號距離**改為解析式,不再取樣輪廓:

$$d_{\text{cyl}} = \lVert \Delta \rVert - a - R,\qquad
d_{\text{box}} = \left\lVert \max(q,\mathbf{0}) \right\rVert + \min\!\left(\max(q_x,q_y),\,0\right) - R$$

其中 $q = \left(|\Delta_x| - \tfrac{a}{2},\; |\Delta_y| - \tfrac{b}{2}\right)$。第二式的第二項在機器人中心進入盒內時為負,給出正確的穿透深度 —— 取樣輪廓在此情況下符號會錯(實測一例:取樣給 −0.283,解析給 −0.317)。

### 3.7 遮罩幾何與群集碎裂

自體遮擋以機體角度固定,環境物不會。21 趟不同位置的交集給出真實遮擋為 **34 束**(±45°、±135° 各 8–9°),而原遮罩丟棄 120 束。

**碎裂條件**。半徑 $R$ 的物體在中心距 $c$ 處的方位張角:

$$\Delta\phi = 2\arcsin\!\left(\frac{R}{c}\right)$$

遮罩中心相隔 90°,乾淨區寬度為 $90° - 2h$($h$ 為半寬)。當

$$\Delta\phi > 90° - 2h$$

可見弧必然橫跨遮罩,單一群集被切成兩段,質心跳動超過關聯閘門 0.80 m,軌跡重建期間 `min_track_age = 3` 使其不被發布。

$h = 15°$ 時乾淨區 60°,而 $R = 0.82$ 在表面距 0.5 m 處 $\Delta\phi = 60.8°$ —— **必然碎裂**。實測 **47/60** 次近距離遭遇的弧被切到,對應大型移動體在 0.5–1.5 m 的 CBF 覆蓋率僅 **31–53%**(小型移動體 82–100%)。$h = 10°$ 時乾淨區 70°,兩個大型移動體在 1 m 內均不再跨越。

### 3.8 EKF 觀測拒絕閘門

`pose0_rejection_threshold` 是對 `/amcl_pose` 的馬氏距離閘門:

$$\text{reject if}\quad (z - H\hat{x})^{\!\top} S^{-1} (z - H\hat{x}) > \tau^2$$

$\tau = 2.5$ 形成**發散鎖死**:估計飄開 → 正確觀測落在閘門外被拒 → 無修正 → 飄更遠。

單趟直接證據:

| 時間 | AMCL 發布次數 | AMCL 誤差 | EKF 誤差 |
|---|---|---|---|
| 30–40 s | 15 | 0.046 | 0.047 |
| 40–50 s | 13 | 0.066 | **0.560** |
| 50–60 s | 20 | 0.046 | **2.313** |
| 60–70 s | 18 | 0.040 | 0.038 |

AMCL 全程正常發布且正確,EKF 仍飄到 2.3 m 再瞬間跳回 —— **修正資料就在那裡,濾波器選擇不用**。

175 趟中 **25 趟(14%)** EKF 峰值超過 0.5 m,最嚴重 8.35 m;同期 AMCL 從未超過 0.31 m。設為 $10^9$(等同關閉)後 26 趟 0 次發散(Fisher $p \approx 0.03$),最大誤差降至 0.267 m。

---

## 四、實驗結果

> 本章數字取自 §0 硬體修正**之前**的配置。配對式的內部比較(同批次內開/關單一機制)方向仍成立;跨方法的效能對照因當時三者運動上限不同而作廢,已移除。

### 4.2 CBF 消融

同 15 條路線、同配置(舊上限),唯一差別是 `cbf:=false` —— **配對實驗,結論方向不受 §0 影響**:

| | 到達 | 碰撞 | 間距中位 | 最差 | 到達時間 |
|---|---|---|---|---|---|
| **CBF 開啟** | 15/15 | **0** | **+0.200** | **+0.050** | 96 s |
| CBF 關閉 | 14/14 | **10** | **−0.044** | **−0.595** | 83 s |

**動態避障完全由 CBF 承擔。** 1 Hz 的全域重規劃沒有接手 —— 規劃器讀 `/scan_filtered`,移動體的雷射回波已被遮蔽,它在原理上看不到它們。關掉 CBF 後到達時間反而快 13 秒,那是安全的代價。

配合 `min_h` 統計(階段 4 的 30 趟、58271 個控制週期):`min_h < 0.4` 佔 **50.6%** 的週期、`min_h < 0` 佔 5.7%,**每一趟都有 barrier 介入**(0/30 趟未進入 danger 區)。

### 4.3 定位品質

| | 中位 | p90 | p99 | 最大 | >0.2 m |
|---|---|---|---|---|---|
| AMCL | 0.046 | 0.075 | 0.121 | 0.240 | 0.1% |
| **EKF(控制器實際使用)** | 0.046 | 0.076 | 0.119 | 0.240 | 0.1% |

修復後兩者一致 —— 融合層不再自己製造誤差,只是把 AMCL 的結果平滑傳下去。

**「到達」宣稱的真值檢查**:175 趟宣稱到達中,28 趟(16%)真值距離終點 > 0.30 m,但其中 24 趟落在 0.31–0.36 m(定位常態誤差),僅 2 趟是真的錯了(2.82 m、0.74 m)。用真值判定,到達率由 98.9% 修正為 **97.7%**。

### 4.4 碰撞的靜態/動態拆分

新增分離量測後(80 條新路線、67 趟已分析;舊上限,數值待重測):

| | 中位 | 最差 | 負值趟數 |
|---|---|---|---|
| **動態** | +0.185 | **−0.150** | **5** |
| 靜態 | +0.250 | −0.050 | 3 |

**動態較多且較深。** 靜態的 3 次全部是 **−0.050 這個同一數值** —— 不是隨機擦碰,而是被 keep-out 推到邊界的穩態(終點或門洞落在 $0.30 + 0.38 = 0.68$ m 的 keep-out 內)。

**關鍵反例**:一趟(seed11)CBF 覆蓋率 91–100% 全程看得見,仍然碰撞 −0.077。因此修復感知不會消除全部動態碰撞。

---

## 五、分類鏈的硬閘門:為什麼修了卻不動

§0–§4 修正的每一項都是真實缺陷,但**沒有任何一項單獨移動了碰撞率**。硬體上限、輪速耦合、硬約束 $k=0$、位姿來源,配對比較全部落在雜訊地板之內(§7)。原因在感知與控制的介面。

### 5.1 可見不等於受約束

`/scan` 到 CBF 之間有五道**硬閘門**:

$$\text{cluster} \to \text{track} \to \text{age} \ge 3 \to \lVert v \rVert \ge 0.10 \to v_{\text{net}} \ge 0.05$$

任一關未通過,該障礙物**不是被弱化,而是完全不存在於 QP 中**。這與「約束不夠強」是不同性質的問題,調整 $\alpha$、margin 或 slack 權重都碰不到它。

### 5.2 決定性案例:route 27

route 27 是唯一穿透雜訊地板的失效 —— 四種不同設定各跑一次全部碰撞,十次重播 **9/10 碰撞**,最深 **−0.300 m**(等於機器人半徑)。逐週期診斷(`/gmpc/tracks_debug`,每趟數千樣本)給出:

| 量 | 值 |
|---|---|
| 接觸前 2 s 內軌跡存在的週期 | **100%** |
| 軌跡 age 中位 | **218** |
| KF 位置準確度 | 與真值一致 |
| **實際發布給 CBF 的週期** | **23%** |
| 拒絕原因:瞬時速度閘門 | **68%** |

**物體全程被穩定追蹤,只是被分類為「不是移動體」。** 成因是 `dyn_obs_5` 的設定速度**就是 0.10 m/s**,而 `min_track_speed` 也是 0.10 —— 量測雜訊逐週期決定它算不算移動體。

### 5.3 調門檻救不了:質心速度的系統性偏差

把門檻降到 0.05(其餘完全不動,10 次重播):

| | 發布率 | 拒絕:瞬時速度 | 拒絕:淨位移 | 碰撞 |
|---|---|---|---|---|
| 門檻 0.10 | 23% | **68%** | 9% | 9/9 |
| 門檻 0.05 | **32%** | **53%** | 15% | 7/10 |

閘門確實是綁定條件(降低它確有效果),但**瞬時速度仍佔 53% 的拒絕**。關鍵在同一批資料的另一欄:

$$\text{KF 估計速度} = 0.049 \;\text{m/s}\qquad \text{真實速度} = 0.100\;\text{m/s}$$

**低估一半。** 量測是 $1.6 \times 0.4$ m 長箱的 cluster **質心**,箱體移動時可見面隨方位角改變,質心沿表面滑動,表觀位移遠小於真實位移。

這使**兩道閘門同時失效**,因為它們吃同一個估計:

| 閘門 | 判準 | 實際值 | 結果 |
|---|---|---|---|
| 瞬時速度 | $\ge 0.05$ | 0.049 | 卡在門檻上 |
| 淨位移(2 s 窗) | $\ge 0.10$ m | $0.049 \times 2 = 0.098$ m | **同樣卡在門檻上** |

因此**移除瞬時閘門、只留淨位移**也不會解決 —— 那只是換一道用同樣偏差估計的門檻。**任何建立在質心速度上的分類都不可靠**,而這是推導出來的結論,不是試出來的。

### 5.4 被否定的假設:群集碎裂

在找到上述機制之前,曾假設近距離的大型物體因自體遮擋遮罩而**群集碎裂**、軌跡重建失敗。依此實作了三項修正並各以 10 次重播驗證:

| 組別 | 內容 | 覆蓋率 | 最長失聯 |
|---|---|---|---|
| T0 | 原始 | 29% | 1.95 s |
| T1 | + 預測位置關聯、Mahalanobis 閘門 | 33% | 1.90 s |
| T2 | + 碎片合併(僅幾何) | **20%** | 1.97 s |

**全部否定。** T1 幾乎無效,T2 反而惡化。決定性證據是每週期碎片數三組都是 0.7(合併從未觸發),以及軌跡存在率 100% —— **東西一直都在,不是失聯**。這三項已實作但**預設關閉**。

---

## 六、Raw-scan Safety Shield

### 6.1 設計

若分類鏈的任一環節都可能讓障礙物消失,則安全不應以分類為前提。Shield 置於 `velocity_smoother` 之後、底盤之前,讀**原始 `/scan`**,只主張一個不變量:

> **只要存在有效回波,就不得繼續朝它逼近。**

對每個回波點 $p_i$,令 $n_i$ 為機器人指向它的單位向量、$r_i$ 為朝向它的車體表面點:

$$n_i^\top\!\left(v + \omega J r_i\right) \;\le\; \alpha\,(d_i - d_{\text{stop},i}),\qquad J = \begin{bmatrix}0 & -1\\ 1 & 0\end{bmatrix}$$

由 $\dot d_i = -n_i^\top v_{\text{surface}}$ 與一階屏障 $\dot d + \alpha(d - d_{\text{stop}}) \ge 0$ 推得。**切向運動與遠離不受任何限制** —— 全車急停在門洞卡死的機率遠高於救援成功。

旋轉項對現行圓形輪廓恆為零($n^\top J n = 0$,自轉不使圓盤表面接近外部點),但**保留不刪**:非圓形輪廓時它是主項。

停止距離由底盤實際數字構成,並**逐點**採用朝該點的接近速度(而非速度模長,否則高速切向會灌大所有停止距離):

$$d_{\text{stop},i} = d_0 + v_{\text{app},i}\,\tau + \frac{v_{\text{app},i}^2}{2a_{\text{brake}}} + \epsilon_{\text{scan}} + \epsilon_{\text{footprint}},\qquad v_{\text{app},i} = \max(0,\, n_i^\top u)$$

$\tau$ 包含 scan 年齡,舊資料自動加寬餘裕。

### 6.2 可行性與降級

求解採**固定次數的逐次投影**(Motzkin 鬆弛,每次投影到最違反的半空間),執行時間有界且確定 —— 最後一層安全不能有無界延遲的求解器。速度上限**納入投影集合**而非事後夾制(事後 clip 可能重新破壞屏障約束)。

固定次數只保證時間有界,**不保證可行**,因此每次求解後計算殘差

$$\rho_{\max} = \max_i \left[n_i^\top(v + \omega J r_i) - \alpha(d_i - d_{\text{stop},i})\right]$$

若 $\rho_{\max} > \varepsilon$,改解較弱的**不接近**條件 $n_i^\top v \le 0$ —— 其可行集恆含 $v = 0$,故必有解;仍失敗則輸出零。保證因而是**分級**的,而非靜默發布未收斂結果。

### 6.3 驗證

**針對性測試**(純數學,無 ROS):

| 情境 | 結果 |
|---|---|
| 正前 0.50 m 全速 | 不修改 |
| 正前 0.10 m 全速 | 轉為後退 −0.096 |
| 切向、後退、圓盤自轉 | 完全不修改 |
| 左右各 0.15 m 夾道前進 | **不修改(門洞不卡死)** |
| 門柱 5 個法向 | 殘差 0.379 → **0.000**,2 次迭代 |
| 投影順序(8 種隨機排列) | 輸出差 **0.0000** |
| 前後夾死 | fallback 啟動,最大接近速度 **0.000** |

**模擬驗證**:

| | route 27 十次重播 | 27 條配對路線 |
|---|---|---|
| 無 shield | **9/10 碰撞** | **5 碰撞** |
| **有 shield** | **0/10** | **0** |
| 統計 | $p < 10^{-4}$ | McNemar $p = 0.0625$(5 個不一致配對全部同向) |
| 到達率 | 10/10 | 27/27 |
| 最差間距 | −0.300 → **+0.097** | −0.300 → **+0.047** |

**代價**:介入僅佔 1.1–5.6% 的控制週期,速度 −0.1%,路徑 +2%,到達率不變。

### 6.4 貢獻歸屬

三組消融把「分類鏈修復」與「獨立安全層」分開:

| | 發布率 | 碰撞(route 27,10 次) |
|---|---|---|
| U0 基線 | 23% | 9/9 |
| U1 門檻 0.05 | 32% | 7/10 |
| **U2 門檻 0.05 + shield** | **30%** | **0/10** |

U2 的**發布率與 U1 相同** —— CBF 對該物體的認知毫無改善。零碰撞**完全來自 shield**,不是分類鏈修好了。這是三組分開跑而非合併成一組的理由。

---

## 七、先前被否定的假設

全部以配對實驗(同路線、同其他設定)驗證,並記錄於 `evaluation/CHANGELOG_spacetime.md`。
負結果本身是貢獻:它們把問題從「感知失聯」逐步縮到「分類門檻」,而 §6 的架構回應正是這條排除鏈的終點。

| 假設 | 實驗 | 結果 |
|---|---|---|
| **α 應更大** | L:α = 1.5,30 趟 | 碰撞 8→13、到達 24/30、路徑 +21%,更差 |
| **第三次定案值可移植** | O:α 3.0 / margin 0.38,0.33 / inflation 0.45 / smoother OFF | 碰撞 13/29、**最差 −0.437**,最差的一批 |
| **靜態誤分類是主因** | P/Q/R/S:窗口 4 s、靜態帶速度、兩者、門檻 0.03 | 四種修法碰撞皆 1–2,與對照無異 |
| **時域太短** | H30(1.5 s)/ H40(2.0 s) | 碰撞 2→3→8 單調變差。p95 solve 2.3→9.8 ms 仍遠低於 50 ms,**非算力問題**,是等速外插在長時域失準 |
| **小物體漏檢** | MCP1:`min_cluster_pts` 2→1,30 趟 | 碰撞 2→3,無改善 |
| **margin 可由煞車物理推導** | 71 條配對 | **碰撞 7→39**、間距中位 +0.161→−0.011、最差 −0.150→−0.475 |
| **硬約束可保證零碰撞** | 靜態 $k=0$ 硬化,38 趟 | 到達 38/38、**無 infeasible**,但碰撞未降(5/38)。感知漏掉的物體沒有約束可硬化 |
| **失效源於群集碎裂** | T0–T2 各 10 次重播 | 覆蓋率 29%→33%→**20%**,碎片合併從未觸發。軌跡存在率 100%,不是失聯(§5.4) |
| **降低分類門檻可解決** | 門檻 0.10→0.05,10 次重播 | 發布率 23%→32%,碰撞 9/9→7/10。**質心速度低估一半,門檻本身不可靠**(§5.3) |

### 推導式 margin 的失敗(值得單獨說明)

從物理推導的 keep-out:

$$m_{\text{static}} = \sigma_{\text{loc}} + \frac{v^2}{2a} + v\,\Delta t,\qquad
m_{\text{dyn}} = m_{\text{static}} + v_{\text{obs}}\,\tau_{\text{percep}} + \sigma_{\text{obs}}$$

當時代入 $a = 1.5$(實際生效值是 0.8,硬體真值是 6.25),全速時靜態 0.139、動態
0.243,**比固定值小 2.7–4 倍**。以硬體真值 $a = 6.25$ 代入,煞車項 $v^2/2a$ 更小,
推導出的 margin 只會**更小**,因此結論的方向不變。

失敗原因:該公式假設**感知在所有距離都可靠**,而這不成立。固定的 0.60 讓機器人保持在「大型移動體會失聯」的距離之外,**一直在無意識地補償一個感知缺陷**。縮小 margin 等於拿掉那層保護。

**這是一個有價值的負結果**:它證明了固定 margin 的真正作用不是煞車緩衝,而是感知不可靠性的補償,並把「先修感知」變成有量化證據支持的優先項。

---

## 八、量測方法學

重建評測工具,修正四個會系統性誤導結論的缺陷。**四個都曾造成錯誤結論**。

### 6.1 車體幾何

gz 模擬 0.45×0.45 方盒(半邊 0.225),分析卻用真實車體 0.300 圓盤。模擬的機器人在軸向比真車小 7.5 cm,**淺於 7.5 cm 的「接觸」在物理上並未發生**。約 230 趟中的 15 次接觸,只有 4 次深於此差距。已將 URDF 碰撞體改為 $r = 0.300$ 圓柱使兩者一致。

### 6.2 量測窗口

機器人到達終點後停在原地,錄影繼續到 250 s 上限,移動體開過來撞上不動的車。這被計入碰撞率,但任務已完成。改為只計到達之前;停等期間另以 `clearance_parked` 分開回報。

修正影響:GMPC 從「碰撞 11、最深 −0.300」變為「8、−0.038」。**此偏誤是單向的** —— GMPC 每趟都到達並停等,基線有大量趟數未到達、不存在停等期,因此舊表對 GMPC 不利。

### 6.3 到達時間的來源

分析器在 bag 目錄與共用結果目錄依序尋找 CSV,後者會**覆蓋**前者。共用檔屬於最近執行的批次,導致每批的到達判定被別批污染 —— 同一批 bag 連跑兩次得到不同結果(間距中位 +0.146 vs +0.162)。改為讀到第一個即停,並印出來源。

### 6.4 定位誤差從未被量測

分析器只檢查 `/amcl_pose` 是否仍在發布,從未量誤差 —— 一個已迷失 8 公尺的濾波器在報表上是「健康」的。新增 `loc_error.py` 同時報 AMCL 與 EKF 對 gz 真值的誤差。

**此缺陷是使用者觀看 GUI 時肉眼發現的,不是量測發現的。**

### 6.5 其他保護

- **`guard_first_trial.sh`**:第一趟 `path_length = 0` 即中止整批。曾有 18 趟因控制器啟動即崩潰而全部無效,而語法檢查與 `install/` 內容檢查都無法察覺。
- **每批獨立 `results.csv`**:放進 bag 目錄,杜絕跨批污染。
- **bring-up 失敗率 1.4%**(3/210):`/amcl_pose` 87 s 逾時,該趟直接捨棄,不計入任何統計。

### 6.6 趟間變異決定了可解析的效應大小

同一條路線、同一套設定重複執行,結果差異極大:

| 路線 / 設定 | 第一次 | 第二次 | 差 |
|---|---|---|---|
| seed1,硬體 box | +0.151 | −0.066 | **0.217 m** |
| seed1,硬體 box + 輪速耦合 | +0.163 | −0.202 | **0.365 m** |

來源是 bring-up 時序抖動:機器人生成、Nav2 啟動、移動體驅動各有數百毫秒的隨機
延遲,使機器人與移動體**相遇的相位**每趟不同。同一條路線可能在門洞處正面遭遇,
也可能完全錯開。

對照之下,配置之間的間距中位差只有 **0.110 m**(both +0.200 對 hwlimits +0.100)。

$$\text{趟間雜訊} \;\approx\; 2\text{–}3 \times \text{設定間效應}$$

**後果**:$n \approx 20$ 的配對批次無法解析個位數碰撞次數的差異。本報告中任何
「碰撞 $a \to b$」且 $|a-b| \le 3$、$n \le 40$ 的比較都不構成證據,只能作為方向性
觀察。要分辨 10% 與 5% 的碰撞率,每組需數百趟。

**應改用的量**:單趟內以數千個控制週期統計的指標 —— 輪速違反比例、指令方向反轉
次數、加速度飽和比例、min_h 分佈、solve 時間分位數。這些在單趟內就有大 $n$,不受
相位運氣影響,而且直接對應機制而非結果。本報告的機制結論(遮罩幾何、EKF 閘門、
輪速耦合)都建立在這類量測上,碰撞次數僅作佐證。

---

## 九、已知限制

1. **零碰撞是觀察值,不是碰撞率。** 100 趟全過對應的 95% 上界仍約 3%;27 趟則是 11%。可寫「在 N 趟中未觀察到碰撞」,不可寫「碰撞率為零」。要把上界壓到 1% 需約 300 趟。
2. **Shield 的降級保證尚未量測。** 完整場景下屏障約有 0.45% 的週期不可行、fallback 啟動。降級後的「不接近」條件由程式結構保證(滿足弱條件或輸出零),但診斷欄位量的是**完整屏障**的殘差,**沒有直接量到弱條件是否守住**。需補 `non_approach_ok`。
3. **Shield 只保證「有有效回波」的範圍。** 完全遮擋、玻璃、低反射、從門後突現皆不在保證內。
4. **質心速度偏差未修,只是繞過。** 分類鏈仍會漏掉延伸物體;shield 承接了後果,但上游的速度估計對長條物體依然低估約一半,這會影響任何需要**預測**的功能。
5. **泛化測試尚未執行。** 場景生成已參數化(`SCENE`,種子 11 逐位元重現開發場景),但所有結果仍來自同一個 `bigarena` —— 也就是調參所用的場景。
6. **參數會被下游覆蓋** —— 本期發生兩次(launch 的 `cbf_overrides` 蓋掉 yaml、`velocity_smoother` 夾掉控制器輸出),各使一個實驗實際上沒有生效。往後改動運動限制必須三處同時檢查,並以**輪端命令**驗證而非只看設定檔。
7. **時域 1.0 s 對 MPPI 的 2.8 s** —— 方法本質差異,非設定差異。跨方法對照待在統一硬體上限下重跑。
8. **碰撞判定依賴 gz 真值里程計** —— 真機需另接真值源。
9. **三方對照仍有一項設定不對等:`v_nominal`。** GMPC 的參考巡航速度設為
   **0.22 m/s**,而 MPPI 實測巡航 0.358、貼著底盤的斜向包絡 0.392
   ($\sqrt2 \times 0.2775$)。在兩者都到達的 68 條路線上,平均速度
   **0.215 對 0.392**,到達時間 **102 s 對 48 s**。兩者的最大指令速度相同
   (0.392),差別在常態巡航。

   0.22 是當初為了「低於移動體 0.30 m/s」而選的;移動體現已上限 0.14、底盤真值
   為 0.2775/0.392,此值從未跟著更新。這與先前的加速度不對等
   (0.8/0.6/1.2 對 1.5/1.0/2.0)屬同一類問題:**比較的是設定而非方法**,而統一
   運動上限時只檢查了速度與加速度的「限制」,沒有檢查參考軌跡的「巡航設定」。

   因此目前無法反駁「GMPC 較安全是否只因較慢」。唯一乾淨的解法是把 `v_nominal`
   提到與 MPPI 實際巡航相當並重跑,無論結果如何都照實報告。

---

## 十、下一步

本期把導航這條線做到了可量測、可歸因的程度。接下來的工作分五節:前四節是**範圍的擴張**,最後一節是**已知但尚未修復的缺陷** —— 後者全部有量測支持,列出是為了讓它們不會在下一期被重新發現一次。

### 10.1 從單一場景到泛化

目前所有結果都來自 `bigarena` —— **也就是調參所用的場景**。這是現階段最大的科學空白:對「換個佈局會怎樣」完全沒有資料。

場景生成已參數化(`SCENE`,種子 11 逐位元重現開發場景),但必須在**參數凍結之後**才生成與執行,否則不構成泛化測試。三個適用條件(感測可見、可逃逸、問題可行)也必須事前定義並對每次遭遇計算,否則會退化成事後解釋。

### 10.2 從模擬到真機

gz 的 `VelocityControl` **沒有致動器模型**,任何命令它都照單全收 —— 本期加入的輪速多面體正是為此而做(先前 7.4% 的控制週期命令了硬體做不到的輪速,最壞 2.0 倍)。這件事在模擬中只能**推導**,不能驗證。

真機會引入模擬裡不存在的量:致動器實際響應與飽和、感測與通訊延遲的真實分佈、里程計漂移、LiDAR 在真實表面上的反射行為。Shield 的 $d_{\text{stop}}$ 直接依賴 $\tau$ 與 $a_{\text{brake}}$,兩者都必須在真機重新量測。

### 10.3 從導航到全身操作

**論文題目的另一半尚未開始。** 所有基準均為 `use_arm:=false`。

全身情境會推翻本期建立的幾件事:碰撞幾何不再是固定圓盤(手臂伸展改變輪廓,§6 中對圓盤恆為零的旋轉項將成為主項)、CBF 需涵蓋連桿、shield 的「車體表面點」定義隨構型改變。

### 10.4 從「未觀察到碰撞」到可宣稱的安全性

96 條路線 1 次接觸、深度 1 mm,對應上界約 3%。要提升為**有條件的安全宣稱**,需要:適用條件可量測且事前凍結、shield 降級後的保證實際量測(§10.5.7)、以及足夠樣本(上界 1% 需約 300 趟)。

### 10.5 已知缺陷與限制

以下全部有量測支持,依主題而非嚴重度排列。

#### 延伸物體的表示極限（四項證據指向同一個根因）

本期最集中的問題是**現有的障礙物表示不足以描述延伸物體**。四項發現彼此獨立量得,但根因相同:

1. **運動:質心速度系統性低估。** $1.6 \times 0.4$ m 長箱真實 0.100 m/s,KF 估 **0.049**。物體移動時可見面改變,質心沿表面滑動,表觀位移遠小於真實位移。
2. **幾何:表面點數固定為 4,對大物體不足。** 依尺寸分組量測(有被表示的週期):半尺寸 < 0.3 m 者低估威脅 > 0.1 m 的比例是 **1.1%**,0.3–0.5 m 是 **1.0%**,而 **> 0.5 m 是 8.7%**,且 p95 為 **+0.221 m**(小、中物體的 p95 皆為負,偏保守)。三組**中位數相同**(−0.296)—— 最近點永遠被涵蓋,差別全在尾端,而碰撞正發生在尾端。
3. **近距離群集碎裂。** 自體遮擋遮罩使大物體的可見弧被切開,單一群集被分成多段(§3.7)。
4. **唯一可重現的失效就是最大的物體。** route 27 撞的正是那個 1.6 m 長箱,它同時中了 1 與 2。

**設計上的不一致**:表面點表示刻意繞開質心 —— `cluster_adjacent()` 的註解寫明質心「作為幾何參考是無用的」;但淨位移分流仍以質心為**運動**參考,而且是對它做差分。質心當幾何參考時誤差是單一偏移量,當運動參考時是兩個時刻偏移量之**差**,視角改變會被讀成位移。因此**不是門檻選得不好,是被量測的量本身不該用**。

修正方向是讓兩者採用同一表示:以**表面點集合**估計剛體位移(對應點的中位平移或簡化 ICP)而非質心差分;並讓點數隨聚類尺寸調整,
$$N_i = \min\!\left(N_{\max},\ \left\lceil 2\,\text{extent}_i / \texttt{min\_sep} \right\rceil\right)$$
每條軌跡實測只有 1–2 個表面點,計算成本很小;p95 solve 目前 2.6 ms,預算 50 ms。

#### 感知到 CBF 的傳遞率

5. **移動體在 2 m 內時,26.7% 的週期完全沒有任何表面點代表它。** 這不是幾何表示的問題(有被表示時偏保守),而是分類鏈的硬閘門把它擋掉。目前由 shield 承接後果,但**任何需要「預測」的功能仍拿不到那個物體** —— 包括 CBF 的等速外插本身。

#### 控制層

6. **CBF 對 $k \ge 1$ 在參考軌跡上線性化**($X_k = X_{\text{ref,win}}[k]$)而非預測狀態。這解釋了 H30/H40 的碰撞**單調變差**:時域越長,參考與實際的累積偏離越大,約束建在越錯的位置。修法是 SQP 式迭代(用上一次的解 rollout 後重新線性化),跑 2 次即可。**這是唯一完全未動過的控制層缺陷。**

7. **三方對照仍有一項設定不對等:`v_nominal`。** GMPC 參考巡航 **0.22 m/s**,MPPI 實測 **0.358**(貼著斜向包絡 0.392)。兩者最大指令速度相同,差在常態巡航。在兩者都到達的 68 條路線上,平均速度 0.215 對 0.392、到達時間 102 對 48 s。0.22 是當初為了低於移動體 0.30 m/s 而選,移動體現已上限 0.14 而此值未更新。**目前無法反駁「較安全是否只因較慢」**,需對齊後重跑。

#### 安全層

8. **Shield 降級後的保證未量測。** 完整屏障不可行時會改解「不接近」條件,該條件的可行集恆含 $v=0$ 故必有解;但診斷欄位量的是**完整屏障**的殘差,**沒有直接量到弱條件是否守住**。100 趟中 fallback 佔 0.23%(修正後 0.04%),需補 `non_approach_ok` 欄位。
9. **$\epsilon$ 未由感測器規格推導。** $d_{\text{stop}}$ 中的 $\epsilon = \epsilon_{\text{scan}} + \epsilon_{\text{footprint}} = 0.03 + 0.02 = 0.05$ m。其中 $\epsilon_{\text{footprint}} = 0.02$ 由 §8.1 的車體量測支持(圓盤近似誤差上界 0.015);$\epsilon_{\text{scan}} = 0.03$ 是保守估計,應由測距雜訊、角解析度 $r\Delta\theta/2$ 與掃描期間的相對移動三項推導。與 $\tau$、$a_{\text{brake}}$ 同屬一組延遲/誤差預算,宜在真機一併量測。

#### 工程與量測基礎設施

10. **參數會被下游覆蓋。** 本期發生兩次(launch 的 `cbf_overrides` 蓋掉 yaml、`velocity_smoother` 夾掉控制器輸出),各使一個實驗實際上沒有生效。往後改動運動限制必須三處同時檢查,並以**輪端命令**驗證而非只看設定檔。
11. **參數之間的相依關係未被追蹤。** 動態 margin 由 0.38 提高到 0.60 時,`detour.py` 中三處硬寫的 0.38 與 launch 的 `detour_max_offset = 0.35` 都沒有跟著調整,使繞行的目標車道仍落在 keep-out 內。手動調整這些值的嘗試使震盪更嚴重,已還原。
12. **跨場景資料污染。** 分析器依環境變數選擇佔據圖,來自其他場景的 bag 會被對照到錯的地圖並報出 `−0.300`(恰等於車體半徑,即中心落在佔據格)。本期發生兩次,第二次是歸檔腳本用 `*seed*` 掃入其他批次的 bag。凡跨批次分析必須依方法前綴選檔,並以場景中繼資料而非目錄名稱判定。
13. **bring-up 失敗率。** 連續啟動時約 30% 的趟數會遇到 `/amcl_pose` 逾時(AMCL 卡在 `Configuring`,lifecycle 服務回應逾時),開 GUI 時更容易觸發。目前以趟間延遲緩解,未根治。
14. **趟間變異大於設定間效應。** 同路線同設定重複的間距差為 0.043–0.479 m,而設定之間的差約 0.11 m。$n \le 40$ 的碰撞次數比較不構成證據;應以單趟內數萬個樣本的每週期統計為主。

### 未列入

- RL residual 平滑化:訓練結果為負,該線暫停
