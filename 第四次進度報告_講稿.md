# 第四次進度報告 — 講稿

約 15–18 分鐘。粗體是必須講出口的數字,其餘可依時間刪減。

---

## p1 封面（15 秒）

老師好,我是陳文顥。今天報告的題目是「動態環境下移動機械臂的預測導航與受約束操作」,這是第四次進度報告。

---

## p2 報告大綱（15 秒）

今天分三個部分:先回顧上次的架構,然後是這次的進度,最後是未來工作。

---

## p3 研究目標（40 秒）

先說一下整體的定位。這個研究的最終情境是「機器人到書房、開門、從抽屜拿筆」。

第一階段是**導航** —— 在有未知靜態與動態障礙物的環境中安全移動;第二階段是**受約束操作** —— 開門、拉抽屜這類需要手臂與底盤協同的動作。

**今天報告的全部內容都屬於第一階段。** 第二階段目前還沒開始,這點我在未來工作會再說明。

---

## p4 導航系統架構（60 秒）

這是整體架構。LiDAR 進來之後分兩條路:

一條走**感知鏈** —— 聚類、追蹤、Kalman 濾波,輸出障礙物的位置與速度,給 CBF 當約束;
另一條給**規劃器**建立全域路徑。

中間是 **SE(2) 幾何 MPC**,它同時處理軌跡追蹤與 CBF 的安全約束,解一個 QP 得到底盤速度指令。

這次新增的是**最下面那一層** —— Raw-scan Safety Shield,它直接讀原始掃描,不經過感知鏈。等一下第九頁會詳細說明為什麼需要它。

---

## p5 進度回顧（90 秒）

上次報告建立了三個東西。

**第一是障礙物預測。** 用四狀態等速度模型,LiDAR 聚類後以 Hungarian 關聯維持軌跡,再用 Kalman 濾波遞迴估計位置與速度。

**第二是 SE(2) 幾何 MPC。** 這裡的關鍵是把底盤位姿放在 SE(2) 李群上,用對數映射建立測地誤差 —— 這樣角度跨越正負 π 時不會有不連續。線性化是沿參考軌跡多點時變的,不是只在當前狀態線性化一次。

**第三是全時域預測式 CBF。** 不只避開障礙物「現在」的位置,而是對時域的**每一步**都加約束 —— 用等速外插預測障礙物未來位置,對每一步要求 $\dot h + \alpha h \ge -\varepsilon$。

這三個是這次工作的基礎。

---

## p6 硬體層:實際輪系限制（100 秒）

這次的第一項改進是硬體層。

**問題是:先前底盤的運動限制是估計值,不是從輪系推導的。** 這次拿到實際參數 —— 輪半徑 5 公分、輪距 24.5 公分、輪最大角速度 5.55、最大角加速度 125。

由全向輪運動學重新推導,得到 **平移上限 0.2775 公尺每秒、旋轉 1.1327、加速度 6.25**。

**修正之後發現兩件事:**

第一,原本設定的 $v_x = \pm 0.35$ **超出硬體 26%** —— 那是物理上做不到的命令。

第二,更重要的是,推導出來的加速度是 6.25,而系統實際在用的是 0.8 —— **只有硬體能力的八分之一**。原因是 launch 檔的覆寫值蓋掉了設定檔,而這件事之前沒有被發現。

修正後,這組數字成為 GMPC、CBF、velocity smoother 與停止距離計算的**共同物理基準** —— 先前每一層各自用不同的假設。

> 如果被問「這對結果影響多大」:誠實回答是**單獨這一項沒有改變碰撞率**,它的價值在於讓後面所有的推導(特別是可逃逸速度與停止距離)建立在真實硬體上,而不是估計值上。

---

## p6 下半 場景改進（60 秒）

同一頁的右半是場景。

**原本的場景是開放空間、起終點固定**,所以無法證明方法能泛化到不同的空間結構。

新場景 `bigarena` 是 20 乘 20 公尺、**八個隔間、交錯門洞**,有 18 個已知障礙、**7 個不在地圖上的未知靜物**、以及 **10 個移動體**,而且**起訖點每趟隨機取樣**。

這個場景暴露了原本被掩蓋的問題 —— 同一套配置在新場景的到達率一度掉到八成以下。後面的所有實驗都在這個場景上做。

---

## p7 感知層:表面點表示（90 秒）

第二項是感知層的幾何表示。

**原本把每個 LiDAR 聚類壓縮成「質心加半徑」。** 這有三個問題:它對未知物體強加了圓形假設;外接圓對細長物體過度保守;而且**可見區域改變時質心會跳動**。

具體的例子:一個 1.2 乘 0.3 公尺的推車,外接圓半徑要 0.65,加上安全餘裕就是**超過一公尺的禁區** —— 比它實際能通過的縫隙還寬。

改進的做法是**直接保留原始回波點**,不做任何形狀擬合。取離機器人最近的點開始,要求彼此間隔至少 20 公分、範圍 2.5 公尺內、每個聚類最多保留 4 個點。

**這樣 CBF 約束的是實際量到的表面,不是一個假設的形狀。**

---

## p8 感知層:淨位移動靜分流（90 秒）

第三項是靜態與動態的分流。

**問題是:原本只用瞬時的 KF 速度來判斷障礙物在不在動。** 但靜止物體也會產生假的瞬時速度 —— 遮擋、質心滑動都會造成。我們實際量到過**靜止圓柱的表觀速度到每秒 2 公尺**。

所以加入第二個條件:**滑動時間窗內的淨位移**。一個真的在移動的物體會累積位移,而在原地抖動的靜物不會。判定條件是三個同時成立:軌跡夠成熟、瞬時速度夠快、而且**兩秒窗內的淨位移速度也要夠快**。

> **這裡要誠實補一句(重要):**
> 我們後來量了這個閘門的實際效果 —— 靜態物體被誤判為移動體的比例從 **39% 降到 31%**,**只改善了 8 個百分點**。
>
> 原因是**兩道閘門吃的是同一個估計** —— 都建立在聚類質心上。而對延伸物體來說,質心速度是**系統性低估**的:一個 1.6 公尺長的箱子,真實速度 0.10,KF 估出來只有 **0.049,低估一半**。因為箱子移動時可見面在變,質心沿著表面滑動,表觀位移遠小於真實位移。
>
> 所以淨位移閘門解決了它設計要解決的問題,但**在延伸物體上有明確的邊界**。這正是下一頁的動機。

---

## p9 安全層:Raw-scan Safety Shield（120 秒 — 本次重點）

這是這次最主要的貢獻。

**核心的發現是這句話:感測看得見,不等於進入 CBF。**

從掃描到 CBF 中間有五道**硬性關卡** —— 聚類、建立軌跡、軌跡年齡、速度分類、淨位移。**任何一關沒過,那個障礙物就不是「約束變弱」,而是完全不存在於 QP 裡。**

我們有一個決定性的案例。有一條路線,一個 1.6 乘 0.4 公尺的箱子撞上機器人。事後逐週期分析發現:**那條軌跡 100% 的週期都存在、成熟度中位數 218、位置估計準確** —— 東西一直都在。但它**只有 23% 的週期被發布給 CBF**,68% 的拒絕來自速度閘門,因為那個箱子的設定速度**就是 0.10,而門檻也是 0.10**。

我們試過把門檻降到 0.05,發布率只從 23% 升到 32% —— 因為剛才說的質心偏差。**所以任何建立在質心速度上的門檻都不可靠,這是推導出來的結論,不是試出來的。**

因此設計了 Shield。它放在 **smoother 之後、底盤之前**,直接讀**原始掃描**,只主張一個不變量:**只要存在有效回波,就不得繼續朝它逼近。**

約束是這一式:限制車體表面朝每個回波的接近速度,上限是 $\alpha$ 乘上「距離減去停止距離」。**距離越近,容許的接近速度越小;進到停止距離內就要求退離。** 停止距離由實際的延遲與煞車能力構成,而且**逐點**用朝該點的接近速度計算。

**關鍵是它不做急停** —— 切向移動與遠離完全不受限制。全車停住在門洞卡死的機率,遠高於救援成功。

分工是:**GMPC-CBF 負責預測與提前繞行,Shield 負責分類失效時的最後保護。**

---

## p10 成果（180 秒 — 最重要的一頁）

上面那排是**三方對照**,下面那排是**消融**。

### 三方對照（上排）

先說對照。這裡有一件事要先講:**上次報告的對照表其實不公平** —— 當時 GMPC 用的加速度是 0.8,MPPI 是 1.5,**比的是設定不是方法**。這次三個方法跑**完全相同的硬體上限**、相同的 100 條隨機路線。

結果是:

- **碰撞:我們 1 次,MPPI 7 次,RPP 34 次**
- **到達率:我們 100%,MPPI 77%,RPP 72%**
- **最深穿透:我們 0.1 公分,MPPI 36 公分,RPP 57 公分**

三個指標同時領先,而且**到達率的差距比碰撞率更大**。

要特別說明的是:基線的低碰撞**不是因為它們沒走完全程** —— MPPI 未到達的那些趟,路徑反而比到達的趟**長 127%**,是繞了更久卻到不了。

RPP 的側移速度維持 0,因為純追蹤法本身不產生側移指令 —— 那是演算法特性,不是我們削弱它,但在全向底盤上確實是劣勢。

### 消融（下排）

下面這排是 CBF 與 Shield 的 2×2 消融,同 28 條配對路線,**四組都是 28/28 到達**,沒有任何一組是靠卡住換安全。

- **A 兩層都關:22 次碰撞**,最深 51 公分
- **B 只有 CBF:3 次**
- **C 只有 Shield:2 次**
- **D 兩者都有:0 次**,而且**最差間距是正的**

這裡有一個原本沒預期到的結果:**C 單獨的表現和 B 一樣好**,而且**比 B 更快、路徑更短**。所以不能說「Shield 不夠好所以需要 CBF」。

**真正的理由在中間那張圖。** B 和 C 的碰撞數幾乎一樣,但**分佈完全不同**:

- **CBF 把整個分佈抬高**(中位 +0.18)—— 它在遠處就繞開
- **Shield 的分佈明顯低**(中位 +0.08)—— 它貼著走但沒撞到
- **只有 D 的箱體與鬚全部在零以上**

**所以兩層做的是不同的事:CBF 買的是提前性與餘裕,Shield 買的是最後幾公分的保護。疊加才把殘餘清掉。**

我們也從逐週期的角度驗證了同一件事:**CBF 約束了 90% 的控制週期,Shield 只介入 1.8%** —— 而那 1.8% 裡有 **28% 發生在 CBF 認為完全安全的時刻**,那正是分類鏈漏掉物體的情況。

> **最後要誠實說明統計上的限制:**
> 「1 次碰撞 / 96 趟」對應的 95% 上界大約是 **3%**。可以說「在 96 條路線中觀察到一次接觸、深度 1 公釐」,但**不能說碰撞率是零** —— 要把上界壓到 1% 需要大約 300 趟。

---

## p11 未來工作（90 秒）

投影片上寫了兩條,都是關於手臂:**把機械手臂模型導入現有框架**,以及**把 SE(2) 底盤 GMPC 擴充成 SE(3) 的全身協同規劃**。

這是主線,因為**論文題目的另一半到現在還沒開始** —— 目前所有的評測都是手臂收起來的狀態。

全身情境會**推翻這次建立的幾個前提**:碰撞幾何不再是固定圓盤,手臂伸展會改變輪廓 —— Shield 那條約束裡對圓盤恆為零的旋轉項,到時候會變成主要項;CBF 的約束也要涵蓋連桿。

> **如果時間允許,補充另外兩個方向:**
>
> **第一是泛化。** 目前所有結果都在同一個場景上,**而那也是調參用的場景**。場景生成已經參數化了,可以用不同種子產生新佈局,但必須在**參數凍結之後**才生成,否則不算泛化測試。這是目前最大的科學空白。
>
> **第二是真機。** 模擬器沒有致動器模型 —— 任何命令它都照單全收。這次加入的輪速耦合約束正是為此而做,因為我們量到先前有 **7.4% 的控制週期命令了硬體做不到的輪速**。但這件事在模擬裡只能推導,不能驗證。

---

## p12 結語（15 秒）

以上是這次的進度,謝謝老師。

---

# 公式補充講稿

以下內容不需要全部逐字念。報告時先說公式的用途，再挑最重要的符號解釋；老師追問時再補完整定義。

## p5 障礙物預測模型

### 四狀態等速度模型

$$
x=\begin{bmatrix}p_x&p_y&v_x&v_y\end{bmatrix}^{\top}
$$

可以這樣說：

> 每一條障礙物軌跡使用四個狀態表示，前兩項是平面位置，後兩項是平面速度。上標 $\top$ 代表將這四個量寫成欄向量。Kalman filter 根據連續量測，遞迴估計這四個狀態。

- $x$：障礙物的狀態向量。
- $p_x,p_y$：障礙物在 $x$、$y$ 方向的位置。
- $v_x,v_y$：障礙物在 $x$、$y$ 方向的速度。
- $\top$：矩陣轉置。

### 障礙物等速度外推

$$
o_i(k)=o_i(0)+v_i k\Delta t
$$

可以這樣說：

> 這一式假設短時間內障礙物維持等速度。現在的位置加上速度乘以未來經過的時間，就是第 $k$ 個預測步的障礙物位置。

- $i$：第 $i$ 個障礙物。
- $o_i(0)$：障礙物目前的位置。
- $o_i(k)$：障礙物在第 $k$ 個預測步的位置。
- $v_i$：障礙物估計速度向量。
- $k$：預測步索引。
- $\Delta t$：相鄰預測步之間的時間。
- $k\Delta t$：從現在到第 $k$ 步所經過的時間。

### 預測式 CBF 條件

$$
\dot h_i(k)+\alpha h_i(k)\geq-\varepsilon_k
$$

可以這樣說：

> $h$ 用來表示安全程度，安全邊界外為正，邊界上為零，進入危險區域後為負。這條限制要求安全程度不能下降得太快；越接近障礙物，控制器越必須降低接近速度或產生遠離動作。

- $h_i(k)$：第 $k$ 個預測步相對第 $i$ 個障礙物的安全函數值。
- $\dot h_i(k)$：安全函數的變化率。
- $\alpha>0$：CBF 收斂係數；數值越大，允許靠近安全邊界的速度通常越高，但實際效果仍與 $h$ 的定義有關。
- $\varepsilon_k\geq0$：slack 鬆弛變數，在限制無法完全滿足時避免 QP 無解。

這條公式的重點不是保證 $h$ 永遠很大，而是限制系統接近安全邊界的方式。若初始狀態已經不安全，控制器也應優先讓 $h$ 回升。

### GMPC 輸入平滑成本

$$
\Delta u_k^{\top}S\Delta u_k
$$

可以這樣說：

> 這一項懲罰相鄰控制週期的命令變化。命令突然改變得越大，成本越高，因此可以減少控制抖動與不必要的橫向切換。

- $u_k$：第 $k$ 步的控制輸入，通常包含 $v_x,v_y,\omega$。
- $\Delta u_k=u_k-u_{k-1}$：相鄰兩步的輸入變化。
- $S$：輸入變化的權重矩陣，用來調整各方向平滑程度。

## p6 實際輪系限制

### 已知硬體參數

$$
r=0.05\ \mathrm{m},\qquad L=0.245\ \mathrm{m}
$$

$$
\omega_{B,\max}=5.55\ \mathrm{rad/s},\qquad
\alpha_{B,\max}=125\ \mathrm{rad/s^2}
$$

可以這樣說：

> $r$ 是輪子半徑，$L$ 是底盤中心到輪組作用位置的等效距離。$\omega_{B,\max}$ 與 $\alpha_{B,\max}$ 分別是單輪可達到的最大角速度與最大角加速度。

下標 $B$ 在這裡代表底盤輪系參數，不是機器人的旋轉角速度。

### 平移速度上限

$$
v_{x,\max}=v_{y,\max}=r\omega_{B,\max}
$$

代入數值：

$$
0.05\times5.55=0.2775\ \mathrm{m/s}
$$

可以這樣說：

> 輪緣線速度等於輪半徑乘以輪角速度，因此得到底盤單一平移方向的速度上限為每秒 $0.2775$ 公尺。

### 底盤旋轉速度上限

$$
\dot\phi_{\max}=\frac{r}{L}\omega_{B,\max}
$$

代入數值：

$$
\frac{0.05}{0.245}\times5.55
=1.1327\ \mathrm{rad/s}
$$

可以這樣說：

> 輪子的線速度除以底盤的等效力臂，就得到底盤可達到的最大偏航角速度。

- $\phi$：底盤偏航角。
- $\dot\phi$：底盤角速度，也可寫成 $\omega$。

### 平移與旋轉加速度上限

$$
a_{x,\max}=a_{y,\max}=r\alpha_{B,\max}
=6.25\ \mathrm{m/s^2}
$$

$$
\ddot\phi_{\max}=\frac{r}{L}\alpha_{B,\max}
=25.51\ \mathrm{rad/s^2}
$$

可以這樣說：

> 這兩式和速度推導相同，只是將輪角速度換成輪角加速度。它們提供 velocity smoother、控制器輸入限制與停止距離計算一致的硬體基準。

補充：這些是由輪系參數得到的理論軸向界限。多個 $v_x$、$v_y$ 與 $\omega$ 同時作用時，仍需透過輪速耦合多面體限制，確保每一顆輪子都沒有超限。

## p7 表面點表示

### 原本的質心與半徑模型

$$
\mathcal O_i=(c_{x,i},c_{y,i},r_i)
$$

可以這樣說：

> 原本將第 $i$ 個 LiDAR cluster 壓縮成三個量：質心的 $x$、$y$ 座標，以及代表物體大小的半徑。這種表示簡單，但是等同以圓形近似未知物體。

- $\mathcal O_i$：第 $i$ 個障礙物的幾何模型。
- $c_{x,i},c_{y,i}$：cluster 質心座標。
- $r_i$：由 cluster 尺寸得到的代表半徑。

### 選擇第一個表面點

$$
q_1=\underset{q\in P_i}{\arg\min}\,\lVert q-p_r\rVert_2
$$

可以這樣說：

> 在第 $i$ 個 cluster 的所有回波中，先選擇距離機器人最近的點，作為第一個、也是最重要的安全約束點。

- $P_i$：第 $i$ 個 cluster 的全部 LiDAR 回波點集合。
- $p_r$：機器人的參考位置，通常是底盤中心。
- $q$：$P_i$ 中任一候選回波点。
- $q_1$：最靠近機器人的第一個表面點。
- $\lVert q-p_r\rVert_2$：兩點之間的歐氏距離。
- $\arg\min$：回傳「讓距離最小的那一個點」，不是回傳最小距離本身。

### 候選點距離範圍

$$
q_j\in P_i,\qquad \lVert q_j-p_r\rVert_2\leq2.5\ \mathrm{m}
$$

可以這樣說：

> 後續候選表面點必須屬於同一個 cluster，而且只處理距離機器人 2.5 公尺內、與目前避障直接相關的回波。

- $q_j$：第 $j$ 個候選表面點。
- $j$：表面點索引。

### 最近鄰間距與數量限制

$$
\underset{q'\in S_i}{\min}\lVert q_j-q'\rVert_2
\geq0.20\ \mathrm{m},
\qquad |S_i|\leq4
$$

可以這樣說：

> 新候選點與所有已保留點的最近距離，至少要有 20 公分，避免在同一小塊表面重複建立約束；每個 cluster 最多保留四個代表點，以控制 QP 的約束數量。

- $S_i$：第 $i$ 個 cluster 稀疏化後的表面點集合。
- $q'$：$S_i$ 中任一個已保留點。
- $\min_{q'\in S_i}$：找出候選點與既有表面點之間最小的距離。
- $|S_i|$：集合中的點數。

這組方法可稱為 **nearest-neighbor gated sparsification**；它使用歐氏距離選點，概念上與 Poisson-disk sampling 的最小間距要求相近，但這裡是確定性的距離排序與閘門，不必直接宣稱使用完整的隨機 Poisson-disk 演算法。

## p8 淨位移動靜分流

### 瞬時速度閘門

$$
\lVert\hat v_i(t)\rVert_2\geq v_{\mathrm{inst,min}}
$$

可以這樣說：

> 首先計算 Kalman filter 估計速度的大小。如果速度低於瞬時速度門檻，就不把該軌跡判為移動體。

- $\hat v_i(t)$：第 $i$ 條軌跡在時間 $t$ 的 KF 速度估計向量。
- $\lVert\hat v_i(t)\rVert_2$：不考慮方向的速度大小。
- $v_{\mathrm{inst,min}}$：瞬時速度門檻。
- 帽號 $\hat{\ }$：代表估計值，不是真值。

### 滑動窗平均淨位移速度

$$
\bar v_{\mathrm{net},i}
=\frac{\lVert\hat c_i(t)-\hat c_i(t_0)\rVert_2}{t-t_0}
$$

可以這樣說：

> 取軌跡目前位置與時間窗起點位置的直線距離，再除以經過時間。真正移動的物體會累積淨位移；只在原地前後抖動的物體，起點和終點仍然接近，所以這個值較小。

- $\hat c_i(t)$：第 $i$ 條軌跡目前的估計位置。
- $\hat c_i(t_0)$：滑動時間窗起點的估計位置。
- $t_0$、$t$：時間窗的起點與目前時間。
- $t-t_0$：實際觀察時間長度；目前設定約為 2 秒。
- $\bar v_{\mathrm{net},i}$：時間窗內的平均淨位移速度。
- 上方橫線 $\bar{\ }$：表示時間窗平均量。

它不是將每一幀速度取平均，而是用「起點到終點的淨位移」除以時間，因此可以抵消部分來回抖動。

### 最終動態分類條件

$$
\mathrm{dynamic}_i=
(\mathrm{age}_i\geq3)
\land
(\lVert\hat v_i\rVert_2\geq v_{\mathrm{inst,min}})
\land
(\bar v_{\mathrm{net},i}\geq v_{\mathrm{net,min}})
$$

可以這樣說：

> 三個條件必須同時成立：軌跡至少成功接受三次量測、瞬時速度超過門檻，而且滑動窗的淨位移速度也超過門檻，才發布為動態障礙物。

- $\mathrm{age}_i$：第 $i$ 條軌跡成功接受量測更新的累積次數，不是以秒為單位的年齡。
- $\mathrm{age}_i\geq3$：至少累積三次有效觀測，避免剛建立的不穩定軌跡立即被當成移動體。
- $v_{\mathrm{net,min}}$：淨位移速度門檻。
- $\land$：邏輯上的「而且」，表示所有條件都必須成立。

要補充其限制：瞬時速度與淨位移都來自 cluster 質心，因此若延伸物體的可見表面改變，兩個指標可能一起產生偏差。它能降低靜態誤判，但不能保證所有真實移動體都會通過分類。

## p9 Raw-scan Safety Shield

### 車體表面的速度

$$
v_{\mathrm{surface},i}=v+\omega Jr_i
$$

可以這樣說：

> 車體某個表面點的速度包含兩部分：底盤整體平移速度，以及底盤旋轉在該表面點產生的切向速度。

- $v=[v_x,v_y]^{\top}$：底盤平移速度向量。
- $\omega$：底盤偏航角速度。
- $r_i$：從底盤中心指向第 $i$ 個車體表面點的向量。
- $J=\begin{bmatrix}0&-1\\1&0\end{bmatrix}$：平面旋轉 90 度的矩陣。
- $Jr_i$：表面點因旋轉而運動的切向方向。
- $\omega Jr_i$：底盤旋轉造成的表面點線速度。

對理想圓盤而言，若 $r_i$ 與法向量平行，則 $n_i^{\top}Jr_i=0$，自轉不會改變圓盤到外部點的徑向距離。公式仍保留旋轉項，是因為未來換成非圓形車體或加入手臂後，旋轉項會影響接近速度。

### Shield 安全約束

$$
n_i^{\top}(v+\omega Jr_i)
\leq\alpha(d_i-d_{\mathrm{stop},i})
$$

可以這樣說：

> 左邊是車體表面朝第 $i$ 個障礙物回波方向的接近速度；右邊則是目前剩餘安全距離所允許的最大接近速度。距離越近，右邊越小，因此控制器必須降低朝障礙物靠近的速度。

- $n_i$：由車體表面指向第 $i$ 個 LiDAR 回波的單位法向量。
- $n_i^{\top}v_{\mathrm{surface},i}$：表面速度投影到障礙物方向後的接近速度分量。
- $d_i$：第 $i$ 個回波與車體表面的實際間距。
- $d_{\mathrm{stop},i}$：考慮延遲與煞車後所需的停止距離。
- $d_i-d_{\mathrm{stop},i}$：目前剩餘的安全距離。
- $\alpha>0$：把距離裕度轉換成允許接近速度的係數，單位為 $\mathrm{s^{-1}}$。

三種狀態可以直觀解釋：

- $d_i>d_{\mathrm{stop},i}$：仍有安全裕度，可以有限度地靠近。
- $d_i=d_{\mathrm{stop},i}$：允許接近速度為零，但仍可切向移動或遠離。
- $d_i<d_{\mathrm{stop},i}$：右側為負，要求產生遠離障礙物的速度分量。

### 動態停止距離

$$
d_{\mathrm{stop},i}
=d_0+v_{\mathrm{app},i}\tau_{\mathrm{eff}}
+\frac{v_{\mathrm{app},i}^{2}}{2a_{\mathrm{brake}}}
+\epsilon
$$

可以這樣說：

> 停止距離由四部分組成：基本安全距離、感測與控制延遲期間繼續前進的距離、底盤真正煞停所需的距離，以及量測和車體模型的不確定度緩衝。

- $d_0$：車體外額外保留的基本安全距離。
- $v_{\mathrm{app},i}$：底盤朝第 $i$ 個回波方向的接近速度，通常只取非負的接近分量。
- $\tau_{\mathrm{eff}}$：有效延遲，包含 scan age、處理時間與控制延遲。
- $v_{\mathrm{app},i}\tau_{\mathrm{eff}}$：延遲期間尚未開始煞車所前進的距離。
- $a_{\mathrm{brake}}$：底盤可保證的煞車減速度大小。
- $v_{\mathrm{app},i}^{2}/(2a_{\mathrm{brake}})$：由 $v^2=2as$ 得到的物理煞車距離。
- $\epsilon$：LiDAR 誤差、scan 離散性與車體幾何近似的額外緩衝。

停止距離不是固定常數。接近速度越快或感測資料越舊，$d_{\mathrm{stop},i}$ 就越大，Shield 也會更早介入。

## p11 SE(2) 與 SE(3) 記號

- $SE(2)$：平面剛體運動群，包含 $x$、$y$ 平移與偏航角 $\phi$，用於描述全向底盤位姿。
- $SE(3)$：三維剛體運動群，包含三維位置與三維姿態，可用於描述機械手臂末端執行器。

可以這樣說：

> 目前 GMPC 最佳化的是平面底盤的 $SE(2)$ 位姿。未來加入機械手臂後，末端執行器需要使用 $SE(3)$ 描述，並與底盤、關節限制及連桿避障一起求解。

---

# 預期問題與回答

**Q:「碰撞率降到零了嗎?」**
> 沒有。96 趟裡有 1 次接觸,深度 1 公釐。統計上能說的是「碰撞率低於 3%,95% 信心」。要宣稱接近零需要約 300 趟。

**Q:「Shield 是不是讓 CBF 變得多餘?」**
> 不是,而且我們特別測了這一點。只有 Shield 的那組(C)仍有 2 次碰撞、最深 30 公分;只有 CBF 的(B)有 3 次。**兩者的碰撞數接近,但間距分佈差很多** —— CBF 的中位是 +0.18,Shield 只有 +0.08。CBF 提供的是提前避讓,Shield 是最後保護。逐週期看,CBF 約束 90% 的週期,Shield 只動 1.8%。

**Q:「你們比較慢,是不是因為比較保守?」**
> 是,而且這裡有一個**設定上的不對等我要先說明**。我們的參考巡航速度設在 0.22,而 MPPI 實測跑到 0.358 —— 貼著底盤的斜向上限。兩者的**最大**速度相同,差在常態巡航。所以「是不是只因為比較慢」這個問題,**目前的資料還無法回答**,需要把參考速度對齊後重跑。這是下一步要做的。

**Q:「為什麼不直接把安全餘裕加大就好?」**
> 試過。我們做過從煞車物理推導餘裕的實驗,結果是**碰撞從 7 次變成 39 次**。原因是那個公式假設「感知在所有距離都可靠」,而這不成立 —— 固定的較大餘裕實際上一直在**無意識地補償感知缺陷**。這也是為什麼後來選擇修架構而不是調參數。

**Q:「這些改進哪一項最有效?」**
> 從消融看,**兩層安全機制缺一不可**:各自單獨都還有 2–3 次碰撞,疊加才到零。至於硬體限制與輪速耦合這兩項,**單獨都沒有改變碰撞率** —— 它們的價值是讓命令在物理上可執行(先前有 7.4% 的週期做不到),以及讓所有推導有共同基準。

**Q:「這個場景有多難?」**
> 移動體速度 0.10 到 0.14,而底盤單軸上限是 0.2775。用 CBF 的可行性邊界推導,對最快的移動體**淨脫離速度只有 0.098** —— 也就是說,正面遭遇時只有三分之一的底盤能力可用於脫離。場景速度上限就是這樣定出來的,不是任意選的。
