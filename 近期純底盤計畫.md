# 近期純底盤計畫（Near-Term Pure-Chassis Plan）

> 定位：本檔只涵蓋「近期」可執行的工作，是 `底盤.txt` 完整 roadmap 的前段落地版。
> 不含機械臂、Whole-body MPC、完整 GMPC-CBF 整合（那些屬於中長期）。
> 對應 roadmap：Phase 0 收尾 + Phase 1 驗證 + Phase 2 評估框架 + Phase 3 Step 1 GMPC 離線原型。

---

## 0. 現況盤點（基於實際檔案，2026-06-01）

| 項目 | 檔案 | 狀態 |
|---|---|---|
| Baseline B 設定（SmacPlanner2D + MPPI Omni） | `src/ammr_navigation/config/nav2_params_mppi.yaml` | ✅ 已修好（vy、AMCL Omni、motion_model Omni、min_y_velocity_threshold 都對） |
| Baseline B launch | `src/ammr_navigation/launch/nav2_omni_mppi.launch.py` | ✅ 已接 `nav2_params_mppi.yaml` |
| Omni 控制器運動學 + odom | `src/ammr_bringup/ammr_bringup/omni_drive_controller.py` | ✅ cmd/odom 都正確含 vy |
| Baseline A（NavFn + RPP） | `src/ammr_navigation/config/nav2_params.yaml` | ⚠️ AMCL 已是 Omni，但 velocity_smoother 的 vy 仍為 0.0 |
| 舊壞檔（MPPI + DiffDrive） | `src/ammr_bringup/config/nav2_params.yaml` | ❌ DiffDrive 殘留，容易誤跑，需處理 |
| 評估框架（logging / metrics / plots） | — | ❌ 完全沒有，`ammr_navigation` 等套件只有空 `__init__.py` |
| GMPC 離線原型 | — | ❌ 未開始，`ammr_wholebody_mpc` 為空 |
| 動態障礙物 | `dynamic_obstacle_driver.py` 等 | ⚠️ 已開工，但屬 Phase 5，順序太早 → 近期降為低優先 |

**結論：底盤 Omni 設定基本完成，瓶頸在「沒驗證 + 沒評估框架 + 沒 GMPC」。**

---

## Sprint 1 — 鎖定並驗證 Omni Baseline（Phase 0 收尾 + Phase 1）

**目標：** 把 Baseline B 變成可重現、已驗證的基準，並消除三個 params 檔造成的混亂。

### 任務
1. **整理 params 檔，定義唯一正規組合：**
   - Baseline A = `nav2_params.yaml`（NavFn + RPP）→ 把 velocity_smoother 的 vy 補上（或在檔頭註明「RPP 僅用 vx+wz」）。
   - Baseline B = `nav2_params_mppi.yaml`（SmacPlanner2D + MPPI Omni）→ 維持。
   - 舊檔 `ammr_bringup/config/nav2_params.yaml`（DiffDrive）→ 刪除或改名加 `.deprecated`，避免誤跑。
2. **手動側移測試**（驗證硬體/控制器層 vy 通路）：
   ```bash
   ros2 topic pub /cmd_vel geometry_msgs/msg/Twist \
   "{linear: {x: 0.0, y: 0.15, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}" --rate 10
   ```
   觀察：純側移、方向正確、無異常旋轉；`ros2 topic echo /odom` 的 twist.linear.y 有值。
3. **Baseline B 四種導航測試**（送 goal，確認 `/cmd_vel` 有 linear.y、能到點）：
   - 前進（主要 vx）／側移（明顯 vy）／斜向（vx+vy 同時）／保持姿態平移。
4. **每個測試錄 rosbag**（topics 見 Sprint 2），作為評估框架的輸入。

### 完成定義（DoD）
- 三個 params 檔角色清楚、不會誤跑舊 DiffDrive 檔。
- 四種測試的 `/cmd_vel`、`/odom` 都確認含正確 linear.y，截圖/影片留存。
- 每個測試有對應 rosbag。

---

## Sprint 2 — 建立評估框架（Phase 2）★ 最關鍵基礎建設

**目標：** 一行指令把 rosbag → CSV + 圖。這是 GMPC vs MPPI 一切量化比較的前提，沒有它後面無法比。
**放哪：** 建議在 `ammr_navigation`（或新開 `ammr_evaluation`）放分析腳本。

### 任務
1. **錄製 wrapper：** 統一錄 `/odom /cmd_vel /cmd_vel_nav /plan /scan /tf`。
2. **分析腳本（bag → CSV）：** 算出指標 — success、arrival_time、path_length、tracking_rmse（實際 vs `/plan`）、smoothness（Δu）、jerk（u 二階差分）、freeze_count、mean/max_solve_time（先留欄位給 GMPC）。
3. **自動畫圖：** vx/vy/wz 時間曲線、實際路徑 vs `/plan` 疊圖。
4. **跑 Sprint 1 的 bag：** 產出第一張 **RPP vs MPPI Omni** 比較表。

### 完成定義（DoD）
- `python analyze.py <bag>` → 自動產生 CSV + PNG。
- 有第一版 baseline 指標表（即使只有 A vs B 兩個方法）。

---

## Sprint 3 — GMPC 離線原型（Phase 3 Step 1）★ 先把論文核心數學釘死

**目標：** 純 Python、不接 ROS，先確認 SE(2) GMPC 的數學與 QP 完全正確。
**理由：** 上一版 MPC 就是栽在線性化（見 memory `project_mpc_lessons`）。離線把 H,g,A,lb,ub 驗對，再接 ROS2，省大量 debug。
**可與 Sprint 2 並行**（純 Python、互不依賴）。

### 任務
1. **SE(2) 工具：** `hat/vee`、`exp/log`、`X_ref^{-1} X` 幾何誤差 `e = log(E)^vee`。
2. **線性化誤差動態 → QP：** 組出 `H, g, A, lb, ub`（含速度/加速度上下限約束）。
3. **OSQP 求解迴圈 + Omni 運動學模擬**（`x_dot = vx cosθ − vy sinθ` 等）。
4. **五種參考軌跡測試：** 直線、側向、斜向、S-curve、yaw wrap-around。
5. **畫圖：** 追蹤軌跡、tracking RMSE、solve time。

### 完成定義（DoD）
- 五種軌跡都能追上、RMSE 有界、無數值爆掉。
- solve time 量出來（之後對比 MPPI 的賣點之一）。
- 數學鎖定後，才進 Phase 3 Step 2（ROS2 standalone node）。

---

## 依賴與建議順序

```text
Sprint 1（驗證 baseline）──┐
                          ├─→ 有 baseline + 有評估 → 之後 GMPC 才能被量化比較
Sprint 2（評估框架）──────┘
Sprint 3（GMPC 離線）──── 可與 1、2 並行，純數學獨立
```

- **先做 Sprint 1 + 2**（互相依賴，baseline 要被評估框架吃）。
- **Sprint 3 並行進行**，不要等。
- 三者都穩之後，才進「Phase 3 Step 2：GMPC ROS2 node」。

---

## 明確不在近期範圍（避免發散）

- ❌ CBF safety filter（Phase 4）
- ❌ 動態障礙物預測 / KF（Phase 5）——**目前已開工的 `dynamic_obstacle_*` 先暫停或降為低優先**，等 baseline + 評估 + GMPC 離線穩定再回來。
- ❌ GMPC-CBF 整合（Phase 6）、完整 benchmark 300 trials（Phase 7）
- ❌ 機械臂 / Whole-body

理由：動態障礙物在「沒有可量化的 baseline、沒有 GMPC」之前先做，會無法評估好壞，也無法支撐論文論述。先把可比較的地基打好。

---

## 風險與提醒

1. **論述誠實性：** 不要預設「GMPC-CBF 完勝 MPPI」。MPPI 在擁擠/非凸場景 success rate 常更高；GMPC-CBF 的賣點是「更低計算成本 + 更平滑 + 安全」，代價是擁擠時可能 infeasible。用數據說話。
2. **GMPC 動機：** Omni 是 holonomic，一般線性 MPC 本來就堪用。SE(2) 的真正價值是「統一處理姿態誤差 + 為未來 whole-body SE(2)/SE(3) 幾何鋪路」，不要賣成「omni 非用李群不可」。
3. **CBF relative degree：** 速度控制的 holonomic 底盤，`h_dot` 直接含 u（relative degree 1），CBF 是乾淨線性不等式——這是選 omni + 速度層 CBF 的優勢，報告值得明講。

---

## Whole-Body 橋樑（Future Work，非近期）

> 這節是「願景」，用來在報告裡展示架構擴充性。**實作上明確標為後期**，近期不碰。
> 一句話定位：近期只「留好接口」，不「蓋好房子」——接口免費先留，房子（ESO / passivity / 高維 QP）後蓋。

### 近期就保留的接口（幾乎零成本，建議留）

- **Twist / 螺旋輸出介面：** 把 GMPC 控制輸出明確定義為李代數 twist `ζ = (vx, vy, wz) ∈ se(2)`。
  - 為什麼免費：你本來就在 SE(2) 求解，輸出本來就是 twist，差別只在「明確命名 + 文件化」。
  - 未來價值：透過伴隨矩陣 `Ad_G` 可把底盤 twist 與手臂 Jacobian **線性疊加**，whole-body 運動學不必重寫成大型非線性問題。
  - 近期動作：在 Sprint 3 的 GMPC 原型裡，把輸出變數註解清楚為 se(2) twist 即可，不需額外實作。

### 明確延後到 Whole-Body 階段（每個都是獨立子題，不是「順手加」）

| 項目 | 為什麼現在不做 | 觸發時機 |
|---|---|---|
| **ESO 擴展狀態觀測器** | 除非刻意建模打滑，Gazebo 純底盤幾乎無擾動可補；它是獨立觀測器子系統，需設計 / 調 L 增益 / 證穩定 | 手臂接觸產生反作用力、底盤被耦合干擾時 |
| **Passivity 無源性約束** | 無物理接觸 → 無能量可約束，「預留 QP 空間」等於沒做 | 受約束操作（拉櫃 / 開抽屜）時 |
| **Acados / HPIPM 遷移** | 3-DOF 底盤 QP 用 OSQP 亞毫秒即解；Acados 的 code-gen + C build 成本高，易卡 build error | whole-body 高維稠密 QP 出現時 |
| **ProxQP（可選）** | 若只想要全域收斂保證，其 API 近似 OSQP，屬低成本替換 | 任何時候想換都行，**非必要** |

### 報告用的關係圖

```text
近期（純底盤，本檔）                  後期（Whole-Body，Future Work）
─────────────────────              ──────────────────────────────
SE(2) GMPC ── 輸出 twist ζ ──┐
                             ├─ Ad_G ─→ 與手臂 Jacobian 線性疊加 → Whole-Body QP
CBF safety（速度層線性約束）─┘                 + ESO（抵消接觸反作用力）
OSQP 求解                                      + Passivity（接觸穩定）
                                               + Acados/HPIPM（高維求解）
```

---

## 進度報告 Elevator Pitch（誠實版）

> 原則：**可被數據支持才講**。用「在…假設下」「以…衡量」「預期」取代「保證 / 完勝 / 全面」。
> 一個收得緊、完整跑完、誠實 benchmark 的工作，比一堆 buzzword 各做一半更有份量。

「目前我用 Nav2 MPPI 當動態避障 baseline。它避障能力強，但有兩個結構性代價：一是靠大量取樣（每步上千條 rollout），CPU / 功耗成本高；二是安全是用 cost 權重『軟』處理、且要調很多 critic 權重，沒有形式化的安全條件。另一方面，傳統 (x, y, θ) 線性化 MPC 在大角度與全向側移時，會有角度 wrap-around 與線性化精度問題。

因此我的下一步是開發一套 **SE(2) 幾何模型預測控制 (GMPC)**：把底盤姿態與參考軌跡表示為 SE(2)，用李代數誤差動態做追蹤，避免角度 wrap-around 與大角度線性化問題。避障則用**控制障礙函數 (CBF)**——因為它對速度輸入是線性不等式，可以直接塞進同一個 QP。所以整個問題是一個小型凸 QP，用 OSQP 就能亞毫秒等級求解，計算是確定性的，正好對比 MPPI 的取樣成本。

**驗證方式**：用我已建好的 `generate_map.py` 隨機場景，在相同 seed、相同動態障礙物下跟 Nav2 MPPI 量化比較——比 tracking RMSE、solve time、控制平滑度 / jerk、最小障礙物距離、碰撞率。我**預期** GMPC-CBF 在計算延遲與平滑度上有優勢、能降低碰撞率，同時會**誠實呈現**擁擠場景下的 feasibility / freezing 行為。

最後，這套架構為未來的 whole-body（開櫃 / 拉抽屜）**留好了接口**：GMPC 的控制輸出是李代數 twist，未來可透過伴隨矩陣 `Ad_G` 與手臂 Jacobian 線性疊加；至於接觸階段需要的抗擾動 (ESO) 與接觸穩定 (passivity) 則列為後續工作。也就是說——**現在先用最小、可被量化的方式取代 MPPI，同時把通往 whole-body 的數學接口鋪好。**」

### 30 秒濃縮版（口頭用）

「我要用 SE(2) 幾何 MPC + CBF 取代 Nav2 MPPI：把追蹤與避障寫成一個小型凸 QP，計算成本低且確定性高，並用隨機場景跟 MPPI 量化比較 RMSE / solve time / 平滑度 / 碰撞率。控制輸出是李代數 twist，未來能用伴隨矩陣直接接上手臂、延伸到 whole-body——ESO 與 passivity 留作接觸階段的後續工作。」
